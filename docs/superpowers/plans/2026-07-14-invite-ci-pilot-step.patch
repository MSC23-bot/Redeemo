diff --git a/.github/workflows/ci.yml b/.github/workflows/ci.yml
index 0005e2ec..01de7ad2 100644
--- a/.github/workflows/ci.yml
+++ b/.github/workflows/ci.yml
@@ -310,0 +311,10 @@ jobs:
+      # Invite/referral correction round 3: the concurrency regression suite
+      # (one-lead-per-business under race; atomic 10-cap; duplicate
+      # idempotency at the cap boundary) runs against the same disposable
+      # loopback service. These are exactly the semantics unit mocks cannot
+      # prove. Advisory like its siblings; PASS/FAIL surfaced below.
+      - name: Run invite-concurrency integration pilot (advisory)
+        id: invites_pilot
+        continue-on-error: true
+        run: npm run test:integration:invites
+
@@ -327,0 +338,5 @@ jobs:
+          if [ "${{ steps.invites_pilot.outcome }}" = "success" ]; then
+            echo "Advisory invite-concurrency pilot: PASS" >> "$GITHUB_STEP_SUMMARY"
+          else
+            echo "Advisory invite-concurrency pilot: FAIL (non-blocking)" >> "$GITHUB_STEP_SUMMARY"
+          fi
