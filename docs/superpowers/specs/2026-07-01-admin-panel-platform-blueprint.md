# Redeemo Admin Panel: Platform Operating Model, Information Architecture & Existing-Capability Relocation Blueprint

## 0. Front matter

- **Status:** BLUEPRINT (design intent). This document describes the complete Admin Panel needed to operate Redeemo. It is NOT a statement that these surfaces are built. Every claim about what exists is tagged and source-cited; everything else is design.
- **Authority:** This is the platform-level target operating model and information architecture. It supersedes the shape of the current `apps/admin-web` enabling slice (see 1.1). Where it names something as built, `origin/main` source is the authority.
- **Freshness:** verified against `origin/main` @ base SHA `37cc0f69483500b7f512881953283e66ce52531a` (`docs(crypto-rotation) #344`). Re-verify before relying on any "built" claim.
- **Scope of this artifact:** documentation only. It does not authorize implementation, schema, API, auth, provider, deployment, or Claude Design work. Approved process decisions: D1 (operating-model-and-IA-first), D2 (durable platform blueprint before Claude Design), D14 (one master blueprint plus separate wave-specific Claude Design prompt packs). D20 (NHS / key-worker / community partnership schemes) is owner-decided FUTURE / DEFERRED. All other product, security, legal, provider and schema decisions remain PROPOSED and separately gated.
- **How to read:** read Section 1 (framing) and Section 2 (cross-product) first, then Section 3 (personas), Section 4 (IA), and Section 5.0 (the five boundary contracts) which govern every module. Sections 5 to 9 are the domain design. Section 12 is the binding "nothing silently discarded" relocation contract. Section 13 lists what stays gated. Section 15 sequences the prototype waves. Section 17 is the evidence base.

### 0.1 Corrections carried (binding, verified this pass)

- Branch photo review/apply is **ENGINEERED** (PR #313, commits `45ac3773`/`2442f214`); `EDIT_PHOTO_APPLY_NOT_SUPPORTED` is no longer thrown (dead code). The CLAUDE.md/memory "photo-apply deferred" line is stale.
- Merchant Insights provides an existing **aggregation + eligibility foundation** (`src/api/merchant/insights/*`, PR #331 / `00582a6e`), reusable as a **reference**. **Admin analytics is NOT BUILT** (no admin-wide analytics API, authz model, or UI). Redemption merchant attribution already works via `VoucherRedemption -> Branch -> Merchant` (`branch.merchantId`); `VoucherRedemption.merchantId` denormalisation is **not** a settled prerequisite.
- Merchant Insights `gate.ts`: operational aggregates run un-gated; **behavioural** metrics (repeat-rate, new-vs-returning, customer-history, event-level CSV) are DPIA fail-closed by `behaviouralGateOpen()` (default-off, production-fail-closed).
- `apps/merchant-web` is engineered/merged and a useful **design reference**; it is **not** staging-accepted or production-proven.
- The current Admin UI is an enabling slice, not the final Admin IA.

### 0.2 Style

British English. No emojis. No em-dashes (use `:` `;` `()` `·` and hyphens). Real Redeemo brand hexes referenced by name where relevant: brand red `#E20C04`, coral `#E84A00`, navy `#010C35`, cream `#FFF9F5`. Note: `apps/admin-web` today is neutral-brand (no brand fonts); brand alignment is a separate design decision, not locked here.

---

## 1. Operating model and principles

### 1.1 Enabling slice vs final Admin Panel (the governing framing)

The current `apps/admin-web` was built primarily as an **operational enabling slice** so Merchant Portal onboarding, approvals and lifecycle operations could function end to end. It is the "first operational slice, not the launch bar" (verbatim, `docs/superpowers/specs/2026-06-14-admin-panel-actioner-design.md` §18.3): it exists to operate the previously write-only approval chokepoint (`2026-06-07-merchant-admin-platform-strategy.md` §2, §3) so a merchant can move from lead to live through the product rather than through seed scripts.

It is therefore **not** the approved final Admin Panel operating model, information architecture, module hierarchy, navigation, homepage, page composition, naming, layout, or visual design. The full Admin Panel is a larger, later scope (strategy §4.2 enumerates roughly fourteen domains).

**Design rules for this blueprint:**

1. Design the complete Redeemo Admin Panel operating model and IA from the platform level first (Sections 1 to 4).
2. Then map every existing capability into the module, screen and hierarchy where it properly belongs (Section 12).
3. Preserve valid backend capabilities, authorization boundaries, business rules, safety controls, tests and proven workflow behavior.
4. Make no assumption that the current sidebar, queue-first homepage, grouping, labels, layouts or visual composition survive.
5. Allow existing frontend surfaces to be retained, redesigned, moved, split, combined or superseded.
6. Ensure nothing already built is silently discarded (Section 12 is the binding contract).
7. Represent the complete Admin Panel needed to operate Redeemo, not an enlarged merchant-support console.

### 1.2 Mission

The Admin Panel is Redeemo's internal operational control centre. It governs the two-sided marketplace end to end: merchant supply (leads, onboarding, verification, approvals, lifecycle, relationships, assisted operations), customer demand (accounts, subscriptions, support, consent, trust and safety), the content and taxonomy that shape discovery, the commercial and communications operations, and the platform governance (roles, audit, oversight, operational status) that makes powerful tools safe.

### 1.3 Principles (load-bearing; each cross-checked in Section 17)

- **Act FOR the merchant, never AS the merchant.** Admins may correct, co-build and assist, always as the real ADMIN actor with reason and audit; they never impersonate, never accept terms, never set or learn a merchant password. (Verified: no admin contract-signing path; no admin password path; `EditActor` mandatory reason on every on-behalf mutation.)
- **Minimise back-and-forth with the merchant (correction-first, owner-decided D37).** Repeated request-changes loops put merchants off (a commercial + operational cost). Where an operator can correct a submission on behalf of the merchant, they should author the fix in place before approving rather than bouncing it back. "Request changes" is demoted to the FALLBACK, reserved for items only the merchant can supply (for example a missing document). Corrections split into two tiers: COSMETIC corrections apply directly (operator Apply-and-approve, goes live, audited); MATERIAL corrections (voucher VALUE / saving / discount amount, and the business / trading NAME) route to the merchant for approval before going live. See 6.1.4 and D37.
- **No weaker path.** Every admin-on-behalf action runs the same shared core (`fnCore`) the merchant path runs, with a tighter (never looser) input allow-list. (Verified: `src/api/merchant/shared.ts` `EditActor`; Option B B1 to B5.1.)
- **Every intake has an outlet.** Any data the platform captures from users (reported reviews, fraud/screenshot telemetry, suggested tags, bounced-email signals) must have an admin surface to action it, or be explicitly scoped out. Silent dead-ends are prohibited (see the DEAD-ENDED disposition in Section 12).
- **Safety by default.** Destructive and high-risk actions require mandatory reason, confirmation, capability gating, transactional audit, and (proposed) step-up. Customer-impacting and lifecycle actions route through the approval lane, not direct edit.
- **Capability-gated, backend-enforced.** `requireAdminCapability` over `AdminRole` is the authz spine; any UI mirror is UX only and never the enforcement.
- **No customer-home mapping.** Merchant and branch locations and anonymized regional supply may be mapped; individual customer home addresses and precise coordinates must never be plotted (see boundary contract 4).
- **Native Redeemo relationship operations, not a generic CRM.** Redeemo owns relationship operations that require live platform state or in-product side-effects; general prospecting, nurture and conversational support are candidates for external integration (see boundary contract 5).
- **PII and DPIA discipline.** Customer surfaces are PII-heavy and inherit the DPIA fail-closed posture for any behavioural or demographic aggregate; PII access is gated and audited; deletion is anonymise-in-place today (see Section 6.2 and boundary contract 4).
- **Honest status.** The blueprint annotates maturity so nothing pretends to be built; those annotations are review artifacts, not mandated UI badges (see Section 4.3).

### 1.4 Cross-product invariants the Admin Panel must respect

Branch-first cardinality; domain canon (`redeemo.co.uk`; never emit `redeemo.com`; `admin.redeemo.co.uk` not provisioned); subscription-anchored cycles; no-emoji/no-em-dash/brand-hex style; Node 24 for admin-web/backend/CI; insights eligibility cleanliness (exclude `isTestData` on redemption+branch+merchant, QA emails, `User.status=DELETED`; join redemptions via `branch.merchantId`); the Decimal-as-string coercion rule (`z.coerce.number()` for Prisma `Decimal` in any admin-web schema); plan-first Tier 2/3 discipline; git-safety hooks. Merged is the floor, not the bar: the Definition of Complete (all states incl. denial, a11y, responsive, wire-accurate contract tests, staging acceptance) is the completion standard.

---

## 2. Cross-product operating model

The Admin Panel is one surface in a coordinated platform. Its actions have live-query downstream effects (no event bus; a customer sees the effect the instant the DB transaction commits).

| Connects to | What the Admin Panel does / must respect | Evidence |
|---|---|---|
| **Customer app / web** | Governs customer accounts, subscriptions, consent, trust and safety; its approve/suspend actions change discovery visibility and redemption eligibility. No admin customer surface exists today (net-new). | discovery filters on `merchant.status=ACTIVE` + `voucher.status=ACTIVE`+`approvalStatus=APPROVED` + `!isTestData`; redemption gate `src/api/redemption/service.ts` |
| **Merchant Portal** | Operates the chokepoint the portal feeds; edits on behalf via the same cores; approves what the portal submits. Merchant billing does not exist on either side. | `src/api/merchant/**`; Option B B1 to B5.1 |
| **Public website / content** | Owns specific versioned content and taxonomy; legal/FAQ pages are static and owner/legal-signed (the hard launch gate); `CmsContent` is unwired by runbook decision. | boundary contract 2; `deploy-security-runbook` §12 |
| **Backend / schema** | Reuses the shipped authz, audit, approval, edit-on-behalf, notification and insights primitives; most net-new domains need new schema (flagged per module). | Section 17 |
| **Providers** | Monitors and acts on Stripe (subscriptions/refunds/disputes: no admin surface, disputes not even webhook-handled), Twilio (SMS caps), Resend (email dark; read-only per-merchant comms only), FCM (Phase 6), R2 (docs; flag-gated). | `src/api/subscription/**`, `shared/smsLimiter.ts`, `webhooks/resend.ts`, `shared/storage.ts` |
| **Support / finance / legal / privacy / ops** | Native relationship operations, DSAR/retention, commercial operations, legal/agreement evidence, and platform governance; largely net-new and gated. | Sections 6 to 9, 13 |

**Downstream-effect summary (built, verified):** approve to go-live makes a merchant and its RMVs discoverable and redemption-eligible; suspend is immediate takedown plus session revoke plus in-flight cycle refund; confirm-location earns a map pin and go-live eligibility; approve/reject voucher, apply/reject edit (including branch photos), approve/reject branch lifecycle are precisely scoped, allow-listed, ADMIN-audited mutations.

---

## 3. Personas and authorization model

### 3.1 Persona ladder (complete operating model; some future/gated)

Do not omit FINANCE, SUPPORT or CONTENT because their current backend capabilities are empty. Each persona is modeled fully; implementation may be future or gated.

| Persona | Operating role | Backend state today |
|---|---|---|
| **SUPER_ADMIN** | Owner / root / break-glass; platform-critical powers (role management above tier, kill-switches, hard-delete/purge, legal/pricing config, irreversible actions). | `AdminRole.SUPER_ADMIN`; superuser short-circuit holds every current and future capability (`capability.ts:113`). |
| **OPERATIONS** | Day-to-day actioner: queue, review, merchant lifecycle, edit-on-behalf (operational tier), trust and safety triage. | `AdminRole.OPERATIONS` holds `ALL_SLICE1_CAPS` (10 caps). |
| **SALES / LEAD OWNER** | Supply recruitment: leads, qualification, acquisition-source attribution, representative-assisted onboarding, relationship notes; may initiate, never approves its own merchants. | No `AdminRole` value; **PROPOSED**. `MerchantLead`/`MerchantSource` design-only. |
| **FINANCE** | Billing, refunds, disputes, payouts, commercial operations. | `AdminRole.FINANCE` exists but holds zero capabilities; commercial code largely absent. |
| **SUPPORT** | Customer relationships, cases, DSAR intake, read-only view-as. | `AdminRole.SUPPORT` exists but holds zero capabilities; a natural home for CRM-case ownership; no support routes/tables. |
| **CONTENT** | Content and taxonomy management, comms/broadcast, announcements. | `AdminRole.CONTENT` exists but holds zero capabilities. |
| **Platform Manager (PROPOSED)** | Broad operational plus financial oversight, can manage department staff, cannot hold platform-critical. | **PROPOSED persona label only.** There is NO `AdminRole.ADMIN` / Platform-Manager enum value or backend role today; do not imply one exists. |

**Persona-model evolution notes (all DESIGN-ONLY, clearly-labelled FUTURE on the 1.9 Team and Roles screen).** Three DESIGN-ONLY prototype decisions touch this ladder: (a) COMBINED / multi-module access (D57): today one admin holds exactly ONE fixed role (`AdminRole` is a scalar, not an array), and Super Admin is the only "everything" role; holding several modules at once (Operations plus Finance plus Content) is a FUTURE concept via one of two net-new models (multi-role union / per-person capability grants), model plus timing a later capability-matrix / authz decision (see 3.2, D57). (b) TEAM AND ROLES staff-reframe (D58): the people in this ladder are Redeemo STAFF / team members who hold admin-panel access, DISTINCT from the merchant-side "Staff and access" (a merchant's own branch / portal staff, D44); the screen holds the basic operational plus employment fields to onboard a staff member to the PANEL and manage their access, and it is NOT the HR system (fuller HR / employment-lifecycle data lives in the dedicated HR tool, for example Zoho People; see D58). (c) ASSIGN-THEN-CLAIM (D59, DEFERRED to Wave 2): certain ASSIGNER roles (a net-new assign capability; likely Super Admin plus a lead / senior-Operations tier, possibly the proposed Platform-Manager mid-tier persona) can ASSIGN a queue item / merchant / task to a specific user, who is then notified and CLAIMS it (assignment ROUTES, claim COMMITS; single-actor claim-to-act accountability preserved; see Section 9, D59).

### 3.2 Capability model (current, verified) and its evolution

- Current: 15 capabilities (10 in `ALL_SLICE1_CAPS` held by OPERATIONS + 5 SUPER_ADMIN-only = 15); a de-facto two-tier gate: OPERATIONS holds `ALL_SLICE1_CAPS` (10 caps); the five higher-bar caps (`merchant:edit-identity`, `merchant:edit-category`, `merchant:manage-branches`, `merchant:propose-edit`, `merchant:manage-documents`) are SUPER_ADMIN-only via the short-circuit; FINANCE/CONTENT/SUPPORT hold none. (`capability.ts:87-115`.)
- **Owner-decided reclassification (D38, 2026-07-02; NOT-YET-IN-CODE):** VAT number, company number, category and tags move from SUPER-ADMIN-only to OPERATIONS-editable (Super Admin still holds everything via the short-circuit). Rationale: Super Admin is not in day-to-day merchant management; Operations is, so the fields Operations corrects during review should be Operations-editable. Ground truth: today the backend gates these to Super-Admin-only via the short-circuit on `merchant:edit-identity` (VAT / company number) and `merchant:edit-category` (category / tags), so this is a capability CHANGE to implement, not the current state. The exact per-role grant matrix (which caps each role holds after the move) is a LATER decision; do NOT present a finished matrix. The business / trading NAME does NOT move: name change stays on the material Correct-on-behalf lane (routes to the merchant per D37). See 6.3 and D38.
- Evolution (design intent, not locked): three capability classes (operational / financial / platform-critical) and per-user grants. **`AdminCapabilityGrant` is design-only and unshipped.** Define required responsibilities, restrictions and access outcomes WITHOUT locking that schema or a granular grant engine. Hard constraint to surface: fixed-`AdminRole` RBAC cannot express per-person exception grants until `AdminCapabilityGrant` ships, so delegating a specific power to one operator is blocked on that net-new model.
- **Combined / multi-module admin access (FUTURE concept, DESIGN-ONLY prototype decision D57, shown clearly-labelled on the 1.9 Team and Roles screen).** Today an admin holds EXACTLY ONE fixed role: the `AdminRole` enum is a scalar `role` field (SUPER_ADMIN / OPERATIONS / FINANCE / CONTENT / SUPPORT), not an array, and Super Admin is the only role that holds "everything" (over-broad via the superuser short-circuit, NOT a curated subset). Giving ONE person several modules at once (for example Operations plus Finance plus Content) is PLANNED via one of two NET-NEW models, both to be shown as clearly-labelled FUTURE on the Team and Roles screen (never as a live toggle): (1) MULTI-ROLE assignment: a user holds several whole roles and their access is the UNION of those roles (the simple "combine a few roles" model; schema `role` -> `roles[]` or a join table); (2) PER-PERSON CAPABILITY GRANTS: start from a base role, then add or remove individual capabilities for that one person (the finer-grained model; this is the design-only `AdminCapabilityGrant` per-person-grants future item noted just above). The exact model (multi-role union vs per-person grants) AND when to build it are a LATER capability-matrix / Tier-3 authz decision; do NOT present either model as chosen or built. See D57 in Section 13 and Section 16.
- New capabilities implied by the Members and Revenue domain (all net-new, verified-absent today; there are ZERO admin-facing customer/subscription/member endpoints on `origin/main`): proposed `customer:read` (member list + Customer 360 read), `subscription:read` (subscription lifecycle read), and `revenue:view` (MRR/ARR/conversion read). These give the currently-empty FINANCE role a real home (billing/revenue read-and-monitor), symmetric with how OPERATIONS holds the merchant-supply caps. Per-person grants for these remain FUTURE (blocked on `AdminCapabilityGrant`, as above). Higher-bar customer PII read (`customer:read-pii`) and any customer mutation (`customer:action`) stay separate and gated per Section 6.2.
- New capabilities implied by the Merchant 360 on-behalf domain (all net-new, verified-absent today; owner-decided D44 / D46 / D48): `merchant:manage-staff` (Staff and access on-behalf management: portal-access members + app / branch logins; D44, 6.1.6); `branch:manage-pin` (reveal-on-demand + reset of the branch redemption PIN on the Branches tab; D46, 6.1.6; the reveal / reset is reason-required and audited, the PIN value is never logged); `redemption:view-customer` and `redemption:reveal-customer` (role-gated customer identity on individual redemption rows / detail / lookup; D48, 6.1.6). The redemption identity split is: AGGREGATE surfaces never show identity (no capability reveals it there); INDIVIDUAL identity is visible-by-default for customer-facing roles (`redemption:view-customer`), reveal-with-reason for restricted roles (`redemption:reveal-customer`), no-access for roles with no legitimate need, and EVERY view or reveal is audited. The exact per-role-to-tier mapping for all four caps is set with the capability matrix (illustrative in the prototype); per-person grants stay FUTURE (blocked on `AdminCapabilityGrant`, as above).
- New capabilities implied by the assisted-onboarding channel and the internal-notes surface (all net-new, verified-absent today; owner-decided D49 / D51, DESIGN-ONLY prototype decisions): `merchant:assisted-onboard` (the heavy on-behalf onboarding-wizard capability that lets an authorized operator build an ENTIRE merchant account on the merchant's behalf in a full-screen presentation-mode flow; distinct from the light `merchant:create-draft` which is REAL and only shell-creates a draft, D49, 8.2); `notes:read` and `notes:write` (the internal operator-notes surface on Merchant 360: `notes:read` views the note list + per-note history, `notes:write` adds / edits / retracts, every add / edit / retract audited; D51, 6.1). The assisted-onboard wizard runs inside the operator's OWN audited admin session (presentation mode is a merchant-safe focus mode, NOT a separate login or a security boundary), and the operator can NEVER accept the merchant contract or set / see a merchant password (the owner signs in person and takes ownership via a tokenised claim link). These are DESIGN-ONLY: no assisted-onboarding wizard endpoints and no notes model exist today (a notes model is net-new schema). The exact per-role mapping is set with the capability matrix (illustrative in the prototype); per-person grants stay FUTURE (blocked on `AdminCapabilityGrant`, as above).
- New capabilities implied by the Wave-2 relationship, trust-and-safety, support and communications domains (all net-new, verified-absent today; introduced with D60 to D65 and the Wave-2 screens): `tag:moderate` (suggested-tag approve / reject, M13); `fraud:read` (the fraud-signals feed read, M11), with `redemption:reverse` held SEPARATELY from the read (the reversal action itself, schema-gated); `review:moderate` (reviews-moderation actions, M10); a support-cases capability (case queue + lifecycle, M14; the exact name settles with the build-vs-integrate decision, D15); `dsar:manage` (TIGHTLY HELD: Super Admin plus designated staff only, M15); `viewas:use` (read-only view-as, Support + Super Admin only, M16); `comms:read` + `comms:send` (the comms log reader and the template-based consent-checked one-off send, M7 / 7.1, per D61); `task:assign` (the D59 assign action; task CREATE and CLAIM are open to every role, M8); `lead:manage` (the prospect pipeline, M4; held by the Sales / Lead-owner persona alongside Operations and Super Admin, noting SALES is not in the role enum today); and `health:read` (the Account Health watchlists, M9). The D60 member-PII tiering builds on the proposed `customer:read` / `customer:read-pii` split (sees-by-default vs reveal-with-reason vs no-access; every access audited; see 6.2). The exact per-role mapping is set with the capability matrix (illustrative in the prototype); per-person grants stay FUTURE (blocked on `AdminCapabilityGrant`, as above).

### 3.3 Grant, step-up and view-as: OUTCOMES only (all verified-absent, net-new, gated)

- **Step-up / re-auth** before destructive actions: absent (grep zero). Represent the OUTCOME (a re-authentication gate on suspend, identity edit, category change discarding RMVs, branch/document delete, reversal, bulk) without locking httpOnly/BFF/cookie mechanism. Admin session/auth architecture is a separate security decision.
- **Maker-checker / four-eyes** on a single mutation: absent. The shipped `AdminApproval` submit/approve split has **separate capabilities and workflow stages, but is NOT role/actor separation, maker-checker or four-eyes**: both `merchant:submit` and `approval:action` are granted to OPERATIONS (`capability.ts`) with no submitter-vs-approver service guard, so one OPERATIONS admin can currently submit, claim and approve the same item. Different-actor countersign is NET-NEW; do not describe the current split as role separation or dual-control anywhere in this blueprint.
- **Impersonation / view-as:** absent; the one `impersonate` string in source is an anti-forgery warning (`customer/plugin.ts:19`). Model only a **read-only, fully-audited view-as**; never write-impersonation. GATED (security). Seven locked guardrails (Wave-2 prototype, M16): (1) NEVER impersonation: a server-side read-projection through the admin's own authenticated session, never session-token impersonation; (2) absolute read-only (every mutating control disabled); (3) reason required to start, with start and end audited; (4) a 30-minute time-box plus an end-now control; (5) a permanent always-visible banner (viewing-as + read-only + ends-at); (6) the D60 PII tiering still applies inside the projection (view-as is not a side door to data the role could not see); (7) `viewas:use` held by Support + Super Admin only.

### 3.4 Governance early, without approving its schema

Audit, admin-user management (the Team and Roles surface, reframed per D58: these are Redeemo STAFF who hold panel access, NOT the merchant-side "Staff and access", and the panel is NOT the HR system) and permission oversight may appear early in the prototype because powerful operational tools require governance. This does not approve their implementations or schemas. Their substrate today: enforcement is solid; admin-user CRUD, bootstrap, `AdminCapabilityGrant`, the richer staff record (D58), the combined-access model (D57) and a global audit query endpoint are all net-new (Section 12, Section 13).

---

## 4. Information architecture and global shell

### 4.1 Proposed navigation groups (to validate during IA design; not an evolution of the current two-item nav)

Rationale: operations-first (the daily work surface); trust and safety separated so every intake has an outlet; relationships (Merchant 360 / Customer 360) as first-class; the customer-commercial revenue engine (members, subscriptions, revenue) grouped as its own top-level domain because subscriptions are the platform's revenue, not a merchant-supply concern; merchant-paid commercial (campaigns, featured, trending) grouped behind one billing gate; governance visible early; gated and future surfaces honestly labeled.

1. **Operations:** Ops Home (role-aware) · Approvals / Work Queues · Merchant Directory · Leads and Onboarding.
2. **Relationships:** Merchant 360 · Customer 360 · Communications · Tasks and Follow-ups · Account Health.
3. **Trust and Safety:** Reviews Moderation · Fraud and Redemption Reversals · Media/Photo Review · Suggested-tag Moderation.
4. **Support and Cases:** Case queue · DSAR / Data Requests · read-only View-as (audited).
5. **Members and Revenue:** Members and Subscriptions · Revenue and Billing · Promo Codes and Trials · Redemptions and Engagement (aggregate) · Conversion.
6. **Growth and Commercial:** Campaigns · Featured Placement · Trending.
7. **Content and Taxonomy:** Taxonomy management · Versioned Legal/T&C/FAQ · Announcements.
8. **Insights and Reporting:** platform analytics (reuse Insights foundation as reference).
9. **Platform:** Admin Users and Roles · Global Audit / Activity Explorer · Operational Status · Feature flags / config · Notifications / bell.

**Reorg (owner-decided 2026-07-02, see Section 16 D27):** "Subscriptions / Billing / Refunds" and "Promo Codes" moved OUT of "Growth and Commercial" INTO the new "Members and Revenue" group; "Growth and Commercial" now holds only the merchant-paid items (Campaigns, Featured Placement, Trending). Rationale: merchants are the supply and members/subscriptions are the revenue, so the customer-commercial surfaces belong in one revenue-engine domain rather than mixed with merchant-paid placement. **Customer 360 STAYS in the Relationships group** (symmetry with Merchant 360); the Members and Subscriptions list in group 5 cross-links into it.

The current console's two nav items (Approval queue, Merchants) relocate into Operations and Relationships; the queue-first homepage is superseded by a role-aware Ops Home. The mockup's hollow "Branches / Audit log / Settings" nav-with-no-screen is the anti-pattern to avoid.

**Cross-cutting: operational-commercial vs behavioural/demographic (the customer split).** Member, subscription and revenue data splits into two postures that must never be conflated: (a) OPERATIONAL-COMMERCIAL data (member base by tier, subscription lifecycle, MRR/ARR, past-due, promo/trial funnel, aggregate redemption engagement, conversion) is surfaced richly in "Members and Revenue"; (b) BEHAVIOURAL and DEMOGRAPHIC analysis of members (where members are from, age/gender distribution, interest cohorts, retention cohorts) stays DPIA-gated in "Insights and Reporting" (default-off, minimum-cohort suppression, "not available" until the privacy gate opens). Never surface (b) as operational, and never let a "Members and Revenue" surface answer a demographic or behavioural-cohort question; those route to the gated Insights surface.

### 4.2 Global shell behavior

Role-aware landing (Ops Home) instead of a redirect; capability-filtered navigation (a persona sees only what it holds); a global top bar with search, notifications/bell (built; reuse), actor identity and role, logout; a consistent detail/workspace pattern for 360 surfaces; denial states for every gated route; responsive behavior (the current tables are desktop-only, a DoD gap).

**List-vs-workspace distinction (D40).** The Merchant DIRECTORY (M3, Operations group) is the LIST of all merchants (the supply-window per D39). MERCHANT 360 (M5, Relationships group) is ONE merchant's deep WORKSPACE. They are different surfaces and must not be conflated. Merchant 360 is reached by: drilling in from the Directory; from global search; from a review item; OR from the Merchant 360 nav-entry itself. **The Merchant 360 nav-entry must NOT be a dead link:** when no merchant is selected, it lands on a PICKER (search + recently-viewed) so the operator can choose a merchant, rather than a hollow no-screen (the "Branches / Audit log / Settings" nav-with-no-screen anti-pattern noted in 4.1). Customer 360 follows the same picker-when-unselected pattern by symmetry.

### 4.3 Maturity annotations vs live status (correction, binding)

The tags `ENGINEERED / PARTIAL / GATED / FUTURE / EXTERNAL` are **blueprint and prototype-review annotations**. Do not assume they appear as badges throughout the final Admin UI. Distinguish three orthogonal axes:

- **Implementation maturity** (a design/review annotation): ENGINEERED / PARTIAL / GATED / FUTURE / EXTERNAL.
- **Feature availability / configuration** (operator-facing where useful): whether a feature is enabled/configured (e.g. email dark, storage off).
- **Live operational status** (operator-facing, first-class UI): healthy / degraded / failed / pending / approved, for real entities and jobs.

Show user-facing availability or gating copy only where it helps an operator understand why something is unavailable or needs configuration (for example a disabled action with "requires a signed contract", or a panel reading "email delivery is not enabled in this environment"). Maturity annotations otherwise stay in this document and the review process, not the shipped chrome.

Status-state definitions used by this blueprint: **ENGINEERED** (built and merged), **PARTIAL** (some parts built), **GATED** (will be an admin surface, blocked; sub-typed gated-by-decision vs gated-by-dependency), **FUTURE** (designed, not started, no blocker beyond sequencing), **EXTERNAL** (deliberately not an in-app admin surface: the panel links out or defers to code/owner).

---

## 5.0 The five boundary contracts

Each contract is a bright line with an IN list and an OUT list; every relevant module (Section 5) and every relocation row (Section 12, including 12.2, 12.3 and 12.4) names its governing contract. Row tags: REUSE (existing substrate), NET-NEW (build), GATED (owner/security/legal/provider), EXTERNAL (out of the admin app by design). Two clarifications used throughout: BC-3 also governs single-actor high-risk mutations (its safeguards envelope of dry-run + step-up + maker-checker + per-row audit + reversibility applies wherever a destructive mutation needs guarding, not only bulk); and BC-5 is the default governing contract for platform-governance and relationship surfaces that have no external-CRM alternative (for example Admin Users and Global Audit).

### BC-1: Bounded Admin operational status vs external APM/provider telemetry

| IN (bounded in-app status, existing signals) | OUT (external only, link out, never fake) |
|---|---|
| Outbox delivery health: `CommunicationLog GROUP BY status` (QUEUED backlog / FAILED / BOUNCED) [REUSE, read-side NET-NEW] | Server CPU/memory/disk, p99/p50 latency, uptime %, error-rate histograms [EXTERNAL] |
| Queue depth / failed-job counts via BullMQ `getJobCounts()` (never called today) [read-side NET-NEW] | Container restarts, Neon pool internals, Redis/Postgres server metrics, distributed traces [EXTERNAL] |
| Config/feature-flag health (`EMAIL/STORAGE/MODERATION/CAPTCHA_ENABLED`, `RESEND_WEBHOOK_SECRET` presence) [REUSE] | Railway/Vercel deploy status and host dashboards [EXTERNAL: deep-link] |
| SMS/email spend vs cap (Redis global-daily counter vs `globalDailyCap()`) [REUSE] | |
| Key-rotation parity + per-service last-seen (`KeyringFingerprint`) [REUSE] | |
| Stale-claim count (indexed `AdminApproval` predicate); webhook recency (`StripeWebhookEvent`, `CommunicationLog.externalId`) [REUSE] | |

The IN column is an explicit allow-list of bounded, redacted, capability-gated status projections (the rows above), NOT "anything a DB/Redis/env read can answer". Hard rules: never expose secret values, credentials, or connection strings, and no unrestricted environment reads; no arbitrary or unbounded DB/Redis queries; configuration status may surface only approved booleans or status labels (for example `EMAIL_ENABLED` on/off), never the underlying secret. The existing config/env, Redis counters, and `KeyringFingerprint` rows are substrate; the Admin status read API and UI are NET-NEW (bounded projections over that substrate). Provider/APM telemetry remains external. GATED (needs new persistence): an Incident/StatusEvent model, historical time-series, ack/mute, and the three reserved alert emitters (`ADMIN_DELIVERY_FAILED`, `ADMIN_OWNER_EMAIL_BOUNCED`, `ADMIN_REVIEW_ASSIGNED`). Evidence: `src/api/app.ts:117` (`/health` returns a literal, no dependency roll-up); `outboxReconciler.ts`, `claimStaleSweep.ts` (compute counts, discard them); `smsLimiter.ts`; `KeyringFingerprint`.

Five-layer classification (apply to every provider and status signal; keep the layers separate: source wiring is not runtime activation). For each, distinguish: (1) source capability (ABSENT / SCAFFOLDED / WIRED / ENGINEERED in the codebase); (2) configured capability (BOOT-REQUIRED config, FEATURE-GATED / dark-by-default, OWNER-CLI-only, or SCAFFOLDED / deferred; a required secret is configuration, not proof a provider is active); (3) deployed runtime state (VERIFIED or UNVERIFIED; not inspected under the active provider / no-use holds, never inferred from source or from a historical probe); (4) provider / account state (VERIFIED, OWNER-REPORTED, or UNVERIFIED; label owner-reported where applicable); (5) Admin observability maturity (REUSABLE SIGNAL, READ-SIDE NET-NEW, or EXTERNAL). Infrastructure state stays separate from application-consumer state: an instance (for example Redis) can be up while its consumer (the worker) is down; Neon project / control-plane, branch / compute, and application binding are distinct axes. Google Places is OWNER-CLI-only (consumed by `prisma/suggest-branch-pin.ts`, not by the deployed API); its local `.cache` usage file is not directly reusable by a deployed Admin surface, so any central Admin visibility is EXTERNAL or READ-SIDE NET-NEW until a safe ingestion source is designed. M22 must render dynamic states (healthy / degraded / down / unverified / external / gated / unavailable-not-built), never a hardcoded snapshot of any one day; transient deployment facts belong in the evidence / freshness appendix (Section 17), not the operating model. Preserve, explicitly: `/health` proves process liveness only; DB / Redis dependency health is NOT established by `/health` (net-new); APM / error monitoring is absent (external when adopted); deployed-SHA / version reporting is absent (net-new); the queue / outbox / keyring / notification substrates exist but several Admin read APIs are net-new; raw logs, unrestricted queries, environment values, secrets and provider telemetry remain outside Admin; Admin may show bounded status, freshness, responsible role, runbook and safe provider deep links, and configuration status may expose only approved booleans or bounded labels, never values, secrets or connection strings.

### BC-2: Specific versioned content/taxonomy controls vs generic CMS

| IN (specific Redeemo controls) | OUT / EXTERNAL (generic CMS, out of scope) |
|---|---|
| Taxonomy management: Category/Tag/SubcategoryTag/Amenity/CategoryAmenity/MerchantHighlight/RmvTemplate/Interest CRUD + activation [NET-NEW UI over existing seed models] | Blog / page-builder / marketing CMS [OUT] |
| Taxonomy AND RULES module (D50 / D54): the categories / subcategories / cuisines / specialties / tags PLUS their dependent content (per-category voucher suggestion chips + per-type examples, the category-to-term caution flags, amenity-per-category eligibility via `CategoryAmenity`, and other category / subcategory-dependent config) [NET-NEW schema + backend: today ALL of it is HARDCODED in code and read by BOTH the merchant-portal voucher builder AND the admin assisted builder; making it admin-managed means promoting hardcoded config to DATA with one source of truth feeding both surfaces] | TRANSACTIONAL email template editing (templates are code, read-only in the panel per D63: they carry security-critical content such as reset links and branch PINs) [EXTERNAL]; marketing templates become panel-editable LATER via the D55 module, not a generic CMS |
| Suggested-tag moderation (`MerchantSuggestedTag`) [NET-NEW on BOTH intake and outlet: no create-path, no approve/reject path today] | Merchant agreement legal text (external `redeemo.co.uk/merchant-terms`) [EXTERNAL] |
| Agreements, Policies and Content module (D55): the Redeemo Merchant Agreement plus other contracts / policies / legal content shown across the customer app, customer website and merchant portal, version-controlled, so edits happen from the admin panel [GATED + NET-NEW: today the agreement text is placeholder / MVP, the real binding legal text is owner / legal-owned and is a launch sign-off GATE, the static customer-web legal pages are the launch source of truth, and `CmsContent` is unwired / deferred; D55 = that `CmsContent` path made real, versioned and surfaced across the three product surfaces = substantial net-new backend] | |
| Announcements / notices [NET-NEW schema; no model today] | |
| Versioned legal/T&C/FAQ + effective-date [GATED: static customer-web JSX is the launch source of truth; owner/legal sign-off is the hard launch gate] | |

Critical flags: `CmsContent` is UNWIRED by runbook §12 decision (sole writer is the seed) and is not the legal source of truth; `RmvTemplate` has `isActive` but **no `version` field** (add version where versioning is required); `TermsClause`/`VoucherTermsClause` (curated voucher-terms library) are DESIGN-ONLY. Two owner-decided admin surfaces extend this contract (both PARKED as their own surfaces, both net-new): the **Taxonomy and Rules management module (D50 / D54)** promotes the hardcoded category config map + term flags + `CategoryAmenity` seed (top-level granularity only) to admin-managed DATA with one source of truth feeding both the merchant-portal and admin assisted voucher builders; it pairs with the existing Suggested-tag Moderation (management = the create side, moderation = the review side), and its "operator add-your-own" policy (immediate vs route-to-moderation vs hybrid) is an OPEN question to settle when it is designed (taxonomy is platform-wide, so lean controlled). Until it exists, the assisted flow's "add your own" subcategory / cuisine / specialty stays deferred-labelled. The **Agreements, Policies and Content module (D55)** is the `CmsContent` path made real and versioned across the three product surfaces (net-new backend), alongside `MerchantContract.tcVersion` (the agreement version the assisted flow references); it PARKS alongside the D50 / D54 taxonomy module. D50 and D54 are the SAME taxonomy module (D54 expands D50, adding the dependent content: suggestion chips, per-type examples, category-to-term flags and amenity eligibility). See D50 / D54 / D55 in Section 13 and Section 16.

### BC-3: Safeguarded bulk workflows vs unsafe mass operations

| IN (future safeguarded bulk) | OUT (unsafe mass operations) |
|---|---|
| Scoped selection (built on the single-winner conditional-claim idiom) [REUSE pattern] | Client-supplied id list or CSV import with no scoped selection [OUT] |
| Server-computed preview / dry-run diff (before/after) [NET-NEW] | No preview / no dry-run [OUT] |
| Step-up re-auth [NET-NEW, security-gated] | No re-auth [OUT] |
| Maker-checker countersign by a DIFFERENT admin [NET-NEW; the existing submit/approve split is separate capabilities/stages but NOT role/actor separation: both caps are OPERATIONS-held with no guard, so one admin can submit + claim + approve] | Single-actor irreversible mass mutate [OUT] |
| Per-row transactional audit (`writeAuditLogTx`) [REUSE] + rate/quota bound (`atomicLimiter`) [REUSE] + reversibility where the before-snapshot allows [data REUSE; revert engine NET-NEW] | No per-row audit, no bound, no reversibility [OUT] |

Verified: zero bulk-mutate routes today (every `updateMany` targets a single guarded entity); step-up and maker-checker absent; before/after audit exists but no revert engine.

### BC-4: Merchant/branch coverage vs prohibited customer-home mapping (three states)

| ALLOWED (business locations, anonymized supply) | GATED (regional customer aggregate) | PROHIBITED (customer home) |
|---|---|---|
| Branch/merchant pins via `Branch.latitude/longitude/localityId` [REUSE] | Members-per-locality or any customer-derived regional aggregate: inherits the DPIA fail-closed posture (`insights/gate.ts`); needs a DPIA + minimum-cohort suppression [GATED] | Plotting individual customers by home: `User.addressLine1/2`, `postcode`, `latitude/longitude`, `localityId` [PROHIBITED] |
| Anonymized regional supply: branch-counts-per-Locality, coverage-per-Market, category-coverage-per-region, supply-vs-`Market.targetMerchantCount` [NET-NEW view over existing substrate] | | The legacy `admin-panel-spec.docx` "map all registered customers by home address" pattern is explicitly struck [PROHIBITED] |

The raw customer geo fields exist in schema, which is exactly why the prohibition must be explicit and named in Section 1.3 and enforced in Section 6.2. Any customer address/coordinate view must be reveal-on-demand and audited, never plotted.

### BC-5: Native Redeemo relationship operations vs external general-purpose CRM

| IN (Redeemo must own: needs live platform state or in-product side-effects) | OUT / INTEGRATE [EXTERNAL] (candidate for Zoho/HubSpot/Salesforce) |
|---|---|
| Platform-linked merchant/customer onboarding and lifecycle operations [REUSE + NET-NEW] | Broad generic sales automation, deal-stage forecasting before a Merchant row exists [EXTERNAL] |
| Support and cases that must read/write platform state [NET-NEW] | Generic enterprise CRM customization [EXTERNAL] |
| Tasks/follow-ups triggered by platform state (the `lastStaleAlertAt` pattern) [NET-NEW; reference pattern REUSE] | Unrestricted mass marketing automation / nurture sequences [EXTERNAL] |
| Relationship context, account health derived from platform metrics (redemptions, subscription status, review/supply) [NET-NEW] | Pre-platform prospect contact management; conversational helpdesk/ticketing [EXTERNAL] |
| Audited platform actions; consent-aware communications; interaction history composed on `AuditLog + CommunicationLog + Notification` [REUSE] | |

The line: if a CRM concept must read live redemption/subscription/approval state or write an in-product action, Redeemo owns it; if it is pre-platform prospecting, nurture, or conversational support, it is a build-vs-integrate decision. CLAUDE.md Open Decisions records the "Zoho One for CRM + contracts + helpdesk alongside the custom platform (not instead of it)" direction, so support-ticketing and general CRM are an explicit build-vs-integrate gate, not an assumed in-house build.

---

## 5. Module / domain catalogue

Each module: purpose, key sub-modules/screens, primary personas, status, governing boundary contract(s), dependencies. States are inventoried in Section 11.

| # | Module (group) | Purpose | Status | Boundary | Dependencies / notes |
|---|---|---|---|---|---|
| M1 | **Ops Home** (Operations) | Role-aware landing: work summary, launch-readiness/supply signal, alerts. | FUTURE (net-new); replaces `redirect('/queue')` | BC-1 (status), BC-4 (supply) | Supply signal reuses coverage substrate; behavioural KPIs are GATED. |
| M2 | **Approvals / Work Queues** (Operations) | Unified queue over onboarding/voucher/edit/branch-lifecycle plus new intakes (reviews, suggested tags). Two-court split (D37): "Needs you" (the operator's court) vs "Awaiting merchant" (the merchant's court). Correction-on-behalf lets an operator author a fix in place before approving (COSMETIC applies directly; MATERIAL routes to the merchant); per-item audit history rolls into the Global Audit. | ENGINEERED (relocate + redesign; two-court tabs + correction event types are NET-NEW) | BC-3 (if bulk added) | Claim-to-act, urgency, freshness all built. Two-court queue + correction-on-behalf per D37 (6.1.4). |
| M3 | **Merchant Directory** (Operations) | The operator's window into the merchant SUPPLY base (D39): is supply healthy, growing and being used, and where are the gaps. Supply-health summary + enriched table + three views (table / UK supply map / group-by) + richer filters + Export CSV + a gated message-a-segment bulk action. Entry to Merchant 360. This is the LIST of all merchants (distinct from Merchant 360, which is one merchant's workspace; see M5 and 6.2). | ENGINEERED core (relocate + redesign); supply-health roll-ups / activity-dormancy / renewal / map / export / bulk-message are NET-NEW | BC-4 (map: business locations only, never customer homes), BC-5 | `branchCount` counts soft-deleted (known follow-up). Supply-window redesign per D39 (see 6.2.1). |
| M4 | **Leads and Onboarding** (Operations) | Both onboarding channels; lead qualify to draft to handoff. A pre-draft PROSPECT PIPELINE (stages Lead / Contacted / Visit booked, plus a Lost-with-reason audited exit) sits on the existing hub ahead of the two admin-created routes (Wave-2 prototype; see 8.2). | PARTIAL (self-serve + create-draft built; lead pipeline net-new) | BC-5 | `MerchantLead`/`MerchantSource` design-only (the lead model is entirely net-new). Lead management = the Sales / Lead-owner persona alongside Operations and Super Admin (proposed `lead:manage`); the customer-app "Request a merchant" stub is a FUTURE intake. |
| M5 | **Merchant 360** (Relationships) | One merchant's deep WORKSPACE (Section 6.1), distinct from the Directory LIST (M3). 12-tab workspace: Overview (primary-contact block, D47), Business identity, Branches (staffing + notification routing D45 + PIN reveal / reset D46), Vouchers, Performance (enriched aggregate, D42), Redemptions (individual + role-gated + lookup, D43 / D48), Documents, Staff and access (on-behalf, D44), Comms, Commercial, Audit, Insights. Edit-on-behalf per the correction model (D37) + the D38 reclassification + the operator-parity-on-behalf principle (D44). Customer-facing banners (merchant cover + per-branch banner / logo) are SHOWN on Business identity + Branches (D40). Nav-entry lands on a picker when no merchant is selected (never a dead link). | PARTIAL (rich read substrate; many admin actions net-new: Performance roll-ups, per-merchant Redemptions viewer + lookup, on-behalf staff management, PIN reveal / reset, branch notification routing) | BC-5, BC-2, BC-4, BC-3 | Cross-check table Section 6.3. List-vs-workspace + banners + nav-entry per D40 (4.2 / 6.2); Performance / Redemptions / Staff-and-access / routing / PIN / contact per D42 to D48 (6.1.6). |
| M6 | **Customer 360** (Relationships) | Full customer workspace (Section 6.2). | FUTURE (read substrate rich; zero admin surface today) | BC-4, BC-5 | PII/DPIA-heavy; Tier 3. Identity / contact visibility follows the D60 tiering (6.2); read-only for messaging per D61 (7.1). |
| M7 | **Communications** (Relationships) | Per-entity comms/interaction timeline; delivery monitoring; the SENDING HOME per D61 (four-channel honest status, template library, template-based consent-checked one-off send; see 7.1). | PARTIAL (per-merchant timeline built; the log reader / template library / one-off send are read-and-send-side net-new over the REAL outbox; broadcast net-new) | BC-1, BC-5 | Reuse timeline pattern (payload never selected; erased at terminal state). `comms:read` / `comms:send` net-new; D63 template ownership split; D64 planned automated sends. |
| M8 | **Tasks and Follow-ups** (Relationships) | Assignable tasks, reminders, next actions. Realises and extends the D59 assign-then-claim workflow (assignment ROUTES, claim COMMITS). | FUTURE (task model entirely net-new schema) | BC-5 | Reference patterns: claim, `lastStaleAlertAt`. Claim semantics IDENTICAL to the approval queue (claim = acceptance + ownership); assigned-but-unclaimed items over 24h raise a stale flag mirroring the real 24h stale-claim sweep; `task:assign` gated, create / claim open to every role. |
| M9 | **Account Health** (Relationships) | Merchant/customer health and risk signals: EIGHT net-new watchlists derived live over REAL data (customer: dormant members, payment-failed PAST_DUE, cancel-at-period-end pending losses, trial-ending; merchant: declining redemptions, no active vouchers, stale voucher content, no portal logins), each with its derivation stated inline. | FUTURE (net-new; Redeemo-owned; no stored health score) | BC-5 | Source signals Redeemo-only; build in-house. Thresholds are ADJUSTABLE CONCEPTS pending a product decision; row actions route to Tasks (pre-filled create-follow-up, M8) and Communications (consent-gated template send, M7); NO automated sending; `isTestData` excluded; `health:read`. |
| M10 | **Reviews Moderation** (Trust and Safety) | Action reported AND auto-flagged reviews; hide/resolve. Single outlet for the D62 hybrid three-net screening model (hard-block at submission; auto-flag borderline into this queue; customer reports). | FUTURE (report data exists, no consumer; the D62 screening engine is net-new backend in the review-system v2 workstream) | BC-2 | `ReviewReport`/`isReported` dead-ended today (this queue is the outlet). Reporter identity stays MASKED for every role (reason-gated reveal only; retaliation-risk data-minimisation); reviewer identity follows D60. Pre-moderation explicitly rejected (D62); `review:moderate` net-new. |
| M11 | **Fraud and Redemption Reversals** (Trust and Safety) | Fraud telemetry review; reverse/void a redemption. | FUTURE (the signals feed is the FIRST read outlet over real capture, read side net-new; reversal is schema-gated) | BC-3 (reversal); telemetry review is a T&S read surface | `RedemptionScreenshotEvent` is REAL + LIVE capture with no consumer until this surface (proposed `fraud:read`); reversal needs schema (`redemption:reverse`, held separately from the read); the `Review.redemptionId` verified-review coupling (does a reversed redemption strip the Verified badge?) is an OPEN decision to settle when reversal is designed. |
| M12 | **Media/Photo Review** (Trust and Safety) | Review and apply branch media changes. | ENGINEERED apply lane (relocate); scanner + count-cap net-new | BC-2 | Photo apply built (PR #313); scanner is a stub, bypassed by design. |
| M13 | **Suggested-tag Moderation** (Trust and Safety) | Approve/reject merchant-suggested tags. | FUTURE (net-new BOTH intake and outlet; the model is REAL in the middle) | BC-2 | `MerchantSuggestedTag` model REAL; discovery consumption of `status=APPROVED` REAL + LIVE (approving has an immediate customer-facing search effect); intake AND moderation outlet net-new (proposed `tag:moderate`); pairs with the D50 / D54 Taxonomy and Rules module (management = create side, moderation = review side). |
| M14 | **Support and Cases** (Support and Cases) | Case queue and lifecycle for BOTH customer and merchant cases from all three intake surfaces once live (with a per-case "Raised via" value). | FUTURE / build-vs-integrate (an explicitly RECORDED open decision: build the in-platform `SupportTicket` model vs integrate Zoho Desk with panel deep-links; no winner chosen, D15) | BC-5 | `SupportTicket`/`MerchantRequest` frontend-stub-only; Zoho direction. Three-surface intake honesty: the customer-app Get-help entry has a REAL six-topic dropdown but submission is UNWIRED (Coming-soon stub, Sub-PR 2); the merchant-portal /help route is an honest placeholder; customer web has NO support form at all (known gap). Nothing files a ticket today. |
| M15 | **DSAR / Data Requests** (Support and Cases) | Data-subject request intake, export, erasure, SLA. The surface is a PROCESS PREVIEW pending legal review (identity-verification approach itself a legal-gated design decision). | FUTURE / GATED (legal) | BC-4 | Self-serve deletion is REAL and complete (anonymise-in-place; admin-initiated erasure would REUSE it, prefer self-serve); everything admin-side is net-new (no request tracking, no export-bundle generator, no rectification workflow). One-month statutory deadline handling designed in (countdown, overdue flag, two-month complex-case extension, refusal-must-cite-grounds); `dsar:manage` TIGHTLY HELD (Super Admin + designated staff only). |
| M16 | **View-as (read-only)** (Support and Cases) | Audited read-only lens on a merchant/customer account. | GATED (security) | BC-5 | Never write-impersonation. Seven locked guardrails (see 3.3): never impersonation (server-side read-projection); absolute read-only; reason + audit on start and end; 30-minute time-box + end-now; permanent banner; D60 PII tiering still applies; Support + Super Admin only (`viewas:use`). |
| M17 | **Campaigns / Featured / Trending** (Growth and Commercial) | Merchant-paid commercial operations and placement control. | FUTURE / GATED (provider/billing) | BC-1 (payment health) | Models exist, no admin surface; `FeaturedMerchant` discovery does not gate on `paymentStatus` (integrity fix). Promo Codes and Subscriptions/Billing/Refunds relocated to Members and Revenue (M25 to M29) under D27. |
| M18 | **Content and Taxonomy** (Content and Taxonomy) | Taxonomy CRUD; versioned legal/FAQ; announcements. | PARTIAL / GATED / EXTERNAL (see BC-2) | BC-2 | `CmsContent` unwired; legal external. |
| M19 | **Insights and Reporting** (Insights and Reporting) | Platform analytics reusing Insights foundation as reference. | FUTURE (NOT BUILT; foundation is a reference) | BC-1, BC-4 | Operational aggregates un-gated; behavioural DPIA-gated; separate authz/privacy/architecture review. |
| M20 | **Team and Roles** (Platform; formerly "Admin Users and Roles", reframed per D58) | Onboard / invite / deactivate Redeemo STAFF who hold admin-panel access; role assign; bootstrap; grant outcomes; basic staff record (name, work email, role, job title, department / team, manager, start date, employment type, plus optional preferred name / work phone / staff ID / working location / invited-by + invited-date). DISTINCT from the merchant-side "Staff and access" (M5, D44). Shows the combined-access FUTURE concept (D57) and the assign-then-claim Wave-2 workflow (D59) as clearly-labelled concepts. | FUTURE (enforcement built; CRUD / grant / bootstrap net-new; the richer staff record is net-new schema) | BC-5 | `AdminCapabilityGrant` design-only; the panel is NOT the HR system (fuller HR data stays in the HR tool, e.g. Zoho People); REAL fields = name / work email / role / status (isActive) / last-active (lastLoginAt) / account-created; the richer staff fields are NET-NEW. See D57 / D58 / D59. |
| M21 | **Global Audit / Activity Explorer** (Platform) | Query audit by actor/entity/event across the platform. | FUTURE (net-new; only per-merchant timeline exists) | BC-3 | Sequence EARLY (oversight backstop). |
| M22 | **Operational Status** (Platform) | Bounded in-app status (BC-1); five-layer source / config / runtime / account / observability classification; dynamic states (healthy / degraded / down / unverified / external / gated / unavailable). | PARTIAL (substrates + REUSABLE signals exist; the Admin read APIs and panels are net-new) | BC-1 | v1 bounded read panels + per-panel responsible-role + runbook / provider deep-link; Incident model, history, ack/mute, worker-heartbeat, dependency-health, deployed-SHA/version are net-new; today's Web-down state is an evidence snapshot (Section 17), not the design. |
| M23 | **Feature flags / config** (Platform) | Read/observe config and gate state (approved booleans / labels only, never values or secrets). | FUTURE (minimal); config is env-only today (no config table), read-side net-new | BC-1 | Toggle-in-UI is a later decision. |
| M24 | **Notifications / bell** (Platform) | Personal admin alert feed; preferences. | ENGINEERED bell (relocate); preferences net-new | BC-1 | Three reserved alert enums unemitted (`ADMIN_DELIVERY_FAILED`/`_OWNER_EMAIL_BOUNCED`/`ADMIN_REVIEW_ASSIGNED`). |
| M25 | **Members and Subscriptions** (Members and Revenue) | Member base by tier (Free = no active subscription / Monthly GBP 6.99 / Annual GBP 69.99); subscription lifecycle over the real status enum; searchable member list drilling into Customer 360. | FUTURE (read substrate rich; zero admin surface today) | BC-4, BC-5 | Real subscription/member data; admin read-layer is net-new. Free tier is the ABSENCE of a `Subscription` row (no `Free` plan row). Lifecycle = `SubscriptionStatus` (TRIALLING / ACTIVE / PAST_DUE / CANCELLED / EXPIRED). PII/DPIA discipline of M6 applies. Row identity AND member search respect the D60 tiering (search results are not a side door to PII a role cannot see; list rows never carry address / DOB / sensitive fields, the full record lives on Customer 360). |
| M26 | **Revenue and Billing** (Members and Revenue) | MRR/ARR derived from active subscriptions x plan price; new vs churned; past-due / dunning view. | FUTURE (MRR/ARR derived + read-side net-new); refunds/chargebacks/richer-dunning/payment-source-breakdown/admin-grant are gated-future | BC-1 (payment health) | No revenue table or aggregation exists today; MRR/ARR/churn are computed read-side from active subs x `SubscriptionPlan.priceGbp` x interval. `Subscription.source` enum (STRIPE/APPLE/GOOGLE/ADMIN) is PLANNED, not built (Stripe only today); admin-grant commented, not built. Refunds, chargebacks and dunning beyond PAST_DUE are NOT built (no `RefundLog`/`DisputeLog`). |
| M27 | **Promo Codes and Trials** (Members and Revenue) | Promo management (code, discount, uses, expiry, Stripe coupon) + trial funnel (Trialling). | PARTIAL (PromoCode + Stripe-coupon -> TRIALLING is real; admin management UI net-new) | BC-1 | `PromoCode` (`code`, `discountType` PERCENTAGE/FIXED_GBP, `discountValue`, `maxUses`, `usesCount`, `expiresAt`, `isActive`, `stripeCouponId`) is real; the Stripe coupon path yields TRIALLING subscriptions. Relocated from the old M17 commercial cluster (D27). |
| M28 | **Redemptions and Engagement (aggregate)** (Members and Revenue) | Redemptions per period, active-member rate, redemption rate, total member savings, spread across merchants. AGGREGATE ONLY; never an individual customer identity in a row (this is the aggregate half of the D48 split; INDIVIDUAL redemption identity lives on the role-gated + always-audited per-merchant Redemptions tab / lookup, M5 / 6.1.6, NOT here). | FUTURE (data real; aggregation net-new) | BC-4 | `VoucherRedemption` (`estimatedSaving`, `isValidated`/`validatedAt`, `isTestData`) exists; savings are aggregated on-the-fly (no snapshot table). Per-period aggregation is net-new. Redemption rows have NO customer-name field (identity only via `User` join); the aggregate posture and Insights eligibility cleanliness (exclude `isTestData`, QA emails, DELETED) apply. Per D48: aggregate surfaces (this module) never show identity; individual identity is role-gated (`redemption:view-customer` / `redemption:reveal-customer`) and always audited. |
| M29 | **Conversion** (Members and Revenue) | Registered-to-subscribed funnel (top-line). | FUTURE (derived, net-new) | BC-4 | Derived read-side from `User` count vs active-`Subscription` count; behavioural/demographic breakdowns of conversion stay DPIA-gated in M19. |

Cross-cut for M25 to M29: they realise the customer-commercial revenue engine (Section 4.1 group 5). They inherit the operational-vs-gated customer split (Section 4.1): OPERATIONAL-COMMERCIAL member/subscription/revenue data lives here; BEHAVIOURAL/DEMOGRAPHIC member analysis stays DPIA-gated in M19. Customer 360 (M6) stays in Relationships and is cross-linked from M25.

---

## 6. Merchant 360 and Customer 360

### 6.1 Merchant 360 workspace

A full workspace, not a directory row or an approval record. Panes and their substrate:

| Pane | Substrate today | Status |
|---|---|---|
| Business / account profile + legal/commercial identity | `Merchant` (SENSITIVE vs DIRECT field split, `profile/service.ts:8/16`) | ENGINEERED read; edit per authority matrix |
| Owner, accountable signatory, contacts | `MerchantMembership` (OWNER), `MerchantAdmin` | ENGINEERED read (owner/contacts); accountable-signatory identity/authority-to-bind fields are net-new (see 8.4); ownership transfer net-new |
| Branches, locations, verification, hours, amenities, media | `Branch` (+`LocationConfidence`), `BranchOpeningHours`, `BranchAmenity`, `BranchPhoto` | ENGINEERED read; many admin edits NOT-YET-SUPPORTED (Section 6.3) |
| Vouchers/offers, terms, status, approval history, performance | `Voucher` (+RMV), approval lane | ENGINEERED read/approve; custom-voucher admin edit not supported |
| Redemptions and operational exceptions | `VoucherRedemption` (join via `branch.merchantId`) | Read; no admin per-merchant redemption viewer today |
| Staff, Branch Managers, invitations, roles, branch scope | `MerchantMembership`, `BranchUser` | Merchant-managed (OWNER-only); no admin management today |
| Documents, agreements, versioned acceptance/signature evidence | `MerchantDocument`, `MerchantContract` (method/version/timestamp/IP) | Docs admin-managed (SUPER_ADMIN); signature read-only; evidence thin (Section 8.4) |
| Notifications, communications, relationship timeline | `AuditLog + CommunicationLog + Notification` (timeline pattern) | ENGINEERED read (per-merchant) |
| Support cases, tasks, follow-ups | net-new (CRM, Section 7) | FUTURE |
| Internal operator notes (the Notes tab) | net-new schema (no notes model today) | BUILT in the prototype (D51); backend net-new |
| Subscription, billing, commercial context | absent both sides | FUTURE / GATED |
| Insights / reporting, account health | `merchant/insights/*` (merchant-view only) | NOT-BUILT for admin; reference-reusable |
| Lifecycle status, audit history, outstanding work | `MerchantStatus`/`OnboardingStep`/`AuditLog`/`AdminApproval` | ENGINEERED |

**Merchant 360 as a per-merchant WORKSPACE (owner-decided D40, 2026-07-02).** Merchant 360 (M5) is one merchant's deep workspace, DISTINCT from the Merchant Directory (M3), which is the LIST of all merchants (the supply-window per D39). It is reached by drilling in from the Directory, from global search, from a review item, OR from the Merchant 360 nav-entry itself (which lands on a picker of search + recently-viewed when no merchant is selected, so the nav item is never a dead link; see 4.2).

- **Tabs (the full 13-tab workspace, D40 + D42 + D43 + D44 + D51):** Overview, Business identity, Branches, Vouchers, Performance, Redemptions, Documents, Staff and access, Notes, Comms, Commercial, Audit, Insights (mapping to the panes above and the Section 6.3 authority cross-check). The Redemptions tab (D43) is the 12th tab-by-introduction, placed after Performance; the Notes tab (D51) is the internal operator-notes surface (BUILT in the prototype; net-new schema, see below and 6.1.1). Per-tab behaviour introduced by D42 to D51:
  - **Overview (D47):** carries a "Primary contact (account owner)" block: the account owner's name plus email (real, verified) plus phone (optional; show "not set" when null) with Copy / Email / Call affordances; when there is more than one owner, show the primary plus "+N more owners". Honest and schema-grounded: there is NO business-level contact email or phone (the `Merchant` record has only `businessName`, `tradingName`, `websiteUrl`); the merchant contact IS the owner / primary `MerchantAdmin` (email plus optional phone). Branch phone and email are per-branch (see the Branches tab).
  - **Business identity (D40):** shows the merchant cover banner (`Merchant.bannerUrl`, customer-facing); editing the image stays NOT-YET-SUPPORTED.
  - **Branches (D40 + D45 + D46):** each branch's own banner + logo (`Branch.bannerUrl` / `Branch.logoUrl`, customer-facing) are SHOWN (editing NOT-YET-SUPPORTED); the resolved BRANCH MANAGER + the branch's assigned app / scan logins (full staffing) + the notification recipient list + the owner-opt-in toggle (D45); and the reveal-on-demand plus reset of the branch redemption PIN (D46).
  - **Performance (D42):** aggregate-only enrichment (redemptions-over-time, active-member reach, rating trend, top vouchers, plus redemptions-by-branch, redemptions-by-voucher, savings-delivered, validation-rate with a QR-vs-manual split, and a category-performance-benchmark). NEVER shows an individual customer identity, for any role.
  - **Redemptions (D43 + D48):** this merchant's individual redemption history as rows plus a redemption-code lookup; customer identity is role-gated and always audited (D48).
  - **Staff and access (D44):** on-behalf management of portal-access members and app / branch logins (the operator-parity-on-behalf principle).
  - **Notes (D51):** the internal operator-notes surface (BUILT in the prototype; net-new schema). A note list (author + role, timestamp, body, attachments, status active | retracted) with ADD (free text + optional attachment via signed-link storage like documents; audited), EDIT (role-gated; marks the note edited and keeps the prior version in history), RETRACT (SOFT-delete for accountability: the note stays visible, marked "Retracted by X on date" with an optional reason, and is NEVER hard-deleted), and a per-note HISTORY (add / edit / retract, each with who + when). Every add / edit / retract is AUDITED to the Global Audit (M21). Role-gated by `notes:read` (view) and `notes:write` (add / edit / retract). Framing: these are internal B2B merchant notes, less sensitive than customer notes but still audited and factual / professional. Cross-ref: per-DOCUMENT operator notes are also captured on the assisted Documents step (8.2.1 step 6, operator-internal). Customer 360 notes are DEFERRED because they are personal data and DSAR-disclosable (see 6.2 and 13).
- **Edit-on-behalf (edit lanes, D37 + D56):** per the correction model (D37, see 6.1.4), the capability reclassification (D38, see 3.2 / 6.3), and the operator-parity-on-behalf principle (D44, see 6.1.6). COSMETIC corrections apply directly; MATERIAL corrections (voucher value / saving, business / trading name) route to the merchant. **D56 (Merchant 360 edit-on-behalf parity + live / pending state, PARKED-BUT-NEXT, its own focused pass):** admins / reps should be able to edit ALMOST ALL fields for EXISTING merchants from Merchant 360 (parity with the assisted-onboarding capture; this completes D44). TWO LANES: (1) admin-DIRECT (applies immediately, live, audited, no pending) for platform-owned + corrective + operational fields (category / reclassification, status suspend / reactivate, location map-confirm, PIN reset, verification / documents / notes, amenities, photo moderation); (2) merchant-CONFIRM-required (PENDING until the merchant confirms, then live) for the merchant's self-representation + commercial + legal-identity fields (legal / registered name, description, public contact, voucher offers / terms, opening hours, public branch name). STATE MODEL: each field / section is Live, Pending (the OLD value stays live and customer-visible; shown as an old-to-new diff with "awaiting merchant confirmation"), or optionally Draft; a 360-level "N pending changes" indicator surfaces the count; admin-direct fields NEVER enter Pending. GO-LIVE for the pending lane = the merchant confirms (a portal notification / their next login) OR the admin attests in person ("confirmed with owner") for portal-non-using / rep-managed merchants (mirrors the assisted contract in-person model; evidence-trailed). D56 EXTENDS the existing pending-edit model (`MerchantPendingEdit` / `BranchPendingEdit`) and the Option B applier (which shipped the merchant-to-admin approve / reject direction); D56 adds the admin-DIRECT lane plus the admin-to-merchant-CONFIRM direction. The exact field-to-lane assignment is an OWNER DECISION to lock when the pass is designed. Two-tier previews already surface in the prototype (cosmetic changes apply directly; a value or business / trading-name change routes to the merchant for approval). Real-vs-net-new: the pending-edit substrate + the Option B merchant-to-admin applier are REAL; the admin-direct lane, the admin-to-merchant-confirm direction, and the per-field Live / Pending / Draft state model are NET-NEW. See D56 in Section 13 and Section 16.
- **Customer-facing banners (D40):** the MERCHANT cover banner (`Merchant.bannerUrl`) and each BRANCH's own banner + logo (`Branch.bannerUrl` / `Branch.logoUrl`) are shown to customers in the customer app, so the operator must be able to SEE them here: the merchant cover banner on the Business identity tab, and each branch's banner + logo on the Branches tab. These are REAL fields; editing the images stays NOT-YET-SUPPORTED (see 6.3). This closes the gap where a customer-facing image was not visible to the operator reviewing or managing the merchant.

#### 6.1.6 Merchant 360 Performance, Redemptions, Staff-and-access, branch routing, PIN, and contact (owner-decided D42 to D48, 2026-07-02)

**Performance tab enrichment (D42, aggregate only).** The Performance tab keeps redemptions-over-time, active-member reach, rating trend and top vouchers, and ADDS: redemptions-by-branch (branch-attributed); redemptions-by-voucher (the full list with counts); savings-delivered (the sum of estimated savings for the period); validation-rate (validated vs total, with a QR-vs-manual method split); and a category-performance-benchmark (where this merchant sits versus category peers on redemptions-per-active-voucher, a position marker plus the category median). ALL of it is AGGREGATE-ONLY and NEVER shows an individual customer identity. Real-vs-net-new: the new roll-ups are NET-NEW (per-merchant aggregation is not built today; `VoucherRedemption` has no `merchantId` column, so per-merchant aggregation joins via `branch.merchantId` or via the voucher; savings are computed on the fly from `estimatedSaving`). The category benchmark needs cross-merchant category aggregation that is NOT built; its real inputs are this merchant's own metrics plus its category, never another merchant's detail (the same honesty as the voucher-level category value benchmark in D41). See M28 (aggregate-only redemptions and engagement) for the platform-wide equivalent.

**Redemptions tab plus individual-redemption lookup (D43, support traceability).** A new tab (the 12th, after Performance) holds this merchant's individual redemption history as rows: redemption code, voucher (title plus type), branch, redeemed date and time, validated status plus method (QR or manual) plus validated time, estimated saving, and the customer (governed by D48). Filters: by branch, by voucher, by date range, by validation status; plus a search-by-redemption-code box. A single redemption detail (drill-in) shows the full record plus a link to open Customer 360. Global-search entry: the top-bar search recognises a redemption code and surfaces a "Redemption \<code\>" result that opens the single redemption detail. The same lookup is reachable from the global search, from Customer 360, and from this per-merchant tab (this tab is the per-merchant slice). Purpose: the operator support scenario "a customer says they redeemed a voucher at this merchant; track it down." Real-vs-net-new: this is a NET-NEW admin capability (per-merchant redemption history and aggregation are aggregation-to-build; there is no admin per-merchant redemption viewer today, see 6.3). Individual redemption rows never show identity in aggregate; identity is governed by D48.

**Staff and access on-behalf management plus the operator-parity-on-behalf principle (D44).** The Staff tab becomes a real management surface (it was read-only). Principle: an operator can perform, ON THE MERCHANT'S BEHALF, the same account-maintenance a merchant does in the Merchant Portal, for merchants who cannot or will not use the portal themselves. Two clearly-separated populations mirroring the real staged identity model:

1. **PORTAL ACCESS members** (`MerchantAdmin` plus `MerchantMembership`: Owner / Branch-manager / Staff, with branch scope and a `canManageVouchers` capability), with per-row actions: Invite (via a tokenised claim link so the person sets their OWN password; the operator never sets a password; reject an email already belonging to a merchant admin); Change role or scope; Toggle can-manage-vouchers; Re-issue access link (the reset path for a member: re-sends the tokenised claim / reset link; operators never type or see a password); and Remove (soft: the record is retained, status Removed) with a last-remaining-Owner guard (cannot remove or demote the last Owner).
2. **APP / BRANCH logins** (`BranchUser` scan-and-validate staff) grouped by branch, with Reset password and Deactivate / Reactivate (mirrors what the portal supports for app users today); adding a brand-new app login is a thinner NET-NEW / limited capability.

Lane: Operations-capable and audited (Super-Admin is a superset; day-to-day merchant management is Operations, not Super-Admin-only, consistent with D38). Direct action (no merchant round-trip, because the merchant cannot act), but every action carries actor plus reason plus before / after into the per-merchant Audit that rolls up to the Global Audit (M21). Locks: NO impersonation and NO "log in as merchant"; the branch redemption PIN is never shown on this tab (PIN reveal / reset lives only on the Branches tab, D46); operators never set or see passwords; `canManageVouchers` is the only real per-member capability today (the finer per-action capability matrix is a placeholder to define). Real-vs-net-new: the Merchant Portal Staff and Access model is REAL and shipped; the admin-side on-behalf equivalent is NET-NEW (there are no admin staff-management endpoints today, see 6.3). New capability name: `merchant:manage-staff`.

**Operator-parity-on-behalf (the broader cross-cutting principle, D44).** For non-portal-using merchants, an operator can perform the same account-maintenance the merchant would perform in the Merchant Portal, subject to the D37 correction lanes: account-maintenance / cosmetic actions are direct plus audited; money or identity-name actions are merchant-approval where a merchant can approve; commercial stays gated. This principle governs the Staff-and-access tab (D44), the branch PIN view / reset (D46), and the branch notification routing (D45), and it is the operator-side complement to "Act FOR the merchant, never AS the merchant" (1.3).

**Branch manager plus notification routing (D45, refines the D36 go-live checklist).** Every branch should always resolve to at least one manager. Rather than a hard go-live gate, the branch manager DEFAULTS to the OWNER when the merchant assigns no dedicated user, with a clear nudge to assign one (or to explicitly choose owner-as-branch-manager). This refines D36: the "each branch has an assigned manager" item is a SOFT advisory (defaults to owner plus a nudge), NOT a hard blocker (consistent with the five enforced gates, see 8.1). Rationale: branch managers receive automated branch emails (redemption notifications, retention, anything branch-associated); a multi-branch owner does not want to be bombarded by every branch's emails, so the branch-assigned manager is the DEFAULT recipient and the owner opts in per branch (off by default). Recipient identity = the branch's assigned Branch-manager (a portal member with the Branch-manager role scoped to that branch, who has an email); defaults to the owner if none. The Merchant 360 Branches tab shows the resolved manager plus the branch's assigned app / scan logins (full staffing) plus the recipient list plus the owner-opt-in toggle; assigning a Branch-manager scoped to the branch is done on the Staff and access tab. Real-vs-net-new: the recipient-routing and per-branch / owner notification preferences are NET-NEW / Phase 6 (Phase 6 comms are unwired; alert-recipients and notification-preferences are un-built schema gaps; redemption email to branches is deferred). Label it as a concept, not live.

**Branch PIN admin on-behalf view and reset (D46, REVERSES the "PIN never shown" invariant).** The Admin Panel CAN VIEW and RESET the branch-specific redemption PIN on the merchant's behalf, because no PIN means no redemption, staff / managers / owners forget it, and a merchant who cannot reach the Merchant Portal needs an operator recovery path. Model (security-conscious): NOT always-visible. Reveal-on-demand plus audited. On the Merchant 360 Branches tab the PIN is masked by default; a "Reveal PIN" affordance requires a reason and logs actor plus reason plus timestamp to Audit; a "Reset PIN" affordance requires a reason, regenerates or sets the PIN, and delivers the new PIN to the branch via the existing send channel. The audit records THAT a PIN was revealed or reset and by whom and why, but NEVER the PIN value. Real infra: `Branch.redemptionPin` is AES-256-GCM encrypted (decryptable server-side); the Merchant Portal already has PIN get / put / send routes (SMS via Twilio is live; email via Resend is Phase 6); the admin on-behalf view / reset is a NET-NEW capability. New capability name: `branch:manage-pin`. IMPORTANT PRECISION (do not conflate): the PIN send / delivery channel (SMS to the branch, live) is distinct from the Phase-6 email notification-recipient routing of D45; email delivery of the PIN to a manager is NOT live. INVARIANT REVERSAL: the prior invariant "branch PIN never shown / hidden by policy" is REPLACED by "the PIN value never appears in logs or audit; the PIN is revealed deliberately on the Branches tab, reason-required and audited; it is never shown on other surfaces (for example the Staff and access tab)." See the boundary contract note in 6.3 and the reversal applied at 6.1.1 / 6.3.

**Role-gated customer identity on redemptions (D48, REPLACES the flat "redemption rows never show individual customer identity" invariant).** Owner-locked model: (i) AGGREGATE surfaces (the Performance tab, M28) NEVER show an individual identity, for all roles (pure data-minimisation); (ii) INDIVIDUAL redemption identity (the Redemptions tab, the single detail, and the lookup) is ROLE-GATED: visible by default for customer-facing roles (Support, senior Operations, Super-Admin), reveal-with-reason for restricted roles, and no-access for roles with no legitimate need; (iii) EVERY identity access (a view OR a reveal) is written to Audit regardless of role. UK-GDPR grounding: the controller has lawful access for legitimate purposes (support, fraud, operations), but data-minimisation, access-control (Article 32) and accountability (Article 5(2)) still apply to internal access; over-broad unlogged internal PII browsing is a risk. Capability names: `redemption:view-customer` and `redemption:reveal-customer`. The exact per-role-to-tier mapping is set with the capability matrix (illustrative in the prototype). Cross-reference the D32 retained-demographic-data / DSAR concern: the access audit supports accountability. INVARIANT REPLACEMENT: the old blanket invariant "redemption rows never show individual customer identity" is REPLACED by "aggregate surfaces never show individual customer identity; individual redemption identity is role-gated and always audited."

#### 6.1.1 Onboarding-review field inventory (the actioner review surface, verified against the real onboarding data model)

The onboarding review surface (the Approvals/Actioner detail panel in M2, and its read counterpart on the Merchant 360 onboarding pane) must show the FULL set of fields the merchant onboarding flow actually collects, so the actioner can judge quality before go-live. The inventory below is REAL unless a field is tagged NET-NEW / NOT-BUILT / PLANNED. Grouped by block; every value is captured by the real merchant onboarding path today unless tagged.

**Field parity across four surfaces (DESIGN-ONLY prototype consistency).** The comprehensive captured field set below (the full category chain, the richer business profile, the owner / responsible-person block, multi-window branch hours, the full branch fields, staff, documents-with-notes, and the contract evidence) now appears CONSISTENTLY across four surfaces so a field that exists in one place exists in all: (1) the assisted CAPTURE (the rep-led wizard, 8.2.1); (2) MERCHANT 360 (the Business identity / Branches / Vouchers / Notes tabs, 6.1); (3) the onboarding-review DETAIL (the Approvals / Actioner surface reached from the Approval Queue, M2); and (4) the Merchant DIRECTORY (M3, 6.1.5). Where a field is NET-NEW in the capture (for example the business type, the owner / responsible-person block, the signatory name / title), it is NET-NEW everywhere it shows; the parity is a prototype presentation consistency, not new backend. **Card structure (prototype):** branch and voucher records render as COLLAPSIBLE cards with a TWO-BAND layout that separates Customer-facing content (name, description, banner / photos, offers, terms) from Operations content (PIN handling, location confirmation, verification, moderation state, internal notes), so the operator can read the customer-facing story and the operational controls without conflating them. The Merchant Directory rows show the resolved category descriptor per row and (in progress) a real-taxonomy category filter with subcategory drill-down as a dropdown (see 6.1.5).

**Merchant profile block (REAL, merchant-wide):**

- Identity + presentation: `businessName`, `tradingName`, `description`, `logoUrl`, `bannerUrl` (cover image), `websiteUrl`. Logo and cover are part of the profile block; the review surface shows both. **Cover banner (`Merchant.bannerUrl`) is CUSTOMER-FACING** (rendered in the customer app), so the operator must be able to SEE it here on the Business identity view (and see per-branch banners on the Branches tab, see below); D40, 2026-07-02. `bannerUrl` / `logoUrl` are REAL fields; editing the images stays NOT-YET-SUPPORTED (see 6.3). The business / trading NAME is a MATERIAL field under D37: an admin correction to the name routes to the merchant for approval, it does not apply directly.
- Legal / commercial identifiers: `companyNumber` and `vatNumber` are FREE TEXT and are NEVER validated (no Companies House / HMRC cross-check today). The review surface must render them as unverified free text, not as a proven company or VAT registration. **Edit authority (D38, owner-decided 2026-07-02, NOT-YET-IN-CODE):** VAT number and company number reclassify from SUPER-ADMIN-only to OPERATIONS-editable (Super Admin still holds all); the review surface should present these as Operations-editable direct corrections, with the caveat that the reclassification is not yet wired in code (today the backend still gates them to Super Admin). Business type (limited company vs sole trader vs charity vs partnership), charity registration and a registered / head-office address distinct from branches are NOT captured today (thin area; see below).
- Taxonomy: `primaryCategoryId` (subcategory-level) plus the `MerchantCategory` join (additional categories); `primaryDescriptorTagId` (cuisine descriptor) plus the `MerchantTag` join (specialty tags). The review surface shows the full taxonomy so the actioner can spot mis-categorisation before it affects discovery. **Edit authority (D38, owner-decided 2026-07-02, NOT-YET-IN-CODE):** category and tags reclassify from SUPER-ADMIN-only to OPERATIONS-editable (Super Admin still holds all); same caveat as above (not yet wired in code; today gated to Super Admin via `merchant:edit-category`).

**Branch block (REAL, per branch; amenities, opening hours and photos live on the BRANCH, not the merchant):**

- Core: `name`, address (`addressLine1`, `addressLine2`, `city`, `postcode`, `country`), `phone`, `email`, `websiteUrl`, `logoUrl`, `bannerUrl`, `about` (600-char), `isMainBranch`. **Each branch's own banner (`Branch.bannerUrl`) and logo (`Branch.logoUrl`) are CUSTOMER-FACING** (rendered per-branch in the customer app), so the operator must be able to SEE each branch's banner + logo on the review surface and on the Merchant 360 Branches tab (D40, 2026-07-02). These are REAL fields; editing the images stays NOT-YET-SUPPORTED (see 6.3).
- Opening hours (`BranchOpeningHours`) and amenities (`BranchAmenity`) per branch.
- Photos (`BranchPhoto`) with per-photo moderation state (pending / approved). The review surface shows the photo set and its moderation state.
- `redemptionPin`: SECRET, AES-256-GCM encrypted. On the ONBOARDING REVIEW surface (and every surface other than the Merchant 360 Branches tab) it is NOT shown; the onboarding review must display "branch PINs are managed on the merchant's Branches tab, not here". The PIN value never appears in logs or audit. The PIN IS revealable on demand (reason-required and audited) and resettable on the Merchant 360 Branches tab per D46 (`branch:manage-pin`, see 6.1.6 and 6.3); this is the owner-decided reversal of the prior blanket "branch PIN never shown / hidden by policy" invariant.
- Location today = postcode centroid only (`LocationConfidence = POSTCODE_CENTROID`). Google Places is wired for address autofill and produces a suggestion that reaches the admin-review metadata, but that suggestion NEVER becomes the branch pin today. The `ADDRESS_GEOCODED` confidence tier exists in the enum but is never written. Admin location confirmation is covered in 6.1.2.

**Voucher block (REAL, per voucher; the 2 mandatory RMVs plus any custom RCVs). Owner-decided D41 (2026-07-02): show the FULL voucher detail the merchant entered wherever vouchers appear (the onboarding review surface, the voucher review lane M2, and the Merchant 360 Vouchers tab M5) so the operator can judge value and quality before go-live:**

- Core / presentation: `type` (one of the 8 voucher types), `title`, `description`, `terms` (the full terms and conditions the merchant entered), `imageUrl` (the voucher IMAGE / banner the customer sees), `estimatedSaving` (the value / saving), `expiryDate`, `status`, `approvalStatus`, `isRmv` / `isMandatory` (RMV vs RCV), `rmvTemplateId`.
- Type-specific: `cooldownSeconds` (REUSABLE cooldown), the AVAILABILITY WINDOW (TIME_LIMITED `availableFrom` / `availableUntil`).
- Approval + usage read: `approvalStatus` plus the per-voucher approval HISTORY (submitted / approved / rejected / changes-requested), and per-voucher REDEMPTIONS (aggregate; `VoucherRedemption` joined via `branch.merchantId`, Insights eligibility cleanliness applies; never an individual customer identity in a row).
- `merchantFields` bag: `askHelp` (the merchant assistance flag "ask Redeemo to help with this offer"; REAL) and `adminProposed` (the admin concierge proposal; REAL). The review surface should surface `askHelp` as an assistance flag so the actioner can offer concierge help.
- Advisory value indicator (weak / good / strong): CLIENT-SIDE ADVISORY ONLY. Ground truth: the score is computed client-side (`scoreVoucher`, a ~GBP 5 advisory floor); it is NOT persisted, there is NO enforced minimum-saving GATE, and `RmvTemplate.minimumSaving` is a DEFAULT, not a gate. The review surface may show the advisory value indicator, but must LABEL it advisory and must NOT imply a hard minimum is enforced.
- Per-voucher CATEGORY VALUE BENCHMARK (NET-NEW; the underlying data is REAL, the aggregation is NOT built): show where this voucher's value / saving stands versus peer vouchers of the SAME category from OTHER merchants: a position marker on a peer-distribution plus the category median and a percentile / quartile (for example "saves about GBP 12; category median about GBP 8; top quartile"). This gives the operator a supply-quality, cross-platform lens on whether an offer is competitive and good value. Ground truth: `estimatedSaving` + category are REAL, but the cross-merchant category aggregation / percentile is NOT built. Label it NET-NEW clearly; NEVER present a live benchmark as built (see 13 and D41).

**Agreement / contract-evidence block (REAL evidence; some fields thin):**

- `MerchantContract` captures `signedAt`, `ipAddress`, `tcVersion`, `signatureMethod` (`CLICK_TO_AGREE` live; `ZOHO_SIGN` enum-only, unused). `@unique(merchantId)` means one acceptance (no versioned re-signature history).
- Thin areas (NET-NEW, do NOT present as captured): the signatory NAME and TITLE are NOT persisted (only `tcVersion`); `contractEndDate` exists on `Merchant` but is NEVER populated (the 12-month end is not materialised); no authority-to-bind attestation, no `userAgent`, no in-person / device provenance. The contract text itself is a placeholder pending legal.

**Acquisition-source block (thin; REAL only as a coarse binary):**

- There is NO `source` / `lead` / `referral` field on `Merchant` today. The only real distinction is self-serve-register vs admin-created-draft, derivable from `AuditLog` event strings, not from a queryable column. A richer acquisition-source model (`MerchantLead`, `MerchantSource`, campaign / UTM, `Merchant.source` / `leadId`) is DESIGN-ONLY (see 8.2). The review surface may show the self-vs-admin-draft origin honestly; anything richer is a labelled FUTURE concept.

**Thin-area flags the review surface must show as not-yet-captured (NET-NEW):** business type / charity status / registered-office address; the owner / responsible-person block (title / first / last / position / email / mobile / landline, plus an optional secondary contact with full parity) that the assisted capture collects (8.2.1 step 2) but the current model does not persist as structured fields; signatory name + title + contractEndDate; the drawn-signature artefact (8.4); richer acquisition source; sector-specific evidence document types with a required-document gate; and PER-DOCUMENT operator-internal notes (captured on the assisted Documents step, 8.2.1 step 6, and mirrored by the D51 notes model). These are "thin area" flags for data the model does not yet capture, not silent omissions. The merchant-wide internal operator NOTES themselves (the Notes tab, D51, 6.1) are a separate net-new schema surface (a note list with add / edit / retract + per-note history, audited, `notes:read` / `notes:write`), BUILT in the prototype but backend net-new.

#### 6.1.2 Admin location confirmation (owner decision 2026-07-02: AUTOMATED map-confirm, no manual coordinates; see D33)

This REVERSES the blueprint's prior position that admin location confirmation is "manual lat/lng, no map". Owner direction 2026-07-02: an operator must NEVER type raw coordinates (operator-error risk). The DECIDED design intent is:

- Auto-resolve the branch pin via Google-Places geocode (with the postcode centroid as a baseline), then present the candidate on a MAP for the operator to VISUALLY verify and confirm with ONE CLICK. The operator can nudge or flag a wrong pin, but never types a latitude / longitude.
- On confirm, write the `ADDRESS_GEOCODED` confidence tier (today `POSTCODE_CENTROID` is the only tier ever written; `ADDRESS_GEOCODED` exists in the enum but is unused).

Ground-truth today (label REAL vs NET-NEW faithfully): admin confirm-location is `branch:confirm-location` (OPERATIONS), and the CURRENT implementation is manual lat/lng with no map. Google Places produces an address suggestion that reaches admin-review metadata but never becomes the pin. The net-new parts of the decision (promote the Google suggestion to the pin, write `ADDRESS_GEOCODED`, build the map-confirm UI) are FUTURE / net-new build. The automated map-confirm (no manual coordinates) is the DECIDED design intent even though parts are net-new; it must be designed as the target, with the manual-lat/lng current state shown only as the honest as-built baseline it supersedes.

#### 6.1.3 Business verification (labelled CONCEPT, NOT built)

Business verification is NOT built today. `verificationStatus = VERIFIED` is a MANUAL admin flag stamped only inside `approveApproval` (`approval:action`); `companyNumber` / `vatNumber` are unvalidated free text; there is NO Companies House, FHRS food-hygiene, Google Place Details cross-check or in-house duplicate detection; a merchant CANNOT upload verification documents (documents are admin-managed on-behalf only, `merchant:manage-documents`, SUPER_ADMIN). A DESIGN spec for the future verification concept EXISTS at `docs/superpowers/specs/2026-06-10-merchant-portal-admin-onboarding-design.md` §5 (Google Place Details cross-check, Companies House, FHRS food-hygiene, in-house duplicate detection, a findings snapshot, and an "auto-approve nothing" posture). The onboarding review surface should carry a clearly-labelled NOT-BUILT business-verification findings panel that reflects that §5 concept: manual review is the only verification today, findings are a future signal, and nothing is auto-approved. See D34.

#### 6.1.4 Edit-on-behalf model on the onboarding surface (real model plus correction-on-behalf, D35 + D37)

The actioner can edit merchant data on-behalf in two REAL modes (both run the same shared cores, tighter input allow-list, mandatory ADMIN-actor reason and audit; see Section 9):

- **DIRECT edits** (applied immediately): `websiteUrl`, `vatNumber`, `companyNumber`, `primaryCategoryId` / tags, branch contact fields, documents, suspend / reactivate, confirm-location. (Owner decision D38, 2026-07-02: `vatNumber`, `companyNumber`, category and tags move from SUPER-ADMIN-only to OPERATIONS-editable; the exact per-role grant matrix is a later decision, and this reclassification is NOT-YET-IN-CODE. See 3.2 and 6.3.)
- **PROPOSE lane** (sensitive identity edits, e.g. `businessName` / `tradingName` / `description`, branch address text): the edit is written as a `MerchantPendingEdit` / `BranchPendingEdit` row into an `AdminApproval`, then applied by the `editApplier`. IMPORTANT: this propose lane is ADMIN SELF-REVIEW today (one admin proposes, an admin approves through the lane), NOT a merchant sign-off and NOT true four-eyes (both capabilities are admin-held; see Section 9 L3 / L4 and BC-3).

##### Correction-on-behalf model (owner-decided D37, 2026-07-02): minimise back-and-forth

The operating principle (see 1.3): rather than bouncing a sub-standard submission back with a request-changes note (which puts merchants off), an operator should be able to CORRECT it on behalf of the merchant, authoring the fix in place before approving. "Request changes" is demoted to the FALLBACK for items only the merchant can supply (for example a missing document). The correction affordance splits by the owner-decided line of money and identity name:

- **COSMETIC corrections (apply directly; operator Apply-and-approve; goes live; audited):** voucher wording / title / description / terms / image / availability windows; merchant `description`; opening hours; `websiteUrl`; contact fields; category. The operator fixes the field, applies it, and the submission proceeds; no merchant round-trip.
- **MATERIAL corrections (SENT TO THE MERCHANT for approval before going live):** voucher VALUE / saving / discount amount; and the business / trading NAME. The merchant must accept a material correction before it takes effect, because it changes money or the trading identity.

**Real-vs-net-new (label honestly):**

- REAL today: the voucher concierge already routes admin voucher corrections to the merchant (`merchantFields.adminProposed`; the merchant reviews the proposal). This is the existing round-trip substrate.
- NET-NEW (the correction flow to build): operators applying COSMETIC corrections DIRECTLY with no round-trip; and routing IDENTITY (name) corrections to the merchant. The MATERIAL vs COSMETIC split, the direct-apply COSMETIC path, and the identity-correction-to-merchant path are net-new; do NOT present them as built.

##### Two-court queue (D37)

The Approval Queue splits into two courts so it is always clear whose action is being waited on:

- **"Needs you"** (the operator's court): unclaimed items, and items claimed by the current operator. This is where the operator acts (review, correct, approve, reject, request changes).
- **"Awaiting merchant"** (the merchant's court): items in changes-requested, and items awaiting the merchant's approval of a material correction. On these rows the waiting-age tracks how long the MERCHANT has had the item (not how long the operator has). When the merchant acts (supplies the change, or accepts / rejects a material correction), the item returns to "Needs you".

Presentation: the two courts show as tabs, plus a per-row whose-court pill so a mixed view still reads correctly. (Net-new: the two-court tabs, the whose-court pill, and the merchant-side waiting-age are all part of the net-new correction flow; today's queue is single-court and operator-centric.)

##### Per-item audit history (D37)

Each review item carries a chronological timeline (actor + reason + before / after) that reads the item's own life, for example: Submitted -> Claimed -> Corrected-on-behalf (before / after; MATERIAL tag where applicable) -> Sent-to-merchant -> Merchant-accepted -> Approved. Secrets and the branch PIN are redacted. This per-item history rolls up into the platform-wide Global Audit (M21). Honesty micro-note: the audit-trail infrastructure is REAL (`AuditLog` before / after / reason + the per-merchant timeline reference in M7), but the send-to-merchant and merchant-accepted-correction event TYPES are part of the net-new correction flow, not existing event types.

##### Clearly-labelled FUTURE concepts (NOT-BUILT; do NOT present as available)

"send this edit to the merchant for THEIR approval" (merchant sign-off of an admin edit) and a per-merchant preference for whether admin edits apply directly vs must be proposed. Neither exists today; both are labelled future concepts on the edit affordances. See D35. Note the relationship to D37: the MATERIAL-correction-to-merchant path (D37) is a specific, owner-decided instance of routing an admin change to the merchant for approval, scoped to voucher value / saving and business / trading name; the broader D35 "send ANY edit to the merchant" and the per-merchant apply-direct-vs-propose preference remain unscoped future concepts.

#### 6.1.5 Merchant Directory as the operator's supply-window (owner-decided D39, 2026-07-02)

The Merchant Directory (M3) is reframed from a flat register into the operator's window into the merchant SUPPLY base: is supply healthy, growing and being used by customers, and where are the gaps. It is the LIST of all merchants; Merchant 360 (6.1 / 6.2) is one merchant's deep WORKSPACE. They are different surfaces; the Directory is the drill-in entry point to Merchant 360.

**Elements:**

- **Supply-health summary:** total merchants; live / pending / suspended counts; verification coverage; region and category distribution; active-vs-dormant; featured count.
- **Enriched table:** logo + business name + trading name; category + subcategory PLUS the RESOLVED CATEGORY DESCRIPTOR per row (the generated descriptor label from the category chain, so the operator reads the merchant's positioning at a glance); town + region; branch count; lifecycle status + verification status; ACTIVITY block = active-voucher count + redemption count + rating + a DORMANT flag (the supply-to-demand link: a merchant with live vouchers but no recent redemptions is dormant supply); commercial block = featured / campaign status + contract status + renewal / expiry; per-row actions.
- **Three views:** the table; a UK SUPPLY MAP with merchant + branch pins (merchant / branch business locations MAY be mapped; individual customer homes are NEVER mapped, per BC-4); group-by region or category.
- **Richer filters** across lifecycle, verification, region, category, activity / dormancy, commercial status. The category filter is (in progress) a REAL-TAXONOMY category filter with SUBCATEGORY DRILL-DOWN presented as a dropdown (it reads the actual taxonomy tree rather than a flat text match); its full "one source of truth" backing depends on the D50 / D54 taxonomy-as-data module (BC-2), so treat the real-taxonomy filter + drill-down as NET-NEW / in-progress until that module lands.
- **Export CSV** (real).
- **Gated "message a segment" bulk action** (FUTURE; a safeguarded bulk workflow under BC-3, provider-gated).

**Real-vs-net-new (label honestly):**

- REAL today (per-merchant reads that already exist): category, location / region, branches, active vouchers, redemptions (aggregate), rating, featured status, contract status, created date.
- NET-NEW (aggregation or surface to build): the supply-health summary roll-ups; the activity / dormancy signal; contract renewal / expiry (`contractEndDate` exists on `Merchant` but is never populated, see 6.1.1); the UK supply map surface; the CSV export; the message-a-segment bulk action. These are net-new aggregation or new surfaces, not existing reads.

The map surface is bounded by BC-4: only business (merchant / branch) locations and anonymised regional supply may be plotted; individual customer home addresses and precise coordinates must NEVER be plotted. See 13 for the gated / net-new register rows.

### 6.2 Customer 360 workspace

Read substrate is rich and needs no schema change; every admin surface, capability and mutation is net-new; the module is PII/DPIA-heavy (Tier 3).

| Pane | Substrate | Status |
|---|---|---|
| Profile, identity, contact | `User` (email, phone, DOB, gender, address, geo, status) | Read substrate ENGINEERED; no admin route |
| Subscription, plan, cycle, promo | `Subscription`/`SubscriptionPlan`/`PromoCode` | Read; mutation (grant/cancel/refund) net-new; admin-grant is Phase-5 plan-only |
| Redemption history, savings | `VoucherRedemption`, `UserVoucherCycleState` | Read (reuse savings aggregation) |
| Reviews and reports | `Review`/`ReviewReport`/`ReviewHelpful` | Read; moderation net-new (data-only) |
| Favourites, interests | `FavouriteBranch/Voucher/Merchant`, `Interest/UserInterest` | Read |
| Devices, sessions, SSO | `UserSession`, `UserSsoProvider` | Read; admin session-revoke net-new |
| Comms, in-app messages | `CommunicationLog`, `Notification` (recipientType USER) | Read (payload NEVER selected) |
| Consent | `User.newsletterConsent`, `tcConsentVersion/At`, `marketingConsentAt` (coarse: one marketing boolean plus terms version) | Read; granular preference centre + consent-change history net-new |
| Lifecycle, DSAR | `UserStatus` (ACTIVE/INACTIVE/SUSPENDED/DELETED); self-serve anonymise-in-place | Admin lifecycle + DSAR net-new, GATED |
| Internal operator notes on a customer | net-new schema (no notes model today) | DEFERRED (D51): customer notes are personal data + DSAR-disclosable; net-new schema, GATED |

**DPIA facts, binding:** deletion is anonymise-in-place (`auth/customer/routes.ts:211-217`): scrubs email/phone/name/passwordHash, sets `deletedAt`+`status=DELETED`, but retains DOB, gender, address, postcode, precise lat/lng and consent. A Customer 360 will show retained fields on DELETED rows (intentional retention); any admin-initiated erasure must mirror or exceed the same scrub set, never silently widen retention. **Precise-geo prohibition (BC-4):** never plot `User.latitude/longitude`; `addressLine1/2` and full postcode never appear in LIST rows and are never plotted; on the Customer 360 record their visibility follows the D60 tiering below. Any PII view/action writes an ADMIN audit row and is gated by a dedicated new capability (proposed `customer:read` / higher `customer:read-pii` / `customer:action`), tiered per D60.

**Member PII visibility tiering (owner-decided D60, 2026-07-04; REPLACES blanket customer-PII masking).** Customer 360 and the member surfaces apply the D48-style role-tiered model to member PII: SEES-BY-DEFAULT for customer-facing roles (Support, Operations, Super Admin: identity, contact and the registration record inline, with EVERY access still written to Audit); REVEAL-WITH-REASON for restricted roles (Finance); NO-ACCESS for roles with no legitimate need (Content, Sales). Rationale: customers are members and these staff serve them directly; UK-GDPR is satisfied by role-gating + auditing + minimisation, not by hiding data from the people who need it. SEARCH respects the same tiering: search results are never a side door to PII a role cannot see (masked-handle roles cannot search by email or identity). List rows stay minimised (never address, DOB or sensitive fields in a row; the full record lives on Customer 360). Two things D60 does NOT change: the precise-geo prohibition above (BC-4) and the aggregate rule (aggregate surfaces never show an individual identity, D48). One deliberate exception: REPORTER identity on review reports stays MASKED for every role (reason-gated reveal only; a retaliation-risk data-minimisation call, M10). See D60 in Section 13 and Section 16.

**Internal notes on a customer are DEFERRED (D51).** The merchant-side internal-notes surface (the Merchant 360 Notes tab, 6.1) is BUILT in the prototype, but CUSTOMER 360 notes are DEFERRED: customer notes are personal data and DSAR-disclosable, so they must be designed as if the subject could read them (factual / professional / role-gated / audited) and route through the DPIA / DSAR posture that governs the rest of Customer 360. Net-new schema throughout. The related onboarding-section general notes are also deferred. See D51 in Section 13 and Section 16.

**Relationship to Members and Revenue (M25):** Customer 360 STAYS in Relationships (symmetry with Merchant 360) and is the drill-in target from the Members and Subscriptions list (M25). Its panes (subscription, cycle, redemption history, savings, favourites, consent, DSAR) are the per-customer enrichment behind the aggregate member/revenue surfaces; the same reveal-on-demand + audited PII rule and the precise-geo prohibition govern both. The member/subscription/revenue surfaces in group 5 are the aggregate, operational-commercial view; Customer 360 is the single-record deep view they link into.

### 6.3 Merchant Portal capability cross-check (per capability: merchant vs admin-today)

Source-cited; drives the authority matrix (Section 9). Note column values: DIRECT, DIRECT+notify, PROPOSE, FOUR-EYES, PROHIBITED, NOT-YET-SUPPORTED.

| Capability | Merchant can do | Admin can do today | Disposition |
|---|---|---|---|
| Profile `websiteUrl` (DIRECT field) | Edit directly | DIRECT edit (`merchant:edit`, OPERATIONS) | DIRECT+notify |
| Profile `businessName/tradingName/description` (SENSITIVE) | Draft-window direct; else edit-request | PROPOSE via `merchant:propose-edit` to B1 lane | PROPOSE then FOUR-EYES |
| Profile `logoUrl/bannerUrl` (SENSITIVE media; customer-facing) | Upload (OWNER) | Rejected by B2.5 body; no route | DISPLAY: operator SEES the merchant cover + logo on Business identity (D40, customer-facing). EDIT: NOT-YET-SUPPORTED |
| Branch `logoUrl/bannerUrl` (customer-facing media) | Upload (OWNER) | No route | DISPLAY: operator SEES each branch banner + logo on the Branches tab (D40, customer-facing). EDIT: NOT-YET-SUPPORTED |
| Identity `vatNumber/companyNumber` | Edit directly | DIRECT (`merchant:edit-identity`, SUPER_ADMIN today, confirm:true) | DIRECT+notify. D38 (owner-decided 2026-07-02, NOT-YET-IN-CODE): reclassify to OPERATIONS-editable (Super Admin still holds all); exact per-role grant matrix is a later decision |
| `primaryCategoryId` + tags | Set in onboarding; blocked once RMVs live | DIRECT (`merchant:edit-category`, SUPER_ADMIN today; RMV re-provision) | DIRECT+notify (side-effecting). D38 (owner-decided 2026-07-02, NOT-YET-IN-CODE): reclassify to OPERATIONS-editable (Super Admin still holds all); exact per-role grant matrix is a later decision |
| Business / trading NAME (`businessName/tradingName`) | Draft-window direct; else edit-request | PROPOSE via `merchant:propose-edit` to B1 lane (admin self-review) | MATERIAL correction (D37): routes to the MERCHANT for approval before going live (NET-NEW); does NOT move to OPERATIONS-direct under D38 |
| Branch contact `phone/email/websiteUrl/isActive` | Edit directly | DIRECT (`merchant:edit`) | DIRECT+notify |
| Branch `address/name/city/postcode` (SENSITIVE) | Edit-request | No text route; location confirm only (see below) | NOT-YET-SUPPORTED |
| Branch create | OWNER; non-first stages BRANCH_CREATE approval | DIRECT create (`merchant:manage-branches`, SUPER_ADMIN) | DIRECT for admin; merchant path is FOUR-EYES |
| Branch close/soft-delete | close-request stages BRANCH_CLOSE approval | DIRECT soft-delete (guards) | DIRECT for admin; merchant path FOUR-EYES |
| Branch location | Submit candidate token | Confirm (`branch:confirm-location`, OPERATIONS); TARGET = automated map-confirm, no manual coordinates (D33, see 6.1.2); current as-built = manual lat/lng, no map | DIRECT (admin-owned) |
| Branch hours / amenities / photos / redemption-alerts | Manage directly (staged for hours/photos) | No admin route (photos reviewable via approval only) | NOT-YET-SUPPORTED |
| Branch redemption PIN | GET/PUT/send | View + reset on-behalf on the Branches tab: reveal-on-demand (reason + audit) + reset (reason + regenerate + deliver via the existing send channel); PIN value never in logs or audit; never shown on other surfaces (`branch:manage-pin`, NET-NEW) | REVEAL-ON-DEMAND + RESET (D46, owner-decided; reverses the prior PROHIBITED / "never shown" position) |
| Custom (RCV) vouchers CRUD | OWNER / canManageVouchers | Admin covers RMV only; reviews go-live | NOT-YET-SUPPORTED (B5.2 future) |
| RMV flagship (edit allowedFields / submit) | create/edit/submit | Co-build: edit + submit (`merchant:manage-vouchers`) | DIRECT+notify; go-live stays FOUR-EYES |
| Voucher go-live | submit only | approve/reject/request-changes (`approval:action`) | FOUR-EYES |
| Onboarding submit / go-live | submit only | submit-on-behalf (B3) + approve via lane | submit DIRECT+notify; go-live FOUR-EYES |
| 12-month contract acceptance | click-to-agree (OWNER session) | No admin sign path | PROHIBITED (legal) |
| Verification documents | not merchant-facing (admin-only) | list/upload/delete (`merchant:manage-documents`, SUPER_ADMIN) | DIRECT+notify (admin-managed) |
| Staff and Access (invite/role/scope/deactivate) | OWNER-only | On-behalf management on the Staff and access tab: portal-access members (invite via tokenised claim link, change role / scope, toggle can-manage-vouchers, re-issue access link, soft-remove with last-Owner guard) + app / branch logins (reset password, deactivate / reactivate; add new = thinner limited cap); no impersonation, no password set / view, PIN never shown here (`merchant:manage-staff`, NET-NEW) | DIRECT + audited on-behalf (D44, owner-decided; reverses the prior NOT-YET-SUPPORTED position; operator-parity-on-behalf) |
| Insights / reports | read-only role-gated | No admin route | NOT-YET-SUPPORTED |
| Redemptions log + validation | read + validate | Per-merchant Redemptions viewer + individual-redemption lookup (by code) on the Redemptions tab; aggregate Performance roll-ups on the Performance tab; customer identity role-gated + always audited (`redemption:view-customer` / `redemption:reveal-customer`, NET-NEW); no admin validate path | READ + LOOKUP (D43, owner-decided; reverses the prior NOT-YET-SUPPORTED read position; identity per D48). Validate stays NOT-YET-SUPPORTED |
| Merchant lifecycle suspend/reactivate | not self | DIRECT (`merchant:suspend`, OPERATIONS) | DIRECT+notify (high-risk) |
| Create draft merchant | self-register instead | DIRECT (`merchant:create-draft`) | DIRECT+notify |
| Billing / featured-payment | absent | absent | NOT-YET-SUPPORTED (both sides) |
| Ownership transfer / multi-owner | multi-owner exists; no self transfer | No route | NOT-YET-SUPPORTED (high-risk) |

Note on "FOUR-EYES" in this table: it denotes routing through the independent approval lane (a separate approval step: `approval:action` / `approval:apply-edit`), which today is single-actor claim-and-approve. Countersign by a DIFFERENT admin (true four-eyes / maker-checker) is NET-NEW (see Section 14 and BC-3); the term here does not assert dual-control exists.

---

## 7. CRM and relationship operations

Redeemo needs native relationship operations but is not a complete CRM today. Design separate but connected domains: Merchant Relationships (Merchant 360), Customer Relationships (Customer 360), Leads and Onboarding, Support and Cases, Tasks and Follow-ups, Communications, Account Health, Interaction and Audit History.

**Compose-on-existing (reference implementations exist):** interaction history and activity feeds compose on `AuditLog` (actor to entity to event, before/after, reason) plus `CommunicationLog` (delivery) plus `Notification` (in-app). The admin merchant timeline (`src/api/admin/timeline/service.ts`) is the working read-only reference (payload never selected). Reference patterns (do not over-claim reuse; both are hardcoded to the approval queue): assignment via `AdminApproval.claimedById` (atomic claim, release guard); follow-up via `AdminApproval.lastStaleAlertAt` (stale sweep). The eighth CRM domain, Interaction and Audit History, is deliberately realised as this composed timeline (M7) plus the Global Audit / Activity Explorer (M21), not a standalone module.

**Net-new schema (every stateful CRM primitive):** internal Notes (author-attributed, multi-entry, timestamped); Tasks/Reminders/Follow-ups (assignee, due, status, completion); entity-level Assignment/ownership on Merchant/Customer; Cases/Tickets; Segments/saved audiences; admin-internal Tags/flags (VIP, at-risk, do-not-contact); Account Health/risk scoring. None exist (verified grep). `SupportTicket`/`MerchantRequest` are customer-app frontend stubs only, backend deferred to the un-started Sub-PR 2.

**Build-vs-integrate gate (BC-5):** account health and segmentation are Redeemo-owned (source signals are platform-only) and should be built in-house. Support-ticketing and general prospect CRM are an explicit build-vs-integrate decision given the CLAUDE.md Zoho One direction. Do not lock a schema merely because these concepts appear in the prototype.

### 7.1 Communications: the sending home (D61) + honest channel status + templates (D63 / D64)

**Four-channel honest status (verified; never present a dark channel as live):**

- **Email: BUILT, NOT LIVE.** The outbox is REAL (`CommunicationLog`: recipient / channel / type / subject / status default QUEUED / externalId / sentAt). The rendered `payload` exists so the reconciler can re-enqueue and is ERASED (nulled) at terminal state because it can carry reset links or branch PINs: the log shows METADATA, never content, long-term (a deliberate privacy property; see BC-1 and Section 14). Email is dark (Resend SDK installed, unimported); sends queue as QUEUED.
- **SMS: LIVE, transactional only** (Twilio OTP + the branch PIN send). Marketing / notification SMS not built.
- **Push: NOT SET UP** (no FCM integration; no device / push tokens in schema).
- **In-app: LIVE for merchants and admins** (real writer + read endpoints). The customer app has a bell icon but NO feed behind it (no customer notification read endpoint).

**Nine real transactional email templates (code-owned, system-triggered):** password reset; merchant account setup; customer sign-in code; business sign-in code; business email verification; business account deletion; admin new-merchant-submitted; admin merchant-resubmitted; branch redemption PIN. D64 gap: there is NO customer email-verification template among them (only the business variant), despite the customer app hard-blocking on email verification.

**D61 send rules (owner-decided 2026-07-04): Communications is the sending home.** Customer 360 stays READ-ONLY for communications (it shows the comms timeline and consent, never a send button); anything that SENDS lives in the Communications screen, Support case replies (M14), and (later) Campaigns for bulk (M17, Wave 3). Sends are TEMPLATE-BASED only (no free-form composition to customers), CONSENT-CHECKED (marketing requires stored marketing consent; transactional is exempt), AUDITED, and PROVIDER-GATED: while email is dark the UI says "Queued; sends when email goes live", never "Sent".

**D63 template ownership split (recommended 2026-07-05, encoded in the prototype):** transactional templates stay CODE-OWNED and read-only in the panel (they carry security-critical content such as reset links and branch PINs; UI editing of them is a real risk); marketing templates become panel-editable LATER via the parked Agreements, Policies and Content module (D55).

**D64 planned automated sends (raised 2026-07-05; shown as FUTURE, nothing wired):** (a) a redemption-details email to the merchant branch manager on each validated redemption (maps to the deferred merchant-portal redemption-notification slice and the alert-recipients schema-ledger item; cross-ref D45); (b) the monthly merchant statement to branch managers and owners (already in the locked merchant-portal Phase 4 architecture); (c) the missing customer email-verification template above; (d) an advisory Phase 6 customer-lifecycle list: welcome email, subscription payment receipt, renewal reminder, payment-failed notice, cancellation confirmation, customer account-deletion confirmation, password-changed security notice. Real send-wiring belongs to Phase 6 (email enablement) plus the merchant-portal Phase 4 decisions; nothing here authorises a provider.

Capabilities: `comms:read` (log + template library + channel status) and `comms:send` (the template-based one-off send), both net-new (3.2).

---

## 8. Onboarding channels and in-person agreement

### 8.1 Two shipped channels (build-on-code)

1. **Self-serve:** register to email-verify to auto-login (`auth/merchant/service.ts:548/654`), non-enumerating; creates Merchant(REGISTERED) plus first OWNER membership; terms consent recorded in audit metadata (not a column).
2. **Admin create-draft plus secure handoff:** `createMerchantDraft` NEVER sets a password (`passwordSetupRequired:true`, no token returned); owner claims via a single-use 7-day claim token (`issueMerchantClaim`; token never returned/logged). This is the secure owner handoff.

Both land on the same OWNER membership and the same uniform gate set. There is no expedited/fast-track bypass today.

**Real go-live gates (verified; the review surface checklist must match these EXACTLY, not CLAUDE.md rule 7's intended list):**

- **Submit gate** (`computeOnboardingChecklist`): >=1 branch exists; `contractStatus = SIGNED`; the 2 RMVs configured (each `status` in `PENDING_APPROVAL` | `ACTIVE`).
- **Go-live gate** (re-run in-transaction inside `approveApproval`): the submit gate PLUS a main branch (`isMainBranch` / `isActive`) PLUS the main-branch location confirmed (`LocationConfidence = MANUALLY_CONFIRMED`).

So the REAL five gates are: (1) branch exists; (2) contract SIGNED; (3) 2 RMVs configured; (4) main branch present; (5) main-branch location confirmed.

**Intended-but-NOT-enforced (a gap vs CLAUDE.md rule 7, do NOT present as gates):** "documents uploaded", "branch-user assigned" and "mandatory-fields filled" are NOT enforced by any gate today. Rule 7 states them as the intended approval bar, but the shipped checklist does not check them. The review surface must present these as intended-not-enforced (thin-area flags the actioner can weigh manually), never as hard go-live gates.

### 8.2 Representative-assisted channel (DESIGN-ONLY; net-new)

A full representative-assisted channel should let an authorized operator: create and qualify a lead; record acquisition/referral source; create a merchant draft; record an in-person visit; capture business-verification evidence; capture business/owner/branch/contact info; configure branches; co-create vouchers with the merchant; prepare documents and agreements; initiate or complete permitted verification and approval steps; hand the account securely to the owner; continue managed/assisted service where agreed.

Net-new substrate required (all design-only or absent): `MerchantLead`, `MerchantSource` (+campaign/UTM), `Merchant.source`/`leadId`, an in-person visit record, field-verification provenance on `MerchantDocument` (capturedBy/at/geo), a risk-tier/expedite field, and `AdminCapabilityGrant` for per-actor verify/fast-track. Onboarding provenance taxonomy (self-serve / rep-assisted / admin-created / partner-imported) is recoverable today only from AuditLog event strings, not a queryable column.

**Prospect pipeline on the Leads and Onboarding hub (Wave-2 prototype; pre-draft stages only).** The existing hub (inbound self-serve pointer + the two admin-created route cards + the in-progress assisted list) gains a PROSPECT PIPELINE section covering ONLY the pre-draft stages: Lead, Contacted, Visit booked, converting into the two existing admin-created routes (create-draft-and-hand-off, or the assisted wizard), with a Lost-with-reason AUDITED exit available at any stage. Post-conversion stages are already represented by the existing hub surfaces (the in-progress assisted list and the approval queue); the pipeline does not duplicate them. Lead anatomy: business name, category guess, locality, contact, source (rep visit / inbound enquiry / customer request as a FUTURE intake via the customer-app "Request a merchant" stub), assigned rep (D59 assign-then-claim semantics), next action + due date. Real-vs-net-new: the lead model is ENTIRELY NET-NEW (no lead table anywhere); the convert step lands on the REAL create-draft flow. Capability: `lead:manage`, held by the Sales / Lead-owner persona ALONGSIDE Operations and Super Admin (Finance / Content / Support stay restricted; assignment rights stay with the D59 assigner tier; the SALES role itself remains a proposed persona, not in the role enum today).

#### 8.2.1 The rep-led on-behalf onboarding wizard (owner-decided D49, DESIGN-ONLY prototype)

**Two onboarding channels, restated (D49):** (A) SELF-SERVE (the merchant registers online, then admin approval; this is REAL, 8.1), and (B) ASSISTED / REP-LED (a Redeemo representative, physically WITH the business, builds the ENTIRE account on-behalf on the admin panel, so a non-technical merchant never touches the Merchant Portal). The assisted channel is built in the prototype as a 9-step FULL-SCREEN PRESENTATION-MODE wizard: the admin chrome is hidden so the surface is a merchant-safe focus mode the rep can turn to face the owner, with an Exit-to-admin affordance. PRECISION: presentation mode is still the OPERATOR's own audited admin session; it is NOT a separate login and NOT a security boundary. The 9 steps:

1. **Category and identity (CATEGORY-FIRST, a hard prerequisite):** primary category, then subcategory, then cuisine (conditional on the category), then specialties, then a generated descriptor label. Changing the top-level category DISCARDS drafts (the taxonomy drives the downstream builder, so a category swap invalidates category-keyed content).
2. **Business profile:** REAL public fields (logo, banner, description, website); REAL registration fields (legal name, trading name, company number, VAT); and NET-NEW richer fields (business type plus conditional charity number / UTR; head-office address / phone / email; an owner-and-responsible-person block with title / first / last / position / email / mobile / landline, plus an optional secondary contact with full parity; values; social links; category-conditional service options; price range).
3. **Branches:** the full branch record: name / description; a Google-Places address lookup; contact / website; MULTI-WINDOW opening hours; category-eligible amenities; a banner plus up to 8 moderated photos; a branch logo (inherit-or-override); a 4-digit redemption PIN; and an automated location map-confirm (per D33, 6.1.2). The FIRST branch is the main branch; the rep can add more.
4. **Vouchers:** the 2 mandatory flagship RMVs, authored through the category-driven builder (a faithful port of the merchant-portal flagship builder; see D52 / 8.2.2 and the D53 terms model at 8.2.3).
5. **Staff [OPTIONAL]:** portal managers invited via claim-link, branch / till scan users, and branch-manager assignment (per D44 / D45, 6.1.6).
6. **Documents [OPTIONAL, not a gate]:** verification uploads captured on-behalf and routed to moderation, with per-document operator-internal notes (D51, 6.1). NOT a go-live gate (consistent with the five enforced gates, 8.1 / D36).
7. **Contract:** the OWNER signs in person on the rep's device; the OPERATOR CANNOT accept. A hand-to-owner owner-only accept captures name plus title plus confirm, with an OPTIONAL finger / stylus signature that is NOT a gate. Evidence captured = signatory name + title (NET-NEW: not persisted today, see 8.4), timestamp, IP, method, and agreement version. Admin-never-signs is a LEGAL LOCK (1.3 / 8.4).
8. **Go-live review:** the 5 enforced go-live gates (8.1) plus a rep IN-PERSON verification attestation; the rep approves INLINE; single-actor honesty applies (this is NOT four-eyes, see 8.3 / BC-3), and rep-created-and-approved merchants are flagged for light POST-HOC audit sampling.
9. **Handover:** transfer to the owner's email via a tokenised claim link; the owner sets their OWN password and takes ownership; the operator NEVER sets or sees a password.

**Real-vs-net-new (label honestly):** the shell-create plus claim / transfer are REAL (`createMerchantDraft` plus `issueMerchantClaim`, 8.1); the full on-behalf onboarding wizard is NET-NEW. Capabilities: `merchant:create-draft` (the light, REAL shell-create) plus `merchant:assisted-onboard` (the heavy, NET-NEW wizard; see 3.2). Go-live fork LOCKED for the assisted channel: the rep approves INLINE, the action is audited, the in-person verification is captured as evidence, and rep-created-and-approved merchants are flagged for post-hoc audit. Cross-ref 8.4 for the signature step: the assisted flow adds the OPTIONAL drawn signature plus the net-new signatory name / title evidence.

#### 8.2.2 Assisted voucher builder = a faithful port of the merchant-portal flagship builder (owner-decided D52, DESIGN-ONLY prototype)

Step 4 of the assisted wizard (and the Merchant 360 Vouchers context, 6.1) uses a faithful port of the merchant-portal flagship builder: category-driven fields, placeholders, suggestion chips, per-type examples and terms, all keyed off the Step-1 category resolved to its TOP-LEVEL PARENT (11 parents plus a fallback; the config is top-level granularity only). It uses the REAL `scoreVoucher` advisory maths: title-clarity, description-personalised (editing the auto-suggestion and reaching >= 30 chars lifts the score), a saving floor (~GBP 5) and generosity with the percentage surfaced in the copy, term-stacking (too many or too restrictive terms drag the score toward Too weak), and photo; the tier is Too weak / Good / Great. The score is ADVISORY, NOT a submit gate: a Too-weak voucher can still be SAVED, and admin review is the quality backstop (consistent with the D41 advisory-not-a-gate lock, 6.1.1). There is NO fake peer benchmark: the motivational "compares to similar businesses" line carries NO real numbers; a real cross-merchant category-median benchmark is NET-NEW / not built (same honesty as the D41 / D42 category benchmark). Presentation: a sticky score-plus-preview SIDE RAIL (not a bottom block). There is NO concierge "ask the Redeemo team" toggle in the assisted flow (the rep IS the builder). Type picker = 5 eligible (BOGO recommended / Spend and save / Discount fixed + percent / Freebie / Package) plus 2 disabled (Time-Limited / Reusable = custom day-2). Two mandatory flagship RMVs; custom vouchers are day-2. Real-vs-net-new: `scoreVoucher` is REAL advisory logic; the category-median peer benchmark is NET-NEW / not built.

#### 8.2.3 Always-on voucher terms in the assisted flow = generic-only (owner-decided D53, DESIGN-ONLY prototype; a deliberate deviation from the merchant-portal core)

For the admin assisted flow the locked always-on CORE terms are generic-only: tell-staff, show-in-app and no-cash-value. "Not valid with any other voucher" and "One voucher per customer each visit" MOVE to OPTIONAL (pre-ticked, removable). Per-type Fair terms plus category-conditional Caution terms (service businesses show NONE) plus a Discount-percent Restrictive minimum-spend plus add-your-own (80 chars, auto-tagged Restrictive on restrictive wording). This is an owner-confirmed DEVIATION from the merchant-portal CORE, which keeps tell-staff plus no-combine plus per-visit as its 3 fair core terms, all as unlocked checkboxes; the deviation applies to the admin assisted flow only. See D53 in Section 13 and Section 16.

### 8.3 Field verification and fast-track (POLICY decision)

A representative visit is EVIDENCE, not authorization. Today `verificationStatus=VERIFIED` is a MANUAL admin flag stamped only inside `approveApproval` (`approval:action`), which re-runs the go-live checklist and main-branch-location gate in-transaction; no rep-side write can flip it; FHRS/Companies-House/verification pre-score are absent (Google Places exists but only for branch geocoding). The future business-verification concept (Google Place Details cross-check, Companies House, FHRS, in-house duplicate detection, findings snapshot, auto-approve-nothing) is a labelled NOT-BUILT concept per 6.1.3 / D34, not a shipped capability. Present for owner decision: rep verifies and a separate Operations Admin approves; a specifically authorized senior representative verifies and approves lower-risk items; sensitive/legal/financial/high-risk always require independent approval; emergency/platform-risk actions follow a separate override policy. Do not mislabel the current claim/approve process as true four-eyes.

### 8.4 Agreement and signature (compare channels; do not approve one legally)

The owner or authorized signatory must personally accept/sign. Redeemo staff must never accept terms, sign as the merchant, set the merchant password, or become the accountable owner merely by creating the draft. (Verified hard rule: the only writer of `MerchantContract` + `contractStatus=SIGNED` is the merchant onboarding path, merchant-session-bound; admin code only reads it.)

Evidence captured today (`MerchantContract`, `signedAt` + `ipAddress` + `tcVersion` + `signatureMethod`; `@unique(merchantId)` = one acceptance): method (`SignatureMethod` = CLICK_TO_AGREE live; ZOHO_SIGN enum-only), version (client-supplied, not re-validated server-side), timestamp, IP. Absent (net-new): signatory name/title, authority-to-bind attestation, `userAgent` on the row, in-person/controlled-device context provenance (device, capture geo, in-person flag; cross-ref 8.2 field-verification `capturedBy/at/geo` so an in-person accept is distinguishable from a remote one), `contractEndDate` value never populated (the column exists on `Merchant` but the 12-month end is never materialised), versioned re-signature history (drop `@unique`), OTP-verified binding.

Acceptance channels to compare without legal approval: online checkbox/versioned acceptance; in-person e-sign on a controlled device; secure sign-link or OTP on the merchant's own device; external e-sign provider; paper/offline with verified upload. Every channel must route the actual accept through the merchant's own authenticated session (or a device sign-link/OTP proving merchant possession); the in-person section is "admin facilitates, merchant signatory personally attests", a net-new flow, not a reuse. Legal validity, identity assurance level and retention are separately gated legal/security decisions; the binding merchant legal text is external (`redeemo.co.uk/merchant-terms`).

**Assisted-channel signature (D49, DESIGN-ONLY prototype):** the rep-led wizard's Contract step (8.2.1 step 7) is a concrete instance of the in-person channel above: the OWNER signs in person on the rep's device, the operator CANNOT accept, and a hand-to-owner owner-only accept captures name plus title plus confirm. It adds an OPTIONAL finger / stylus DRAWN SIGNATURE (not a gate) on top of the click-to-agree accept. Evidence captured in the assisted flow = signatory name and title (NET-NEW: not persisted today, per the thin-area list above), timestamp, IP, method, and agreement version (`MerchantContract.tcVersion`). The signatory name / title and the drawn-signature artefact are NET-NEW evidence fields, not existing captures; admin-never-signs remains the LEGAL LOCK.

**Third channel = internal evidence-pack signature; no external e-signature setup (owner-decided D65, 2026-07-05).** The third contract-signature channel is an INTERNAL upgrade of the shipped click-to-agree, not an external provider: version-controlled contract text (owned by the planned Agreements, Policies and Content module, D55); a real signing ceremony (scroll-through, typed name, an OPTIONAL drawn signature); a generated signed-PDF snapshot of the EXACT contract version, stored in platform document storage; and an evidence record (authenticated identity, timestamp, IP and device via the audit log, plus a HASH of the signed file for tamper detection) with an admin verification view. External providers (for example Zoho Sign) are deliberately NOT set up; they remain a future special-case option only. Sufficiency is confirmed within the already-planned pre-launch solicitor review (no new legal gate is added). Code grounding: the shipped `MerchantContract` model already stores `signedAt`, `ipAddress`, `tcVersion` and `signatureMethod`, with a nullable `zohoSignRequestId`, so the internal path slots into the existing evidence model. The prototype's channel-comparison surface (Wave-2 screen 2.12, behind the Legal and Agreements area) lays the three channels side by side with honest status labels (click-to-agree REAL-SHIPPED; in-person assisted accept PROTOTYPE-DESIGNED; internal evidence-pack PLANNED / NET-NEW) and frames the choice as an OPEN owner decision (no winner picked). See D65 in Section 13 and Section 16.

---

## 9. Authority / action matrix

Model the matrix on the existing capability tiers and the `fnCore`/approval seams; do not invent a parallel scheme. Two locked invariants on every level: mandatory non-empty reason on the audit row; no weaker path (admin routes call the same shared core). Routine authorized support changes must not be forced through unnecessary merchant-approval delays; sensitive/legal/financial/ownership/high-risk-access/material-customer-impacting actions require explicit policy.

**Six authority levels (mapped to shipped capabilities):**

| Level | Meaning | Capability substrate (verified) |
|---|---|---|
| L0 READ | View entity/context | `merchant:read` (OPERATIONS + SUPER_ADMIN) |
| L1 DIRECT-LOW | Direct write, operational fields | `merchant:edit` (websiteUrl; branch contact/isActive), OPERATIONS |
| L2 DIRECT-HIGH | Direct write, higher bar | `merchant:edit-identity`, `merchant:edit-category`, `merchant:manage-branches` (SUPER_ADMIN; confirm where side-effecting) |
| L3 PROPOSE | Propose sensitive change into the pending-edit lane | `merchant:propose-edit` (SUPER_ADMIN proposes; never applies its own) |
| L4 INDEPENDENT-APPROVAL (target: four-eyes; today: single-actor claim-and-approve) | Approve/apply customer-impacting/lifecycle changes through the approval lane; today one admin can both claim and approve, so true countersign-by-a-different-admin is NET-NEW (see BC-3 and Section 14) | `approval:apply-edit`, `approval:action` |
| L5 LIFECYCLE/TAKEDOWN | Operational lifecycle, immediate effect | `merchant:create-draft`, `merchant:submit`, `merchant:suspend`, `branch:confirm-location` |

**Two policy-gated columns the matrix must surface as not-yet-buildable-without-decision:**

- **(A) LEGAL/FINANCIAL:** contract-sign-on-behalf and billing are PROHIBITED/ABSENT today; keep policy-gated or explicitly out of scope.
- **(B) ACCESS/OWNERSHIP:** admin staff-management and ownership-transfer are genuinely absent; each needs its own high-risk tier with step-up plus audit before any build. Never expose/set merchant passwords; owner transfer and role elevation require stronger controls; high-risk access changes require step-up. No decision is locked by the prototype.

Apply the matrix across: merchant profile and legal identity; branches and locations; vouchers and terms; staff/users, invitations and roles; documents; agreements; lifecycle; redemptions/reversals; billing/commercial; customer-impacting changes; and the representative-assisted onboarding actions (lead creation, in-person visit record, field-verification evidence capture) which default to outcome 4 (independent Redeemo approval), with any senior-rep fast-track as the D17-gated exception. The six-outcome authority classes to use per action: (1) Direct Admin action; (2) Direct plus merchant notification; (3) Admin proposal requiring merchant review/acceptance; (4) Independent Redeemo approval (four-eyes countersign by a DIFFERENT actor is NET-NEW; the approval lane is single-actor today); (5) Emergency/platform-safety override; (6) Prohibited. Do not let any level collapse a four-eyes/approval action into a direct edit; treat every NOT-YET-SUPPORTED row (logo/banner, branch address, hours/amenities/alerts, custom vouchers, staff management, insights, redemptions viewer, billing, ownership) as an explicit build-plus-policy decision, not an implicit tier extension.

**Assign-then-claim workflow (DESIGN-ONLY prototype decision D59, DECIDED, DEFERRED to Wave 2 as the first focused pass).** Today an admin only SELF-CLAIMS a queue item (`AdminApproval.claimedById`); nobody can hand an item to a named person. D59 adds a directed-assignment layer ON TOP OF the two-court queue plus claim model (D37, see 6.1 / the Approval Queue) WITHOUT weakening single-actor accountability. Certain ASSIGNER roles (a NET-NEW capability, for example `approval:assign` or `task:assign`; likely Super Admin plus a lead / senior-Operations tier, possibly the proposed Platform-Manager mid-tier persona) can ASSIGN a queue item / merchant / approval / task to a SPECIFIC user, instead of everyone only self-claiming. The assignee RECEIVES a notification (the `ADMIN_REVIEW_ASSIGNED` notification TYPE is REAL, but its emitter is currently DEFERRED precisely because this assign-flow does not exist yet: this is the "emitter deferred" case flagged on the 1.11 Notifications screen and the reserved-alert-emitters register in Section 13) and then CLAIMS the item to pick it up. KEY ACCOUNTABILITY INVARIANT: assignment ROUTES, claim COMMITS. The single-actor claim-to-act accountability is preserved: assignment says "this is yours to pick up", the assignee still claims-to-act before acting; self-claim and assign COEXIST (an assigned item can still be self-claimed if the assignee is unavailable, per policy to settle when the pass is designed). NET-NEW: `AdminApproval` has `claimedById` (self-claim) but NO `assignedToId` / `assignedById` / `assignedAt` columns, so D59 needs a small schema addition PLUS the assign capability PLUS firing the existing `ADMIN_REVIEW_ASSIGNED` notification PLUS an assigned-state on the queue, ALL audited (who-assigned, who-assigned-to, who-claimed). DEFERRED: owner-decided as the FIRST focused Wave-2 pass, NOT built in Wave 1. See D59 in Section 13 and Section 16.

---

## 10. Workflow and lifecycle maps

State machines the Admin Panel drives or reads (enums verified in schema):

- **Merchant:** REGISTERED to PENDING_APPROVAL to ACTIVE, ACTIVE to/from SUSPENDED/INACTIVE, to DELETED; cross-cut by `OnboardingStep` (REGISTERED, BRANCH_ADDED, CONTRACT_SIGNED, RMV_CONFIGURED, SUBMITTED, UNDER_REVIEW, APPROVED, LIVE, NEEDS_CHANGES, REJECTED, SUSPENDED) and `VerificationStatus`, `ContractStatus`. Registration, submission and lifecycle services also write Merchant state; the actioner owns the atomic approval/go-live transition, not the entire state machine.
- **Approval:** PENDING to (CHANGES_REQUESTED loop) to APPROVED/REJECTED, over ApprovalTypes MERCHANT_ONBOARDING / VOUCHER / MERCHANT_IDENTITY_EDIT / BRANCH_IDENTITY_EDIT / BRANCH_CREATE / BRANCH_CLOSE (plus MERCHANT_PROFILE_EDIT, a dead lane: listable but no applier; decision D12).
- **Branch:** triple-axis: `isActive` (reversible) vs `deletedAt` (soft-delete) vs `lifecycleStatus` (PENDING_CREATE, LIVE, PENDING_CLOSE, CLOSED), plus `LocationConfidence`.
- **Voucher:** DRAFT to PENDING_APPROVAL to ACTIVE, ACTIVE to/from INACTIVE, to EXPIRED; times `ApprovalStatus`; Model-1 approved-waiting nuance.
- **Subscription:** TRIALLING/ACTIVE/CANCELLED/EXPIRED/PAST_DUE (read today; admin mutation net-new).
- **Redemption:** create to validated; no reversal transition exists (net-new schema; mind `Review.redemptionId @unique ON DELETE SET NULL` coupling).
- **Lead (proposed):** Lead to Contacted to Visit booked, then CONVERTED into one of the two admin-created routes (create-draft, or the assisted wizard), or LOST-with-reason (audited exit at any stage); the pre-draft prospect-pipeline stages per the Wave-2 prototype (design-only `MerchantLead`; post-conversion stages are carried by the existing hub surfaces and the approval queue; see 8.2).
- **DSAR (proposed):** received to identity-verified to in-progress to export-produced/erasure-confirmed to closed, with SLA clock and audit (no model today).
- **Case (proposed):** open to in-progress to waiting to resolved to closed (net-new; or Zoho-integrated).

---

## 11. Screen and complete state inventory

Every screen handles the DoD state set: loading, empty, error, permission-denied, stale/conflict, success, destructive-confirmation, partial-data, responsive. The built spine handles most (error-vs-empty distinguished, invalidate-on-success-and-error, mandatory-reason plus confirm on destructive) but is desktop-only (responsive is the weakest area) and lacks Next `error/loading/not-found` boundaries.

| Module | Key screens | Built? | Critical states |
|---|---|---|---|
| Ops Home (M1) | landing, launch-readiness | net-new | loading skeleton; GATED-analytics empty; partial-data |
| Approvals (M2) | queue, review (5 dispatched panels), action bar/dialogs | built (evolve) | claimed-by-other read-only, orphan, gate-fail banner, destructive-confirm, signed-link-unavailable |
| Merchant Directory (M3) | list, filters | built (evolve) | empty, permission-denied, stale, responsive |
| Leads/Onboarding (M4) | lead board, lead detail, create-draft, assisted flow | partial | empty, SLA-aging, assignment conflict, handoff |
| Merchant 360 (M5) | workspace tabs (Section 6.1) | partial | partial-data ("not set"), cap-gated affordances, denial |
| Customer 360 (M6) | workspace tabs (Section 6.2) | net-new | permission-denied, PII reveal-on-demand, DELETED-row disclosure, DPIA notices |
| Communications (M7) | timeline, delivery monitor, broadcast | partial | delivery-failure list, dark-email notice |
| Tasks (M8) | task list, task detail | net-new | empty, overdue, reassignment |
| Trust and Safety (M10-M13) | moderation queues, media review, reversal | mixed | empty (no reports), destructive-confirm, schema-gated notice, review-coupling warning |
| Support/DSAR/View-as (M14-M16) | case queue, DSAR queue, view-as | net-new/gated | legal-gated notice, SLA clock, audited-read banner |
| Commercial (M17) | campaigns/featured/trending | net-new/gated | gated notice, paymentStatus integrity, date-bounded |
| Members and Revenue (M25-M29) | member list + member/subscription detail, revenue and billing (MRR/ARR/past-due), promo codes and trials, redemptions and engagement (aggregate), conversion | net-new/gated | loading skeleton; empty (no members match); permission-denied (customer:read / subscription:read / revenue:view); aggregate-only (no individual identity in redemption/engagement rows); PII reveal-on-demand into Customer 360; derived-net-new / not-built notices for MRR-ARR-refunds; DPIA "not available" for behavioural/demographic |
| Content/Taxonomy (M18) | taxonomy CRUD, legal, announcements | mixed | unwired notice, external-source-of-truth caveat |
| Insights (M19) | dashboards, reports | net-new/gated | default-off/fail-closed notice, suppression, "not available" |
| Platform (M20-M24) | admin users, global audit, ops status, flags, notifications | mixed | last-owner guard, empty-role default views, step-up, EXTERNAL link-out |

---

## 12. Existing-capability relocation contract ("nothing silently discarded")

**Mechanism (operationalized, verifiable):**

- **Reproducible inventory (source of truth):** every `requireAdminCapability('X')` literal in `src/api/admin/**` (15 capabilities); every `src/api/admin/**` route file (capability-gated files are subsumed by their capability rows in 12.1; the capability-free recipient-scoped notification route file, and the admin-auth backend that backs the login frontend, are dispositioned explicitly in 12.4); and every `apps/admin-web` page/module (the `lib/` data-access clients/hooks and shared `components/ui` primitives + `providers.tsx` relocate WITH the page/feature-module they serve, not silently dropped). Countable and grep-anchored.
- **Two-way set-equality invariant:** every source capability has at least one disposition, and every disposition names its source; the completeness check is set-equality (no source unmapped, no disposition orphaned). Split/merge is allowed.
- **Disposition values:** RETAIN-IN-PLACE, RETAIN-BUT-REDESIGN, MOVE, SPLIT, COMBINE, SUPERSEDE (requires reason plus replacement), DEAD-ENDED (substrate-only: forces a build-both-sides or scope-out decision).

### 12.1 Backend capability inventory (15) to module

| Capability | Purpose | Target module | Disposition | Boundary |
|---|---|---|---|---|
| `merchant:read` | read merchant/directory/detail/docs-view/RMV-view | M3, M5 | RETAIN-BUT-REDESIGN | BC-5 |
| `merchant:create-draft` | create draft merchant | M4 | MOVE (into Leads/Onboarding) | BC-5 |
| `merchant:submit` | submit-on-behalf | M4, M5 | RETAIN-BUT-REDESIGN | BC-5 |
| `merchant:suspend` | suspend/reactivate | M5 | RETAIN-BUT-REDESIGN | BC-5 |
| `merchant:edit` | direct-low edits | M5 | RETAIN-BUT-REDESIGN | BC-5 |
| `merchant:edit-identity` | vat/company (SUPER_ADMIN) | M5 | RETAIN-BUT-REDESIGN | BC-5 |
| `merchant:edit-category` | category (SUPER_ADMIN) | M5, M18 | SPLIT (edit vs taxonomy admin) | BC-2, BC-5 |
| `merchant:manage-branches` | branch create/soft-delete | M5 | RETAIN-BUT-REDESIGN | BC-5 |
| `merchant:propose-edit` | propose sensitive edit | M5 | RETAIN-BUT-REDESIGN | BC-5 |
| `merchant:manage-documents` | doc upload/delete | M5 | RETAIN-BUT-REDESIGN | BC-5 |
| `merchant:manage-vouchers` | RMV co-build | M5 | RETAIN-BUT-REDESIGN | BC-5 |
| `branch:confirm-location` | location confirm (target: automated map-confirm, no manual coordinates, D33; current as-built: manual lat/lng) | M5 (+ coverage read informs M22/BC-4) | SPLIT | BC-4, BC-5 |
| `approval:read` | queue/review read; per-merchant timeline | M2, M7 | SPLIT (queue vs timeline) | BC-5 |
| `approval:action` | claim/approve/reject/request-changes; voucher | M2 | RETAIN-BUT-REDESIGN | BC-3 |
| `approval:apply-edit` | apply pending/branch-lifecycle edits (incl. photos) | M2, M12 | SPLIT (edit vs media review) | BC-2 |

### 12.2 Admin-web surfaces to module

| Surface | Target | Disposition | Boundary |
|---|---|---|---|
| `(auth)/login` (email OTP) | Platform/Auth shell | RETAIN-BUT-REDESIGN (session model = separate decision) | BC-5 |
| `(app)/page` (redirect to /queue) | M1 Ops Home | SUPERSEDE (reason: a redirect is not an approved homepage; replaced by role-aware Ops Home) | BC-1 |
| `queue` list | M2 | RETAIN-BUT-REDESIGN | BC-3 |
| `queue/[id]` (5 type-dispatched panels) | M2 (+ M12 media) | SPLIT/RETAIN-BUT-REDESIGN | BC-3, BC-2 |
| `merchants` list | M3 | RETAIN-BUT-REDESIGN | BC-5 |
| `merchants/[id]` detail + edit-on-behalf | M5 | SPLIT (tabs) / RETAIN-BUT-REDESIGN | BC-5 |
| `merchants/new` | M4 | MOVE | BC-5 |
| `features/queue`, `features/review`, `features/merchants`, `features/timeline`, `features/shared` | M2/M5/M7 | RETAIN-BUT-REDESIGN | BC-3, BC-5 |
| `notification-bell`, `admin-shell` | Platform shell (M24) | RETAIN-BUT-REDESIGN | BC-1 |

### 12.4 Capability-free / foundational backend route files to module

Backend route files that carry no `requireAdminCapability` gate (so they are not subsumed by a 12.1 capability row) but back a mapped frontend:

| Backend route file | Purpose | Target module | Disposition | Boundary |
|---|---|---|---|---|
| `src/api/admin/notifications/routes.ts` (4 recipient-scoped endpoints, no `requireAdminCapability`) | admin notification read API (list / unread-count / read / read-all) | M24 | RETAIN-BUT-REDESIGN | BC-1 |
| `src/api/auth/admin/routes.ts` (login / otp-verify / refresh / logout / forgot / reset) | admin auth backend behind the `(auth)/login` frontend | Platform / Auth shell (M20 + shell) | RETAIN-BUT-REDESIGN (session model = separate decision, D4) | BC-5 |

### 12.3 DEAD-ENDED substrate (must be dispositioned, not discarded)

| Artifact | State | Disposition | Boundary |
|---|---|---|---|
| `MerchantSuggestedTag` (status enum) | live discovery consumer of `status=APPROVED`, but NO intake writer AND no admin outlet | DEAD-ENDED: M13 (build both ends) or scope-out | BC-2 |
| `ADMIN_DELIVERY_FAILED`, `ADMIN_OWNER_EMAIL_BOUNCED`, `ADMIN_REVIEW_ASSIGNED` (NotificationType) | reserved, no emitter (REVIEW_ASSIGNED also needs an assign-flow) | DEAD-ENDED: M22/M24 (delivery/bounce) + M8/M21 (review-assigned) or scope-out | BC-1 |
| `RedemptionScreenshotEvent` (fraud telemetry) | written, no admin consumer | DEAD-ENDED: M11 (build review) or scope-out | BC-3 |
| Reversibility (`AuditLog` before/after) | data exists, no revert engine | DEAD-ENDED: M11 (build revert) or scope-out | BC-3 |
| `MERCHANT_PROFILE_EDIT` (ApprovalType) | listable, no applier | DEAD-ENDED: M2 (remove or add applier; D12) | BC-3 |
| `CmsContent` (unwired), `RmvTemplate` (no version) | substrate present, gated/incomplete | GATED/NET-NEW field, per BC-2 | BC-2 |

The blueprint's implementation stage must render 12.1 and 12.2 as a full table proving set-equality (each source appears with at least one disposition; each disposition names a source). 12.3 forces the dangling artifacts into an explicit decision.

---

## 13. Gated and future register

Sub-typed: gated-by-decision (owner/legal) vs gated-by-dependency (net-new primitive). None is prototype-lockable.

| Item | Type | Shown vs withheld | Decision awaited / owner |
|---|---|---|---|
| Admin session/auth redesign (httpOnly/BFF) | by-decision | show secure-session OUTCOME; not implementation | security architecture |
| Step-up re-auth | by-dependency | show the re-auth gate outcome | security |
| Impersonation / view-as | by-decision | read-only audited view-as only; seven locked guardrails (Wave-2 prototype, M16 / 3.3): never impersonation (server-side read-projection), absolute read-only, reason + audit, 30-minute time-box + end-now, permanent banner, D60 PII tiering still applies, Support + Super Admin only (`viewas:use`) | security |
| Redemption reversal schema | by-dependency | show reversal flow, label schema-gated | eng + owner |
| Customer/DSAR processing | by-decision + dependency | show DSAR queue; gate real PII/erasure | legal + owner |
| Analytics / privacy (behavioural/demographic) | by-decision | operational aggregates OK; behavioural DPIA-gated ("not available") | owner + legal |
| Payment/provider operations (billing/refunds/featured payment; Resend/FCM) | by-dependency + provider | show flows; label provider/billing-gated; fix `FeaturedMerchant.paymentStatus` | owner + provider |
| Members and Revenue read layer (member list, subscription lifecycle, MRR/ARR/churn/conversion) | by-dependency | build the read-side surfaces (net-new; no admin customer/subscription endpoints today); MRR/ARR derived, no revenue table | eng + owner |
| Subscription payment-source breakdown + admin-grant (complimentary) subscriptions | by-dependency | show the OUTCOME; label FUTURE (Stripe-only today; `Subscription.source` enum not built; admin-grant commented, not built) | owner + eng |
| Refunds / chargebacks / dunning beyond PAST_DUE (`RefundLog`/`DisputeLog`) | by-dependency + provider | show flows; label NOT-built (no refund/dispute tracking today; dunning stops at PAST_DUE) | owner + provider |
| Member behavioural / demographic analytics (member locality, age/gender, interest + retention cohorts) | by-decision | keep DPIA-gated in Insights (M19); operational member/subscription/revenue aggregates are fine; behavioural/demographic default-off, minimum-cohort, "not available" | owner + legal |
| Deployment / `admin.redeemo.co.uk` + CORS + strict posture | by-decision | note as pre-launch gate | platform |
| `AdminCapabilityGrant` / per-person delegation | by-dependency | show grant OUTCOMES; note fixed-role cannot express per-person grants | authz |
| `MerchantLead`/`MerchantSource` / rep-assisted channel / fast-track / provenance | by-dependency (design-only-in-specs) | design as build-on-spec | owner + eng |
| Automated map-confirm for admin location confirmation (no manual coordinates; write `ADDRESS_GEOCODED`) | by-decision (DECIDED) + dependency | owner-DECIDED design intent (D33, 6.1.2); net-new build (promote Google suggestion to pin, write the tier, map-confirm UI); reverses the prior manual-lat/lng position | owner (decided) + eng |
| Business verification concept (Google Place Details / Companies House / FHRS / duplicate detection / findings snapshot) | by-dependency | NOT-built; design a labelled findings panel per the onboarding design spec §5 (D34, 6.1.3); manual `verificationStatus` flag + admin-uploaded docs only today; auto-approve nothing | owner + eng |
| Admin edit "send to merchant for approval" + per-merchant apply-direct-vs-propose preference | by-dependency | NOT-built (D35, 6.1.4); today = DIRECT edits + admin-self-review PROPOSE lane; label as future concepts, not available | owner + eng |
| Correction-on-behalf flow: direct-apply COSMETIC corrections, MATERIAL-correction-to-merchant, two-court queue tabs / whose-court pill / merchant-side waiting-age, and the send-to-merchant / merchant-accepted event types | by-dependency (DECIDED design intent, D37) | owner-DECIDED (D37, 6.1.4); REAL substrate = voucher concierge round-trip + `AuditLog` before / after; the direct-apply COSMETIC path, identity-correction-to-merchant, the two-court split, and the correction event TYPES are net-new; design as the target, label the net-new parts | owner (decided) + eng |
| Operations-editable VAT / company number / category / tags | by-decision (DECIDED, D38) + dependency | owner-DECIDED (D38, 3.2 / 6.3); NOT-YET-IN-CODE (backend still gates to Super Admin via `merchant:edit-identity` / `merchant:edit-category`); capability CHANGE to implement; exact per-role grant matrix is a later decision (do NOT present a finished matrix); business / trading NAME does NOT move (stays on the D37 material lane) | owner (decided) + authz/eng |
| Merchant Directory supply-window roll-ups / activity-dormancy / contract renewal-expiry / UK supply map / CSV export / message-a-segment bulk | by-dependency (DECIDED redesign, D39) | owner-DECIDED (D39, 6.1.5 / M3); REAL = category, location / region, branches, active vouchers, redemptions (aggregate), rating, featured, contract, created; NET-NEW = the summary roll-ups, activity / dormancy, renewal / expiry (`contractEndDate` never populated), the map surface (BC-4: business locations only, never customer homes), export, bulk-message (BC-3, provider-gated) | owner (decided) + eng |
| Customer-facing banner editing (merchant cover + per-branch banner / logo) | by-dependency | owner-DECIDED display (D40, 6.1 / 6.1.1 / 6.3): the operator SEES `Merchant.bannerUrl` + `Branch.bannerUrl` / `Branch.logoUrl` (REAL, customer-facing); EDITING the images stays NOT-YET-SUPPORTED (no admin route today) | owner + eng |
| Per-voucher CATEGORY VALUE BENCHMARK (cross-merchant same-category value / saving distribution: peer-position marker + median + percentile / quartile) | by-dependency (DECIDED design intent, D41) | owner-DECIDED (D41, 6.1.1 / M2 / M5); the underlying data is REAL (`estimatedSaving` + category), but the cross-merchant category aggregation / percentile is NOT built (net-new). Design as the target supply-quality lens; NEVER present a live benchmark as built | owner (decided) + eng |
| Advisory voucher value indicator is ADVISORY, not a gate (D41 note) | by-decision (DECIDED, D41) | owner-DECIDED (D41, 6.1.1): the weak / good / strong indicator is CLIENT-SIDE ADVISORY ONLY (`scoreVoucher`, ~GBP 5 advisory floor); NOT persisted, NO enforced minimum-saving gate, `RmvTemplate.minimumSaving` is a DEFAULT not a gate. Show it labelled advisory; do NOT imply a hard minimum. An enforced value floor is a separate future decision, not built | owner + eng |
| Merchant 360 Performance tab aggregate enrichment (redemptions-by-branch / -by-voucher, savings-delivered, validation-rate + QR-vs-manual split, category-performance-benchmark) | by-dependency (DECIDED design intent, D42) | owner-DECIDED (D42, 6.1.6 / M5 / M28); AGGREGATE-ONLY, never an individual identity; per-merchant aggregation is NOT built (`VoucherRedemption` has no `merchantId` col; join via `branch.merchantId` or via the voucher; savings on the fly from `estimatedSaving`); the category benchmark needs cross-merchant category aggregation NOT built (real inputs = this merchant's own metrics + its category, never another merchant's detail). Design as the target; label net-new | owner (decided) + eng |
| Merchant 360 Redemptions tab + individual-redemption lookup (per-merchant history rows + code lookup + global-search entry + Customer 360 link) | by-dependency (DECIDED design intent, D43) | owner-DECIDED (D43, 6.1.6 / M5 / 6.3); NET-NEW admin capability (no admin per-merchant redemption viewer today; per-merchant history + aggregation are aggregation-to-build); reachable from global search, Customer 360, and the per-merchant tab; individual rows never show identity in aggregate, identity is governed by D48. Design as the target; label net-new | owner (decided) + eng |
| Merchant 360 Staff and access on-behalf management + operator-parity-on-behalf (`merchant:manage-staff`) | by-dependency (DECIDED design intent, D44) | owner-DECIDED (D44, 1.3 / 6.1.6 / 6.3 / 9); REAL = the Merchant Portal Staff and Access model (shipped); NET-NEW = the admin-side on-behalf equivalent (no admin staff-management endpoints today) covering portal-access members (invite via tokenised claim link, role / scope, can-manage-vouchers, re-issue link, soft-remove + last-Owner guard) + app / branch logins (reset password, deactivate / reactivate; add-new = thinner cap); no impersonation, no password set / view, PIN never shown here; operator-parity-on-behalf subject to the D37 lanes. Design as the target; label net-new | owner (decided) + eng |
| Branch manager + notification routing (branch manager defaults to owner + nudge; per-branch / owner notification-recipient preferences) | by-dependency (DECIDED design intent, D45) | owner-DECIDED (D45, 6.1.6 / 8.1 / D36); refines D36 (the assigned-manager item is a SOFT advisory, defaults to owner + nudge, NOT a hard gate); recipient = the branch's assigned Branch-manager (defaults to owner if none), owner opts in per branch (off by default). NET-NEW / Phase 6: recipient-routing + per-branch / owner notification preferences (Phase 6 comms unwired; alert-recipients + notification-preferences un-built schema gaps; redemption email to branches deferred). Label as a concept, not live | owner (decided) + eng |
| Branch PIN admin on-behalf view + reset (`branch:manage-pin`; reveal-on-demand + reset, reason-required + audited) | by-decision (DECIDED, D46) + dependency | owner-DECIDED (D46, 6.1.1 / 6.1.6 / 6.3); REVERSES the prior "branch PIN never shown / hidden by policy" invariant. REAL infra: `Branch.redemptionPin` AES-256-GCM encrypted (server-decryptable); the Merchant Portal has PIN get / put / send routes (SMS via Twilio live; email via Resend Phase 6). NET-NEW: the admin on-behalf view / reset. PIN value never in logs or audit; never shown on other surfaces. PRECISION: the SMS send / delivery channel (live) is DISTINCT from the Phase-6 email notification-recipient routing of D45; email delivery of the PIN to a manager is NOT live | owner (decided) + eng |
| Merchant 360 Overview primary-contact block (account owner name + email + optional phone; Copy / Email / Call; multi-owner "+N more owners") | by-dependency (DECIDED design intent, D47) | owner-DECIDED (D47, 6.1 / 6.1.6); schema-grounded: NO business-level contact email / phone (`Merchant` has only `businessName` / `tradingName` / `websiteUrl`); the merchant contact IS the owner / primary `MerchantAdmin` (email + optional phone); branch phone / email are per-branch. Design as the target | owner (decided) + eng |
| Role-gated customer identity on individual redemptions (`redemption:view-customer` / `redemption:reveal-customer`; view / reveal always audited) | by-decision (DECIDED, D48) + dependency | owner-DECIDED (D48, 3.2 / 6.1.6 / 6.3 / M28); REPLACES the flat "redemption rows never show individual customer identity" invariant with "aggregate surfaces never show individual customer identity; individual redemption identity is role-gated and always audited". UK-GDPR grounding: lawful access for legitimate purposes, but Article 32 access-control + Article 5(2) accountability + data-minimisation still apply to internal access. Aggregate NEVER shows identity; individual identity visible-by-default for customer-facing roles, reveal-with-reason for restricted, no-access for others; EVERY view / reveal audited. Exact per-role-to-tier mapping = with the capability matrix (illustrative in the prototype). Cross-ref D32 (retained-demographic-data / DSAR; the access audit supports accountability) | owner (decided) + eng + legal |
| Representative-assisted (rep-led) on-behalf onboarding wizard (`merchant:assisted-onboard`; 9-step full-screen presentation-mode flow) | by-dependency (BUILT in the prototype, DESIGN-ONLY, D49) | owner-DECIDED prototype (D49, 8.2 / 8.2.1 / 8.4); channel B (a rep physically with the business builds the ENTIRE account on-behalf so a non-technical merchant never touches the portal). REAL = shell-create + claim / transfer (`createMerchantDraft` + `issueMerchantClaim`) + `merchant:create-draft`; NET-NEW = the full on-behalf wizard + `merchant:assisted-onboard`. Presentation mode is a merchant-safe focus mode, NOT a separate login or a security boundary (still the operator's own audited session). LEGAL LOCKS: the operator never accepts the contract (owner signs in person) and never sets / sees a password (owner takes ownership via a tokenised claim link). Go-live fork LOCKED = rep approves inline, audited, in-person verification captured as evidence, rep-created-and-approved merchants flagged for post-hoc audit. Design as the target; label the wizard net-new | owner (decided) + eng |
| Admin Taxonomy and Rules management module (D50 / D54; taxonomy + dependent content promoted from hardcoded config to admin-managed DATA) | by-dependency (PARKED; net-new schema + backend) | owner-DECIDED as its own PARKED admin surface (D50 / D54, extends BC-2). Manage categories / subcategories / cuisines / specialties / tags PLUS their dependent content (per-category voucher suggestion chips + per-type examples, category-to-term caution flags, `CategoryAmenity` amenity-per-category eligibility, other category-dependent config). ARCHITECTURAL NOTE (honest): today ALL of this is HARDCODED in code (config map + term flags + `CategoryAmenity` seed; top-level granularity only) and is read by BOTH the merchant-portal voucher builder AND the admin assisted builder; making it admin-managed = promoting hardcoded config to DATA with one source of truth feeding both surfaces (net-new schema + backend). Pairs with Suggested-tag Moderation (management = create side, moderation = review side). OPEN when designed: operator "add your own" = immediate vs route-to-moderation vs hybrid (taxonomy is platform-wide, lean controlled). D54 EXPANDS D50 (same module; D54 adds the dependent content). Until it exists, the assisted flow's "add your own" subcategory / cuisine / specialty stays deferred-labelled | owner + eng |
| Admin Agreements, Policies and Content management module (D55; version-controlled, surfaced across the 3 product surfaces) | by-decision (GATED) + by-dependency (net-new backend) | owner-DECIDED as its own PARKED admin surface (D55, extends BC-2). Manage the Redeemo Merchant Agreement plus other contracts / policies / legal content shown across the customer app, customer website and merchant portal, with version control, so edits happen from the admin panel. ARCHITECTURAL NOTE (honest): today the agreement text is placeholder / MVP, the real binding legal text is owner / legal-owned and is a launch sign-off GATE, the static customer-web legal pages are the launch source of truth, and an unwired / deferred `CmsContent` concept already exists in code (plus `MerchantContract.tcVersion`, the agreement version the assisted flow references). D55 = that `CmsContent` path made real, versioned and surfaced across the three product surfaces = substantial net-new backend. PARKS alongside the D50 / D54 taxonomy module | owner + legal + eng |
| Internal operator notes surface (merchant Notes tab BUILT; customer + onboarding notes DEFERRED; `notes:read` / `notes:write`) | by-dependency (net-new schema throughout) | owner-DECIDED prototype (D51, 6.1 / 6.1.1 / 6.2 / 3.2). MERCHANT Notes tab is BUILT in the prototype (net-new schema): note list (author + role, timestamp, body, attachments, status active | retracted), ADD (free text + optional attachment via signed-link storage; audited), EDIT (role-gated; marks edited + keeps prior version), RETRACT (SOFT-delete: stays visible, "Retracted by X on date" + optional reason, NEVER hard-deleted), per-note HISTORY, every add / edit / retract AUDITED to Global Audit; `notes:read` / `notes:write`. Per-DOCUMENT operator notes are also captured on the assisted Documents step. DEFERRED: Customer 360 notes (personal data + DSAR-disclosable; factual / professional / role-gated / audited; design as if the subject could read them) + onboarding-section general notes. Net-new schema throughout | owner + eng (+ legal for customer notes) |
| Assisted voucher builder = faithful port of the merchant-portal flagship builder (D52) | by-dependency (BUILT in the prototype, DESIGN-ONLY) | owner-DECIDED prototype (D52, 8.2.2 / 6.1). Category-driven fields / placeholders / suggestion chips / per-type examples / terms, keyed off the Step-1 category resolved to its top-level parent (11 parents + a fallback; top-level granularity only). Uses the REAL `scoreVoucher` advisory maths (title-clarity + description-personalised >= 30 chars + saving floor ~GBP 5 with generosity percentage + term-stacking + photo; tier Too weak / Good / Great); ADVISORY, not a submit gate (a Too-weak voucher still saves; admin review is the backstop). NO fake peer benchmark (the "compares to similar businesses" line has NO real numbers; a real cross-merchant category-median benchmark is NET-NEW / not built). Sticky score-plus-preview side rail. NO concierge "ask the Redeemo team" toggle in the assisted flow (the rep IS the builder). Type picker = 5 eligible (BOGO recommended / Spend and save / Discount fixed + percent / Freebie / Package) + 2 disabled (Time-Limited / Reusable = custom day-2). REAL = `scoreVoucher`; NET-NEW = the category-median benchmark | owner (decided) + eng |
| Always-on voucher terms in the assisted flow = generic-only (D53; a deliberate deviation from the merchant-portal core) | by-decision (DECIDED prototype) | owner-DECIDED prototype (D53, 8.2.3). The locked always-on CORE = tell-staff + show-in-app + no-cash-value; "Not valid with any other voucher" and "One voucher per customer each visit" MOVE to optional (pre-ticked, removable). Per-type Fair + category-conditional Caution (service businesses show none) + Discount-percent Restrictive minimum-spend + add-your-own (80 chars, auto-tagged Restrictive on restrictive wording). This is an owner-confirmed DEVIATION from the merchant-portal CORE (which keeps tell-staff + no-combine + per-visit as its 3 fair core, all as unlocked checkboxes), for the admin assisted flow ONLY | owner (decided) + eng |
| Merchant 360 edit-on-behalf parity + live / pending state model (D56; admin-direct + merchant-confirm lanes) | by-dependency (PARKED-BUT-NEXT; net-new) | owner-DECIDED as its own focused pass (D56, 6.1 / 16). Admins / reps edit almost all fields for EXISTING merchants from Merchant 360 (parity with the assisted capture; completes D44). TWO LANES: (1) admin-DIRECT (applies immediately, live, audited, no pending) for platform-owned + corrective + operational fields (category / reclassification, status, location map-confirm, PIN reset, verification / documents / notes, amenities, photo moderation); (2) merchant-CONFIRM-required (PENDING until the merchant confirms, then live) for self-representation + commercial + legal-identity fields (legal / registered name, description, public contact, voucher offers / terms, opening hours, public branch name). STATE MODEL: each field / section is Live, Pending (old value stays live + customer-visible; old-to-new diff + "awaiting merchant confirmation") or optionally Draft; a 360-level "N pending changes" indicator; admin-direct fields never enter Pending. GO-LIVE for the pending lane = merchant confirms (portal notification / next login) OR admin attests in person ("confirmed with owner") for portal-non-using / rep-managed merchants (mirrors the assisted in-person model; evidence-trailed). EXTENDS `MerchantPendingEdit` / `BranchPendingEdit` + the Option B applier (merchant-to-admin direction shipped); D56 ADDS the admin-direct lane + the admin-to-merchant-confirm direction. Exact field-to-lane assignment = an OWNER DECISION to lock when the pass is designed. REAL = the pending-edit substrate + the Option B applier; NET-NEW = the admin-direct lane, the admin-to-merchant-confirm direction, the per-field Live / Pending / Draft state model | owner (decided) + eng |
| Combined / multi-module admin access (D57; multi-role union OR per-person capability grants) | by-dependency (FUTURE concept, DESIGN-ONLY) | owner-DECIDED DESIGN-ONLY prototype (D57, 3.1 / 3.2 / 1.9 Team and Roles). Today an admin holds EXACTLY ONE fixed role (`AdminRole` is a scalar, not an array; Super Admin is the only "everything" role, over-broad not a curated subset). Giving one person several modules at once (Operations + Finance + Content) is a FUTURE concept via one of TWO net-new models, both shown clearly-labelled FUTURE on the Team and Roles screen (never a live toggle): (1) MULTI-ROLE assignment (access = the UNION of several whole roles; schema `role` -> `roles[]` or a join table); (2) PER-PERSON CAPABILITY GRANTS (base role + add / remove individual caps for that one person; the design-only `AdminCapabilityGrant`). The exact model AND when to build are a LATER capability-matrix / Tier-3 authz decision; do NOT present either model as chosen or built | owner + authz |
| Team and Roles staff record (D58; basic staff / employment fields for panel onboarding + access) | by-dependency (BUILT in the prototype; richer fields net-new schema) | owner-DECIDED DESIGN-ONLY prototype (D58, 3.1 / M20 / 1.9). The former "Admin Users and Roles" (1.9) is reframed as "Team and Roles": these are Redeemo STAFF / team members who hold admin-panel access, DISTINCT from the merchant-side "Staff and access" (a merchant's own branch / portal staff, D44). Holds the BASIC operational + employment fields to onboard a staff member to the PANEL and manage access: name, work email, role, job title, department / team (Operations / Finance / Support / Content / Trust and Safety / Leadership), manager (reports-to), start date, employment type (Employee / Contractor), plus optional preferred name, work phone, staff ID, working location, invited-by + invited-date. Invite captures the essentials; the record holds the fuller set (via a View-record expand). BOUNDARY: the admin panel is NOT the HR system; fuller HR / employment-lifecycle data (pay, DOB, home address, personal non-work contacts, HR documents) lives in the dedicated HR tool (for Redeemo, e.g. Zoho People), NOT here. (Historical note: the owner briefly considered making the panel the single centralized store of all staff / HR data, then RETRACTED that; the HR system stays.) REAL = name, work email, role, status (isActive), last-active (lastLoginAt), account-created (the lean `AdminUser` record today); NET-NEW (needs schema) = every richer staff field above | owner + eng |
| Assign-then-claim workflow (D59; assigner roles assign a queue item / merchant / task, assignee notified then claims) | by-decision (DECIDED) + by-dependency (net-new schema; DEFERRED to Wave 2 first pass) | owner-DECIDED DESIGN-ONLY prototype (D59, 3.1 / 9 / 1.11 Notifications). Certain ASSIGNER roles (a NET-NEW capability, e.g. `approval:assign` or `task:assign`; likely Super Admin + a lead / senior-Operations tier, possibly the proposed Platform-Manager mid-tier persona) can ASSIGN a queue item / merchant / approval / task to a SPECIFIC user, instead of everyone only self-claiming. The assignee RECEIVES a notification (the `ADMIN_REVIEW_ASSIGNED` notification TYPE is REAL, but its emitter is currently DEFERRED precisely because this assign-flow does not exist yet: the "emitter deferred" case flagged on the 1.11 Notifications screen and the reserved-alert-emitters register). ACCOUNTABILITY INVARIANT: assignment ROUTES, claim COMMITS; single-actor claim-to-act accountability preserved (assignment says "this is yours to pick up", the assignee still claims-to-act before acting); self-claim and assign COEXIST. NET-NEW: `AdminApproval` has `claimedById` (self-claim) but NO `assignedToId` / `assignedById` / `assignedAt` columns, so this needs a small schema addition + the assign capability + firing the existing notification + an assigned-state on the queue, ALL audited (who-assigned, who-assigned-to, who-claimed). EXTENDS the two-court queue + claim model (D37, 6.1 / the Approval Queue). DEFERRED: owner-decided as the FIRST focused Wave-2 pass, NOT built in Wave 1 | owner (decided) + eng |
| Member PII visibility tiering on Customer 360 and the member surfaces (D60; REPLACES blanket customer-PII masking) | by-decision (DECIDED) + dependency | owner-DECIDED (D60, 6.2 / M25 / M10): sees-by-default (Support / Operations / Super Admin; every access audited) / reveal-with-reason (Finance) / no-access (Content / Sales); SEARCH respects the same tiering (results are never a side door to PII a role cannot see); reporter identity on review reports stays MASKED for every role (reason-gated reveal); precise-geo prohibition (BC-4) and the D48 aggregate rule unchanged. The customer read capabilities + access-audit plumbing are net-new | owner (decided) + eng + legal |
| Customer messaging / the sending home (D61) + the comms read-and-send layer (`comms:read` / `comms:send`) | by-decision (DECIDED) + dependency + provider | owner-DECIDED (D61, 7.1 / 6.2 / M7): Customer 360 read-only for communications; anything that sends lives in Communications, Support case replies, and (later) Campaigns bulk; sends template-based only + consent-checked (marketing needs stored consent; transactional exempt) + audited + provider-gated (while email is dark the UI says "Queued; sends when email goes live", never "Sent"). The log reader / template library / one-off send are net-new over the REAL `CommunicationLog` outbox | owner (decided) + eng + provider |
| Hybrid review-screening engine (D62: hard-block + auto-flag + customer reports) | by-dependency (DECIDED design intent) | owner-DECIDED (D62, M10): three nets: (1) hard-block outright profanity / slurs at submission (word-list + normalisation; customer asked to rephrase; never publishes); (2) auto-flag borderline content (possible defamation, accusations, aggression: publishes but lands in the Reviews Moderation queue immediately, no customer report needed); (3) customer reports remain the third net and the ONLY net that exists today. The screening engine is NET-NEW backend belonging to the review-system v2 workstream; pre-moderation explicitly rejected | owner (decided) + eng |
| Template ownership split (D63: transactional code-owned; marketing panel-editable later) | by-decision (RECOMMENDED, encoded in the prototype) | RECOMMENDED (D63, 7.1): transactional email templates stay code-owned + read-only in the panel (they carry security-critical content such as reset links and branch PINs); marketing templates become panel-editable LATER via the parked D55 Agreements / Policies / Content module. Template-management UI net-new | owner + eng |
| Planned automated sends (D64) + the customer email-verification template gap | by-dependency | shown as FUTURE "Planned automated sends" (D64, 7.1): redemption-details email to the branch manager per validated redemption (deferred merchant-portal redemption-notification slice + alert-recipients schema-ledger item); monthly merchant statements (locked merchant-portal Phase 4 architecture); the MISSING customer email-verification template (only the business variant exists among the nine real templates, despite the app hard-blocking on email verification); the advisory Phase-6 customer-lifecycle list. Wiring = Phase 6 + merchant-portal Phase 4 decisions | owner + eng |
| Internal evidence-pack signature channel (D65; no external e-signature setup) | by-decision (DECIDED) + dependency | owner-DECIDED (D65, 8.4): the third signature channel = an internal upgrade of click-to-agree (version-controlled contract text via D55; scroll-through + typed-name ceremony + optional drawn signature; a signed-PDF snapshot of the exact version in platform document storage; an evidence record with identity / timestamp / IP / device via the audit log + a hash of the signed file; an admin verification view). External providers (e.g. Zoho Sign) deliberately NOT set up (future special-case only); sufficiency sits inside the already-planned pre-launch solicitor review. Net-new build on the REAL `MerchantContract` evidence fields (`signedAt` / `ipAddress` / `tcVersion` / `signatureMethod` + nullable `zohoSignRequestId`); the comparison surface sits behind Legal and Agreements | owner (decided) + legal + eng |
| CMS content / `CmsContent` wiring; legal content | by-decision | leave `CmsContent` unwired; legal external, owner/legal sign-off (hard launch gate) | owner + legal |
| Incident/StatusEvent model, ops-status history, alert emitters | by-dependency | v1 read panels only; Incident model gated | eng |
| Deployed-SHA / version endpoint (source-vs-deploy drift) | by-dependency | show version/drift when built; until then show external/unavailable, never fabricate | eng (platform/backend) |
| Dependency health (DB/Redis) beyond `/health` liveness; worker heartbeat | by-dependency | show liveness + external for real dependency health; heartbeat net-new | eng (platform/backend) |
| APM / error-monitoring / log aggregation | by-decision + provider | ABSENT today; deep-link out when adopted; never fake in-app | owner + provider |
| Support/case system | by-decision | build-vs-integrate (Zoho) | owner |
| Reserved alert emitters (`ADMIN_DELIVERY_FAILED`/`_OWNER_EMAIL_BOUNCED`/`ADMIN_REVIEW_ASSIGNED`) | by-dependency | smallest gated increment (delivery/bounce on email-enable; review-assigned needs an assign-flow) | eng |
| Membership / partnership / referral schemes (university/student D19; NHS/key-worker/community D20; referral/ambassador/creator D21; consumer waitlist D22) | by-decision | preserve as FUTURE ideas; NO prototype module/screen/eligibility-workflow/schema without owner approval; NHS/key-worker/community is owner-DEFERRED (D20); IA placement (dedicated module vs Growth-and-Commercial submodule vs Relationship-Operations submodule vs cross-module workspace) is undecided; every eligibility scheme carries the standard gate set (lawful basis, purpose limitation, minimisation, verification method, retention/deletion, access control + audit, fraud/abuse controls, DPIA screening) and needs Article 9 / legal-specialist review only if its chosen evidence collects or reveals special-category data | owner (+ privacy) |

---

## 14. Cross-cutting concerns

- **Observability of admin actions (Global Audit / Activity Explorer, M21):** genuinely absent (only per-merchant timeline). Sequence EARLY as the oversight backstop for bulk and delegated grants. `AuditLog` has actor/entity/event/before/after/reason; a global query endpoint is net-new.
- **Platform / infrastructure Control Room (BC-1 bounded status):** M22 presents safe summaries, status, freshness, responsible role, runbook and safe provider deep links; it is NOT an embedded replacement for Railway, Neon, Vercel, GitHub, Stripe, Resend or a future APM. Apply the five-layer classification (source / configured / deployed-runtime / provider-account / observability), keep infrastructure state separate from application-consumer state, and treat provider / runtime bindings as UNVERIFIED (owner-reported where applicable), never inferred. `/health` is liveness-only; DB / Redis dependency health, worker heartbeat, deployed-SHA / version and APM are net-new or external; the queue / outbox / keyring / notification substrates exist but their Admin read APIs are net-new. Provider credential / rollout operations (for example applying rotated Resend or Google keys to their verified consumer) are an incident / provider-operations workstream, not an Admin product surface.
- **Data-export safety (purpose-scoped, NOT one universal filter):** a cross-cutting minimum applies to every export (`CommunicationLog.payload` never selected; branch `redemptionPin` never exposed; PII minimised to the authorized purpose; every export audited). Beyond that, policy splits by purpose, each with purpose-specific authorization, minimisation, redaction and retention:
  - **Analytics / report exports** apply the Insights eligibility cleanliness (exclude `isTestData` on redemption+branch+merchant, QA emails, `User.status=DELETED`) plus cohort/privacy controls and suppression. These are analytics-cleanliness rules, NOT universal operational or legal rules.
  - **Operational / support exports** are scoped to the support purpose and may legitimately include test-owned or edge records that the analytics rule excludes.
  - **DSAR / legal / retention exports** MUST include the data subject's records even when `DELETED`/anonymised (that record is the point), governed by legal basis + retention schedule.
  - **Audit / incident exports** are audit-trail evidence, governed by incident/audit authorization.
  DELETED or test-owned records must not be universally hidden where the authorized purpose requires them. Two Merchant-side CSV exports exist, with different privacy and gating semantics: an operational redemption export (`/api/v1/merchant/redemptions/export.csv`, `src/api/merchant/redemptions/routes.ts`) and a DPIA-gated event-level Insights export (`/api/v1/merchant/insights/export.csv`, `src/api/merchant/insights/routes.ts`). Neither is an Admin export, and neither belongs to blueprint module M3 (Merchant Directory); a cross-cutting Admin export contract remains NET-NEW.
- **Notification preferences:** the admin bell exists; a per-admin mute/preference model is net-new; the three reserved alert enums (`ADMIN_DELIVERY_FAILED`/`_OWNER_EMAIL_BOUNCED`/`ADMIN_REVIEW_ASSIGNED`) have no emitter.
- **Segregation of duties as policy (not per-module):** the shipped approval submit/approve split is separate capabilities and workflow stages but NOT role/actor separation, maker-checker or four-eyes (both `merchant:submit` and `approval:action` are OPERATIONS-held with no submitter-vs-approver guard, so one OPERATIONS admin can submit, claim and approve the same item); different-actor countersign is net-new; money-sensitive actions follow the target SALES-initiates / FINANCE-processes split.
- **PII and DPIA:** dedicated customer capabilities, audited PII access, precise-geo prohibition, retention-on-deletion parity, behavioural/demographic fail-closed.
- **Security hardening outcomes (design the outcome, not an approved mechanism):** the outcomes are to reduce unattended-session risk, to require renewed assurance before destructive / lifecycle actions, and to restrict Admin access according to an approved contextual / network-access policy. Candidate controls may include idle-session timeout, step-up re-auth and IP allow-listing, but NO mechanism is selected by this blueprint; selection remains gated by D4 / D5 and the security architecture review.
- **Richer merchant identity, evidence and locality signals (net-new, proposed):** a fuller business / legal identity (company / VAT number, registered / head-office address distinct from branches, accountable-signatory record), sector-specific evidence document types with a required-document gate, post-live merchant monitoring (for example FHRS drop, Google closed / stale, voucher-floor breach), and per-locality supply-coverage / launch-readiness signals are net-new substrate and are proposed, not approved implementation.
- **Accessibility and i18n:** a11y basics are a DoD requirement (roles/labels/focus/keyboard); i18n is deferred (UK-only launch) but stated, not omitted.
- **Brand:** admin-web is neutral-brand today; brand alignment (borrow merchant-web tokens/fonts as a reference, keep admin density) is a separate design decision, not locked here.

---

## 15. Prototype scope and wave plan

Oversight and governance concepts visible early, without pretending their implementations are approved.

- **Wave 1 (foundation + spine):** global shell + role-aware Ops Home (the Ops Home also carries an OPERATIONAL member/subscription/revenue snapshot: members by tier, MRR, active-member rate, new subs today, past-due count, beside the merchant supply signal; the gated member-analytics KPIs stay gated); Approvals/Review (relocated, redesigned, all lanes incl. media/photo); Merchant Directory; Merchant 360 (read + existing edit-on-behalf per the authority matrix); Global Audit / Activity Explorer + Admin Users and Roles (oversight-first, backend net-new but design-early); Operational Status (v1 read panels); Notifications.
- **Wave 2 (relationships + trust and safety + leads + core member/revenue):** Customer 360; Members and Subscriptions (M25 core: member list + member/subscription detail, cross-linked into Customer 360) + a Revenue and Billing (M26) snapshot; Leads and Onboarding (both channels, assisted flow design); Communications + Tasks + Account Health; Reviews Moderation, Suggested-tag Moderation, Fraud/Reversals (schema-gated); Support and Cases (build-vs-integrate framing); DSAR and read-only View-as.
- **Wave 3 (commercial + members-revenue depth + content + insights, mostly gated/future):** Campaigns/Featured/Trending; the deeper Members and Revenue surfaces (Revenue and Billing MRR/ARR + dunning, Promo Codes and Trials, Redemptions and Engagement aggregate, Conversion funnel) plus the DPIA-gated member insights routed to Insights and Reporting; Content and Taxonomy (versioned legal/FAQ as EXTERNAL/GATED, taxonomy CRUD, announcements); Insights and Reporting (reuse foundation as reference; operational vs behavioural split); Feature flags/config.

Claude Design packaging (D14): one shared master blueprint/context, plus separate reviewable wave-specific prompt packs (not written yet). Oversight/governance concepts appear in Wave 1.

**Wave status (prototype checkpoints; recorded per sign-off).** Wave 1 = 11/11 screens complete; CP-1 approved and locked by the owner on 2026-07-03. Wave 2 = 13/13 screens complete (2.1 Customer 360 · 2.2 Communications · 2.3 Tasks and Follow-ups · 2.4 Account Health · 2.5 Reviews Moderation · 2.6 Fraud and Reversals · 2.7 Suggested-tag Moderation · 2.8 Support and Cases · 2.9 DSAR · 2.10 Read-only View-as · 2.11 Leads pipeline amendment · 2.12 Signature channels · 2.13 Members and Subscriptions); **CP-2 approved and locked by the owner on 2026-07-05**, carrying decisions D60 to D65 (Section 16). CP-2 carries (non-blocking, tracked for the next touch): (1) the 2.1 Customer 360 honest-status banner still carries the pre-D60 blanket-mask wording (update to the tiered framing on the next Customer-360 touch); (2) the MRR definition lock: the prototype's derived MRR excludes Trialling and Past-due (a conservative choice), and the definition is deferred to the owner / Finance when revenue reporting is built; (3) the Account Health permission-denied chip is absent from the state toggle (the state itself is reported present); (4) verify the Media/Photo Review nav item (un-gated in the sidebar; confirm what sits behind it); (5) CP-1 leftovers: the Finance Ops-Home visual eyeball, the 1.1 submitting spinner, the exact role-sets for the three demo-broadened capabilities (audit read, operational status, admin manage), and the D57 combined-access model choice; (6) the nav rename to "Legal and Agreements".

---

## 16. Decision register (status recorded per row)

Approved process decisions: D1, D2, D14. D20 is owner-DECIDED (FUTURE / DEFERRED, see below). D27 is owner-DECIDED (the Members and Revenue domain + Growth reorg, 2026-07-02). D33 is owner-DECIDED (automated map-confirm for admin location confirmation, reversing the prior manual-lat/lng position, 2026-07-02). D37, D38, D39, D40 and D41 are owner-DECIDED (2026-07-02): the correction-on-behalf model + two-court queue + per-item audit (D37); the VAT / company number / category / tags capability reclassification to Operations-editable (D38); the Merchant Directory supply-window redesign (D39); Merchant 360 as a per-merchant workspace distinct from the Directory list, with customer-facing banner display (D40); and the voucher-block full-detail enrichment + advisory value indicator + per-voucher category value benchmark (D41). D42, D43, D44, D45, D46, D47 and D48 are owner-DECIDED (2026-07-02): the Merchant 360 Performance tab aggregate enrichment (D42); the Merchant 360 Redemptions tab + individual-redemption lookup (D43); the Staff and access on-behalf management + the operator-parity-on-behalf principle (D44); the branch-manager + notification routing refinement of D36 (D45); the branch PIN admin on-behalf view + reset, reversing the "PIN never shown" invariant (D46); the merchant primary-contact block on the Overview tab (D47); and the role-gated customer identity on individual redemptions, replacing the flat "redemption rows never show identity" invariant (D48). D49 to D56 are owner-DECIDED DESIGN-ONLY prototype decisions (2026-07-03): the representative-assisted (rep-led) 9-step on-behalf onboarding wizard (D49, BUILT in the prototype; shell-create + claim REAL, the wizard NET-NEW); the Admin Taxonomy and Rules management module (D50 / D54, PARKED, net-new; D54 expands D50 and is the SAME module); the internal operator-notes surface (D51, merchant Notes tab BUILT, customer + onboarding notes DEFERRED, net-new schema); the assisted voucher builder as a faithful port of the merchant-portal flagship builder (D52, BUILT in the prototype; `scoreVoucher` REAL logic, the category-median benchmark NET-NEW); the generic-only always-on voucher terms in the assisted flow (D53, BUILT in the prototype; a deliberate deviation from the merchant-portal core); the Admin Agreements, Policies and Content management module (D55, PARKED, net-new); and the Merchant 360 edit-on-behalf parity + live / pending state model (D56, PARKED-BUT-NEXT, net-new). D57 to D59 are also owner-DECIDED DESIGN-ONLY prototype decisions (2026-07-03): the combined / multi-module admin access FUTURE concept on the 1.9 Team and Roles screen (D57, two net-new models: multi-role union / per-person capability grants; today a single fixed role; model + timing a later capability-matrix decision); the Team and Roles staff-reframe (D58, BUILT in the prototype: the reframe + a basic staff record for panel onboarding + access; boundary = NOT the HR system, HR data stays in the HR tool e.g. Zoho People; richer staff fields net-new schema); and the assign-then-claim workflow (D59, DECIDED, DEFERRED to Wave 2 as the first focused pass; assigner roles assign a queue item / merchant / task, the assignee is notified via `ADMIN_REVIEW_ASSIGNED` then claims; assignment routes / claim commits; net-new schema `assignedToId` / `assignedById` / `assignedAt` + the assign capability + audited). D60, D61, D62 and D65 are owner-DECIDED Wave-2 prototype decisions (D60 / D61 / D62 on 2026-07-04; D65 on 2026-07-05): the member PII visibility tiering replacing blanket customer-PII masking (D60); Communications as the sending home with Customer 360 read-only for messaging (D61); the hybrid three-net review screening with pre-moderation explicitly rejected (D62); and the internal evidence-pack signature as the third contract channel with NO external e-signature setup (D65). D63 (the transactional-vs-marketing template ownership split) is RECOMMENDED (2026-07-05) and encoded in the prototype; D64 records the planned automated sends and the customer email-verification template gap (raised 2026-07-05). All other rows are PROPOSED and separately gated. Provider credential / rollout actions (for example applying rotated Resend or Google keys) are deliberately NOT in this register: they are an incident / provider-operations workstream, not an Admin Panel product decision.

| # | Decision | Recommended default (proposed) | Gating |
|---|---|---|---|
| D3 | Impersonation vs read-only view-as | read-only audited view-as only | security |
| D4 | Admin session model (localStorage to httpOnly/BFF) | normalize up at build time; show outcome only | security architecture |
| D5 | Step-up auth before destructive actions | design the gate in | security |
| D6 | Admin analytics reuse + gating | reuse Insights foundation as reference; operational un-gated, behavioural DPIA-gated; separate architecture/authz/privacy review; denorm not a prerequisite | analytics/privacy |
| D7 | Redemption reversal | design; label schema-gated (mind review coupling) | eng + owner |
| D8 | Admin-user management + bootstrap + grants | design Settings/Admin-Users; env-gated bootstrap; grant OUTCOMES without locking `AdminCapabilityGrant` | authz |
| D9 | Lead pipeline scope | minimal lead-to-qualify-to-assign-to-convert; rep-assisted channel design | product |
| D10 | Commercial cluster + `FeaturedMerchant.paymentStatus` integrity | design future-labeled; fix the payment-status gate | provider/billing |
| D11 | Personas for prototype lenses | SUPER_ADMIN, OPERATIONS, SALES/LEAD in Wave 1; FINANCE/SUPPORT/CONTENT modeled, future | product |
| D12 | `MERCHANT_PROFILE_EDIT` dead lane | remove or add an applier | eng |
| D13 | Platform: `admin.redeemo.co.uk` + CORS + strict posture; DPIA-Q1 to DPIA-Q6 (the Insights DPIA questionnaire series, distinct from this D-register); staging admin-OTP inbox | owner/platform track | platform/legal |
| D15 | Support/case: build in-house vs Zoho integrate | decide via BC-5 | owner |
| D16 | Signature channels + evidence strengthening | owner/legal choose valid channels; add evidence fields; never an admin-signs path | legal + eng |
| D17 | Fast-track / rep-verify authority | owner policy (rep verifies, Ops approves; senior-rep lower-risk; sensitive always independent) | owner |
| D18 | Platform Manager persona / `AdminRole` extension | keep PROPOSED; do not imply an enum exists | authz |
| D19 | University / student membership + eligibility scheme | PROPOSED; default OUT of launch-v1; labelled FUTURE concept; eligibility is PII (standard eligibility-gate set applies); no prototype module/screen without approval | owner + privacy |
| D20 | NHS / key-worker / community partnership schemes | DECIDED: FUTURE / DEFERRED (owner); outside the current prototype; NO module, screen, eligibility workflow, schema or implementation now; preserved as a separate future idea; revisit only via a separate owner-approved partnership workstream | owner (decided) |
| D21 | Referral / ambassador / creator programme | PROPOSED; default OUT of launch-v1; net-new attribution + anti-abuse; reward is financial (FINANCE + provider) | owner |
| D22 | Consumer waitlist + acquisition attribution (customer-web + an Admin outlet) | PROPOSED; scope only if a waitlist launch is planned; consent + bot-mitigation on the public form | owner |
| D23 | Control Room v1 signal set for M22 | PROPOSED; ship REUSABLE read-side signals; show external / gated / unverified for the rest | owner + eng |
| D24 | Deployed-SHA / version endpoint | PROPOSED platform / backend outcome (low-cost, no PII); until built the prototype shows external / unavailable | eng |
| D25 | APM / error-monitoring adoption | PROPOSED provider / platform outcome; deep-link from M22; until then show absent / external | owner + eng |
| D26 | Email-send atomic limiter before enabling production Resend (cross-ref the SEC.1 pre-Resend item) | PROPOSED security / backend outcome; SMS already has an atomic limiter | eng |
| D27 | New top-level "Members and Revenue" customer-commercial domain (M25 to M29) + Growth-and-Commercial reorg (Subscriptions/Billing/Refunds + Promo Codes move out of Growth into Members and Revenue; Growth keeps Campaigns/Featured/Trending) | DECIDED (owner, 2026-07-02): the blueprint under-represented the customer side (subscriptions are the revenue engine; merchants are the supply). Add the domain; do the reorg. Customer 360 stays in Relationships, cross-linked from the Members list. | owner (decided) |
| D28 | MRR / ARR / churn / conversion reporting for the revenue engine | PROPOSED read-side net-new outcome: no revenue table or aggregation exists; MRR/ARR/churn/conversion are derived from active subs x `SubscriptionPlan.priceGbp` x interval. Build the read-side aggregation; label until built. | eng + owner |
| D29 | Subscription payment-source breakdown + admin-grant (complimentary) subscriptions | PROPOSED FUTURE: Stripe-only today; `Subscription.source` enum (STRIPE/APPLE/GOOGLE/ADMIN) is not built and admin-grant is commented, not built. Show the outcome; do NOT present as available. | owner + eng |
| D30 | Refund / chargeback / dunning-beyond-PAST_DUE tracking | PROPOSED FUTURE: no `RefundLog`/`DisputeLog`; disputes are not even webhook-handled; dunning stops at PAST_DUE. Design the flows; label NOT-built. | owner + provider |
| D31 | Member behavioural / demographic analytics (locality, age/gender, interest + retention cohorts) | PROPOSED: keep DPIA-gated in Insights (M19), default-off, minimum-cohort, "not available" until the privacy gate opens; NEVER surfaced as operational in Members and Revenue. Operational member/subscription/revenue aggregates are fine. | owner + legal |
| D32 | STOP-AND-FLAG (data minimisation / DSAR): account deletion retains DOB, gender, interests and postcode/location (anonymise-in-place scrubs only name/email/phone) | STOP-AND-FLAG, NOT resolved here: surfacing member data made this retention posture visible. It needs a data-minimisation / DSAR review by legal + backend (is retaining DOB/gender/interests/postcode on DELETED rows justified by a lawful basis + retention schedule, or should the scrub set widen?). The prototype must show the retained-fields-on-DELETED-row fact honestly and flag it; it must NOT quietly resolve, widen or narrow the retention. | owner + legal + eng |
| D33 | Admin location confirmation = AUTOMATED map-confirm (no manual coordinates) | DECIDED (owner, 2026-07-02): REVERSES the prior "admin confirm = manual lat/lng, no map" position. The operator must never type raw coordinates (operator-error risk). Target design: auto-resolve the pin via Google-Places geocode (postcode centroid as baseline), present it on a MAP, operator verifies visually and confirms with one click (nudge / flag if wrong, never type coordinates); write the `ADDRESS_GEOCODED` tier. Net-new parts (promote the Google suggestion to the pin, write `ADDRESS_GEOCODED`, build the map-confirm UI) are FUTURE / net-new build; the automated map-confirm is the decided design intent even though parts are net-new. See 6.1.2. | owner (decided) + eng |
| D34 | Business verification concept (per the onboarding design spec §5) | PROPOSED as a labelled CONCEPT, NOT built: today `verificationStatus` is a manual admin flag, company# / VAT are unvalidated free text, merchants cannot upload verification docs (admin-on-behalf only), and nothing is auto-approved. Design a NOT-BUILT findings panel reflecting `docs/superpowers/specs/2026-06-10-merchant-portal-admin-onboarding-design.md` §5 (Google Place Details cross-check, Companies House, FHRS food-hygiene, in-house duplicate detection, findings snapshot, auto-approve-nothing). Show as concept + manual-review-today; never as available. See 6.1.3. | owner + eng |
| D35 | Admin edit "send to merchant for approval" + per-merchant apply-direct-vs-propose preference | PROPOSED FUTURE (NOT built): today admin edit-on-behalf is DIRECT (operational fields) plus a PROPOSE lane that is ADMIN SELF-REVIEW (not merchant sign-off, not true four-eyes). "Send this edit to the merchant for their approval" and a per-merchant preference for whether admin edits apply directly vs must be proposed are both net-new; label as future concepts on the edit affordances, never as available. See 6.1.4. | owner + eng |
| D36 | Correction: real go-live gates + intended-not-enforced gap | CORRECTION (verified): the REAL gates are branch exists + contract SIGNED + 2 RMVs configured (submit), plus main branch + main-branch location confirmed (go-live) = five gates. "Documents uploaded / branch-user assigned / mandatory-fields filled" from CLAUDE.md rule 7 are INTENDED but NOT enforced by any gate today. The review-surface checklist must match the real five gates; the rule-7 items show as intended-not-enforced thin-area flags, never as hard gates. See 8.1. | eng + owner |
| D37 | Correction-on-behalf model + two-court queue + per-item audit | DECIDED (owner, 2026-07-02): operators CORRECT a submission on behalf of the merchant before approving, rather than bouncing it back (minimise back-and-forth; "request changes" is the fallback for merchant-only items). Two-tier apply: COSMETIC corrections (voucher wording / title / description / terms / image / availability; merchant description; hours; website; contact; category) apply DIRECTLY; MATERIAL corrections (voucher value / saving / discount amount, and business / trading NAME) route to the MERCHANT for approval before going live. Two-court queue: "Needs you" (operator's court) vs "Awaiting merchant" (merchant's court; waiting-age tracks the merchant), with tabs + a per-row whose-court pill. Per-item audit history (Submitted -> Claimed -> Corrected-on-behalf -> Sent-to-merchant -> Merchant-accepted -> Approved), redacting secrets + branch PIN, rolling into the Global Audit. REAL: voucher concierge already routes admin voucher corrections to the merchant. NET-NEW: direct-apply COSMETIC corrections, identity-correction-to-merchant, the two-court tabs / whose-court pill / merchant-side waiting-age, and the send-to-merchant / merchant-accepted event types. See 1.3, 6.1.4, and M2. | owner (decided) + eng |
| D38 | Capability reclassification: VAT / company number / category / tags to OPERATIONS-editable | DECIDED (owner, 2026-07-02): move VAT number, company number, category and tags from SUPER-ADMIN-only to OPERATIONS-editable (Super Admin still holds everything). Rationale: Super Admin is not in day-to-day merchant management; Operations is. Ground truth: today the backend gates these to Super-Admin-only via the short-circuit on `merchant:edit-identity` / `merchant:edit-category`, so this is an owner-decided RE-CLASSIFICATION to IMPLEMENT (NOT-YET-IN-CODE); the exact per-role grant matrix is a LATER decision (do NOT present a finished matrix). The business / trading NAME does NOT move: name change stays on the material Correct-on-behalf lane (routes to the merchant, D37). See 3.2, 6.1.1, 6.3. | owner (decided) + authz/eng |
| D39 | Merchant Directory = the operator's supply-window (redesign) | DECIDED (owner, 2026-07-02): reframe the Merchant Directory (M3) from a flat register into the operator's window into the merchant SUPPLY base (is supply healthy, growing and used; where are the gaps). Supply-health summary (total; live / pending / suspended; verification coverage; region + category distribution; active-vs-dormant; featured count); enriched table (logo + business + trading; category + subcategory; town + region; branches; lifecycle + verification; activity = active-vouchers + redemptions + rating + a DORMANT flag; commercial = featured / campaign + contract + renewal; actions); three views (table; UK supply MAP with merchant + branch pins - business locations MAY be mapped, customer homes NEVER, per BC-4; group-by region or category); richer filters; Export CSV (real); a gated message-a-segment bulk action (FUTURE, BC-3). REAL: category, location / region, branches, active vouchers, redemptions (aggregate), rating, featured, contract status, created. NET-NEW: the summary roll-ups, activity / dormancy, contract renewal / expiry, the map surface, export, bulk-message. See 6.1.5, M3. | owner (decided) + eng |
| D40 | Merchant 360 = per-merchant workspace (distinct from the Directory list) + customer-facing banners | DECIDED (owner, 2026-07-02): the Merchant DIRECTORY (M3) is the LIST of all merchants; MERCHANT 360 (M5) is one merchant's deep WORKSPACE. Different surfaces; do not conflate. Merchant 360 is reached from the Directory, global search, a review item, OR a Merchant 360 nav-entry that lands on a PICKER (search + recently-viewed) when no merchant is selected (the nav item must not be a dead link). Tabs: Overview, Business identity, Branches, Vouchers, Performance, Documents, Staff & roles, Comms, Commercial, Audit, Insights. Edit-on-behalf per the correction model (D37) + the reclassification (D38). BANNERS: the merchant cover banner (`Merchant.bannerUrl`) and each branch's own banner + logo (`Branch.bannerUrl` / `Branch.logoUrl`) are CUSTOMER-FACING, so the operator must SEE them on Business identity (merchant) and the Branches tab (per-branch). These are REAL fields; editing the images stays NOT-YET-SUPPORTED. See 4.2, 6.1 (Merchant 360 workspace), 6.1.1, 6.1.5, 6.3. | owner (decided) + eng |
| D41 | Voucher-block full detail + advisory value indicator + per-voucher category value benchmark | DECIDED (owner, 2026-07-02): wherever vouchers are shown (the onboarding review surface, the voucher review lane M2, and the Merchant 360 Vouchers tab M5), the voucher block shows the FULL voucher detail the merchant entered so the operator can judge value and quality before go-live: `type`, `title`, `description`, `terms`, the voucher IMAGE / banner (`imageUrl`), value / `estimatedSaving`, `expiryDate`, the AVAILABILITY WINDOW (TIME_LIMITED), `cooldownSeconds` (REUSABLE), RMV / RCV, `approvalStatus` + approval history, per-voucher redemptions (aggregate; never an individual customer identity), and the `merchantFields.askHelp` assistance flag + the concierge proposal (all REAL). ADVISORY value indicator (weak / good / strong) is CLIENT-SIDE ADVISORY ONLY (`scoreVoucher`, ~GBP 5 advisory floor): NOT persisted, NO enforced minimum-saving gate, `RmvTemplate.minimumSaving` is a DEFAULT not a gate; label it advisory, never a hard minimum. Per-voucher CATEGORY VALUE BENCHMARK is NET-NEW: show where this voucher's value / saving sits versus peer vouchers of the SAME category from OTHER merchants (peer-position marker + median + percentile / quartile). The data is REAL (`estimatedSaving` + category) but the cross-merchant aggregation / percentile is NOT built; label it NET-NEW, never present a live benchmark as built. See 6.1.1, 13, M2, M5. | owner (decided) + eng |
| D42 | Merchant 360 Performance tab aggregate enrichment | DECIDED (owner, 2026-07-02): the Performance tab keeps redemptions-over-time, active-member reach, rating trend and top vouchers, and ADDS redemptions-by-branch (branch-attributed), redemptions-by-voucher (full list with counts), savings-delivered (sum of estimated savings for the period), validation-rate (validated vs total, with a QR-vs-manual method split), and a category-performance-benchmark (where this merchant sits versus category peers on redemptions-per-active-voucher: a position marker plus the category median). ALL of it is AGGREGATE-ONLY and NEVER shows an individual customer identity (for any role). NET-NEW: per-merchant aggregation is not built (`VoucherRedemption` has no `merchantId` column; join via `branch.merchantId` or via the voucher; savings computed on the fly from `estimatedSaving`); the category benchmark needs cross-merchant category aggregation that is not built, and its real inputs are this merchant's own metrics plus its category, never another merchant's detail (same honesty as the voucher-level benchmark in D41). See 6.1.6, M5, M28. | owner (decided) + eng |
| D43 | Merchant 360 Redemptions tab + individual-redemption lookup | DECIDED (owner, 2026-07-02): a new 12th tab (after Performance) holds this merchant's individual redemption history as rows (redemption code, voucher title + type, branch, redeemed date / time, validated status + method QR / manual + validated time, estimated saving, and the customer governed by D48), with filters (by branch, voucher, date range, validation status) + a search-by-redemption-code box, a single-redemption detail (drill-in) with a link to Customer 360, and a global-search entry (the top-bar search recognises a redemption code and surfaces a "Redemption \<code\>" result opening the single detail). The same lookup is reachable from global search, from Customer 360, and from this per-merchant tab (this tab is the per-merchant slice). Purpose: the operator support scenario "a customer says they redeemed at this merchant; track it down." NET-NEW admin capability (per-merchant redemption history + aggregation are aggregation-to-build; no admin per-merchant redemption viewer today). Individual rows never show identity in aggregate; identity is governed by D48. See 6.1.6, M5, 6.3. | owner (decided) + eng |
| D44 | Merchant 360 Staff and access on-behalf management + operator-parity-on-behalf | DECIDED (owner, 2026-07-02): the Staff tab becomes a real management surface (it was read-only). Principle: an operator can perform, on the merchant's behalf, the same account-maintenance a merchant does in the Merchant Portal, for merchants who cannot or will not use the portal. Two separated populations mirroring the staged identity model: (1) PORTAL ACCESS members (`MerchantAdmin` + `MerchantMembership`: Owner / Branch-manager / Staff, branch scope, `canManageVouchers`) with Invite (tokenised claim link so the person sets their OWN password; reject an email already a merchant admin), Change role / scope, Toggle can-manage-vouchers, Re-issue access link (re-send the tokenised claim / reset link; operators never type / see a password), Remove (soft, status Removed) with a last-remaining-Owner guard; (2) APP / BRANCH logins (`BranchUser`) grouped by branch, with Reset password + Deactivate / Reactivate (adding a new app login = thinner NET-NEW cap). Lane: Operations-capable + audited (Super-Admin is a superset). Direct action (no merchant round-trip), every action carries actor + reason + before / after into the per-merchant Audit rolling into Global Audit. Locks: NO impersonation, NO "log in as merchant", PIN never shown on this tab, operators never set / see passwords, `canManageVouchers` is the only real per-member cap today (finer matrix = placeholder). REAL: the Merchant Portal Staff and Access model is shipped. NET-NEW: the admin-side on-behalf equivalent (no admin staff-management endpoints today). New capability: `merchant:manage-staff`. Broader operator-parity-on-behalf principle: for non-portal-using merchants the operator does the merchant's own account-maintenance, subject to the D37 lanes (account-maintenance / cosmetic = direct + audited; money or identity-name = merchant-approval where a merchant can approve; commercial stays gated). See 1.3, 6.1.6, 6.3, 9. | owner (decided) + eng |
| D45 | Branch manager + notification routing (refines D36) | DECIDED (owner, 2026-07-02): every branch should always resolve to at least one manager. Rather than a hard go-live gate, the branch manager DEFAULTS to the OWNER when the merchant assigns no dedicated user, with a clear nudge to assign one (or to explicitly choose owner-as-branch-manager). Refines D36: the "each branch has an assigned manager" item is a SOFT advisory (defaults to owner + nudge), NOT a hard blocker (consistent with the five enforced gates). Rationale: branch managers receive automated branch emails (redemption notifications, retention, anything branch-associated); a multi-branch owner does not want to be bombarded by every branch's emails, so the branch-assigned manager is the DEFAULT recipient and the owner opts in per branch (off by default). Recipient identity = the branch's assigned Branch-manager (a portal member with the Branch-manager role scoped to that branch, with an email); defaults to the owner if none. The Merchant 360 Branches tab shows the resolved manager + the branch's assigned app / scan logins + the recipient list + the owner-opt-in toggle; assigning a scoped Branch-manager is done on the Staff and access tab. NET-NEW / Phase 6: recipient-routing + per-branch / owner notification preferences (Phase 6 comms unwired; alert-recipients + notification-preferences are un-built schema gaps; redemption email to branches deferred). Label as a concept, not live. See 6.1.6, 8.1, D36. | owner (decided) + eng |
| D46 | Branch PIN admin on-behalf view + reset (REVERSES "PIN never shown") | DECIDED (owner, 2026-07-02): the Admin Panel can VIEW and RESET the branch-specific redemption PIN on the merchant's behalf (no PIN = no redemption; staff / managers / owners forget it; a merchant who cannot reach the portal needs an operator recovery path). Model (security-conscious): NOT always-visible; reveal-on-demand + audited. On the Merchant 360 Branches tab the PIN is masked by default; "Reveal PIN" requires a reason and logs actor + reason + timestamp to Audit; "Reset PIN" requires a reason, regenerates / sets the PIN, and delivers the new PIN via the existing send channel. The audit records THAT a PIN was revealed / reset and by whom and why, but NEVER the PIN value. REAL infra: `Branch.redemptionPin` is AES-256-GCM encrypted (decryptable server-side); the Merchant Portal has PIN get / put / send routes (SMS via Twilio live; email via Resend Phase 6). NET-NEW: the admin on-behalf view / reset. New capability: `branch:manage-pin`. PRECISION: the PIN send / delivery channel (SMS to the branch, live) is DISTINCT from the Phase-6 email notification-recipient routing of D45; email delivery of the PIN to a manager is NOT live. INVARIANT REVERSAL: the prior "branch PIN never shown / hidden by policy" is REPLACED by "the PIN value never appears in logs or audit; the PIN is revealed deliberately on the Branches tab, reason-required and audited; it is never shown on other surfaces (for example the Staff and access tab)." See 6.1.1, 6.1.6, 6.3. | owner (decided) + eng |
| D47 | Merchant primary-contact block on the Merchant 360 Overview | DECIDED (owner, 2026-07-02): the Overview tab gains a "Primary contact (account owner)" block: the account owner's name + email (real, verified) + phone (optional; show "not set" when null) with Copy / Email / Call affordances; when there is more than one owner, show the primary plus "+N more owners". Honest and schema-grounded: there is NO business-level contact email or phone (the `Merchant` record has only `businessName`, `tradingName`, `websiteUrl`); the merchant contact IS the owner / primary `MerchantAdmin` (email + optional phone); branch phone and email are per-branch. See 6.1 (Merchant 360 workspace), 6.1.6. | owner (decided) + eng |
| D48 | Role-gated customer identity on redemptions (REPLACES "redemption rows never show identity") | DECIDED (owner, 2026-07-02): owner-locked model. (i) AGGREGATE surfaces (the Performance tab, M28) NEVER show an individual identity, for all roles (pure data-minimisation). (ii) INDIVIDUAL redemption identity (the Redemptions tab, the single detail, and the lookup) is ROLE-GATED: visible by default for customer-facing roles (Support, senior Operations, Super-Admin), reveal-with-reason for restricted roles, no-access for roles with no legitimate need. (iii) EVERY identity access (a view OR a reveal) is written to Audit regardless of role. UK-GDPR grounding: the controller has lawful access for legitimate purposes (support, fraud, operations), but data-minimisation, access-control (Article 32) and accountability (Article 5(2)) still apply to internal access; over-broad unlogged internal PII browsing is a risk. Capabilities: `redemption:view-customer` and `redemption:reveal-customer`. The exact per-role-to-tier mapping is set with the capability matrix (illustrative in the prototype). Cross-reference the D32 retained-demographic-data / DSAR concern (the access audit supports accountability). INVARIANT REPLACEMENT: the old blanket "redemption rows never show individual customer identity" is REPLACED by "aggregate surfaces never show individual customer identity; individual redemption identity is role-gated and always audited." See 3.2, 6.1.6, 6.3, M28. | owner (decided) + eng + legal |
| D49 | Representative-assisted (rep-led) on-behalf onboarding wizard (`merchant:assisted-onboard`) | DECIDED DESIGN-ONLY prototype (owner, 2026-07-03): two onboarding channels: (A) self-serve (merchant registers online then admin approval; REAL, 8.1) and (B) assisted / rep-led (a Redeemo rep physically WITH the business builds the ENTIRE account on-behalf on the admin panel so a non-technical merchant never touches the Merchant Portal). Built in the prototype as a 9-step FULL-SCREEN PRESENTATION-MODE wizard (chrome hidden = merchant-safe focus mode; still the operator's OWN audited session, NOT a separate login or a security boundary; Exit-to-admin). Steps: (1) Category and identity (CATEGORY-FIRST hard prerequisite: primary category then subcategory then cuisine [conditional] then specialties then a generated descriptor label; changing the top-level category discards drafts); (2) Business profile (REAL public logo / banner / description / website; REAL registration legal-name / trading / company-number / VAT; NET-NEW richer business-type + conditional charity-number / UTR, head-office address / phone / email, owner-and-responsible-person block [title / first / last / position / email / mobile / landline plus an optional secondary contact with full parity], values, social links, category-conditional service-options, price-range); (3) Branches (full record: name / description, Google-Places address lookup, contact / website, MULTI-WINDOW opening hours, category-eligible amenities, banner plus up to 8 moderated photos, branch logo inherit-or-override, 4-digit redemption PIN, automated location map-confirm; first branch = main; add more); (4) Vouchers (2 mandatory flagship RMVs via the category-driven builder, D52); (5) Staff [OPTIONAL] (portal managers via claim-link, branch / till scan users, branch-manager assignment; D44 / D45); (6) Documents [OPTIONAL, not a gate] (verification uploads on-behalf to moderation; per-document operator notes, D51); (7) Contract (the OWNER signs in person on the rep's device; the operator CANNOT accept; hand-to-owner owner-only accept = name + title + confirm; an OPTIONAL finger / stylus signature that is NOT a gate; evidence = signatory name + title [NET-NEW: not persisted today], timestamp, IP, method, agreement version; admin-never-signs is a LEGAL LOCK); (8) Go-live review (the 5 enforced go-live gates + rep IN-PERSON verification attestation + rep-approves-inline + single-actor honesty + flagged for light post-hoc audit sampling); (9) Handover (transfer to the owner's email via a tokenised claim link; the owner sets their OWN password and takes ownership; the operator never sets or sees a password). REAL = shell-create + claim / transfer (`createMerchantDraft` + `issueMerchantClaim`) + `merchant:create-draft`; NET-NEW = the full on-behalf wizard + `merchant:assisted-onboard`. Go-live fork LOCKED = rep approves inline, audited, in-person verification captured as evidence, rep-created-and-approved merchants flagged for post-hoc audit. See 8.2 / 8.2.1, 8.4, 3.2. | owner (decided) + eng |
| D50 / D54 | Admin Taxonomy and Rules management module (taxonomy + dependent content promoted from hardcoded config to admin-managed DATA) | DECIDED DESIGN-ONLY prototype (owner, 2026-07-03): an admin surface (PARKED as its own surface, extends BC-2) to manage categories / subcategories / cuisines / specialties / tags AND their dependent content (per-category voucher suggestion chips + per-type examples, the category-to-term caution flags, amenity-per-category eligibility via `CategoryAmenity`, and other category / subcategory-dependent config). ARCHITECTURAL NOTE (honest): today ALL of this is HARDCODED in code (the category config map + term flags + the `CategoryAmenity` seed; top-level granularity only) and is read by BOTH the merchant-portal voucher builder AND the admin assisted builder; making it admin-managed = promoting hardcoded config to DATA with ONE source of truth feeding both surfaces (net-new schema + backend). Pairs with the existing Suggested-tag Moderation (management = the create side, moderation = the review side). OPEN when designed: operator "add your own" = immediate vs route-to-moderation vs hybrid (taxonomy is platform-wide, so lean controlled). D50 and D54 are the SAME taxonomy module: D54 EXPANDS D50 (D50 = the taxonomy CRUD; D54 adds the dependent content: suggestion chips, per-type examples, category-to-term flags, amenity eligibility). Until this module exists, the assisted flow's "add your own" subcategory / cuisine / specialty stays deferred-labelled. See BC-2, 6.1.5, 8.2.2, 8.2.3. | owner + eng |
| D51 | Internal operator notes surface (merchant Notes tab BUILT; customer + onboarding notes DEFERRED) | DECIDED DESIGN-ONLY prototype (owner, 2026-07-03): the MERCHANT Notes tab is BUILT on Merchant 360 (net-new schema; no notes model in code today). Note list (author + role, timestamp, body, attachments, status active | retracted); ADD (free text + optional attachment via signed-link storage like documents; audited); EDIT (role-gated; marks edited + keeps the prior version in history); RETRACT (SOFT-delete for accountability: stays visible, marked "Retracted by X on date" + optional reason, NEVER hard-deleted); per-note HISTORY (add / edit / retract, each with who + when); every add / edit / retract AUDITED to the Global Audit (M21); role-gated `notes:read` (view) + `notes:write` (add / edit / retract). Framing: internal B2B merchant notes, less sensitive than customer notes but still audited and factual / professional. Per-DOCUMENT operator notes were also built on the assisted Documents step (operator-internal, D49 step 6). DEFERRED: Customer 360 notes (personal data + DSAR-disclosable; must be factual / professional / role-gated / audited; design as if the subject could read them) + onboarding-section general notes. Net-new schema throughout. See 6.1, 6.1.1, 6.2, 3.2. | owner + eng (+ legal for customer notes) |
| D52 | Assisted voucher builder = faithful port of the merchant-portal flagship builder | DECIDED DESIGN-ONLY prototype (owner, 2026-07-03): category-driven fields, placeholders, suggestion chips, per-type examples and terms, all keyed off the Step-1 category resolved to its TOP-LEVEL PARENT (11 parents + a fallback; the config is top-level granularity only). Uses the REAL `scoreVoucher` advisory maths: title-clarity + description-personalised (editing the auto-suggestion and reaching >= 30 chars lifts the score) + saving floor (~GBP 5) and generosity with the percentage surfaced in the copy + term-stacking (too many or too restrictive terms drop it toward Too weak) + photo; tier Too weak / Good / Great; ADVISORY, not a submit gate (a Too-weak voucher can still be saved; admin review is the quality backstop). NO fake peer benchmark (the motivational "compares to similar businesses" line carries NO real numbers; a real cross-merchant category-median benchmark is NET-NEW / not built). A sticky score-plus-preview SIDE RAIL (not a bottom block). NO concierge "ask the Redeemo team" toggle in the assisted flow (the rep IS the builder). Type picker = 5 eligible (BOGO recommended / Spend and save / Discount fixed + percent / Freebie / Package) + 2 disabled (Time-Limited / Reusable = custom day-2). 2 mandatory flagship RMVs; custom vouchers are day-2. REAL = `scoreVoucher` logic; NET-NEW = the category-median benchmark. See 8.2.2, 6.1, D41. | owner (decided) + eng |
| D53 | Always-on voucher terms in the assisted flow = generic-only (a deliberate deviation from the merchant-portal core) | DECIDED DESIGN-ONLY prototype (owner, 2026-07-03): the locked always-on CORE = tell-staff + show-in-app + no-cash-value; "Not valid with any other voucher" and "One voucher per customer each visit" MOVE to optional (pre-ticked, removable). Per-type Fair + category-conditional Caution (service businesses show none) + Discount-percent Restrictive minimum-spend + add-your-own (80 chars, auto-tagged Restrictive on restrictive wording). This is an owner-confirmed DEVIATION from the merchant-portal CORE (which keeps tell-staff + no-combine + per-visit as its 3 fair core, all as unlocked checkboxes) for the admin assisted flow ONLY. See 8.2.3. | owner (decided) + eng |
| D55 | Admin Agreements, Policies and Content management module (version-controlled) | DECIDED DESIGN-ONLY prototype (owner, 2026-07-03): an admin surface (PARKED as its own surface, extends BC-2) to manage the Redeemo Merchant Agreement plus other contracts / policies / legal content shown across the customer app, customer website and merchant portal, with version control, so edits happen from the admin panel. ARCHITECTURAL NOTE (honest): today the agreement text is placeholder / MVP, the real binding legal text is owner / legal-owned and is a launch sign-off GATE, the static customer-web legal pages are the launch source of truth, and an unwired / deferred `CmsContent` concept already exists in the codebase (plus `MerchantContract.tcVersion`, the agreement version the assisted flow references). D55 = that `CmsContent` path made real, versioned and surfaced across the three product surfaces = substantial net-new backend. PARKS alongside the D50 / D54 taxonomy module. See BC-2. | owner + legal + eng |
| D56 | Merchant 360 edit-on-behalf parity + live / pending state model | DECIDED DESIGN-ONLY prototype (owner, 2026-07-03; PARKED-BUT-NEXT, its own focused pass): admins / reps should edit almost all fields for EXISTING merchants from Merchant 360 (parity with the assisted-onboarding capture; completes D44). TWO LANES: (1) admin-DIRECT (applies immediately, live, audited, no pending) for platform-owned + corrective + operational fields (category / reclassification, status suspend / reactivate, location map-confirm, PIN reset, verification / documents / notes, amenities, photo moderation); (2) merchant-CONFIRM-required (PENDING until the merchant confirms, then live) for the merchant's self-representation + commercial + legal-identity fields (legal / registered name, description, public contact, voucher offers / terms, opening hours, public branch name). STATE MODEL: each field / section is Live, Pending (the old value stays live and customer-visible; shown as an old-to-new diff with "awaiting merchant confirmation"), or optionally Draft; a 360-level "N pending changes" indicator; admin-direct fields never enter Pending. GO-LIVE for the pending lane = the merchant confirms (a portal notification / their next login) OR the admin attests in person ("confirmed with owner") for portal-non-using / rep-managed merchants (mirrors the assisted contract in-person model; evidence-trailed). EXTENDS the existing pending-edit model (`MerchantPendingEdit` / `BranchPendingEdit`) and the Option B applier (which shipped the merchant-to-admin approve / reject direction); D56 ADDS the admin-direct lane plus the admin-to-merchant-confirm direction. The exact field-to-lane assignment is an OWNER DECISION to lock when the pass is designed. Two-tier previews already surface in the prototype (cosmetic changes apply directly; a value or business / trading-name change routes to the merchant for approval). REAL = the pending-edit substrate + the Option B applier; NET-NEW = the admin-direct lane, the admin-to-merchant-confirm direction, the per-field state model. See 6.1. | owner (decided) + eng |
| D57 | Combined / multi-module admin access (FUTURE concept on the 1.9 Team and Roles screen) | DECIDED DESIGN-ONLY prototype (owner, 2026-07-03): today an admin holds EXACTLY ONE fixed role (the `AdminRole` enum SUPER_ADMIN / OPERATIONS / FINANCE / CONTENT / SUPPORT; the `role` field is a scalar, NOT an array), and Super Admin is the only role with "everything" (over-broad via the superuser short-circuit, NOT a curated subset). Giving ONE person several modules at once (for example Operations + Finance + Content) is PLANNED via one of TWO NET-NEW models, both shown clearly-labelled FUTURE on the Team and Roles screen (never a live toggle): (1) MULTI-ROLE assignment: a user holds several whole roles and their access is the UNION of those roles (the simple "combine a few roles" model; schema `role` -> `roles[]` or a join table); (2) PER-PERSON CAPABILITY GRANTS: start from a base role, then add or remove individual capabilities for that one person (the finer-grained model; this is the design-only `AdminCapabilityGrant` per-person-grants future item). The exact model (multi-role union vs per-person grants) AND when to build are a LATER capability-matrix / Tier-3 authz decision; do NOT present either model as chosen or built. See 3.1, 3.2, 13. | owner + authz |
| D58 | Team and Roles staff-reframe (Redeemo STAFF record for panel onboarding + access; NOT the HR system) | DECIDED DESIGN-ONLY prototype (owner, 2026-07-03; BUILT in the prototype): the former "Admin Users and Roles" screen (1.9) is reframed as "Team and Roles". These people are Redeemo STAFF / team members who hold admin-panel access, DISTINCT from the merchant-side "Staff and access" (a merchant's own branch / portal staff, D44). The screen holds the BASIC operational + employment fields needed to onboard a staff member to the PANEL and manage their access: name, work email, role, job title, department / team (Operations / Finance / Support / Content / Trust and Safety / Leadership), manager (reports-to), start date, employment type (Employee / Contractor), plus optional preferred name, work phone, staff ID, working location, and invited-by + invited-date. Invite captures the essentials; the record holds the fuller set (via a View-record expand). BOUNDARY (important): the admin panel is NOT the HR system; the fuller HR / employment-lifecycle data (pay, date of birth, home address, personal non-work contacts, HR documents) lives in the dedicated HR tool (for Redeemo, e.g. Zoho People), NOT here. (Historical note for accuracy: the owner briefly considered making the panel the single centralized platform holding all staff / HR data, then RETRACTED that; the HR system stays, and the panel holds only panel-onboarding + access + basic work context.) REAL = name, work email, role, status (isActive), last-active (lastLoginAt), account-created (the lean `AdminUser` record today); NET-NEW (needs schema) = every richer staff field above. See 3.1, M20, 6.1 (Team screen), 1.9. | owner + eng |
| D59 | Assign-then-claim workflow (assigner roles assign a queue item / merchant / task; assignee notified then claims) | DECIDED DESIGN-ONLY prototype (owner, 2026-07-03; DEFERRED to Wave 2 as the FIRST focused pass): certain ASSIGNER roles (via a NET-NEW capability, e.g. `approval:assign` or `task:assign`; likely Super Admin plus a lead / senior-Operations tier, possibly the proposed Platform-Manager mid-tier persona) can ASSIGN a queue item / merchant / approval / task to a SPECIFIC user, instead of everyone only self-claiming. The assignee RECEIVES a notification (the `ADMIN_REVIEW_ASSIGNED` notification type is REAL, but its emitter is currently DEFERRED precisely because this assign-flow does not exist yet: the "emitter deferred" case flagged on the 1.11 Notifications screen and the reserved-alert-emitters register). KEY ACCOUNTABILITY INVARIANT: assignment ROUTES, claim COMMITS; the single-actor claim-to-act accountability is preserved (assignment says "this is yours to pick up", the assignee still claims-to-act before acting); self-claim and assign COEXIST. NET-NEW: `AdminApproval` has `claimedById` (self-claim) but NO `assignedToId` / `assignedById` / `assignedAt` columns, so this needs a small schema addition PLUS the assign capability PLUS firing the existing `ADMIN_REVIEW_ASSIGNED` notification PLUS an assigned-state on the queue, ALL audited (who-assigned, who-assigned-to, who-claimed). EXTENDS the two-court queue + claim model (D37, 6.1 / the Approval Queue). DEFERRED: owner-decided as the FIRST focused Wave-2 pass, NOT built in Wave 1. See 3.1, 9, 13, 1.11. | owner (decided) + eng |
| D60 | Member PII visibility tiering (REPLACES blanket customer-PII masking) | DECIDED (owner, 2026-07-04): apply the D48-style role-tiered model to member PII on Customer 360 and the member surfaces. SEES-BY-DEFAULT for customer-facing roles (Support, Operations, Super Admin: identity, contact and the registration record inline; EVERY access still written to Audit); REVEAL-WITH-REASON for restricted roles (Finance); NO-ACCESS for roles with no legitimate need (Content, Sales). Rationale: customers are members and these staff serve them directly; role-gating + auditing + minimisation satisfy UK-GDPR better than hiding data from the people who need it. SEARCH must respect the same tiering (search results are not a side door to PII a role cannot see; masked-handle roles cannot search by email or identity). Deliberate exception: REPORTER identity on review reports stays MASKED for every role (reason-gated reveal only; retaliation-risk data-minimisation, M10). Unchanged by D60: the precise-geo prohibition (BC-4) and the D48 aggregate rule (aggregate surfaces never show an individual identity). See 6.2, M25, M10, 13. | owner (decided) + eng + legal |
| D61 | Communications is the sending home (Customer 360 read-only for messaging) | DECIDED (owner, 2026-07-04): Customer 360 stays READ-ONLY for communications (comms timeline + consent, never a send button); anything that SENDS lives in the Communications screen (M7 / 7.1), Support case replies (M14), and (later) Campaigns for bulk (M17, Wave 3). Sends are TEMPLATE-BASED only (no free-form composition to customers), CONSENT-CHECKED (marketing requires stored marketing consent; transactional is exempt), AUDITED, and PROVIDER-GATED: while email is dark the UI says "Queued; sends when email goes live", never "Sent". Customer 360 connects via the Comms timeline (what was sent) and linked support cases; no profile send button. See 7.1, 6.2, M7, 13. | owner (decided) + eng + provider |
| D62 | Hybrid review screening (three nets) | DECIDED (owner, 2026-07-04): three nets. (1) HARD-BLOCK outright profanity / slurs at submission (word-list plus normalisation; the customer is asked to rephrase; never publishes). (2) AUTO-FLAG borderline content (possible defamation, accusations, aggression): publishes but lands in the Reviews Moderation queue immediately WITHOUT waiting for a customer report. (3) Customer reports remain the third net and the ONLY net that exists today. The screening engine is NET-NEW backend belonging to the review-system v2 workstream. Pre-moderation (hold until approved) explicitly REJECTED. See M10, 13. | owner (decided) + eng |
| D63 | Template ownership split (transactional code-owned; marketing panel-editable later) | RECOMMENDED (2026-07-05, encoded in the prototype): transactional email templates stay CODE-OWNED and read-only in the panel (they carry security-critical content such as reset links and branch PINs; UI editing of them is a real risk); marketing templates become panel-editable LATER via the parked Agreements, Policies and Content module (D55). See 7.1, D55, 13. | owner + eng |
| D64 | Planned automated sends + the customer email-verification template gap | RAISED (2026-07-05): (a) a redemption-details email to the merchant branch manager on each validated redemption (maps to the deferred merchant-portal redemption-notification slice and the alert-recipients schema-ledger item; cross-ref D45); (b) the monthly merchant statement to branch managers and owners (already in the locked merchant-portal Phase 4 architecture); (c) GAP found: no CUSTOMER email-verification template exists among the nine real transactional templates (only the business variant), despite the app hard-blocking on email verification; (d) an advisory Phase 6 list: welcome email, subscription payment receipt, renewal reminder, payment-failed notice, cancellation confirmation, customer account-deletion confirmation, password-changed security notice. All shown as FUTURE "Planned automated sends" in the prototype; real send-wiring = Phase 6 plus the merchant-portal Phase 4 decisions. See 7.1, 13. | owner + eng |
| D65 | Internal evidence-pack signature; no external e-signature setup | DECIDED (owner, 2026-07-05): the third contract-signature channel is an INTERNAL upgrade of click-to-agree: version-controlled contract text (owned by the planned agreements module, D55); a real ceremony (scroll-through, typed name, optional drawn signature); a generated signed-PDF snapshot of the exact contract version stored in platform document storage; and an evidence record (authenticated identity, timestamp, IP and device via the audit log, plus a hash of the signed file for tamper detection) with an admin verification view. External providers (e.g. Zoho Sign) deliberately NOT set up; future special-case option only. Sufficiency confirmed within the already-planned pre-launch solicitor review (no new legal gate). Code grounding: the shipped `MerchantContract` model already stores `signedAt`, `ipAddress`, `tcVersion` and `signatureMethod`, with a nullable `zohoSignRequestId`. The comparison surface (Wave-2 screen 2.12) sits behind Legal and Agreements with the channel choice framed as an OPEN owner decision. See 8.4, D16, D55, 13. | owner (decided) + legal + eng |

---

## 17. Source and evidence appendix

**Source hierarchy (authority order):** (1) merged source (`origin/main` by SHA, base `37cc0f69`) = build-on-code; (2) approved owner decisions (D1/D2/D14 approved; D20 owner-decided FUTURE/DEFERRED; the rest proposed/gated); (3) approved specs/plans (strategy §4.2; onboarding design; actioner spec; Option B) = intended behaviour (note: `2026-06-07-merchant-admin-platform-strategy.md` is an uncommitted working-tree artifact, NOT part of the git baseline at `37cc0f69`; its content is accurate but treat it as working evidence, not a reproducible baseline source), with design-only-in-specs models (`MerchantLead`/`MerchantSource`/`AdminCapabilityGrant`/`TermsClause`) treated as build-on-spec, strictly weaker than build-on-code; (4) `docs/PROJECT-STATE.md` = current status (with its `[UNVERIFIED]`/NOT-ACCEPTED caveats); (5) roadmap Definition of Complete = the bar; (6) DPIA + runbooks = legal/provider gates, and runbook decisions can veto otherwise-buildable surfaces (`CmsContent` unwired per §12); (7) Codex checklists = read-only corroboration; (8) corrected audit + delta = working evidence base; (9) legacy `admin-panel-spec.docx` = discarded old-developer artifact, idea-input only (its "map all customers by home address" is a struck prohibited pattern).

**Key verified citations (base SHA `37cc0f69`):**

- Enabling-slice framing: `2026-06-14-admin-panel-actioner-design.md` §18.3, §1; strategy §2/§3/§4.2.
- Capability model: `src/api/admin/capability.ts:21-115`. Admin route groups: `src/api/admin/{approvals,merchants,branches,timeline,notifications}`. Admin-web: `apps/admin-web/app/**`, `features/**`.
- Photo apply ENGINEERED: `src/api/admin/approvals/editApplier.ts:96-104,217-245,501-517`; commits `45ac3773`, `2442f214` (PR #313).
- Insights foundation: `src/api/merchant/insights/{service.ts,eligibility.ts,routes.ts,gate.ts,london.ts,scope.ts}`; PR #331 `00582a6e`; behavioural gate `gate.ts:46-50`; branch join `eligibility.ts:180-184`.
- Merchant billing absent; admin contract-signing absent; staff-management-on-behalf absent; ownership-transfer absent: `src/api/merchant/**` (zero billing hits), `src/api/admin/merchants/routes.ts`, `onboarding/service.ts:168-191`.
- Signature evidence: `MerchantContract` (`schema.prisma:747-757`), `SignatureMethod` (`:409-412`), `acceptContract` (`onboarding/service.ts:168-196`).
- Onboarding channels: `auth/merchant/service.ts:548/654` (self-serve), `admin/merchants/service.ts:311` + `auth/merchant/service.ts:420/463` (create-draft + claim); `computeOnboardingChecklist` (`onboarding/service.ts:23`); VERIFIED stamp `approvals/service.ts:617`.
- Customer 360 substrate: `User` (`schema.prisma:102-164`), `Subscription`/`SubscriptionPlan`/`PromoCode`, `VoucherRedemption`, `Review`/`ReviewReport`/`ReviewHelpful`, `Favourite*`, `Interest`/`UserInterest`, `UserSession`, `UserSsoProvider`, `Notification`/`CommunicationLog`; self-serve anonymise `auth/customer/routes.ts:201-226`; consent fields `User.newsletterConsent/tcConsentVersion/tcConsentAt/marketingConsentAt`.
- CRM absence: no Note/Task/Assignment/Case/Segment/internal-Tag/health models; `SupportTicket`/`MerchantRequest` frontend-stub-only; reference patterns `AdminApproval.claimedById`, `lastStaleAlertAt`; timeline `src/api/admin/timeline/service.ts`.
- Ops-status signals: `src/api/app.ts:117`, BullMQ `getJobCounts` (never called), `CommunicationLog` status, `KeyringFingerprint`, `smsLimiter.ts`, `outboxReconciler.ts`, `claimStaleSweep.ts`.
- Coverage/geo: `Branch` geo + `Locality`/`Market`; prohibited `User.addressLine1/2/postcode/latitude/longitude/localityId`.

**Provider / configuration / runtime classification (reconciliation; evidence-dated).** Source wiring does not mean operationally live; a boot-required secret does not mean provider activation; a historical `/health` = 200 does not prove current variables or bindings. On current `main` the `/health` handler is `src/api/app.ts` and still returns a literal `{status:'ok'}` (liveness-only; the Section 5.0 `:117` citation is at base `37cc0f69`). Boot-required + placeholder-rejected secrets (`src/api/shared/env.ts` `REQUIRED_SECRETS`): `DATABASE_URL`, `ENCRYPTION_KEY`, `REDIS_URL`, the four JWT secrets, `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, and the four `TWILIO_*` vars. Feature-gated / dark-by-default (`FEATURE_GATED_SECRETS`): `RESEND_API_KEY` (`EMAIL_ENABLED`), `R2_*` (`STORAGE_ENABLED`), `TURNSTILE_SECRET_KEY` (`CAPTCHA_ENABLED`). `GOOGLE_PLACES_API_KEY` is owner-CLI-only (not in the API required / gated set; consumed by `prisma/suggest-branch-pin.ts`). Owner-reported account state (not provider-inspected): Stripe and Twilio have no real production account (placeholders); R2 not activated; Turnstile a public / test placeholder. Among the external-provider values assessed during Phase 2A, Google Places and Resend were the only owner-confirmed real provider credentials, and both were rotated with replacements not yet applied to a verified consuming environment. That scope does NOT include internal / runtime credentials (Neon / database, `REDIS_URL`, the JWT secrets, the encryption key): those remain separate security-rotation workstreams, are NOT declared resolved by this blueprint, and are NOT implied to have been placeholders. Current stored secret values and runtime feature-variable states were not inspected during this reconciliation; they remain UNVERIFIED under the active provider / no-use holds; no inference is made from source wiring or historical deployment state.

**Deployment evidence snapshot (transient; dated 2026-07-01, owner-confirmed).** Railway Web = Failed / non-serving; worker = Offline; Web `/health` = HTTP 404; both GitHub auto-deploy controls disabled. An earlier `/health` = 200 is historical (a prior deployment), not current; Redis was owner-confirmed Online and untouched while its worker consumer is Offline (infrastructure state is separate from application-consumer state); no fresh Neon verification is permitted under the no-use hold. This is a point-in-time evidence snapshot for freshness only; it is NOT part of the permanent operating model and must be re-verified before reliance.

**Freshness caveat:** merged is the floor, not the bar. Only the engineered Admin enabling slice (the actioner / review / lifecycle / edit-on-behalf console) is merged-not-staging-accepted; most target blueprint modules are FUTURE, PARTIAL or GATED (not built). Deployed SHA, admin-web deploy state, staging admin-OTP delivery, `STORAGE_ENABLED`/`EMAIL_ENABLED` state, and the DPIA gate state were not inspected under the active provider / no-use holds and must be re-verified before reliance.

---

*End of blueprint. Status: design intent, pending owner and Codex review. No implementation authorized by this document.*
