# Redeemo - Project State (canonical, living)

> **Read this first when resuming any Redeemo work.** This is the compact, claim-verified
> current-state index. It summarizes material facts and **links** to the detailed evidence
> (specs, plans, audits, governance, runbooks, the Merchant Portal roadmap). It deliberately
> does **not** copy per-PR histories or the large deferred archive. This document and the Merchant
> Portal roadmap are a **coordinated, cross-linked pair**: kept as separate PRs only for reviewability,
> but reviewed, approved, and merged together.
>
> **Freshness:** Voucher governed-flows package (#411-#413, sequential SHA-bound squashes `ca1c6991`/`350f941a`/`381452f2`, `origin/main` @ `381452f2`) verified + folded into §4.2 on **2026-07-07**, merged immediately after and reconciled the same day as the Merchant Portal fidelity wave (#401-#409, `73175c8b`) verified + folded into §4.2 earlier the same day; the prior Merchant re-verification before that was Business Profile day-2 v1 (#393-#396) @ `ade3efe5` (2026-07-06). Other programmes last verified @ `434ca4eb` (2026-06-28) and were **not** reverified this pass. **Known lag (still not folded into §4.2): PRs #375-#392 (staff error-surface pins, #377 PIN wire hygiene, D-BM1 #379/#381, smoke journeys #378/#382-#384, #385 security-runbook reconciliation, #386-#390 session cache-isolation wave, #391 docs-architecture migration, #392 transport epoch re-gate) postdate the `a58db583` stamp and still have not been folded in - evidence in git + Codex Vol-2; next Merchant re-reconciliation must absorb them.** Also recorded this pass: #399 (`70961cd0`, merged 2026-07-07 immediately before the fidelity wave) ships the §BP-ADJ1/§BP-ADJ2 hardening (role-gate registered/compliance fields on `getMerchantProfile` + server-side edit-request `businessName` validation); both open-register rows CLOSED (see §4.2 + the register change log). Lines without
> a fresh citation this pass are marked **[UNVERIFIED]** and must be re-checked before reliance.
> A modification date is **never** evidence of completeness - every claim is verified against
> the evidence appropriate to its type (below).

---

## 0. How to use this document

1. Read §1 (authority model) and §2 (Codex reconciliation) so you know which source wins for your question.
2. Read the programme section in §4 for the area you are working on; follow its links for depth. Do **not** load the entire deferred archive or every topic memory.
3. Before starting work, check §5 (cross-product invariants), §6 (open owner decisions), and §7 (warnings) for anything that blocks or constrains your task.
4. **"Built / merged v1" is not "complete."** Completion requires the Definition of Complete in the relevant roadmap (implementation + prototype fidelity or approved divergence + authz + privacy + tests + **staging acceptance** + responsive/a11y + docs + recorded deferrals).
5. After a PR merges, an owner decision lands, a warning is raised/cleared, a staging check runs, or a deferral opens/closes, **update this document** per §9 and add a change-log line.

---

## 1. Fact-type authority model (authority depends on the question)

| Question | Authoritative evidence (in order) | Conflict rule |
|---|---|---|
| **What is actually MERGED** | `origin/main` → Git history → merged PRs (by SHA) | the merged truth. A **local working tree is never authoritative.** |
| **What is actually DEPLOYED** | provider deployment SHA/config + live probes (Vercel/Railway dashboards, `/health`) - **verified separately from merged state** | deployed may lag merged; record both. A local working tree is never authoritative. |
| **What behaviour was approved / intended** | explicit owner decisions → the latest approved governing spec/plan | **if merged source ≠ approved spec → record an implementation/contract conflict.** Do **not** treat shipped source as the intended truth merely because it ships. |
| **What should happen next** | this document (§4/§6/§8) → the approved programme roadmap(s) | latest approved supersedes earlier |
| **Warnings / incidents / tests / provider state** | verified logs, runbooks, CI config, live probes → the Codex checklists (§2) | verify against the live artefact; the checklist corroborates |
| **What Claude remembers (private memory)** | private memory (`MEMORY.md` + `project_*.md`) | **discovery/index aid only** - never overrides merged source, deployed state, or owner decisions |

## 2. Read-only Codex reconciliation protocol

Codex maintains four external workflow checklists (the set may grow; this table is the authoritative list). Claude (and any agent) may **read** them to discover missing decisions, warnings, deferrals, PR history, and owner instructions, and to cross-check this document. Claude must **never** edit, append to, rename, reorganize, delete, or assume maintenance of them - **Codex alone owns them.**

| Checklist | Path | Scope |
|---|---|---|
| Customer App | `/Users/shebinchaliyath/Documents/Playground/redeemo-notes/2026-05-20-customer-app-workflow-checklist.md` | Customer app/website history, deferrals, PR history |
| Admin/Merchant Vol-1 | `/Users/shebinchaliyath/Documents/Playground/redeemo-notes/2026-05-23-admin-merchant-portal-workflow-checklist.md` | Admin + Merchant portal, decisions, PR history |
| Admin/Merchant Vol-2 (continuation) | `/Users/shebinchaliyath/Documents/Playground/redeemo-notes/2026-06-27-admin-merchant-portal-workflow-checklist-continuation.md` | Insights, G1, PR #328, recent decisions/warnings |
| Admin Prototype | `/Users/shebinchaliyath/Documents/Playground/redeemo-notes/2026-07-01-admin-panel-prototype-workflow-checklist.md` | Admin Panel Claude-Design prototype build: stages, owner decisions, doc-sync |

They are **cross-check evidence, not authority over merged source** - each checklist itself trails its programme, so a checklist line is verified against `origin/main`/specs before reliance. Reconciliation is **one-way (read-only)**: record the reconciliation date + any unresolved deltas in §10; never write back.

**Last Codex reconciliation:** 2026-06-28 (all three read; no deltas that change the merged-state claims below; the checklists were not modified by Claude).

---

## 3. Repository pointers (merged & deployed truth)

- **Merged:** `origin/main` @ `381452f2` (latest merge: merchant-web flagship interactivity + governed voucher change/end/withdraw UI #413, closing the voucher-governed-flows package #411-#413 - #411 `ca1c6991` backend/schema, #412 `350f941a` admin-web review lane, #413 `381452f2` merchant-web UI; this package merged immediately after the Merchant Portal fidelity wave #401-#409, closed by #409 `73175c8b`; #399 `70961cd0` also merged the same day as the fidelity wave - the §BP-ADJ1/§BP-ADJ2 hardening, recorded in §4.2 + the open register; the earlier intervening #375-#392 wave remains unfolded - see the freshness header). Verify any "merged" claim with `git log`/the PR.
- **Deployed (staging, provider URLs used as staging):** customer/admin/merchant apps on `*.vercel.app` (all HTTP 200 on the 2026-06-28 probe; re-probe before reliance); backend on Railway env `redeemo/staging` (`web-staging-bf7c.up.railway.app/health` → 200); the staging **worker remains OFFLINE** (emails queue in `CommunicationLog`, none send). **Staging Web ACCEPTED deployment (owner, 2026-07-09):** `c6d04078-e6fa-4075-b925-0799166d8d5e` @ main `f9cc9652` via the dashboard "Deploy latest commit" (SHA-stamped in the deployment metadata; includes D67 `09af1cfa`). A same-day `railway up` worktree deploy (`6f540979`) built + passed healthcheck but was MISSING the new D67 route and was superseded - deploy-verification rule + incident detail in `docs/runbooks/2026-06-25-staging-deploy-runbook.md` §14: **any deploy adding routes must probe the new routes directly; healthcheck alone is not evidence.** Vercel admin-web deployed the same `f9cc9652` commit (no frontend/backend skew). **Custom domains `merchant.redeemo.co.uk` / `api.redeemo.co.uk` are NOT provisioned.** For later dates re-verify in the dashboards: **merged ≠ deployed.**
- **Migration gate (PRODUCTION-only as of 2026-07-09; staging is CURRENT):** verified 2026-07-09 by direct SQL against each Neon branch's `_prisma_migrations` (Codex live review cross-confirmed; staging apply landed with the storage-enablement work, deploy `28f3d75f-3f9a-4c3f-8b01-569e5a72b777`, approved SHA `a5808113`):

  | Neon branch (role) | Applied | `keyring_fingerprint` | `maintenance_alert_types` | `voucher_governed_flows` | `branch_google_place_id` |
  |---|---|---|---|---|---|
  | `dev-screenshot-2` (shared local dev; the repo `.env` target) | 56/56 | yes | yes | yes | yes |
  | `staging` (Railway staging) | 56/56 | yes | yes | yes | yes |
  | `production` (default/primary) | 52 | NO | NO | NO | NO |

  Staging now has the full schema (incl. `VoucherPendingEdit` + `Branch.googlePlaceId`); do not repeat the older "staging lacks these" claim. **PENDING OWNER ACTION (production only):** all four migrations await an owner-approved `prisma migrate deploy` window against the `production` branch. See §4.2, §4.4, and §6.

---

## 4. Programme status index

Status keys: **MERGED** (on `origin/main`) · **NOT-ACCEPTED** (merged, no authenticated staging acceptance) · **PARTIAL** · **PLACEHOLDER** · **NOT-STARTED** · **DEFERRED** · **OWNER-GATED** · **LEGAL-GATED** · **PROVIDER-GATED**.

### 4.1 Customer App + Customer Website
**Status (per CLAUDE.md build-progress, the customer-app closure memories, the Customer App Codex checklist, and merged PRs):** extensive surfaces MERGED - auth/onboarding, Home/Discovery/Search/Categories/Map, Voucher Detail M1-M5, Merchant Profile, Savings, Favourites (branch-level), Profile tab, QR rendering, §DF location fallback, locationContext parity, Home visual system + sticky header + bottom nav. Redemption is mobile-only by design; website has no redemption.
**Also MERGED 2026-07-09 (SHA-bound squash-merges):** #432 (`6987e32d`) global rate-limit backstop raised 100→300/min prod (600 dev under `RATE_LIMIT_RELAX`, `src/api/plugins/rate-limit.ts`) plus a new strict `register` tier (5/hour prod) on customer register - root-causes and fixes the Savings month-tap "Couldn't load" 429 bug (one active customer's focus-invalidation/favourite-toggle/map-pan fan-out reproducibly exhausted the 100/min backstop; repro 110 concurrent GETs → 10×429) and closes the rate-limit half of the `docs/security/2026-07-06-platform-security-audit.md` customer-register finding (the Turnstile/captcha half stays OPEN). #433 (`ccf9baa1`) customer-app Profile tab loading skeleton (`ProfileSkeleton`) replaces the blank screen while `useMe()` is pending - closes the skeleton item on the open-register "Profile Sub-PR 1 minors" row (the other four minors - `Interest` type import, `formatDate` hoist, GetHelpModal test comment, dead `dob` mock - remain OPEN). #434 (`3e20bbeb`) Map in-area reliability slice (plan `docs/superpowers/plans/2026-07-09-map-in-area-reliability-slice.md`) - deterministic rank-first candidate pool in `getInAreaBranches` (`orderBy: id`, `take: max(limit*4, 200)`, caller's `limit` still applied post-rank), opt-in `branchesOnly=1` fast path that skips `getInAreaMerchants` on Map (branch-arm `meta`), and client-side bbox quantization (3dp floor/ceil, ~110m grid) + `staleTime` 30s→120s so panning back to a seen grid cell hits cache. The broader Map polish bucket (open-register §1 "Checklist-only customer-app tail" row; §CK / §BA.1 - clustering, category-differentiated pins, region-accumulation cache, `AbortSignal` cancellation) stays OPEN; this slice is explicitly scoped narrower (see the plan's §3 "Explicitly deferred").
**Also MERGED 2026-07-09 (later the same day, SHA-bound):** PR #436 (`75ef7d41`) customer-app scroll-jank perf batch 1 - memoized Home rails/cards + per-rail stable callbacks (kills the header-collapse full-feed re-render), focus/scroll-gated carousel auto-advance timers, `useScrollActivity` moved to `design-system/motion` and wired onto Merchant Profile + Voucher Detail, `VoucherCardStatePill` pulse dot scrollActivity-reactive (plan `docs/superpowers/plans/2026-07-09-perf-batch1-scroll-jank.md`; Codex-reviewed no findings, CodeRabbit green). Virtualization + image right-sizing remain deferred.
**Verification status:** summarized from the existing records above; **not exhaustively re-claim-verified this pass** (this pass was Merchant-focused, plus the three 2026-07-09 merges above recorded from the merge session) - **[UNVERIFIED at line level]**; run claim-level re-reconciliation at the next Customer App programme switch.
**Detail/links:** root `CLAUDE.md` Phase 3 sections; `docs/customer-flow-current.md` + `docs/customer-flow-changelog.md`; customer-app closure memories; Customer App Codex checklist (§2 - note it itself trails, last Codex update 2026-06-13, so verify its lines against `origin/main` too). Deferrals: §8 + the deferred archive.

### 4.2 Merchant Portal - **active programme**
**Merged (verified 2026-07-04 against `origin/main` + specs):** M0 scaffold, M1 auth/session, M2 onboarding (+ flagship bridge), M3 redemptions + Validate-a-code, M4 notifications, Day-2 Vouchers, Staff & Access, Branches PR1-PR8, **Insights operational** (#329-#333), **global-shell consolidation** (#364, squash `8c4258ba`: active-route highlight, role fail-closed nav incl. least-privilege baseline, 72px collapse + narrow bottom tab bar, account menu + logout confirm, Quick Actions launcher with guarded PIN routing, notification deep-links voucher/redemption, honest placeholder routes for the 5 former dead links, favicon + middleware static-asset exemption, additive viewerCapabilities {canManageVouchers, role, displayName}). All **NOT-ACCEPTED** (no authenticated staging acceptance yet).
**Also MERGED 2026-07-04:** #365 (squash `cda007f7`) - the PR-G1b deterministic local Playwright browser-smoke lane (enforced dead-port + route-mock safety boundary); its CI job is ADVISORY - promotion to a required check is a separate owner decision. Smoke-count metrics restated honestly 2026-07-05: the lane shipped with 17 RUNTIME tests from 15 STATIC test() call sites (roles.spec.ts parametrizes one site over three role fixtures); the earlier "corrected 17 to 15" note conflated the two metrics - the original 17 was the honest runtime count. After #372 the lane runs 26 runtime tests from 24 static call sites. #366 (squash `1eb2b382`) - Vouchers V1 builder parity (terms clause checklist wired to the tested engine, voucher photo upload with storage-dark degrade, custom cooldown, window presets + end date, honestly-constrained saved photo/end-date removal with the nullable-clear contract recorded as a gated follow-up). #368 (squash `8621c9a1`) - Redemptions fidelity slice: Voucher select filter (flagship + custom sources merged with partial-source resilience via allSettled), sort control (Newest first / Biggest saving) with a deterministic total order shared verbatim by list AND CSV export (every orderBy branch ends in the unique PK tie-breaker), and code-or-voucher-title search (normalized-code prefix OR case-insensitive title, inside the untouched tenant/branch-scope where). No Merchant module is claimed COMPLETE: authenticated staging acceptance remains OUTSTANDING portfolio-wide.
**Also MERGED 2026-07-04/05 (wave 3):** #371 (squash `2945bf78`) - staff remove-member confirm-flow page-level integration coverage (test-only; 9 pins incl. scrim/Escape dismissal of the shared Dialog primitive; Opus fault-injection verified 8/8 mutations caught). #370 (squash `29f1801e`) - the nullable-clear design spec + implementation plan pair (docs-only; D1-D3 subsequently owner-APPROVED). #372 (squash `194654a5`) - smoke-lane journeys for redemptions filters (incl. a real-browser allSettled partial-source case), staff access, and notifications, plus a count-bounded expected-console-error guard contract (exact-count two-sided, waitForResponse 500 proof; strict zero-error default preserved for non-opt-in specs). #373 (squash `a58db583`) - the voucher nullable-clear implementation (explicit null clears saved imageUrl/expiryDate on the DRAFT-only merchant PATCH; independent per-field checks pinned both directions; end-date UI stays TIME_LIMITED-only; both obsolete constraint-copy strings deleted; RMV lane byte-identical; no migration).
**Business Profile (day-2 settings) v1 = COMPLETE, MERGED 2026-07-06 (owner-approved Approach A; stacked M1-M4, each SHA-bound squash-merged):** M1 #393 (`b9d110a6`) additive `getMerchantProfile` enrichment - owner contact + agreement blocks, **role-gated to OWNER|BRANCH_MANAGER** (STAFF/unknown fail closed, queries skipped = zero-leak); M2 #394 (`5ab3997c`) read shell (status-aware hero + 5 cards + compliance/agreement view + honest Documents placeholder + pending-edit banner) with the **`/profile` page failing closed for STAFF/non-privileged**; M3 #395 (`6e6e5ec4`) direct-edit registered details ("saves instantly") + business-category change (confirm / `CATEGORY_CHANGE_BLOCKED`); M4 #396 (`ade3efe5`) Public-identity edit-request UI (name/trading/description/logo/banner) - **direct in draft window, propose-via-Redeemo-review when Live** (mirrors the shipped Branches lane), pending/withdraw/one-pending guard. Codex CHANGES-REQUESTED (PII/least-privilege role boundary + nullable trading-name round trip) corrected + Opus-cleared (both findings CLOSED) BEFORE the merge sequence; every restack proven behavior-neutral; NO schema/migration; reuses the already-shipped backend (SENSITIVE edit-request lane + Option B applier). Owner decision LOCKED: BRANCH_MANAGER may view the owner's Business Contact details in v1; a future **business-generic-contact** field may later narrow personal owner details (tracked, open-register §5). **Documents (§BP-DOC) and My Account (§BP-ACC) are NOT part of v1 and remain NOT-STARTED / deferred** (this line does not imply either is complete).
**Merchant Portal fidelity wave (2026-07-07, all SHA-bound squash-merged, owner-approved, every PR Codex-reviewed with corrections applied + CodeRabbit-passed):** preceded the same day by #399 (`70961cd0`) - the §BP-ADJ1/§BP-ADJ2 hardening (role-gate the 7 registered/compliance fields on `getMerchantProfile` for non-OWNER|BRANCH_MANAGER viewers; server-side edit-request `businessName` validation via `MERCHANT_EDIT_REQUEST_INVALID_FIELD` 400 preserving `tradingName:null` clears; Opus-reviewed CLOSED/SHIP + Codex content-approved; both open-register rows CLOSED). Then the wave: #401 (`3c01a1f0`) My Account backend (NO schema) - authenticated `changePassword` + `logoutAll` (revoke-all sessions), the §BP-ACC backend gap this closes. #402 (`94588865`) Staff & Access fidelity polish - People summary-card count bug fixed (headline=total, sub-line active/deactivated), portal+app capability progress bars gain a11y `aria-label`s, portal+app job titles surfaced (additive `listMembers.jobTitle`, NO schema), native-radio segmented branch-scope control (a11y), placeholder/copy/pill-tone polish; the locked Option-C/D7/D9 divergences are intentionally preserved. #403 (`1c70080e`) My Account backend continued - `GET /merchant/account` (curated, `passwordChangedAt` derived from `AuditLog`), `PATCH account` (strict allow-list firstName/lastName/jobTitle), `GET /account/sessions` (own `UserSession` rows, `isCurrent` server-derived). #404 (`cbd9cec0`) action CTAs switched to the red/coral gradient (owner decision, navy dropped) across Validate-a-code topbar+page, the Look-up step, and Apply-suggestions; dismiss buttons made secondary. #405 (`38201d4c`) My Account PAGE toward the prototype - Your-details edit (PATCH-wired), change-password modal (wired to #401; cannot dismiss while in-flight), Sign-out-everywhere as a FULL sign-out including the current device (logout-all + normal-logout compose), Where-you-are-signed-in sessions list (userAgent-parsed device labels, no fabricated geo), login email/phone VIEW with change-email/phone honestly STAGED (contact-us, no fake flow), notification-prefs + Activity-and-reports rendered to design but STAGED (inert toggles, honest note, no backend, email dark); new BFF routes `change-password`/`logout-all` (assertSameOrigin + Bearer forward). #406 (`8f8877a4`) Home DASHBOARD for live businesses (owner decision: Home is a quick overview, not an Insights pointer; reuse-only, NO schema) - trend + busy-times charts, KPI tiles (customers-brought-in, live-vouchers, busiest-day; "New customers this cycle" staged behind the behavioural legal gate), needs-your-attention list (client-derived from profile/branch/voucher pendings), recent redemptions, live-vouchers ranked list, a live-just-started state (zero-all-time-redemptions threshold flagged for owner confirm), gated `canViewInsights` (OWNER|BM); STAFF gets a lean baseline Home (Redemptions + Help only, no Vouchers affordance per the shell role model) - this is NOT the separately-tracked Staff Home surface (still NOT-STARTED, see below). Pre-live lifecycle homes untouched. #407 (`02c75dcd`) Branches + Business Profile polish - cross-module pending-edit DIFF banner (field old→new + submitted date; image fields shown as "(change pending)" labels, never raw URLs), an amenities edit modal with per-amenity `iconUrl`, removal of the stale "Add banner coming soon" affordance, and an amenities-modal close-while-saving guard. #408 (`cc7549b4`) per-voucher analytics on the custom voucher detail page (NO schema) - `GET /vouchers/:id/analytics` (totals/lifecycle/trend/when-used-as-intensity-bands/where-used), reusing the Insights eligibility + London-TZ helpers, when-used mirroring the busy-times band discipline exactly, tenant-scoped, gated `assertMerchantActive` + `assertInsightsAccess` (STAFF-deny) + frontend `canViewInsights`; additionally Opus-adversarial-reviewed = SHIP. #409 (`73175c8b`) Redemptions fidelity polish - detail becomes an action-capable right-side drawer (hero, terms checklist, IN-PERSON section, embedded validate CTA; the View-voucher link is gated OWNER|BM fail-closed; NO reverse, that stays backend-gated/deferred), a "Validate another" affordance + detail block on validate-success, a dedicated format-error state, list info cards + a page-scoped awaiting-count banner, status-pill icons, and secondary dismiss buttons. No Merchant module is claimed COMPLETE by this wave: authenticated staging acceptance remains outstanding portfolio-wide, and several sections shipped honestly STAGED rather than live (see the Gated/not-done line below).
**Voucher governed flows package (2026-07-07, sequential SHA-bound squash-merges immediately after the fidelity wave, owner-approved D1-D5 + the merges; #411 additionally Opus-adversarial-reviewed SHIP; all three CodeRabbit-passed):** #411 (`ca1c6991`) backend/schema (Tier-3) - new `VoucherPendingEdit{kind CHANGE|END}` model + `ApprovalType.VOUCHER_EDIT` + `ApprovalStatus.WITHDRAWN` (migration `20260707135148_voucher_governed_flows`, **local-dev-only at merge time**; since verified APPLIED on staging alongside `keyring_fingerprint` + `maintenance_alert_types` on 2026-07-09 - production-only gate now, see the §3 cross-check table); merchant routes `POST /vouchers/rmv/:id/request-change` (live flagship, mandatory reason, allow-list = `RmvTemplate.allowedFields` ∩ [title,description,estimatedSaving,terms,imageUrl], one-PENDING guard), `POST /vouchers/:id/request-end` (custom-only per D4, flagship rejected), `POST /vouchers/:id/withdraw` (D2 instant self-service: PENDING_APPROVAL custom -> DRAFT, open VOUCHER approval -> WITHDRAWN; approved-waiting refused), `POST /vouchers/pending-edits/:id/withdraw`; voucher reads gain a curated `pendingEdit` summary; admin approvals list rows gain `voucherEditKind`; admin applier extended with a 'voucher' kind (allow-list re-declared on the APPLY path with saving guards; END re-verifies `isRmv:false` pre-mutation in-tx; WITHDRAWN never actionable; claim-audit resolves the real voucher). The plan doc `docs/superpowers/plans/2026-07-07-voucher-governed-flows.md` (APPROVED + as-built §7) merged with it. #412 (`350f941a`) admin-web - VOUCHER_EDIT review lane reusing the Option-B `EditReviewPanel` family (voucher header + flagship badge + merchant reason; END = explicit "stops being available to customers" treatment + "End voucher" confirm; WITHDRAWN = neutral label, actions hidden; identity-edit lanes byte-unaffected). #413 (`381452f2`) merchant-web - flagship cards clickable to detail (reused `/vouchers/[id]` resolving flagship rows from the RMV list; locked notice; #408 per-voucher analytics section) + `RequestChangeModal` (mandatory reason, changed-fields-only, no-dismiss-while-pending) + shared `PendingVoucherEditBanner` (CHANGE diff / END notice + confirm-gated withdraw) + `RequestEndModal` (custom-only) + withdraw-submission (hidden when approved-waiting) + `VoucherGovernedMenu` with D4 UI pins (request-end/edit/delete NEVER render on flagship). Opus verdict on #411: SHIP clean; 2 non-blocking notes adjudicated TRACK-not-build (recorded in the open register as §VG-IDX and §VG-RACE): (a) the one-PENDING-per-voucher guard is app-level not DB-enforced (concurrent duplicate review row possible, no privilege gain; a partial-unique index is a later nicety); (b) a withdraw-vs-in-flight-admin-approve race is last-writer-wins with the ADMIN decision winning (consistent with the approved-waiting refusal; as-designed). No Merchant module is claimed COMPLETE by this package: authenticated staging acceptance remains outstanding portfolio-wide. (The schema-deploy caveat recorded here at merge time is RESOLVED for staging as of 2026-07-09 - see the §3 cross-check table; production still pending.)
**Branch Location Trust Slice 1 (2026-07-09, PR #435 squash `92d0b2bd`, owner-APPROVED per spec `docs/superpowers/specs/2026-07-09-branch-location-trust-model.md`):** owner decision 2026-07-09 supersedes the Branches PR-6 §4b "suggestion is admin-review metadata only" invariant on the CREATE lane - see §6. Create-lane auto-trust: when a merchant applies a Google candidate token, the resolved pin is now PERSISTED (`Branch.googlePlaceId`, migration `20260709095646_branch_google_place_id`) and cross-checked server-side (parsed-postcode match + 1000m centroid-radius sanity check); both pass → `locationConfidence = ADDRESS_GEOCODED` + customer-visible pin; either fails → the existing `POSTCODE_CENTROID` behaviour plus a `NEEDS_REVIEW` stamp for the admin exception queue. Customer exposure gates (`exposeBranchPosition` + the Map in-area queries) widen from `MANUALLY_CONFIRMED`-only to the pre-existing `CONFIRMED_LOCATION_SET` (`MANUALLY_CONFIRMED` + `ADDRESS_GEOCODED`); `POSTCODE_CENTROID`/`NEEDS_REVIEW` redaction is UNCHANGED (locked, PR #81). The migration is verified APPLIED on dev + staging (both 56/56, direct-SQL cross-check 2026-07-09 - see the §3 table); only the `production` Neon branch still awaits it (§6 pending owner action, production-only). **Slice 1b MERGED same day:** PR #439 (`cef158a9`) extends the identical trust pipeline to BOTH edit lanes (reviewed-edit apply via a defensive `applyLocationTrust` in the admin editApplier + draft-window direct sensitive edit), gated on a fresh postcode re-anchor, suggestion metadata staged in both outcomes; closes open-register §LOC-1B. **Slice 2 MERGED 2026-07-09:** PR #444 (`0d137eed`, approved head `697573e5`) admin provenance badges + `LocationTrustPanel` (pin coords · resolved address · external Google Maps link · NEEDS_REVIEW staged-suggestion context covering all three Slice 1/1b lanes with a `source` discriminator) + a "Needs location review (N)" branch-list filter chip; additive ADMIN-scope read only (customer redaction untouched; `redemptionPin` never selected, test-pinned); NO embedded mini-map (admin CSP unchanged); lead adjudications recorded: no "review recommended" nudge on ADDRESS_GEOCODED, staged-suggestion read widened to all three lanes. (Paragraph folded from the superseded/closed PR #446, which also opened §LOC-2B.) **Remaining:** Slice 3 (backend MERGED #452 `ea1cf979` 2026-07-10 with migration `20260709190638_branch_merchant_confirmed_confidence` - staging/prod NOT yet migrated for it, see the change log; merchant-web part + full bookkeeping belong to the Location Trust workstream) · Slice 4 (owner-gated Google backfill) - tracked in `docs/deferrals/open-register.md` §5 (§LOC-2B/§LOC-3/§LOC-4/§LOC-MIGRATE).
**Gated / not-done:** Insights behavioural + event CSV = **LEGAL-GATED** (default-off, fail-closed); Insights demographics = **LEGAL-GATED + NOT-STARTED**; Home Live dashboard = **SHIPPED for live businesses** (#406 `8f8877a4`, 2026-07-07: trend/busy-times charts, KPI tiles, needs-your-attention, recent redemptions, live-vouchers ranked list; MERGED / NOT-ACCEPTED - staging acceptance outstanding; the "New customers this cycle" tile is **LEGAL-GATED** behind the behavioural gate; the live-just-started threshold is flagged for owner confirm); Home Staff view = **NOT-STARTED/OWNER-GATED** (STAFF gets only the lean Redemptions+Help baseline shipped in #406, which is NOT the tracked Staff Home surface); Business Profile (day-2 settings) v1 = **COMPLETE** (M1-M4 merged 2026-07-06; see the Business Profile line above); its Documents (§BP-DOC) sub-surface = **NOT-STARTED/deferred**; My Account (§BP-ACC) is **v1 SHIPPED** (#401 `3c01a1f0` + #403 `1c70080e` backend, #405 `38201d4c` page, merged 2026-07-07: your-details edit, change-password, full sign-out-everywhere incl. current device, sessions list) - do **NOT** imply complete: change-email/change-phone verification, notification-prefs, and Activity-and-reports remain honestly **STAGED/deferred** (inert UI, no backend; the email-facing sections are additionally gated on `EMAIL_ENABLED`); Help & Support = **NOT-STARTED**; Promote, Payments & Billing = **NOT-STARTED/PROVIDER-GATED**; global-shell gaps CLOSED by #364 (logout-confirm, active-route highlight, notification deep-links, Quick-Actions launcher placement, responsive collapse/tab bar, favicon); View-as lens NOT built - the prototype itself labels it "Prototype control only. Not part of the live portal." (owner to record the drop; same for the Demo switcher).
**Corrections that must be preserved:** Staff/BM **can** authenticate into the portal (only the lean Staff Home is unbuilt); Branch PIN reveal has a guarded backend route + on-demand `PinCard` (Quick-Actions placement RESOLVED + merged in #364 - the launcher deep-links to the on-page PinCard; remaining gap = extend the quick action to eligible assigned Branch Managers - the per-branch capability signal itself SHIPPED via D-BM1 #381 `1479e619` 2026-07-05, so only the extension remains); the three Vercel apps are already deployed (do not propose new blank-slate Vercel projects).
**Detail/links:** **`docs/superpowers/roadmaps/merchant-portal-programme-roadmap.md`** (the full module/screen/role/lifecycle/shell/conflict/gap/gate map + Option-C sequencing + Definition of Complete); merged specs/plans under `docs/superpowers/specs|plans` (M0-M4, Day-2 vouchers, staff-access, branches PR1-8, Insights); `docs/superpowers/governance/2026-06-27-insights-dpia/`; Admin/Merchant Codex checklists (§2).

### 4.3 Admin Panel
**Status (per CLAUDE.md + the admin closure memories + the Admin/Merchant Codex checklists + merged PRs):** `apps/admin-web` actioner console M0-M8 MERGED (challenge-bound OTP, two-step login, notification foundation + emitters, approval queue, review-context, action UI, lifecycle/confirm-location, comms/activity timeline); admin follow-ups WP1-WP5 + Option B B1 (pending-edit applier) MERGED.
**Also MERGED (2026-07 admin deltas, verified against `origin/main` 2026-07-09):** #412 (`350f941a`) VOUCHER_EDIT review lane (Option-B `EditReviewPanel` family; WITHDRAWN neutral/never actionable) · #441 (`09af1cfa`) D67 read-only cross-merchant redemptions (`redemption:read` cap, `/redemptions` page; deployed + authenticated-verified on staging 2026-07-09, masked names, no PIN/email/phone) · #443 (`5e8a6377`, approved head `e455f5f2`) security-audit H5 admin-web: refresh token httpOnly-cookie BFF-lite, access token memory-only, new `middleware.ts` route gate (customer-web H5 remains OPEN) · #444 (`0d137eed`, approved head `697573e5`) Branch Location Trust Slice 2 admin surface: provenance badges, `LocationTrustPanel`, NEEDS_REVIEW discoverability, three-lane staged-suggestion read (`STAGED_SUGGESTION_AUDIT_EVENTS` + OPEN pending-edit precedence). All MERGED / NOT-ACCEPTED pending the acceptance walk's ADMIN lane. Admin prototype reference captures (partial, synthetic-data) live at `docs/superpowers/prototype-references/admin-panel/`.
**Owner direction (2026-07-10):** S4 Members & Subscriptions read-only v1 is DEFERRED as implementation (its plan-of-record MERGED as #449 `d99305e2`, owner decisions folded in) and S5 Ops Home is DEFERRED; the next strategic priority is the **merchant-recruitment funnel** (pre-customer-launch phase: rep-driven + self-serve merchant acquisition). Funnel discovery/implementation NOT started - owner wants to discuss plan and sequencing first.
**Admin Panel PROTOTYPE (parallel Claude-Design programme, owner-approved per checkpoint since 2026-07-01):** governed by the Admin Prototype Codex checklist (§2) + the live execution log `docs/design/admin-panel/prototype-execution-log.md` (COMMITTED 2026-07-09 after an owner-directed secret/PII scan came back clean; previously untracked and the single copy of the CP-1/CP-2 sign-offs + D42-D67 evidence, a durability risk now closed). Merged canonical artifacts: `docs/superpowers/specs/2026-07-01-admin-panel-platform-blueprint.md` (PR #345) + `docs/design/admin-panel/upload-bundle/` (PR #346); reconciliations accumulate on OPEN PR #351 (includes an owner-approved CLAUDE.md rule-7 correction - now also carried by the slim CLAUDE.md; resolve the PR #351 CLAUDE.md hunk against the slimmed file at merge time). The blueprint records that the shipped `apps/admin-web` IA is an operational enabling slice, NOT the approved final Admin Panel IA. CP-0/CP-1/CP-2 locked (2026-07-02/03/05); Wave 3 in progress - detail in the execution log (do not rely on this line without re-checking it).
**Verification status:** summarized from the records above; **not exhaustively re-claim-verified this pass** - **[UNVERIFIED at line level]**; re-reconcile at the next Admin programme switch.
**Detail/links:** the admin memories; Admin/Merchant Codex checklists (§2); merged admin PRs.

### 4.4 Cross-product / platform
Backend (Node 24), Prisma 7 + Neon, Stripe/Twilio/FCM; **Resend is WIRED in code** (`src/api/shared/email.ts` imports + wraps the SDK) but **dark by default** (EMAIL_ENABLED off = no client construction, no send; EMAIL_SANDBOX rewrites recipients to an allowlist; production sender/domain/channel NOT enabled per runbook §6 - live production email UNVERIFIED); Redis; **storage (R2) is a feature-flagged capability** (`src/api/shared/storage.ts`; STORAGE_ENABLED off by default so presigning throws STORAGE_NOT_ENABLED; R2 secrets only required when enabled - configured/enabled state UNVERIFIED, not inferable from source). Pre-launch SECURITY/LEGAL/DOMAIN gate code-complete (`2a221522`, PR #170). **Migration gate:** PRODUCTION-only as of 2026-07-09 - dev + staging verified 56/56 with all four recent migrations applied (`keyring_fingerprint`, `maintenance_alert_types`, `voucher_governed_flows`, `branch_google_place_id`); the `production` Neon branch has none of the four and awaits the owner-gated deploy - see the §3 cross-check table. **Storage (R2) is LIVE on STAGING** as of 2026-07-09 (Codex-verified: Railway staging deploy `28f3d75f-3f9a-4c3f-8b01-569e5a72b777`, approved SHA `a5808113`, `STORAGE_ENABLED=true`); the in-code default remains off and production enablement is a separate step. See §5 for invariants, §6 for open decisions.

---

## 5. Cross-product decisions & invariants (visible regardless of programme)

> One-home-per-fact: rows here are the **decision record**; the canonical operative wording
> of instruction-type invariants (style, Node policy, workflow tiers, git safety) lives in
> the root `CLAUDE.md` (and its `.claude/rules/`), which is the always-loaded instruction
> surface. If wording drifts, `CLAUDE.md` wins for "how to behave"; this doc wins for "what
> was decided".

- **Branch-first cardinality** (one tile per branch; favourites/heart = branch). Locked across discovery + favourites.
- **Domain (owner):** Redeemo does **NOT** own `redeemo.com`; canonical = `redeemo.co.uk` (apex web), `api.redeemo.co.uk` (API, D-B), `merchant.redeemo.co.uk` (portal, D-C), `admin.redeemo.co.uk` (admin). `www`→apex 308 (D-E). Phase-6 email sender policy D-F: the Resend client is **wired in code** (see §4.4) but **production email sending stays dark/not-enabled** until the sender-domain is verified + SPF/DKIM/DMARC + provider + monitored inboxes + the runbook §6 pre-send gates are met. Universal-link/DNS (D-D) still open.
- **Subscription gates redemption; monthly cycle is subscription-anchored** (`getCurrentCycleWindow`), not calendar-based.
- **Style:** no emojis, no em-dashes (use `:` `;` `()` `·`); real brand hexes via tokens (red `#E20C04`, coral `#E84A00`, navy `#010C35`, cream `#FFF9F5`).
- **Node policy:** backend/CI/Railway = Node 24; customer-app (jest-expo) = Node 20.19.4 (do not bump without re-verifying jest-expo).
- **Insights eligibility cleanliness:** exclude `isTestData` (redemption+branch+merchant), QA emails, `User.status='DELETED'`; `VoucherRedemption` has **no `merchantId`** (joins via `branch.merchantId`).
- **Workflow tiers** (0-3) + plan-first discipline for Tier 2/3; git-safety hooks (no broad-add, no push-to-main, SHA-bound `gh pr merge`).
- **Authority model (this doc §1) is itself a cross-product invariant.**

---

## 6. Genuine open owner decisions

| Decision | Programme | Consequence | When |
|---|---|---|---|
| Documentation architecture migration (A+C) next steps (slim CLAUDE.md; memory hygiene) | platform | continuity reliability | after this docs foundation |
| Home Live dashboard: on Home (prototype) vs in Insights (source) | Merchant | build a Home dashboard + analytics contract, or keep pointer | before "not-started surfaces" wave |
| Staff Home: build the lean staff home or formally drop | Merchant | build vs mark superseded | same |
| Quick-Actions launcher + PIN-reveal placement | Merchant | RESOLVED by prototype authority in #364 (launcher routes to the guarded on-page PinCard; no inline reveal) - close formally | done (record) |
| View-as role lens: build or drop | Merchant | prototype marks it "Prototype control only. Not part of the live portal." - recommend recording DROP | record |
| Redemption reversal: build (prototype) or keep deferred | Merchant | audit/data-integrity implications | Redemptions slice |
| CI Postgres service + strict-loopback guard | platform/G1 | enables the security test gate | G1a1 |
| Add `@playwright/test` | platform/G1 | CLOSED 2026-07-04: owner-authorized, merged in #365 (advisory lane); the REMAINING decision is promotion of the smoke lane to a required check | closed (promotion open) |
| Seed strategy for the 53 seed-dependent integration suites | platform/G1 | seed disposable DB vs refactor to self-seed | G1a expansion |
| Insights gate-open (D1/D5) + demographics (D2-D4) | Merchant/legal | unblocks behavioural/CSV/demographics | parallel legal track |
| Promote / Payments provider + billing/PCI | Merchant | unblocks commercial modules | before commercial wave |
| Custom-domain provisioning (merchant/api/admin) | platform | trust/launch | pre-launch |
| ~~D67: read-only admin visibility of voucher redemptions REQUIRED before pre-launch staging acceptance can pass~~ **RESOLVED 2026-07-09:** SHIPPED as PR #441 (`09af1cfa`; `redemption:read` capability + `GET /api/v1/admin/redemptions` + admin-web `/redemptions`; v1 defaults D67-a/b/c owner-RATIFIED) and DEPLOYED to staging (`c6d04078` @ `f9cc9652`, route probe 401-gated). Residual: the walk itself must exercise it (acceptance-walk plan A-lane), gated on §ADM-LOGIN (staging admin credential) | Admin/platform | was the staging-acceptance blocker; now unblocked | closed |
| R1 encryption-key-rotation: DEFERRED as Classification C (post-launch security ceremony with a recorded reactivation trigger, 2026-07-05); open sub-decision D-R10 (P9 acceptance-fixture method) | platform/security | post-launch ceremony scheduling | see `docs/runbooks/2026-07-05-security-deployment-state-reconciliation.md` + the security-activation memory thread |
| MerchantLead vs shell-Merchant entry model + `MerchantSource` enum (ORGANIC/REP/EMAIL_CAMPAIGN/SOCIAL/PHONE) - preferred-in-principle 2026-06 (Codex Vol-1 ~L165-171) but never locked or built | Merchant/CRM | Phase-1 recruitment funnel design fork | before recruitment-funnel build |
| Apple IAP for iOS subscriptions (Stripe cannot be used in the iOS app; model is IAP-ready via nullable Stripe fields) | Customer/platform | gates the entire in-app subscribe-purchase surface | before mobile subscription launch |
| Subscription `source` enum (STRIPE/APPLE/GOOGLE/ADMIN) + admin-grant flow | Customer/Admin | complimentary/admin subscriptions | with the admin-grant build (was "Phase 5") |
| GDPR readiness: ICO registration + DSAR-flow confirmation (delete-account exists; DSAR export/process unconfirmed) | platform/legal | legal launch obligation | pre-launch (with legal sign-off) |
| Zoho One scope (CRM + contracts + helpdesk alongside the custom platform) | ops | tooling spend + contract-signing path (click-to-agree vs Zoho Sign) | owner to confirm |
| **PENDING OWNER ACTION (production only):** apply the four Prisma migrations (`keyring_fingerprint`, `maintenance_alert_types`, `voucher_governed_flows`, `branch_google_place_id` - the last added 2026-07-09 by PR #435 `92d0b2bd`) to the `production` Neon branch via `prisma migrate deploy`; staging verified CURRENT (56/56) on 2026-07-09 (see §3 cross-check table) | platform | production schema parity for Voucher governed-flows + Branch Location Trust | separate owner-approved deploy window, not yet scheduled |

Decisions migrated 2026-07-06 from the old root-CLAUDE.md "Open Decisions" list and RESOLVED
(recorded here so nothing is silently dropped): SMS/OTP gateway = Twilio (adopted; live in
redemption PIN SMS since Phase 2D). Website scope = fully defined (no redemption; subscription
purchase supported). White-label = out of scope for now (possible future expansion).
Also RESOLVED (owner, 2026-07-06): **graphify RETIRED** from Redeemo's active workflow - no
prompts to consult it, no regeneration required; artifacts + the optional user-invoked skill
preserved.
Also RESOLVED (owner, 2026-07-07): **voucher governed-flows decision packet D1-D5**
(`docs/superpowers/plans/2026-07-07-voucher-governed-flows.md`, shipped #411-#413) - D1 ONE
shared `VoucherPendingEdit` model (kind CHANGE|END) + a single `ApprovalType.VOUCHER_EDIT`
(not two models/types); D2 withdraw-submission is INSTANT self-service back to DRAFT while
pending (no admin step); D3 add `ApprovalStatus.WITHDRAWN` (not a Rejected-status reuse); D4
request-to-end applies ONLY to custom (`isRmv:false`) vouchers, flagship never merchant-endable;
D5 "Run this again" DEFERRED to a later, separate slice (tracked in the open register).
Also RESOLVED (owner, 2026-07-09): **Branch Location Trust Model**
(`docs/superpowers/specs/2026-07-09-branch-location-trust-model.md`, shipped Slice 1 in
#435 `92d0b2bd`) - APPROVED; the owner-approved auto-trust model (Google pick + server-side
postcode-match + 1000m-centroid-radius cross-checks → `ADDRESS_GEOCODED`, customer-visible)
**supersedes the Branches PR-6 §4b "suggestion is admin-review metadata only" invariant on
the CREATE lane** by deliberate owner direction; `POSTCODE_CENTROID`/`NEEDS_REVIEW`
redaction is unchanged (still locked, PR #81). Slices 1b/2/3/4 remain to be sequenced - see
§4.2 and the open register §5 (§LOC-1B/§LOC-2/§LOC-3/§LOC-4).

## 7. Verified warnings

- **89 of 108 backend integration suites would mutate shared Neon if run locally without a `DATABASE_URL` override** (only the 11 insights harness + 8 mocked are guarded). Evidence: `vitest.config.ts` + grep; `.env DATABASE_URL` = Neon; `tests/setup.ts` does not guard it.
- **Backend integration project (incl. all Insights legal-gate, tenant-isolation, SEC-M2 suspend proofs) is NOT run in CI** (`ci.yml` backend job runs `test:unit` only). **No e2e/browser tests exist repo-wide** - the class behind #324 (ToastProvider crash) and #327 (Decimal-as-string).
- **Stale legacy docs (updated 2026-07-06):** root `CLAUDE.md` was slimmed to durable instructions (its stale build/phase narrative is archived verbatim at `docs/history/claude-md-2026-06-20-archive.md`; treat that archive as 2026-06-20 evidence only). Private memory remains partially stale: `MEMORY.md` (~100 KB) exceeds its ~24.4 KB load cap (only the first ~14 lines load); `project_deferred_followups_index.md` is 705 KB; `project_current_state.md` (Jun 20) trails reality; a memory reconciliation is PREPARED but not applied (owner approval pending). **For CURRENT programme status, prefer this document over the private-memory files and the archive** (they are not authoritative for current status). They may still be valid HISTORICAL evidence, or governing CONTRACT evidence, when a specific claim is claim-verified; and the detailed approved specs/plans may still govern INTENDED behaviour (per the §1 authority model).
- **Insights gates are closed and must stay closed** until owner/legal records D1/D5 (behavioural/CSV) and D2-D4 (demographics).
- **Staging admin-login OTP delivery is UNVERIFIED (from Codex Vol-2, read-only):** an earlier record describes the staging-admin mailbox as not owner-receivable (OTP read from the staging DB as a workaround); a later record describes sandbox email delivery to an owner-controlled inbox. The current flow cannot be established without a live/provider check (not performed) - do not treat the DB-read workaround as current. If the workaround is current it must be replaced with a receivable/admin-controlled inbox. Deployed `favicon.ico` 404 is a known cosmetic item. (PR #327 is MERGED at `9a687393` and is closed. The **Karaara staging cleanup is NOT recorded completed**: Vol-2 §"Karaara Voucher Cleanup" recommends deleting only the inactive-leftover flagship `RMV-886F935E` and keeping the draft flagship `RMV-71C5B59E` + branch + owner data, but records the outcome as not yet recorded - Karaara's current retained/deleted staging state is **UNVERIFIED**; see §8/§10.)
- **Merged ≠ deployed ≠ staging-accepted:** every Merchant module is merged but **not authenticated-staging-accepted**.
- **Public staging posture (owner decision, ~2026-06-25, Codex Vol-1 ~L11216-11230):** anonymous-reachable `*.vercel.app` staging URLs are ACCEPTED **provided protected data stays unreachable without app login**; if protected data is ever reachable without login, stop and treat it as a security bug. (No Deployment Protection / `x-robots-tag: noindex` is currently configured.)

## 8. Open deferrals register (compact; history NOT copied here)

**The live detailed register is `docs/deferrals/open-register.md`** (created 2026-07-06 from the old CLAUDE.md deferred lists + this section; update it, and this summary when relevant, in the same PR as any deferral change). The full historical detail lives in `project_deferred_followups_index.md` (705 KB, an oversized/stale historical reference; **proposed for later freeze/reconciliation but currently UNTOUCHED** - no private-memory change has occurred; do not copy it here, and do not route live status into it). Open, currently-relevant deferrals with owner + trigger (summary of the register):

| ID / area | Owner | Trigger to pick up |
|---|---|---|
| Merchant: flagship "Always live" semantics fix | owner-gated | small data-semantics PR |
| Merchant: flagship read-only voucher detail | owner | CLOSED by #413 (`381452f2`, 2026-07-07) - flagship cards resolve to the same read-only `/vouchers/[id]` detail |
| Merchant: redemption reversal, merchantId denorm, redemption emails | owner | Redemptions slice / decision |
| Merchant: notification deep-link resolver (`redemption`/`voucher`) | eng | CLOSED by #364 |
| Merchant: logout confirmation, active-route highlight, responsive collapse | eng | CLOSED by #364 |
| G1: CI integration gate, security lane, browser smoke, staging acceptance | owner/eng | G1a1→G1c |
| Platform: §SEC.1 atomic limiter (pre-Resend), §SEC.6 SEO, SEC-H6/M1-M5 | owner | pre-email / pre-launch |
| Staging admin-login OTP delivery (UNVERIFIED - needs a receivable/admin-controlled inbox if the DB-read workaround is current) + deployed favicon 404 (cosmetic) | owner/eng | Codex Vol-2 "Active State" (read-only); a live/provider check |
| Karaara staging cleanup (UNVERIFIED) - Vol-2 recommends deleting only the inactive-leftover flagship `RMV-886F935E` (keep draft `RMV-71C5B59E` + branch + owner data); outcome not yet recorded | owner/eng | Codex Vol-2 open item "Record the Karaara narrow voucher-cleanup outcome" (read-only) |
| Customer App / Admin deferrals | owner | per their checklists + the deferred archive |

## 9. Change / update protocol

- **Claim-level, not date-based.** Each status/decision/warning/deferral is verified against the evidence in §1 before it is written; cite the SHA/PR/spec/decision. A claim with no current evidence is **[UNVERIFIED]**, not assumed true.
- **Per-PR/decision update (mandatory step in the PR checklist):** on merge, flip the affected status + add a change-log line (date, PR/SHA, one line). On an owner decision, move it in §6 (open→closed, dated) and reflect it in §5 if cross-product. On a warning raised/cleared or staging check, update §7. On a deferral open/close, update §8.
- **Re-reconciliation cadence:** continuous per-PR updates **plus** a full claim-level re-reconciliation (and a re-read of the Codex checklists per §2) at each **programme switch** or milestone - re-stamp the freshness line with the verified `origin/main` SHA.
- **Programme switching:** read this doc, pick the programme section in §4, follow its links into that programme's roadmap/specs only - do not load every history.

## 10. Codex reconciliation log

| Date | Checklists read (read-only) | Deltas vs this doc |
|---|---|---|
| 2026-07-06 | all four (§2), read-only, via 4 parallel sampling agents (coverage disclosed in the reconciliation log) | Findings folded into this doc + the open register this same day: FOUR checklists (Admin Prototype added to §2); admin-prototype programme block + merged blueprint links (§4.3/§11); D67 + R1-Classification-C/D-R10 + MerchantLead/MerchantSource rows (§6); public-staging posture + freshness-lag note (§7/header); D-BM1 correction (§4.2); CLAUDE.md rule-7 owner-approved correction carried into the slim file; open-register: Home-card-locality row removed (shipped PR #139), Google-Places quota row + checklist-only customer-app tail + decision rows added. Items proposed for CODEX to update (not done by Claude): retro-tick Vol-1 Phase-2/3 checkboxes closed by shipped work; fix Vol-1 line-87 tab-strip self-contradiction; mark §SE.1 closed (PR #320); retire Vol-1's vestigial blueprint-candidate section; record smoke-lane count drift (now ~49 static/51 runtime vs roadmap's 24/26); PR #351 title says D27-D41 but scope is D27-D65. Checklists NOT modified by Claude. |
| 2026-06-28 | all three (§2) | merged-state: no deltas (Insights/G1 corroborated). **PR #327 MERGED** (`9a687393`) - removed from open. **Karaara staging cleanup NOT recorded completed:** Vol-2 §"Karaara Voucher Cleanup" recommends deleting ONLY the inactive-leftover flagship `RMV-886F935E` (keep the draft flagship `RMV-71C5B59E` + branch + owner data) but records the outcome as "not yet recorded" (open item: "Record the Karaara narrow voucher-cleanup outcome"); earlier branch-cleanup reports changed over time - so the **current Karaara retained/deleted staging state is UNVERIFIED** (do NOT assert "2 submitted vouchers" or completed voucher cleanup). **Staging admin-login OTP flow UNVERIFIED** (earlier = DB-read workaround / not-receivable mailbox; later = sandbox email to owner inbox; current flow needs a live/provider check, not performed). Customer App checklist itself trails (Jun 13). Checklists not modified by Claude. |

## 11. Governing-document links

- **Merchant Portal roadmap:** `docs/superpowers/roadmaps/merchant-portal-programme-roadmap.md`
- **Specs/plans:** `docs/superpowers/specs/`, `docs/superpowers/plans/` (per-module; linked from the roadmap)
- **Audits:** `docs/superpowers/audits/`
- **Governance (Insights DPIA):** `docs/superpowers/governance/2026-06-27-insights-dpia/`
- **Runbooks:** `docs/runbooks/` (deploy-security, staging, insights-test-db, insights-demo-fixture, railway-hosting)
- **Customer flow:** `docs/customer-flow-current.md` + `docs/customer-flow-changelog.md`
- **Constitution (stable):** root `CLAUDE.md` (tech stack, business rules, workflow tiers, git safety, style locks, run/test) - slimmed 2026-07-06 to durable instructions only; it routes status questions here. Path-scoped guidance: `.claude/rules/`. Historical build narrative: `docs/history/claude-md-2026-06-20-archive.md`.
- **Open deferrals register (live):** `docs/deferrals/open-register.md` (§8 is its summary).
- **Admin Panel prototype:** `docs/superpowers/specs/2026-07-01-admin-panel-platform-blueprint.md` + `docs/design/admin-panel/upload-bundle/`; live execution log `docs/design/admin-panel/prototype-execution-log.md` (committed 2026-07-09); open reconciliation PR #351.
- **Codex checklists (read-only, §2).**

---

## Change log

- **2026-07-10b** - Merchant 360 Phase A BUILT (owner-directed recruitment build; plan PR #460 + assets PR #459, both open): STACKED PRs, one approval unit, merge bottom-up, #461 never alone: #461 A1 workspace shell (`b1d29001`, 77/1060) <- #462 A2 branches/docs/activity (`175cad40`, 77/1077) <- #463 A3 per-merchant redemptions + RMV co-build wiring (`6564dd06`, 80/1102; global /redemptions kept per owner decision) <- #464 A4 additive backend reads + Staff tab (`b9554e05`, backend 246 files/3046 + admin-web 81/1117; Opus adversarial review SHIP-WITH-NOTES, all claims execution-verified; finding 6 fixed on-branch: dash not fabricated-0; finding 7 recorded as conscious choice: header counts include isTestData, consistent with the D67 default). ALL HELD at the review gate pending Codex source review + owner SHA-bound approval. Cross-workstream note: portal-session PR #455 (backend queue-row merchant-name enrichment, walk WF16) SUPERSEDES plan B2's additive read; Phase B proceeds frontend-only consuming its fields drift-tolerantly. Staging deploy remains stop-gated (§2026-07-10a).
- **2026-07-10a** - Admin close-out round (owner-approved, all SHA-bound): #450 MERGED (squash `86134e33`, verified head `97661d88`: capability-mirror parity + literal pin test, stale shell comment, storage-copy audit = none found, the branches PIN-payload register row was ALREADY hardened by the #377 chain so the row was stale - closed with wire pins). #448 MERGED (squash `4374d97e`, verified head `3b324e3e`: the prototype execution log committed after a clean secret/PII scan). #449 MERGED as PLAN-OF-RECORD ONLY (squash `d99305e2`, owner-approved head `58317277`); S4 implementation + S5 Ops Home DEFERRED by owner direction 2026-07-10 - next strategic priority is the merchant-recruitment funnel (not started). PR #446 confirmed CLOSED (superseded; its two net-new pieces - the §4.2 Slice-2 paragraph and register §LOC-2B - are folded in by THIS entry's PR with provenance). **Staging backend deploy DEFERRED at the stop-gate:** the owner-approved deploy assumed code-only/no-pending-migrations, but #452 (`ea1cf979`, merged mid-round by the parallel session) added migration `20260709190638_branch_merchant_confirmed_confidence` (additive enum value `MERCHANT_CONFIRMED`) - the repo now carries 57 migrations vs staging's 56, so a tip deploy is no longer migration-free; owner decision needed (migrate staging then deploy, or coordinate the deploy with the Location Trust/portal session). Consequence while undeployed: the staging admin onboarding-review screen remains in its failed-to-load state (frontend expects #444's review fields).
- **2026-07-09e** - Admin prototype execution log COMMITTED (docs-only): `docs/design/admin-panel/prototype-execution-log.md` was the untracked single copy of the CP-0/CP-1/CP-2 checkpoint sign-offs, the Wave 1-3 screen trackers, and decisions D42-D67 (the Admin Prototype Codex checklist self-reports ending at CP-0). Owner directed inclusion subject to a secret/PII scan; scan performed (no secret values, no credentials, no phone numbers, no keyed URLs; only public brand contact emails) and came back clean. §4.3 + §11 pointers updated from "untracked" to committed. The log remains the owner-maintained live record; the file continues to be updated in place.
- **2026-07-09d** - Admin S1 (clear-the-in-flight) merges, both SHA-bound + Codex-reviewed + owner-approved: #443 (squash `5e8a6377`, approved head `e455f5f2`, live-compare 4 commits/31 files, 11/11 checks) - admin-web H5: refresh token out of localStorage into an httpOnly cookie via BFF-lite (4 new `app/api/admin-auth/*` routes, same-origin guarded), access token memory-only with refresh-on-mount, new `middleware.ts` closes the audit L7 no-route-gate finding; branch had merged main + migrated the D67 redemptions test to the new SessionValue shape. #444 (squash `0d137eed`, approved head `697573e5`, live-compare 10 commits/16 files, 11/11 checks) - Branch Location Trust Slice 2 admin surface; the pre-merge review round fixed the admin-web CI fixture (TS2322) and widened the staged-suggestion read to ALL THREE Slice 1/1b lanes (OPEN `BranchPendingEdit.__locationSuggestion` precedence-winning `pending_edit`, plus latest-parseable audit across `BRANCH_CREATED`/`BRANCH_UPDATED`; test-pinned event map; reviewed-edit-lane scope adjudication recorded in the plan as-built addenda + register §LOC-2). §4.3 gains a dated "Also MERGED" line absorbing #412/#441/#443/#444 (full line-level re-reconciliation still owed at the next Admin programme switch); register §LOC-2 flipped MERGED. Admin prototype reference captures (5 PNGs + provenance README, partial coverage, synthetic data) added under `docs/superpowers/prototype-references/admin-panel/`. Coordination note: #444 was merged by the parallel Merchant Portal session at the same approved head; this session merged #443 and performed branch/worktree cleanup (both remote branches deleted).
- **2026-07-09c** - D67 SHIPPED + DEPLOYED to staging; acceptance-walk prep (this entry): #441 (squash `09af1cfa`, SHA-bound at Codex-approved `6ac100d1`, scope live-verified 2 commits/17 files) - read-only cross-merchant admin redemptions visibility: new `redemption:read` capability, `GET /api/v1/admin/redemptions` (curated select, no `redemptionPin`/customer email/phone, masked customer name), admin-web `/redemptions` page + nav (fail-closed both layers); NO schema/migration; owner RATIFIED the v1 defaults D67-a (masked name only) / D67-b (list-only, no CSV/detail) / D67-c (test rows shown by default with a Test badge); plan doc `docs/superpowers/plans/2026-07-09-d67-admin-redemption-visibility.md` merged in-PR; closes the §6 D67 acceptance blocker (row updated). #442 (squash `f9cc9652`, SHA-bound at `bc62870d`) - open-register B2 address-search note flipped RESOLVED; superseded #440 CLOSED unmerged (its migration/storage wording was wrong: #438's cross-check table is the source of truth). §3 deployed bullet updated to the ACCEPTED staging deployment `c6d04078-e6fa-4075-b925-0799166d8d5e` @ `f9cc9652` (dashboard "Deploy latest commit") after a same-day `railway up` worktree deploy (`6f540979`) shipped an artifact MISSING the D67 route despite build+healthcheck success - deploy-verification rule recorded in the staging runbook §14 (probe new routes directly). Admin-login investigation (owner-directed, no reset performed): staging `admin@redeemo.com` exists + `isActive` but its `passwordHash` does NOT match the documented seed credential (bcrypt-compared true on dev, false on staging; no hash values exposed); the staging hash was last updated 2026-06-26T19:13Z consistent with a completed password-reset flow (reset email SENT 2026-06-25/26 while sandbox email was live; owner-IP failed logins 22:06-22:10 that evening; a 22:13 second reset email went unused; a 22:17 `admin_otp` was SENT implying one later correct-password login attempt; `lastLoginAt` is NULL so no fully completed session is recorded). Tracked as open-register §ADM-LOGIN (owner decision: reset via the queued-email flow or supply the password). Acceptance-walk plan added: `docs/superpowers/plans/2026-07-09-merchant-portal-staging-acceptance-walk.md`. Module acceptance statuses remain UNCHANGED until the walk runs. Freshness header/merged pointer intentionally NOT re-stamped.
- **2026-07-09** - #432/#433/#434/#435 MERGED (SHA-bound squash-merges), consolidated docs-governance pass (this entry, branch `docs/2026-07-09-merge-bookkeeping`): #432 (`6987e32d`) Savings month-tap 429 fix - global rate-limit backstop 100→300/min prod (600 dev-RELAX) + new strict customer-register `register` tier (5/hour prod), closing the rate-limit half of the `docs/security/2026-07-06-platform-security-audit.md` customer-register finding (Turnstile half stays OPEN). #433 (`ccf9baa1`) customer-app Profile tab `ProfileSkeleton` replaces the blank-screen loading state, closing the skeleton item of the open-register "Profile Sub-PR 1 minors" row (four minors remain OPEN). #434 (`3e20bbeb`) Map in-area reliability slice (plan `docs/superpowers/plans/2026-07-09-map-in-area-reliability-slice.md`) - deterministic candidate pool, opt-in `branchesOnly` fast path, client bbox-quantized caching; the broader Map polish bucket stays OPEN. #435 (`92d0b2bd`) Branch Location Trust Slice 1 (spec `docs/superpowers/specs/2026-07-09-branch-location-trust-model.md`, owner-APPROVED 2026-07-09, supersedes the Branches PR-6 §4b admin-review-only invariant on the CREATE lane) - auto-trusted `ADDRESS_GEOCODED` pins via server-side cross-checks, customer exposure gates widened to `CONFIRMED_LOCATION_SET`, migration `20260709095646_branch_google_place_id` initially recorded as local-dev-only, CORRECTED same day after direct-SQL verification: dev + staging are both 56/56 with all four recent migrations applied (staging apply landed with the storage-enablement deploy `28f3d75f`, approved SHA `a5808113`, which also set `STORAGE_ENABLED=true` on Railway staging); only the `production` Neon branch lacks the four, so the §6 pending owner action is production-only (see the §3 cross-check table); Slices 1b/2/3/4 remain OPEN. §4.1 gained the #432/#433/#434 paragraph + an in-flight note for PR #436 (customer-app scroll-jank perf batch 1, Codex-reviewed no-findings, NOT yet merged - do not mark merged). §4.2 gained the Branch Location Trust Slice 1 paragraph. §3/§4.4 migration-gate counts updated three→four. §6 gained the Branch Location Trust RESOLVED note + the migration-apply pending-owner-action row. `docs/deferrals/open-register.md` updated in the same PR (§1 Profile Sub-PR 1 minors annotated, §1 Map-polish-bucket row annotated, new §5 Branch Location Trust follow-ups block §LOC-1B/§LOC-2/§LOC-3/§LOC-4/§LOC-MIGRATE). Same-day additions during the review of this pass: #436 (`75ef7d41`) perf batch 1 MERGED (the earlier in-flight note in §4.1 replaced with the merged record) and #439 (`cef158a9`) Branch Location Trust Slice 1b MERGED (both edit lanes; §4.2 paragraph updated; open-register §LOC-1B CLOSED). Freshness header/merged pointer intentionally NOT re-stamped by this entry (per the same convention as the #421/#422 and #426/#427 entries above); this is a docs-bookkeeping pass, not a full re-reconciliation.
- **2026-07-08b** - #426 + #427 MERGED (SHA-bound squash-merges): #426 (squash `c6362c06`) merchant-web perf/UX - A9 Insights trend parallel-fetch + insights-tab and LoginSecurity skeleton loading states. #427 (squash `3af11c5e`) Merchant Documents MVP (Option 1) - self-serve upload/view own documents (OWNER upload, BRANCH_MANAGER view, no delete), plus shared-storage content-type hardening, a new admin route, and a stale merchant-web.md BFF doc correction. **Honest lag note: recorded from the merge session only, per the same pattern as the #421/#422 entry above; NOT folded into the §4.2 prose. #414-#420 remain unfolded from that entry (#424 was itself the docs-reconciliation commit for #421/#422, not a product change); the next Merchant re-reconciliation must absorb the full #414-#427 run.** Freshness header/merged pointer intentionally NOT re-stamped by this entry.
- **2026-07-08** - #421 + #422 MERGED (SHA-bound squash-merges, owner-approved at `4f3c6c9d` / `09127d92`, scopes live-compare verified pre-merge: 1 commit / 3 files and 1 commit / 15 files respectively): #421 (squash `6213c7b2`) Validate-a-code dialog visual fidelity to prototype, UI-only (dialog + jest test + e2e spec); owner decision recorded with the merge: the dialog CTAs KEEP the #404 red/coral gradient rather than reverting to prototype navy (the PR already preserved `variant="gradient"`; navy appears only on the icon chip + scan-hint panel). #422 (squash `bf353e34`) merchant-web skeleton loading states (A8) + data-fetch parallelization audit (A9); plan doc `docs/superpowers/plans/2026-07-07-merchant-web-skeleton-loading-a8-a9.md` merged with the PR. **Honest lag note: recorded from the merge session only; NOT folded into the §4.2 prose, and the intervening #414-#420 also remain unfolded - the next Merchant re-reconciliation must absorb #414-#422.** Freshness header/merged pointer intentionally NOT re-stamped by this entry.
- **2026-07-07b** - Voucher governed-flows package MERGED (#411-#413, sequential SHA-bound squash-merges immediately after the fidelity wave, owner-approved D1-D5 + the merges; #411 additionally Opus-adversarial-reviewed SHIP; all three CodeRabbit-passed): #411 `ca1c6991` backend/schema (new `VoucherPendingEdit{kind CHANGE|END}` model, `ApprovalType.VOUCHER_EDIT`, `ApprovalStatus.WITHDRAWN`; migration `20260707135148_voucher_governed_flows` applied to LOCAL dev DB only) · #412 `350f941a` admin-web VOUCHER_EDIT review lane (reuses the Option-B `EditReviewPanel` family) · #413 `381452f2` merchant-web flagship interactivity + governed change/end/withdraw UI. §4.2 gains a dated paragraph recording the package; the decision packet `docs/superpowers/plans/2026-07-07-voucher-governed-flows.md` D1-D5 recorded RESOLVED in §6. Merged pointer + freshness re-stamped @ `381452f2` for the **Merchant programme only** (other programmes not reverified this pass; the #375-#392 lag note is preserved unchanged). Staging/prod migration gate newly noted in §3/§4.4: three migrations (`keyring_fingerprint`, `maintenance_alert_types`, `voucher_governed_flows`) are local-dev-only and await a separately owner-gated `prisma migrate deploy`. §8's "Merchant: flagship read-only voucher detail" deferral row CLOSED by #413 (flagship cards now resolve to the same read-only `/vouchers/[id]` detail). `docs/superpowers/roadmaps/merchant-portal-programme-roadmap.md` updated in the same PR (Vouchers/voucher-detail rows flip to SHIPPED, MERGED/NOT-ACCEPTED preserved; D5 recorded owner-deferred). `docs/deferrals/open-register.md` updated in the same PR (§5): new rows §VG-IDX (one-PENDING partial-unique-index nicety, TRACK-not-build per Opus), §VG-RACE (withdraw-vs-approve last-writer-wins, admin-wins as-designed, TRACK-not-build per Opus), D5 Run-this-again (owner-deferred separate slice), and a staging-migration-gate row; the existing redemption-reversal row cross-referenced as still open / NOT shipped by this package.
- **2026-07-07** - Merchant Portal fidelity wave MERGED (#401-#409, all SHA-bound squash-merged, owner-approved, every PR Codex-reviewed with corrections applied + CodeRabbit-passed; #408 additionally Opus-adversarial-reviewed SHIP): #401 `3c01a1f0` My Account backend (changePassword+logoutAll) · #402 `94588865` Staff & Access fidelity polish (count-bug fix, a11y bars, job titles) · #403 `1c70080e` My Account backend continued (GET/PATCH account, sessions list) · #404 `cbd9cec0` action-CTA red/coral gradient (owner decision) · #405 `38201d4c` My Account page toward prototype (staged sections kept honest) · #406 `8f8877a4` Home Live dashboard for live businesses (reuse-only, NO schema) · #407 `02c75dcd` Branches + Business Profile pending-edit-diff polish · #408 `cc7549b4` per-voucher analytics (NO schema, Opus SHIP) · #409 `73175c8b` Redemptions fidelity polish (drawer, validate-another, format-error). §4.2 "Gated / not-done" flipped: My Account = **v1 SHIPPED** (change-email/phone + notification-prefs + reports remain STAGED/deferred; email-facing sections additionally `EMAIL_ENABLED`-gated - do not imply complete); Home Live dashboard = **SHIPPED for live businesses** (the Home-vs-Insights placement conflict is now CLOSED by owner decision; Staff Home stays NOT-STARTED/OWNER-GATED; the new-vs-returning "New customers this cycle" tile is LEGAL-GATED). Merged pointer + freshness re-stamped @ `73175c8b` for the **Merchant programme only** (other programmes not reverified this pass). #399 `70961cd0` (merged 2026-07-07 just before the wave) confirmed as the §BP-ADJ1/§BP-ADJ2 hardening (Opus-reviewed CLOSED/SHIP + Codex content-approved before merge); both open-register rows CLOSED with citations. `docs/deferrals/open-register.md` updated in the same PR (§5): §BP-ADJ1 + §BP-ADJ2 CLOSED (PR #399 `70961cd0`); §BP-ACC annotated v1 SHIPPED with its email-verification and notification-prefs sub-items split into new tracked rows (§BP-ACC-EMAIL, §BP-ACC-PREFS); new rows opened for Home (§HOME-NVR legal-gated tile, §HOME-THRESH live-just-started confirm, §HOME-DOCS needs `MerchantDocument.expiryDate`), per-voucher analytics (§VA-GRAN), Redemptions true awaiting-count (§RDM-COUNT), and the voucher governed-flows decision packet (`docs/superpowers/plans/2026-07-07-voucher-governed-flows.md`) cross-referenced against the existing redemption-reversal row.
- **2026-07-06d** - Business Profile day-2 v1 SHIPPED (M1-M4 SHA-bound squash-merged: #393 `b9d110a6` additive owner-contact/agreement role-gated to OWNER|BRANCH_MANAGER · #394 `5ab3997c` read shell · #395 `6e6e5ec4` registered-details direct-edit + category-change-confirm · #396 `ade3efe5` Public-identity edit-request UI; owner-approved Approach A; Codex PII/role + nullable-clear findings corrected + Opus-cleared before merge; every restack proven behavior-neutral). §4.2 records it COMPLETE - Documents (§BP-DOC) + My Account (§BP-ACC) are NOT part of v1 and stay deferred (this entry does not imply either is done). Merged pointer + freshness re-stamped @ `ade3efe5` for the **Merchant programme only** (other programmes not reverified this pass). Follow-ups opened in `docs/deferrals/open-register.md` §5: §BP-ADJ1 + §BP-ADJ2 (hardening PR #399 `5d9167aa`, unmerged, Codex content-approved), §BP-DOC, §BP-ACC, business-generic-contact.
- **2026-07-06c** - Four-checklist read-only reconciliation round (see §10 row): admin-prototype programme block added to §4.3 + §11 links; D67, R1-Classification-C/D-R10, MerchantLead/MerchantSource added to §6; public-staging posture decision added to §7; freshness-lag note (#375-#390) added to the header; D-BM1 §4.2 correction; slim CLAUDE.md rule 7 aligned with the owner-approved 2026-07-02 correction (also on open PR #351 - resolve that PR's CLAUDE.md hunk against the slim file at merge time).
- **2026-07-06b** - Owner clarifications round: §2 corrected to FOUR Codex checklists (Admin Prototype checklist `2026-07-01-admin-panel-prototype-workflow-checklist.md` added; it was missing from this doc and the migration); graphify decision CLOSED as RETIRED (owner 2026-07-06) - §6 row removed, resolved note added, Glob/Grep reminder hook deleted, CLAUDE.md §15 now records the retirement; checklist-guard wording updated to four-and-growing (protection was already filename-pattern-based, so the fourth checklist was covered; regression evidence extended).
- **2026-07-06** - Documentation-architecture migration (branch `docs/claude-code-architecture`; closes the §6 "Documentation architecture migration (A+C) next steps" repo-side scope). Root `CLAUDE.md` slimmed to durable instructions (1,065 → ~200 lines) with its full former content archived verbatim at `docs/history/claude-md-2026-06-20-archive.md`; six path-scoped `.claude/rules/` files added; `redeemo-dev-qa-toolkit` skill added; Codex-checklist write-guard hook added (project-level); `docs/deferrals/open-register.md` created as the live deferral register (§8 now summarizes it); the 7 old CLAUDE.md "Open Decisions" migrated into §6 (3 recorded resolved: Twilio, website scope, white-label); §5 one-home-per-fact note added; §7 stale-docs warning updated; `docs/product-decisions.md` marked superseded. Memory reconciliation PREPARED only (apply is owner-gated). Evidence: the reconciliation log in `~/Documents/Playground/redeemo-notes/claude-documentation-reconciliation-log.md`.
- **2026-07-05b** - Wave-3 merge records: #371 (`2945bf78`, staff remove confirm tests), #370 (`29f1801e`, nullable-clear spec+plan; D1-D3 owner-APPROVED after merge), #372 (`194654a5`, smoke journeys + count-bounded error-guard contract), #373 (`a58db583`, nullable-clear implementation - closes the #366 gated follow-up). Smoke-count metrics RESTATED: 26 runtime tests / 24 static call sites after #372 (was 17 runtime / 15 static; the 2026-07-05 "17 to 15" correction had conflated the two metrics - runtime exceeds static because roles.spec.ts parametrizes one site over three roles). Merged pointer + freshness re-stamped @ `a58db583`. Staging acceptance remains outstanding portfolio-wide; all owner/legal/provider gates preserved.
- **2026-07-05** - #368 MERGED (squash `8621c9a1`: Redemptions fidelity slice - voucher filter, deterministic shared list/CSV sort, code-or-title search); Redemptions completion-map row's residual fidelity gaps CLOSED, status stays MERGED / NOT-ACCEPTED (authenticated staging acceptance outstanding); smoke-lane test count corrected 17 → 15 (direct spec grep) here and in the roadmap; Merchant freshness re-stamped @ `8621c9a1`. All owner/legal/provider gates preserved unchanged.
- **2026-07-04c** - #365 + #366 MERGED (squash `cda007f7` / `1eb2b382`); pointers flipped from in-review to MERGED; Quick-Actions "placement undecided" correction line fixed (resolved in #364; residual = BM extension on a per-branch capability signal); @playwright/test decision closed (promotion-to-required stays open); staging acceptance explicitly outstanding portfolio-wide.
- **2026-07-04b** - Governance accuracy round (Codex): completion-map rows corrected against merged #364 (Validate-a-code compact + intentional suspended-enabled; the 5 former dead links = honest placeholder routes with modules still NOT-STARTED; Branch-PIN placement resolved); Branches row records PR-2 BM-writes as SHIPPED (design-doc language historical); Redemptions row clarifies NOT-ACCEPTED = staging gate; PR head pointers refreshed (#365 `410b0c67`, #366 `cf6d510c`, unmerged).
- **2026-07-04** - Merchant shell wave MERGED (#364, squash `8c4258ba`): §4.2 status flipped (shell gaps closed; View-as/Demo prototype-only, drop recommended); §6 Quick-Actions decision resolved by prototype authority; §8 shell-wave deferrals closed. Open in-review PRs recorded: #365 (G1b Playwright smoke lane, `3f9bf74e`), #366 (Vouchers V1 builder parity, `f829ed46`). Merchant freshness re-stamped @ `8c4258ba`.
- **2026-06-28** - Document created (docs-only PR, foundation of the A+C documentation architecture). Establishes the fact-type authority model, the read-only Codex reconciliation protocol, the programme-partitioned status index, cross-product invariants, open owner decisions, verified warnings, the compact deferrals register, and the change/update protocol. Verified against `origin/main` @ `434ca4eb`. Customer App + Admin sections summarized from existing records and marked for line-level re-reconciliation at their next programme switch.
