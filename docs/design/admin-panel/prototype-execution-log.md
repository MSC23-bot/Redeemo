# Redeemo Admin Panel prototype: execution log / tracker

Running record of the Claude Design prototype execution (Stage 0 onward). Maintained after every step so the build can be reconstructed later. Also serves as the manifest §4 checkpoint-summary store (CP-0 / CP-1 / CP-2).

Status legend: [ ] pending · [~] in progress · [x] done · [!] blocked/decision-needed.

## 0. Standing facts, scope and constraints

- **Governing main SHA:** `1d59d5a944abe1e707225f5fca6b8b0a92be0b09` (blueprint + D14 bundle, both reconciliation-amended and merged).
- **Governing sources (read):** `docs/superpowers/specs/2026-07-01-admin-panel-platform-blueprint.md`; `docs/design/admin-panel/upload-bundle/{upload-execution-manifest.md, CLAUDE-DESIGN-RUNBOOK.md, 00-admin-master-context.md, merchant-reference-summary.md}`.
- **Current authorised scope:** Stage 0 / checkpoint **CP-0 only** (shared design system + dense operator shell). No Wave 1/2/3 screens. No Wave packs uploaded/executed.
- **Hard constraints (do not cross):** no Claude Design upload/execution beyond CP-0 assets; no code/schema/provider/infrastructure changes; no touching the parallel Neon/security workstream; no repo/codebase connection; no `.dc.html`, handoff zip, `.env`, secrets, credentials, real data, or transient incident information uploaded; synthetic data only; providers never shown operationally live.
- **Division of labour:** Claude Design is browser-driven by the owner (this assistant cannot click in it or authenticate DesignSync headlessly). The assistant runs the local checks, prepares exact assets/prompts, guides one browser step at a time, and maintains this log. DesignSync is the later PRESERVE tool, not the Stage-0 generator.
- **Doc-sync discipline (owner-directed 2026-07-02):** the prototype and the blueprint must NOT drift. Amend the blueprint (+ affected wave packs) PROMPTLY after each locked prototype decision - do NOT batch/defer to "a natural break". This execution log's pending-blueprint list must stay SHORT and be flushed frequently. Every prototype change (new field, new flow, capability change, IA change, honest label) must be reconciled into the blueprint so the log and the source of truth stay in step. Master context = the pasted frame (not re-amended mid-prototype); manifest/runbook/README/traceability = process aids (no content changes). CLAUDE.md changes only on explicit owner decision (e.g. the open rule-7 call).
- **Governance (updated 2026-07-02, D-CD5):** Codex is OUT of the prototype loop. The assistant is now the design reviewer/driver, making every checkpoint call grounded in the merged blueprint + master context + locked decision register (D1-D26, D-CD*) + safety/privacy boundaries + tier rules. The OWNER is the single approver at each checkpoint (one decision-ready summary per gate, no back-and-forth). The assistant does NOT self-decide: (1) genuinely undecided PRODUCT calls (stop-and-flag list) - designs them as labelled concepts + flags, never fabricates approval; (2) anything crossing the hard boundary (code/schema/providers/Neon). Cadence default: per-wave checkpoint sign-off (CP-1/CP-2/CP-3); owner may loosen to a single end-of-prototype review.
- **This file:** untracked working record under `docs/design/admin-panel/`; not committed to the parallel Neon branch; to be folded into a future approved Admin-prototype PR.

## 1. Claude Design environment

- **Project:** `Redeemo Admin Panel` (URL `claude.ai/design/p/eae5f333-5c1a-4868-9c5c-cdcdadead6fb`). Separate from `Redeemo for Business - Merchant Portal` (not opened/modified).
- **Template:** Interactive prototype + Hi-fi design. **Model:** Claude Opus 4.8 (High).
- **Design system attached:** the shared **"Design System (Mustica Pro, Lato)"** already in the account (= the Redeemo brand foundation; proves the exact fonts are already ingested).

## 2. CP-0 checklist

- [x] Read governing sources.
- [x] Run manifest §2A pre-upload existence + safety checks (present/missing only; no contents printed).
- [x] Verify required fonts present (Mustica Pro SemiBold + Lato x4) -> STOP condition NOT triggered.
- [x] Create the `Redeemo Admin Panel` project (Prototype, Opus 4.8, Design System attached).
- [~] Load Stage-0 context. `merchant-reference-summary.md` attached (verified CURRENT vs main). Optional shell-continuity screenshot `.../merchant-web-branches/01-branches-overview.png` offered (owner's call). Blueprint HELD (D-CD3). On-disk master-context NOT attached (stale on this Neon branch - see §6).
- [x] Paste master-context **Section A** -> acknowledged. Claude Design correctly separated reuse (fonts/palette/Rose-coral single-primary/two-pill status/voucher card/shell chrome/deriveStatusPill label set) from divergence (dense ops register / platform IA / role-aware Ops Home); captured operating principles, privacy boundaries and the stop-and-flag guardrail list; grounded in reference pack + bound design system; held for direction.
- [x] Paste master-context **Section B** -> acknowledged. Claude Design restated the brand lock + density divergence + full-state-set + CP-0-first, AND self-verified type fidelity from INSIDE the bound design system (MusticaPro-SemiBold.otf + 4 Lato weights + @font-face in tokens.css + full token set: palette/neutrals/voucher accents/type scale/radii/4-8 spacing/navy elevation/--shadow-glow CTA). No STOP, no fallback. Held for the build scope.
- [x] Ask for the CP-0 build only: shared design system + grouped 8-section shell, no Wave 1. **Output produced:** single `Redeemo Admin - Foundation.dc.html` with BOTH deliverables behind a top-strip toggle (Operator shell / Design system). Interactivity wired (sidebar collapse, operator role-cycle updating capability-locked items, sortable/selectable table, typed-reason dialog, toasts).
- [x] Capture screenshots of the design system + shell. Have: Operator shell (1 shot) + Design system tab (5 shots, sections 01-13). Outstanding (trivial, non-blocking): sidebar scroll to groups 7 Insights + 8 Platform - deferred to Wave 1 (which builds Platform anyway).
- [x] Assemble CP-0 report -> **VERDICT: PASS.** Assistant recommended APPROVE + lock.
- [x] **Owner signed off CP-0 (2026-07-02): APPROVED + LOCKED.** Approved as shown; cool-neutral canvas kept per assistant rec (owner did not pull back to cream). Wave 1 opened - progress tracked in §10.

## 3. Existence + safety check result (present/missing only)

- TRACKED present: Admin blueprint; `merchant-reference-summary.md`; 22/22 prototype-reference PNGs (all 6 selected anchors present).
- OWNER-LOCAL present (REQUIRED fonts): `MusticaPro-SemiBold.otf`; `Lato-{Bold,Medium,Regular,Semibold}.ttf` (4/4).
- OWNER-LOCAL present (optional): `tokens.css`; `01-foundations.html`, `02-components.html`, `03-voucher-blocks.html`; brand-foundations spec.
- Present but MUST NOT upload: merchant handoff zip / `.dc.html`.
- No secrets, env values, connection strings, credentials, incident data, or file contents were printed during checks.

## 4. Decisions log

- **D-CD1 (fonts):** required fonts present + the account's shared "Design System (Mustica Pro, Lato)" already carries exact Mustica Pro + Lato -> exact typography guaranteed; item-3 STOP/fallback NOT triggered.
- **D-CD2 (design-system attach):** attach the existing shared "Design System (Mustica Pro, Lato)" to the Admin project. Rationale: a design system = brand tokens/typography/base components, NOT the merchant IA (which lives in the merchant *project*), so reuse gives exact brand without making Admin an enlarged Merchant Portal. Owner did this.
- **D-CD3 (blueprint upload) [assistant recommends HOLD; owner deciding]:** Do NOT upload the blueprint, for CP-0 and by default for the whole run. Reasons: (a) HARD - §17 carries the dated transient deployment snapshot, prohibited by owner instruction #7; (b) the master context (§A-§G) is already the design-facing distillation of the blueprint (personas, complete 8-group IA, authority outcomes, status semantics, synthetic data, acceptance) - the manifest classifies the blueprint as a consult reference, not a prompt, because raw-spec upload tends to make the tool design mechanically off the tables; (c) the full blueprint is meta-heavy (source citations, evidence appendix, freshness caveats, SHA/reconciliation trail) = noise to a design tool. Nothing material is lost by holding: CP-0 is driven by master §A/§B; the wave packs are self-sufficient. If a specific wave later needs a specific blueprint detail, paste a SMALL redacted excerpt (never the whole file, never §17). Owner asked for the assistant's view; recommendation given = HOLD.
- **D-CD6 (customer / member / revenue domain gap - reconcile the blueprint) [owner-directed 2026-07-02]:** Owner flagged the Admin Panel (and the blueprint IA) is too merchant/operations-centric and under-represents the CUSTOMER side - which is the platform's revenue engine (subscriptions/members). Locked owner decisions via 3-question steer: (1) SCOPE = full commercial suite (Members & Subscriptions + Revenue/Billing ops + aggregate Redemptions/engagement + Conversion + Customer 360 enrichment); (2) PLACEMENT = new TOP-LEVEL "Members & Revenue" nav group (first-class, alongside Operations); (3) SEQUENCING = PAUSE Wave 1, reconcile blueprint + master context + wave packs first, then resume. Assistant-enforced guardrail (NOT owner-waivable casually - UK-GDPR): split (a) OPERATIONAL-COMMERCIAL member/subscription/revenue data = surfaced richly (NOT the gated stuff) vs (b) BEHAVIOURAL/DEMOGRAPHIC profiling (where-from/interests/cohorts) = stays DPIA-gated + default-off + minimum-cohort. Reconciliation workstream tracked in §11. Grounded audit dispatched (Explore agent) as step 1 so the amendment stays source-faithful. Docs to amend applied against CURRENT main (not the stale Neon working tree).
- **D-CD5 (prototype governance: assistant drives, Codex out) [owner-directed 2026-07-02]:** Owner will not go back-and-forth with Codex for the prototype; delegated design-review + driving to the assistant, grounded in the locked decisions + docs. Owner remains sole checkpoint approver. Assistant disposition on the 4 CP-0 shell notes: notes 1-4 are blueprint-faithfulness fixes the assistant will direct (not owner decisions); only the Growth placement-tools-before-billing nuance is flagged to owner (default = blueprint's group-gated-on-billing). See §0 Governance for the full standing model. Hard boundaries + stop-and-flag list unchanged.
- **D-CD4 (why not upload the whole upload-bundle folder):** owner asked why not upload everything in `docs/design/admin-panel/upload-bundle/`. Answer: the bundle has no secrets, but the whole folder should NOT be loaded at CP-0. (a) The wave-1/2/3 packs are staged deliberately; loading them now would push Wave-1+ screen instructions into context and likely trigger premature screen generation, breaking the CP-0 review gate. (b) Five files (README, manifest, traceability [self-labelled "not uploaded"], runbook, linkage-strategy) are operator/review aids, not design material -> noise/confusion if uploaded. Correct split: PASTE the master context (§A -> ack -> §B); UPLOAD merchant-reference-summary + brand (attached Design System) + 1-2 screenshots; STAGE the wave packs for later; NEVER upload the 5 review aids. Optional enhancement offered: also attach the full `00-admin-master-context.md` as a reference file for the deeper §C-§G (safe, within CP-0 bounds) - owner's call.

## 5. Assets: to upload / held / omitted (Stage 0)

- **Attach as context:** `merchant-reference-summary.md`; 1-2 merchant PNGs (`merchant-web-branches/01-branches-overview.png`, `merchant-web-insights-reports/02-overview-live-established.png`).
- **Covered by attached Design System (no separate upload):** fonts, `tokens.css` (brand tokens).
- **HELD (per D-CD3):** the Admin blueprint (contains §17 transient snapshot).
- **OMITTED / never upload:** `.dc.html`, handoff zip, repo/Codebase, Figma, `.env`, secrets, real data, transient incident info, the Wave 1/2/3 packs, the review-aid docs (README/manifest/traceability/runbook are followed, not uploaded).

## 6. Discoveries beyond the documented anchors

- The account already contains a shared **"Design System (Mustica Pro, Lato)"** with the exact brand fonts ingested. The manifest anchors treated fonts as owner-local uploads that "might not ingest"; in reality the exact brand design system is already available in-account, which fully de-risks exact typography (item 3).
- The blueprint-as-CP-0-reference-upload (manifest §2A) conflicts with owner instruction #7 (no transient incident info) because blueprint §17 carries the dated deployment snapshot -> resolved by D-CD3 (hold / redact).
- **Type fidelity empirically confirmed from inside Claude Design (not just inferred).** On Section B, Claude Design reported the bound design system contains `MusticaPro-SemiBold.otf` + all four Lato weights with `@font-face` declared in `tokens.css`, plus the full token set (palette, warm neutrals, voucher-type accents, functional signals, type scale, radii, 4/8 spacing, navy-tinted elevation, `--shadow-glow` reserved for the CTA). This upgrades D-CD1 from "should be fine" to "verified by the tool"; §B's font-STOP clause definitively not triggered.
- **On-disk bundle is version-split on the parallel Neon branch.** The shared checkout HEAD is on `docs/neon-cu-burn-scheduler` (parallel session), which predates the reconciliation amendment. `git diff <main> -- 00-admin-master-context.md` shows the on-disk §E "Live operational status" bullet is the OLD wording (`healthy/degraded/failed/pending/approved`), NOT the amended Control-Room/five-layer/dynamic-states bullet. `merchant-reference-summary.md` is IDENTICAL to main (unchanged by the amendment). Mitigation: paste Section A/B from the CURRENT main-ref text (extracted to scratch `master-context-current.md`), NOT from the on-disk file; do not attach the on-disk master-context file.

## 7. Chronological execution log

1. Read governing sources; ran §2A existence + safety checks; confirmed fonts present (no STOP).
2. Reported CP-0 pre-flight + honest browser/DesignSync constraint; gave step-1 instruction (create project).
3. Owner created `Redeemo Admin Panel` (Prototype, Opus 4.8, Design System attached); confirmed via screenshot.
4. Flagged D-CD3 (blueprint-hold safety) + gave step-2 instruction (load merchant-summary + 1-2 PNGs; do-not list).
5. Created this execution-log tracker (owner request); committed to updating it after every step.
6. Owner asked why not upload the whole upload-bundle folder -> explained staged waves + review-aids reasoning (D-CD4); offered optional full master-context reference upload.
7. Owner asked for the assistant's view on uploading the blueprint -> recommended HOLD with reasons (D-CD3 resolution).
8. Owner attached `merchant-reference-summary.md`; asked what else + what next. Ran version check: merchant-summary CURRENT; on-disk master-context STALE (Neon branch) -> extracted current master-context to scratch. Answered attachments (offered `01-branches-overview.png` shell anchor; hold blueprint/stale-file/wave-packs/review-aids). Handed owner the exact CURRENT Section A text to paste; explained the A->ack->B->build-ask order; noted §B font-STOP is pre-satisfied by the attached Design System.
9. Owner pasted Section A; Claude Design acknowledged correctly (reuse/divergence split, principles, privacy, guardrails, grounded in references + design system, held for direction). Pulled current §D (nav) + §E (status semantics) from scratch for the build ask. Handed owner Section B (current text) + a "hold for the build scope" control line so it doesn't build on incomplete nav. Awaiting Section B acknowledgement.
10. Owner pasted Section B; Claude Design acknowledged + self-verified fonts from inside the bound design system (see §6 discovery). Handed owner the CP-0 build ask: full §D 8-group nav inlined (gated/future marked), two-pill + operational-status pill set, dense operator component set, full state blocks, top bar with synthetic operator "Shebin C. (Operations)", Ops-Home PLACEHOLDER only (dashboard deferred to Wave 1), synthetic-data-only + PIN-hidden constraints, explicit STOP-and-present-for-review. Awaiting Claude Design output + screenshots.
11. Owner pasted CP-0 build ask; Claude Design produced `Redeemo Admin - Foundation.dc.html` (both deliverables, toggle strip) and paused for review. Owner shared the Operator shell screenshot. Reviewed the shell half: STRONG - honest-status Ops-Home placeholder verbatim (§E maturity pattern: not-built-yet / what-it-will-do / what-unlocks-it / who-decides), dense grouped sidebar + top bar (search/bell-3/SC-Operations/logout), FUTURE+GATED marks, "Platform status: healthy", "synthetic data" footer, capability-gating wired. Requested the Design-system tab screenshots + sidebar scroll for groups 7/8 before assembling the full CP-0 report. Held at CP-0.
12. GOVERNANCE CHANGE (D-CD5): owner delegated prototype design-review + driving to the assistant (Codex out of the prototype loop); owner remains sole checkpoint approver. Assistant set out the operating model + forward path (close CP-0 -> Wave 1 -> Wave 2 -> Wave 3, per-wave sign-off default), and disposition on the 4 CP-0 notes (assistant-directed fixes; only Growth-placement-before-billing flagged to owner). Re-stated the one immediate action: owner to capture the Design-system tab + sidebar-scroll shots so CP-0 can close.
13. Owner shared 5 Design-system screenshots (sections 01-13). Assistant reviewed the full design-system half: PASS (see §9 for the section-by-section record). Delivered the CP-0 report/verdict = PASS + recommend APPROVE-and-lock; disposition on the 4 notes (N1/N2 accept, N3/N4 carry to Wave 2/3); 2 micro-watch items; 1 owner brand-nuance decision (cool-neutral canvas vs cream); 1 trivial deferred capture (sidebar 7/8). Awaiting owner go to lock CP-0 + open Wave 1.
14. Owner signed off CP-0 (APPROVED + LOCKED), said move on. Assistant extracted + read the current Wave 1 pack (12 screens; "one screen at a time in order"). Set the Wave 1 plan (screen-by-screen, assistant reviews each, owner signs off once at CP-1; big screens 1.4/1.6 to be split). Handed owner the first build ask = Wave 1 intro + Screen 1.1 (operator sign-in / two-step email->OTP + signed-out + nav states; design system/shell NOT rebuilt; production session/auth honestly OPEN). Awaiting the 1.1 output/screenshots. Added §10 Wave 1 screen tracker.
15. Owner asked whether to upload documents (e.g. the Wave 1 pack) to Claude Design. Answer: NO - the wave pack is a PROMPT source pasted screen-by-screen (the pack itself says do not paste the whole list at once / do not re-paste the master context); uploading it dumps all 12 screens -> premature multi-screen build, breaks the one-at-a-time review (same as D-CD4). No new upload needed for Wave 1: design system + shell + nav + master context are already in-session from CP-0, merchant-reference-summary attached, and each pasted build ask carries the per-screen spec with locked decisions baked in. Only possible later exception: a SINGLE merchant-portal screenshot as a visual anchor for 1.5 Merchant Directory / 1.6 Merchant 360 (targeted, flagged at that screen; not a bulk upload). Awaiting the 1.1 output.
16. Owner shared Screen 1.1 output (6 screenshots: email/code/submitting/invalid-code/signed-out + nav-states board). Assistant reviewed -> PASS (details in §10 1.1). Dispositioned the 2 CD-raised flags (both correct/honest, not defects) + 1 micro-watch (submitting spinner). Handed owner the Screen 1.2 (Ops Home) build ask: real role-aware landing replacing the CP-0 placeholder; work-summary/launch-readiness/alert-tiles/quick-actions; analytics KPIs labelled "gated on the analytics surface" (no fabricated numbers); Ops default + Sales + Support variants; states loading/gated-analytics/partial-data. Awaiting 1.2 output.
17. Owner shared Screen 1.2 output (5 screenshots: Ops/Sales/Finance/Support/Content homes + gated analytics). Assistant reviewed -> PASS as a MERCHANT-OPS DRAFT (details §10 1.2; closes groups-7/8 confirm). Owner then raised a STRATEGIC gap: the whole Admin Panel + blueprint is too merchant/ops-centric; the CUSTOMER/member/subscription/revenue domain (the platform's revenue engine) is under-represented; asked to update the docs if needed. Assistant affirmed + reframed via the (a) operational-commercial / (b) DPIA-gated split, proposed a source-first reconciliation, and asked 3 forks. Owner locked all 3 recommended: full commercial suite + new top-level "Members & Revenue" area + pause-Wave-1-and-reconcile-first (= D-CD6). Assistant dispatched the grounded customer-domain audit (Explore agent, background) as step 1; Wave 1 PAUSED after 1.2; reconciliation tracked in §11.
18. Audit returned (§11 step 1 findings recorded). Assistant drafted the "Members and Revenue" amendment SHAPE grounded in the audit (5 modules + reorg out of Growth + (a)/(b) split + honest EXISTS/derived-net-new/FUTURE labels + Ops-Home snapshot + the deletion-retains-demographic legal flag) and presented it to the owner for the one confirm gate. Awaiting owner confirm/adjust before amending the docs + cascading + resuming.
19. Owner CONFIRMED ("go with your recommendation and move forward"). Assistant set up a clean worktree/branch (`docs/admin-members-revenue` off origin/main ca453377) and dispatched a general-purpose subagent to author the amendment across the blueprint + master context + 3 wave packs (commit-not-push). Awaiting the subagent; will review the full diff before push + docs PR, then resume the prototype (revise Ops Home).
20. Subagent finished; assistant reviewed the full 5-file diff (faithful; em-dash/emoji clean), applied ONE fix (synthetic MRR/ARR math), amended to commit `c21e43fd`, pushed, opened docs **PR #351** (does not block the prototype). RESUMED: handed owner the shell nav-update paste (9th group "Members and Revenue" + Growth reorg; nav-only). Next = Ops Home (1.2) revision, then Wave 1 (1.3+).
21. Owner applied the nav update; assistant reviewed -> PASS (9 groups correct order, reorg correct, Customer 360 stays, collapsed rail + nav-states board OK, no screen built; GATED-on-revenue-items = capability-tiering, accepted; screen-level (a)/(b) treatment carried to Wave 2/3). Handed owner the Ops Home (1.2) revision build ask (operational member/revenue snapshot + Finance revenue-focused variant; behavioural/demographic stays gated). Awaiting the revised Ops Home output.
22. Owner shared the revised Ops Home; assistant reviewed -> PASS (details §10 1.2). Operational "Subscription snapshot" surfaced with "Operational" pill + honest derived-read-side MRR + no-revenue-table note; gated KPIs untouched; Finance revenue-focused variant accepted by CD confirmation (Finance visual deferred to CP-1). D-CD6 Step 5b DONE. Handed owner the Screen 1.3 (Approval Queue) build ask (unified 4-type read/triage list; age-tinted waiting-age no-SLA; filter chips; 45s freshness; >24h stale flag; full state set; actions deferred to 1.4). Awaiting 1.3 output.
23. Owner shared Approval Queue 1.3 (3 CD screenshots: populated table + narrow cards + error state; the 4th image was the owner's own Claude Code window, ignored). Assistant reviewed -> PASS (details §10 1.3). One non-blocking quick-check logged (narrow cards appearing below the table on desktop Populated - confirm replace-not-stack). Split 1.4 Review/Actioner into Part A + Part B; handed owner the 1.4a build ask (shared split-pane shell + honest claim-to-act single-actor model + sticky bar + onboarding surface + full state set incl. gate-fail banner). Awaiting 1.4a output.
24. Owner shared 1.4a (8 CD screenshots: onboarding detail + all sticky-bar states + request-changes dialog + orphaned + denied + Part-B placeholder). Assistant: 1.4a MECHANICS = PASS (keep). Owner gave RICH content feedback (14 items, §12): onboarding review too thin - must surface the full real onboarding record (voucher detail, all branch fields, subcats/tags/amenities/hours, logo/banner, agreement+signatory, acquisition source) + new features (Google Places location, business verification, voucher-assistance flag, voucher-value indicator, edit direct-vs-propose) + accuracy (checklist vs real gates) + clarity (status-pill row). Assistant reflected all 14 back, dispatched a grounding audit of the merchant onboarding data model (agent a5099...; EXISTS-vs-PLANNED + recover the business-verification discussion), and HELD Part B until the onboarding review is right. On audit return: revise 1.4a + surface the 3 blueprint-touching decisions.
25. Audit returned (§12 ground truth recorded). Assistant delivered the reconciliation (REAL-to-surface / honest-gaps / go-live-gate factual correction / Google-Places nuance) + asked 3 decisions. Owner locked all 3 recommended + a KEY clarification: NO manual lat/lng entry - AUTOMATE location as much as possible (human-error risk). Assistant handed the revised onboarding-review build ask (8 blocks, automated map-confirm, business-verification concept, corrected 5-gate checklist, edit direct/propose+future). Awaiting revised-1.4a output.
26. Owner asked "have you updated the blueprint + other docs?" Assistant answered transparently (Members-and-Revenue DONE via PR #351; onboarding-review NOT yet), and did NOT wait: dispatched a subagent (aebc...) to author the onboarding-review blueprint + wave-1-pack amendment NOW onto the PR #351 branch (new commit). Flagged the CLAUDE.md rule-7 discrepancy as an OWNER DECISION (intended-policy vs correct-to-as-built) rather than silently editing. Noted master context / customer-flow docs need nothing. Parallel to the revised-screen paste. On amendment return: review diff -> push -> broaden PR #351 to both reconciliations.
27. Onboarding-review amendment returned (commit 19859b2f). Assistant reviewed the full diff -> faithful (§6.1.1-6.1.4 + §8.1 D36 + §6.3/§8.3/§12.1 + §13 rows + D33-D36 register; em-dash/emoji clean; nothing not-built shown as built), no fixes needed. Pushed onto PR #351; broadened the PR title + body to cover BOTH reconciliations. Source of truth caught up (pending the owner's CLAUDE.md rule-7 call). Awaiting the revised onboarding-review SCREEN output from Claude Design.
28. Owner shared the REVISED onboarding review (5 CD screenshots: header/profile/acquisition/verification/branches+location/documents/vouchers/checklist/agreement/edit blocks). Assistant reviewed -> PASS (strong); ALL 14 feedback items closed (details §10 1.4). Standouts: automated map-confirm location (no coordinate typing) exactly per D1; honest CONCEPT/FUTURE labels; full real record. Onboarding review LOCKED as the Part-B template. Handed owner the 1.4b build ask (voucher / merchant-edit / branch-lifecycle / media-photo, reusing the shell). Awaiting 1.4b. (CLAUDE.md rule-7 owner call still open, non-blocking.)
29. Owner shared 1.4b (4 CD screenshots: media / merchant-edit / branch-lifecycle / voucher surfaces). Assistant reviewed -> PASS (mechanics strong; honest labels). Owner then raised the CORRECTION-MODEL point (avoid back-and-forth loops; operators correct-in-place before approving; "who edits the proposed" = operator; material changes esp. voucher amount need merchant approval). Assistant reflected + reconciled (voucher concierge->merchant is real; cosmetic-direct + identity->merchant are net-new) and asked the material-line decision. Owner locked "MONEY + IDENTITY NAME". Assistant handed the "Correct on behalf" two-tier revision build ask. Awaiting output; then Screen 1.4 complete -> 1.5 Merchant Directory. Blueprint D37 queued.
30. Owner shared the Correct-on-behalf revision (3 CD screenshots: voucher correct-modal material-trigger + demoted sticky bar + merchant-edit correct-modal). Assistant reviewed -> PASS (two-tier exactly right; material marker + button-flip + net-new labels). Owner then asked the QUEUE-STATE + AUDIT clarification (what happens to a request after acting; awaiting-merchant visibility; grouping; audit history). Assistant answered the full lifecycle/two-court model + audit, and asked the queue-surfacing decision. Owner locked "Court pill + Needs-you/Awaiting-merchant TABS". Assistant handed the build ask (Approval Queue two-court tabs + court pill + merchant-wait-age; per-item audit-history timeline). Awaiting output -> then Screen 1.4/1.3 complete -> 1.5. D37 blueprint (correction model + two-court + audit) queued.
31. Owner shared the queue two-court + audit-history revision (2 CD screenshots: Approval Queue Awaiting-merchant tab w/ court pills + why-notes; Voucher review 6-event audit timeline w/ correction diff + MATERIAL tag). Assistant reviewed -> PASS (both exactly right). **SCREEN 1.4 + 1.3 refinement COMPLETE** (review/approval workflow end-to-end). Milestone: the operational heart of the panel is done. Micro-note logged (audit infra real; correction-flow event types net-new -> D37 caveat). Handed owner the 1.5 Merchant Directory build ask. Awaiting 1.5 output. Wave 1 progress: 1.1/1.2/1.3/1.4 done (the two hardest done), 1.5 in flight.
32. Owner shared 1.5 Merchant Directory (1 CD screenshot: table + filters + pagination + gated actions + states). Assistant: base mechanics PASS. Owner: too thin as content - "think as an operator/rep about the platform" (supply/demand/UK-geography/categories); what info + tools should be on it. Assistant reframed as the operator's window into SUPPLY (activity/demand-link + geography + category lenses + summary hierarchy) and asked the scope decision. Owner locked "Full operator surface". Assistant handed the full redesign build ask (summary strip + enriched table + map/group-by views + richer filters + export). Awaiting output. Directory-redesign folds into blueprint w/ D37.
33. Owner shared the redesigned Merchant Directory (7 CD screenshots: summary strip + enriched table + filter-rescope + Map view + Group-by-category + 2 empty states). Assistant reviewed -> PASS (strong; supply->demand activity link, UK supply map w/ privacy note, group-by, honest labels all landed). **SCREEN 1.5 COMPLETE.** Handed owner the 1.6a build ask (Merchant 360 workspace shell + Overview/Business-Identity/Branches/Vouchers tabs + edit-on-behalf correction model + suspend/reactivate). Awaiting 1.6a. Wave 1 progress: 1.1/1.2/1.3/1.4/1.5 done; 1.6 (last big screen) in flight.
34. Owner shared Merchant 360 Part A (5 CD screenshots: workspace + Overview/Business-Identity/Branches/Vouchers tabs). Assistant reviewed -> PASS (strong; edit-by-lane + honest labels). Owner flagged a NAV GAP (Merchant 360 nav item does nothing; is it same as Directory?) - assistant clarified Directory=list vs 360=one-merchant workspace + a real gap to fix (nav-entry picker). Owner then added 2 more (mid-work): COR1 authority decision (VAT/company#/category/tags = Operations-editable not Super-Admin-only; Super-Admin still does all; day-to-day = Operations) + COR2 (surface the customer-facing merchant + branch banners, were under-shown). Assistant folded all into a combined 1.6b build ask (COR1 + COR2 + nav-entry picker + 7 remaining tabs). Awaiting combined output -> completes Merchant 360. Blueprint D38 (capability reclassification) + banners + nav-entry queued w/ D37.
35. Owner directed a DOC-SYNC reconciliation: make sure the prototype changes reflect in the blueprint + all docs/plans; keep the execution log reconciled with the blueprint as we go (don't leave changes out). Assistant agreed, added the doc-sync discipline to §0 (amend promptly, don't batch), stated the honest doc state (PR #351 = Members-and-Revenue + onboarding-review done; D37-D40 pending), and FLUSHED the pending list: dispatched subagent a220 to author D37-D40 (correction model + two-court + audit; capability reclassification; directory supply-window; 360 workspace + banners + nav-entry) onto PR #351 (blueprint + wave-1 pack). Master context / aids untouched; CLAUDE.md rule-7 left for owner. Parallel to 1.6b. On return: review diff -> push -> broaden PR #351.
36. Owner gave Vouchers-tab feedback (Merchant 360): missing full voucher detail (terms, image/banner, availability, all fields); add the advisory value meter; and add a per-voucher CATEGORY VALUE BENCHMARK (where the voucher stands vs peer vouchers of same category from other merchants - a strong supply-quality lens, net-new). Assistant reflected + designed the enriched voucher block (full detail + advisory + net-new benchmark), RESTRUCTURED the remaining Merchant 360 work into Paste-1 (Part-A corrections COR1/COR2/COR3) + Paste-2 (nav-entry + 7 tabs) to avoid overload, and handed the Paste-1 build ask. Awaiting output. Voucher-enrichment + benchmark = blueprint D41 (fold on D37-D40 review/next, per doc-sync discipline).
37. D37-D40 amendment returned (commit cb727198); assistant reviewed (em-dash/emoji clean; D37-D40 register rows honest + faithful) -> no fixes -> PUSHED; broadened PR #351 to cover D27-D40 (all prototype reconciliations). Dispatched subagent aba55 to author D41 (voucher full detail + advisory + category-value benchmark) as a follow-up commit. Parallel to Paste-1. On return: review -> push. Blueprint reconciled through 1.6 once D41 lands (CLAUDE.md rule-7 still owner-pending).
38. D41 amendment returned (commit 327dc8fa); assistant reviewed (em-dash/emoji scan clean + belt-and-braces 0 em/en-dashes both files; register row faithful) -> no fixes -> PUSHED; PR #351 finalised to "D27-D41". **BLUEPRINT + WAVE-1 PACK FULLY RECONCILED with the prototype through screen 1.6** (D27-D41), honoring the owner's doc-sync direction. Only CLAUDE.md rule-7 remains (owner decision). Awaiting owner's Merchant-360 Paste-1 (Part-A corrections) to continue the prototype.
39. Owner asked what CLAUDE.md rule-7 is; assistant explained plainly (rule 7 = merchant-approval gates in CLAUDE.md's Key Business Rules; out of step w/ code which enforces branch+contract-signed+2RMVs+main-branch+location-confirmed; rule 7 misses contract/location + claims docs-uploaded/branch-user which aren't enforced) + recommended: update rule 7 to the 5 real gates + keep docs-upload/branch-user as intended-not-yet-enforced. AWAITING owner's rule-7 call to edit CLAUDE.md. Owner also gave forward feedback on Merchant-360 Part-B (while Paste-1 builds): (a) PERFORMANCE tab too thin -> enrich (redemptions over-time/by-voucher/by-branch; savings delivered; validation rate; rating trend; active-member reach; category performance benchmark; behavioural DPIA-gated; aggregate-only, net-new agg) = D42. (b) REDEMPTION LOOKUP/traceability (support scenario: customer says they redeemed at merchant X, track it down) -> individual redemption lookup by code/customer/merchant+date -> record (voucher/merchant/branch/redeemed-date-time/code/validated status+method+by+at/saving) + CUSTOMER reveal-on-demand + AUDITED (distinct from aggregate-no-identity); reachable from global search + Customer 360 + Merchant 360 Redemptions view; net-new surface, real data = D43. Assistant to fold D42+D43 into Part-B (Paste-2) + blueprint (per doc-sync). Broader trajectory noted: fraud/dispute/dormant-merchant re-engagement build on this spine.
40. Owner approved the rule-7 recommendation. Assistant edited CLAUDE.md (worktree off main, line 84) -> rule 7 now = the 5 enforced gates (branch + contract-signed + 2 RMVs + main-branch + confirmed-location) + docs-uploaded/branch-user-assigned kept as intended-not-yet-enforced (cross-ref D36/§8.1); em-dash-clean; commit `011926c9` pushed to PR #351; PR body rule-7 note corrected to "resolved". **ALL doc-sync items now CLOSED** (blueprint + wave-1 pack + CLAUDE.md fully reconciled with the prototype through screen 1.6; PR #351 = D27-D41 + rule-7). Only D42 (performance enrichment) + D43 (redemption lookup) remain, to be built in Part B (Paste-2) then captured.

## 8. Open items / next action

- **Owner (next):** paste Merchant 360 **Paste-1 (Part-A corrections: COR1 Operations-editable identity fields + COR2 merchant/branch banners + COR3 voucher-enrichment [full detail + advisory + category benchmark])**; screenshot corrected Business-identity + Branches + enriched Vouchers tab; send back. THEN assistant hands **Paste-2 (nav-entry picker + 7 Part-B tabs)** to complete Merchant 360. Open calls (non-blocking): CLAUDE.md rule-7; merge docs PR #351.
- **Assistant (parallel now):** reviewing the D37-D40 blueprint amendment (subagent a220) -> add D41 (voucher-enrichment + category benchmark) on review -> push to PR #351. Then review Paste-1 -> hand Paste-2 -> completes Merchant 360. THEN lighter governance screens 1.7 Create-draft -> 1.8 Global Audit -> 1.9 Admin Users -> 1.10 Operational Status -> 1.11 Notifications. Slot Members & Revenue per §15. Assemble CP-1 at wave end -> owner sign-off.
- **BLUEPRINT reconciled through screen 1.6 (owner-directed flush 2026-07-02):** D27-D40 all on PR #351 (broadened to "Admin Panel blueprint reconciliations from the prototype (D27-D40)"). Commits: c21e43fd Members-and-Revenue · 19859b2f onboarding-review (D33-D36) · **cb727198 D37-D40 (reviewed clean - em-dash/emoji scan empty, register rows honest REAL/NET-NEW/NOT-YET-IN-CODE - PUSHED)**. **D41 (voucher full detail + advisory + category benchmark) = commit `327dc8fa`, reviewed clean (em-dash/emoji scan empty; belt-and-braces 0 em/en-dashes in both files; register row honest full-detail-REAL / advisory-client-only / benchmark-NET-NEW) -> PUSHED.** PR #351 finalised to "D27-D41". **BLUEPRINT FULLY RECONCILED through screen 1.6** (all prototype decisions D27-D41 captured) EXCEPT CLAUDE.md rule-7 (owner-pending). Going forward: amend promptly per the §0 doc-sync discipline.
- **CP-1 verification carry-list:** Finance Ops-Home visual; 1.3 narrow-view replace-not-stack; 1.1 submitting-spinner; CLAUDE.md rule-7.
- **CP-1 verification carry-list:** Finance Ops-Home visual; 1.3 narrow-view replace-not-stack; 1.1 submitting-spinner; CLAUDE.md rule-7 go-live-gate correction.
- **Deferred (unchanged):** N3 Fraud/Reversal split at Wave 2; N4 Growth gating at Wave 3; 1.1 submitting-spinner micro-watch at CP-1.
- **Deferred (unchanged):** N3 Fraud/Reversal split at Wave 2; N4 Growth gating at Wave 3; 1.1 submitting-spinner micro-watch at CP-1; CP-1 owner sign-off at Wave 1 end.

## 9. Checkpoint summaries (manifest §4)

- **CP-0:** IN REVIEW (partial). Output = single `Redeemo Admin - Foundation.dc.html`, two deliverables behind a toggle.
  - **Operator shell (reviewed, screenshot 1):** faithful + dense. WINS: honest-status Ops-Home placeholder (Future/Wave 1, what-it-will-do / what-unlocks-it / who-decides) - did NOT fake a dashboard; grouped 8-nav with FUTURE (Tasks, Account Health) + GATED (DSAR, Read-only View-as, Subscriptions/Billing) marks; top bar (⌘K search, bell 3, "Shebin C. · Operations", logout); "Platform status: healthy" op-status indicator; "Admin v0 (CP-0) · synthetic data" footer; capability-gating wired (collapse + role-cycle).
  - **CP-0 review notes (verify, not blockers):** (1) count badges 24/7/12 look rose-tinted - confirm token + that it doesn't dilute "Rose = primary action/focus only"; (2) GATED/FUTURE tag lavender - confirm it's a functional-signal token not off-palette; (3) "Fraud and Reversals" tagged GATED whole - blueprint gates only the REVERSAL capability; fraud review must stay reachable (intake-has-outlet) - split it; (4) Growth group header GATED but children look active except Subscriptions/Billing - clarify whole-group-gate vs billing-only + make visual consistent.
  - **Design system (reviewed, screenshots 2-6, sections 01-13): PASS.** Exact palette + usage rules + type scale + 4/8 spacing + glow-reserved-for-CTA (01-04); inputs incl. Redemption PIN = "PIN hidden by policy" + error state + authority-outcome radios Direct/Notify/Route-to-approval (05); STATUS SYSTEM = two-pill lifecycle+approval INCLUDING "Approved, waiting to go live" + full 7-state operational-status pill set (Healthy/Degraded/Down/Unverified/External/Gated/Unavailable-not-built), as components NOT a screen (06); functional-signal set CAUTION/RESTRICTIVE/GATED/FUTURE/EXTERNAL + rose count badge + role chips (07); voucher-type accent chips + admin voucher card w/ review metadata + "PIN: hidden by policy" (08); dense sortable/selectable data table (09); split-pane review + ACT-ON-BEHALF card verbatim (acting-for/reason/audit/no-impersonation/no-legal/no-password/no-PIN) (10); tabs + filter bar (11); FULL STATE SET loading-skeleton/empty/error-distinct/permission-denied-with-capability+role/stale-conflict/success+partial-data (12); destructive dialog w/ typed reason + toasts + step-up re-auth labelled "gated concept" (13). Synthetic data + .example domains throughout. No feature screen built; nothing gated faked.
  - **CP-0 VERDICT: PASS -> LOCKED (owner signed off 2026-07-02).** Foundation frozen as the baseline. Canvas nuance resolved: cool-neutral canvas kept per assistant rec.
  - **Notes disposition:** N1 rose count badges ACCEPT (focus token, distinct from glow CTA); N2 GATED/FUTURE colour ACCEPT (functional-signal set in labelled pills); N3 Fraud-vs-Reversal CARRY to Wave 2; N4 Growth-group gating CARRY to Wave 3. Micro-watch: "Starter (RMV)" -> align to "mandatory (RMV)"; cream staying visibly in brand role. Owner-decision flagged: ops canvas is cool neutral #F8F9FA vs brand cream #FFF9F5 (assistant OK as density choice; owner may veto since brand is locked). Trivial outstanding: sidebar groups 7/8 visual confirm (deferred to Wave 1).
- **CP-1 (Wave 1):** APPROVED + LOCKED (owner sign-off 2026-07-03; all 11 screens 1.1-1.11 PASS; assistant verdict PASS). Was: driving screen-by-screen from the current Wave 1 pack (extracted to scratch `wave-1-pack-current.md`; on-disk == main, not stale). Per-screen progress in §10. Owner signs off once at CP-1 (wave complete).
- CP-2: not started.

## 10. Wave 1 screen tracker (~12 screens, one at a time, in order)

Status legend as §0. Assistant reviews each screen vs the Wave 1 pack; owner signs off once at CP-1.

- [x] **1.1 Sign-in / auth entry - PASS (assistant-reviewed).** Added as 3rd strip segment "Sign-in · 1.1"; CP-0 untouched. Two-step email->6-digit OTP (masked recipient sh•••@redeemo.example, 10-min expiry, resend, use-different-email), single glow per step. Full state set via jump-strip: email/code/submitting/invalid-code(distinct+actionable)/signed-out. "Concept · open decision" note = production session/auth honestly OPEN (guardrail working). Nav-states board: expanded/collapsed-66px/reduced-capability/skeleton-loading + caption (live shell DISABLES locked items; both patterns valid pending capability matrix). Synthetic only. Two CD-raised flags dispositioned = both correct behaviour not defects (reduced-cap nav = honest pattern-demo since per-role matrix undecided; happy-path accepts any 6 digits = fine for prototype). Micro-watch (non-blocking): confirm "submitting" shows a clear in-progress spinner at CP-1.
- [x] **1.2 Ops Home - PASS (assistant-reviewed; REVISED post-D-CD6).** Original merchant-ops draft (role-aware Ops/Sales/Support real; Finance/Content concept banner; analytics honestly GATED; merchant supply signal; groups-7/8 confirmed) THEN revised to add the operational member/revenue snapshot. **Revision (D-CD6 Step 5b):** "Subscription snapshot" card in the right rail beneath "Supply in the launch area", green "Operational" pill; Members 1,240 (Free 820 / Monthly 300 / Annual 120 tier bar); MRR GBP 2,800/mo labelled "derived, read-side"; Active-member rate 34% / New subs today 4 / Trialling 60 / Past-due 12 (amber); footer note "Operational commercial only... no revenue table today... Behavioural and demographic member analytics stay gated below". Gated member-analytics KPIs UNCHANGED (still gated). FINANCE variant = revenue-focused home (revenue work queue: refund-requests[gated]/past-due/failed-payments/disputes; snapshot prominent; supply dropped; concept banner gone) - accepted by CD's detailed confirmation + eval; Finance visual to eyeball at CP-1. (a)/(b) split holds. No M&R module screen built.
- **▶ WAVE 1 RESUMING** (D-CD6 reconciliation authored + PR #351, see §11). Order: shell nav-update (add group 5 "Members and Revenue" + Growth reorg) -> Ops Home (1.2) revision (member/revenue snapshot) -> then 1.3-1.11. New Members and Revenue screens slot into Wave 2 (core M25/M26) + Wave 3 (depth) per §15.
- [x] **1.3 Approval Queue - PASS + REVISED (two-court).** Base PASS: unified 4-type dense sortable table (Merchant/Type/Waiting/Verification/Status/Owner-claim); waiting-age tinted neutral<12h/amber/red, oldest-first, NO SLA; two-pill status + verification dot; claim+OVER-24H stale flag; filter chips w/ counts + skeleton; freshness 45s + manual + last-updated; full state set incl. DISTINCT error + permission-denied + narrow view; read/triage-only. **REVISED (owner queue-state decision): TWO-COURT MODEL** - "Needs you" (default) + "Awaiting merchant" tabs w/ counts; Court column + whose-court pill (Needs you / Claimed by other / Awaiting merchant); awaiting-merchant rows track MERCHANT wait-age + why-note ("with merchant · awaiting correction approval" / "· changes requested"); type-chips + freshness scoped to active tab; returns to Needs-you when merchant acts; narrow view carries court pill. QUICK-CHECK (non-blocking, CP-1): narrow-view replace-not-stack on desktop Populated.
- [~] **1.4 Review / Actioner** (BIG - Part A + Part B). **1.4a onboarding = PASS (revised, assistant-reviewed).** Mechanics kept (split-pane / claim-to-act honest-single-actor / states incl. ORPHANED / sticky bar / gate-fail / docs signed-link / PIN hidden). REVISED onboarding detail closes ALL 14 owner feedback items (§12): header split (1 lifecycle pill + completion row); merchant profile REAL (logo/cover + unvalidated-company#/VAT + full taxonomy + direct-edit + propose-identity); acquisition source (self-vs-admin-draft + future); business-verification CONCEPT-not-built (Google Place Details/Companies House/FHRS/dup-detection + real manual-status + docs); branches full-record + AUTOMATED location (map + auto-resolved pin + postcode->geocoded->confirmed tier stepper + Confirm/Nudge/Flag, NO coordinate typing + net-new label); vouchers full-detail + advisory value meter (weak/good/strong) + MERCHANT-ASKED-FOR-HELP flag + concierge proposal + "approval is its own lane"; go-live checklist = REAL 5 gates; agreement REAL evidence + honest gaps; edit direct/propose + FUTURE concepts; thin-area flags. Honest REAL/CONCEPT/FUTURE labels throughout. **Part B = PASS** (voucher review w/ customer preview + concierge diff; merchant-edit current->proposed diff + photo apply; branch-lifecycle create-w/-automated-map-confirm + close-w/-reason; media grid w/ scanner-stub-honesty; all reuse shell + claim states). **Correction-model = PASS** (two-tier: cosmetic Apply-and-approve / material Send-to-merchant w/ NEEDS-MERCHANT-APPROVAL marker + net-new label; Correct-on-behalf first-class, Request-changes demoted). **Per-item audit history = PASS** (6-event timeline: Submitted->Claimed->Corrected-on-behalf[before/after + MATERIAL tag]->Sent-to-merchant->Merchant-accepted->Approved; actor/reason/timestamp; PIN-redaction note; Global-Audit rollup note; micro-note: audit infra REAL but the send-to-merchant/merchant-accepted event TYPES are net-new correction-flow -> caveat in D37). **SCREEN 1.4 COMPLETE** (review/approval workflow end-to-end: queue -> claim -> review -> correct/act -> merchant round-trip -> audit).
- [x] **1.5 Merchant Directory - PASS (redesigned to full operator supply-window).** Base plumbing PASS; owner reframed too-thin -> think as operator/rep (supply/demand/UK-geography/taxonomy). **REDESIGN = PASS:** (1) supply-health summary strip (16 merchants 8live/2pending/2suspended; 63% verified[DERIVED]; 6 active/2 dormant[NET-NEW]; top-regions+top-categories distribution[DERIVED]; rescopes w/ filters); (2) enriched table (logo+business+trading / category+subcat / town+region / branches / lifecycle+verification pills / ACTIVITY[vouchers·redeemed·★rating(count)·DORMANT flag = the supply->demand link] / commercial[Featured/Campaign + contract + "renewal net-new"] / actions View+map-pin+gated-Suspend); (3) 3 VIEWS table/MAP[UK supply pins colour-coded + "customer homes never mapped" privacy note, NET-NEW surface]/group-by[region|category w/ per-group+live counts]; (4) richer filters rescope everything; (5) Export-CSV real + Message-a-segment FUTURE. Honest DERIVED/NET-NEW/FUTURE labels. Full state set intact. Blueprint: fold directory-as-supply-window into next amendment (w/ D37).
- [~] **1.6 Merchant 360** (BIG - Part A + Part B). **Part A (1.6a) = PASS.** Workspace header (logo/name/pills/town-region/key-metrics[branches·vouchers·redemptions·rating w/ "no rating yet" partial] + capability-gated Suspend/Reactivate immediate-takedown) + tab bar (4 Part-A tabs + 7 Part-B tabs marked). Overview (identity summary + status + OUTSTANDING WORK[pending voucher + awaiting-merchant correction + thin-area] + acquisition source + recent-activity->Global-Audit). Business Identity (full profile + per-field EDIT-BY-LANE: Direct[website]/Super-Admin-only[VAT·company#·category]/Correct-on-behalf-two-tier[name·description]/NOT-YET-SUPPORTED[logo·banner]; company#/VAT free-text-unvalidated; accountable-signatory FUTURE). Branches (per-branch detail + automated map-confirm + PIN-hidden + address/hours/amenities NOT-YET-SUPPORTED + create/close->branch-lifecycle-review). Vouchers (RMV+RCV full detail + approval history + per-voucher redemptions + Correct-on-behalf routing). States populated/loading/permission-denied(merchant:read) + partial-data. **NAV GAP (owner-flagged): the "Merchant 360" nav item does NOTHING when clicked** (only opens by drilling from Directory) - Directory=list, 360=one-merchant workspace (different surfaces). **Part B (1.6b) + nav-fix build ask handed, THEN owner added 2 more (combined ask supersedes):** (COR1 - OWNER AUTHORITY DECISION) reclassify VAT/company#/category/tags from SUPER-ADMIN-ONLY -> OPERATIONS-editable (Super Admin still does everything; day-to-day merchant-mgmt is Operations not Super-Admin). Backend currently gates to Super-Admin (short-circuit on merchant:edit-identity/edit-category caps) so this is a re-classification to implement; full per-role matrix = later decision; business/trading NAME stays material->merchant. (COR2) MERCHANT cover banner + each BRANCH's banner+logo are customer-facing (shown in customer app) - surface the actual images on Business-identity (merchant) + Branches (per-branch); editing stays not-yet-supported; both REAL fields (were under-shown). PLUS the nav-entry picker + 7 Part-B tabs (Performance[aggregate]/Documents[signed-link, admin-upload-only]/Staff&Roles[read, admin-mgmt-not-a-surface]/Comms[timeline, email-dark]/Commercial[billing-gated concept]/Audit[per-merchant slice of Global Audit]/Insights[DPIA-gated reference]). Owner then added COR3 (Vouchers tab): missing full detail (TERMS, voucher IMAGE/banner, AVAILABILITY window, cooldown, all fields) + add the ADVISORY value meter here + add a per-voucher CATEGORY VALUE BENCHMARK (net-new: where this voucher's saving stands vs peer vouchers of same category from OTHER merchants - position marker + category median + quartile; needs cross-merchant aggregation; real data = saving+category). Apply enriched voucher block wherever vouchers show. **RESTRUCTURED to avoid overloading one paste:** Paste 1 = Part-A corrections (COR1 capability + COR2 banners + COR3 voucher-enrichment); Paste 2 = nav-entry + 7 Part-B tabs. **LANDED + ASSISTANT-VERIFIED (both pastes + all 3 corrections).** Paste-1 (COR1 Operations-editable VAT/company#/category/tags + COR2 merchant/branch customer-facing banners + COR3 enriched vouchers = full detail + advisory value meter + category-value benchmark, applied across 360/onboarding/voucher-review) AND Paste-2 (nav-entry landing when no merchant selected + all 7 Part-B tabs: Performance[basic aggregate]/Documents[signed-link, admin-upload-only]/Staff[READ-ONLY]/Comms[timeline, email-dark]/Commercial[billing-gated]/Audit[per-merchant slice of Global Audit]/Insights[DPIA-gated]) both built + verified via DOM + screenshots (all 11 tabs, entry landing, populated/loading/permission-denied states). Blueprint reconciliation DONE via PR #351: D37 (correction model + two-court queue + per-item audit) + D38 (capability reclassification) + D39 (directory supply-window) + D40 (360=workspace) in commit `cb727198`; D41 (voucher benchmark) in `327dc8fa`. **STILL OPEN WITHIN 360 (build asks in flight, 360 stays [~] until they land):** D42 Performance enrichment (redemptions by-voucher/by-branch, savings delivered, validation rate, category performance benchmark; aggregate-only) + D43 Redemptions view & individual-redemption lookup (by code/customer/merchant+date; reveal-on-demand PII, audited) = NEXT paste; D44 Staff-and-access on-behalf management (replaces the read-only Staff tab) = CURRENT paste.
- [ ] **1.7 Create merchant draft + secure handoff** (6-field form; claim email to owner; NO password set here; nothing live until approved).
- [ ] **1.8 Global Audit / Activity Explorer** (query actor/entity/event/time; actor+reason+before/after; redact secrets/payload/PIN; sequence EARLY - governance backstop).
- [ ] **1.9 Admin Users and Roles** (list/invite-tokenised/deactivate/assign-role; capability set per role incl. empty FINANCE/CONTENT/SUPPORT + bootstrap-first-SUPER_ADMIN; per-person grants + step-up = labelled FUTURE, not faked; last-owner guard). Confirm sidebar groups 7/8 here.
- [ ] **1.10 Operational Status** (bounded control room; allow-listed in-app signals only; five-layer per signal; dynamic healthy/degraded/down/unverified/external/gated/unavailable; `/health`=liveness-only; DB/Redis/worker-heartbeat/deployed-SHA = net-new-or-external; APM/logs/secrets EXTERNAL; NO transient one-day snapshot; incident model=FUTURE).
- [ ] **1.11 Notifications / bell** (header feed + notifications page; mark-read/all; deep-link to queue item; per-admin mute/prefs=FUTURE; empty "all caught up" distinct from error).

**Wave 1 acceptance (pack §):** 5 review surfaces distinct but share one shell+bar; claim/approve honest single-actor; docs via signed-link/unavailable badge; PIN never shown; redemption rows never show individual customer identity; Ops Home no fabricated KPIs; Global Audit + Admin Users governance-early w/ grants/step-up FUTURE; Operational Status bounded + providers never operationally-live + no secret rendered + no transient snapshot.

## 11. Customer-domain reconciliation (D-CD6) - IN PROGRESS

Owner-locked: full commercial suite + new top-level "Members & Revenue" nav group + pause-Wave-1-and-reconcile-first. Enforced guardrail: (a) operational-commercial member/subscription/revenue = surfaced; (b) behavioural/demographic profiling = DPIA-gated/default-off/min-cohort.

Steps:
- [x] **Step 1 - Grounded audit DONE** (Explore agent). Ground truth (as of backend 2026-06-25): (1) NO "Free" plan row - free = ABSENCE of a Subscription row (query users w/o active sub). (2) SubscriptionStatus enum EXISTS = TRIALLING/ACTIVE/CANCELLED/EXPIRED/PAST_DUE. (3) `source` enum (STRIPE/APPLE/GOOGLE/ADMIN) = PLANNED, NOT built (Stripe-only today); admin-grant commented-not-built. (4) cycleAnchorDate, cancelledAt, cancelAtPeriodEnd EXIST. (5) MRR/ARR = COMPUTABLE from SubscriptionPlan.priceGbp × active subs × billingInterval, but NO revenue table/aggregation exists (derived-net-new). (6) Refunds/chargebacks/dunning-beyond-PAST_DUE = NOT built (no RefundLog/DisputeLog). (7) Promo trials REAL (PromoCode.stripeCouponId -> Stripe coupon -> TRIALLING; usesCount/maxUses/discountType/expiresAt/isActive exist). (8) Redemption: estimatedSaving Decimal, isValidated/validatedAt/validationMethod, isTestData, windowStartsAt; savings aggregated ON-THE-FLY (no snapshot table); per-merchant/period aggregation NOT built. (9) Redemption reversal NOT built (only merchant-suspend cycle-state reset). (10) Consent fields EXIST (newsletterConsent, tcConsentVersion/At, marketingConsentAt, email/phoneVerified). (11) Deletion = anonymise-in-place; RETAINS by design: id/timestamps/status + historical redemptions/reviews/audit + **DOB/gender/interests/postcode NOT anonymised** (flagged data-min/DSAR concern). No DSAR table; deletion is the only lifecycle action. (12) ZERO admin customer/subscription endpoints; FINANCE/CONTENT hold ZERO capabilities -> Members & Revenue is ENTIRELY net-new (new caps customer:read/subscription:read/revenue:view). (13) Redemption rows have no customer-name field; identity only via User join (locked: aggregate + reveal-on-demand). Report at `tasks/a2cd1a745f6155860.output`.
- [~] **Step 2 - Blueprint amendment SHAPE drafted** (presented to owner, awaiting confirm). New top-level "Members and Revenue" group w/ 5 modules: Members & Subscriptions (real; read-net-new) · Revenue & Billing (MRR/ARR derived-net-new; refunds/chargebacks/dunning+/source-breakdown/admin-grant = FUTURE) · Promo Codes & Trials (real) · Redemptions & Engagement aggregate (real; agg net-new; never individual identity) · Conversion (derived-net-new). REORG: move Subscriptions/Billing/Refunds + Promo codes OUT of Growth into here; Growth keeps merchant-paid (Campaigns/Featured/Trending). Customer 360 stays in Relationships (cross-linked). (a) operational surfaced / (b) behavioural-demographic DPIA-gated in Insights. Ops Home gains operational member/revenue snapshot. NEW decision-register flag: deletion retains demographic data -> legal data-min/DSAR review (stop-and-flag). Wave slot: Ops-Home snapshot=Wave1 revise; core=Wave2; deeper+gated insights=Wave3.
- [x] **Step 3 - Owner CONFIRMED** the shape (2026-07-02): "go with your recommendation and move forward" - all proposed choices approved (5 modules, reorg out of Growth, Customer 360 stays in Relationships, (a)/(b) split, Ops-Home snapshot, honest maturity labels, deletion-retains-demographic legal flag, wave slotting).
- [x] **Step 4 - Amendment AUTHORED + CASCADED + REVIEWED + PR'd.** Clean worktree `.worktrees/admin-members-revenue`, branch `docs/admin-members-revenue` off origin/main `ca453377`. Subagent authored across 5 files (blueprint + master context §A/§C/§D/§E/§F + 3 wave packs = the cascade). Assistant reviewed the FULL diff (faithful: new group blueprint 4.1 group 5 of 9 + master D; M25-M29 catalogue w/ correct BC refs + source citations; §11 inventory row; 4 §13 gated rows; §15 wave sequencing; D27-D32 register incl. D32 deletion-retention STOP-AND-FLAG; 3.2 new caps; (a)/(b) cross-cutting bullet; Growth reorg propagated; Customer 360 stays Relationships). Em-dash/emoji scan CLEAN. ONE assistant fix: synthetic MRR/ARR math (GBP 4,400/53,000 -> 2,800/33,600 to match 300x6.99 + 120x69.99/12). Commit `c21e43fd` (amended), pushed, **PR #351** opened (docs-only; does NOT block the pasted-context prototype; owner merges when ready).
- [x] **Step 5a - Shell nav-update DONE + PASS (assistant-reviewed).** Shell sidebar now runs the 9-group nav (Members and Revenue placed between Support-and-Cases and Growth); reorg correct (Promo + Subscriptions/Billing moved into Members and Revenue as "Promo Codes and Trials" + "Revenue and Billing"; Growth trimmed to Campaigns/Featured/Trending); Customer 360 stays in Relationships; collapsed 66px rail works; nav-states board (Sign-in 1.1) auto-reflects (shared data); NO M&R screen built. Gating on Revenue-and-Billing/Redemptions/Conversion = capability-tiering (revenue views Finance/Super-Admin-scoped) - ACCEPTED as good capability-gating. CARRY to Wave 2/3 screen builds (not a nav change): Redemptions/Engagement + Conversion render as OPERATIONAL aggregates for cap-holders; only behavioural/demographic cuts route to gated Insights (already in wave packs).
- [~] **Step 5b - Ops Home (1.2) revision.** Build ask handed to owner: add operational member/revenue snapshot beside the merchant supply signal (Free 820 / Monthly 300 / Annual 120 = ~1,240; MRR ~GBP 2,800 labelled derived-read-side; active-member rate; new-subs-today; ~60 trialling; ~12 past-due); behavioural/demographic stays gated (existing gated KPIs); FINANCE persona = revenue-focused home (replaces the concept banner). Awaiting output. THEN Wave 1 continues at 1.3 + the M&R screens slot into Wave 2 (core M25/M26) + Wave 3 (depth) per §15.

Design candidates to ground once the audit returns (do NOT fabricate; confirm each EXISTS): members by tier (Free / Monthly £6.99 / Annual £69.99); subscription lifecycle + status enum; subscription source (STRIPE/APPLE/GOOGLE/ADMIN - confirm exists-vs-planned); MRR/ARR computability; refunds/dunning/PAST_DUE; promo-only trials; aggregate redemptions + active-member rate + redemption rate; registered->subscribed conversion; consent + terms version; deletion=anonymise-in-place retained fields; DSAR. Locked rules to preserve: redemption rows never expose individual customer identity; customer PII reveal-on-demand + audited; no customer-home map.

## 12. Onboarding-review completeness feedback (owner, 2026-07-02) - IN PROGRESS

1.4a mechanics PASS; the onboarding-review CONTENT is a thin sketch. Owner feedback = surface the FULL real onboarding record + several new verification/assistance features. Grounding audit dispatched (agent a5099...; merchant onboarding data model + which features EXIST vs PLANNED + recover the earlier business-verification-tools discussion). Then REVISE 1.4a; some items are blueprint-touching decisions to bring back.

**Feedback items (do NOT lose):**
- Show full record: (1) vouchers show TITLE ONLY -> need description/terms/value/type-specifics; (2) branches show almost nothing -> all branch fields the merchant entered; (3) subcategories/tags/amenities/opening-hours -> surface them; (4) merchant LOGO + BANNER missing; (5) signed AGREEMENT + WHO SIGNED (signatory identity); (6) ACQUISITION SOURCE (self-reg / admin-created / referral / invitation).
- New features (ground first): (7) GOOGLE PLACES location verify (map+pin) on Confirm-location like merchant portal - NB blueprint admin D2 currently = manual lat/lng NO map, so this is a decision/reversal; (8) BUSINESS-VERIFICATION tools (genuine-business API + Google + others discussed earlier - recover the discussion); (9) VOUCHER-ASSISTANCE flag (merchant toggled "need help creating"); (10) VOUCHER VALUE/QUALITY indicator (mirror merchant-side "does it stack up"); (11) EDIT from admin = direct-edit-goes-live AND propose-edit->merchant-approves, possibly per a merchant preference.
- Accuracy/clarity: (12) go-live checklist accuracy vs REAL approval gates + mandatory fields; (13) does voucher approval belong in onboarding review or its own lane; (14) header status pills ("Submitted/Pending/Email verified/Agreement signed") confusing - separate ONE lifecycle status from completion checkmarks.

**Likely blueprint-touching decisions to surface with the audit:** Google Places in admin (vs D2 manual-lat/lng); which business-verification tool (new capability + module); per-merchant edit-application preference (direct-live vs propose). May warrant a small blueprint amendment (onboarding-review field inventory + business-verification concept + these decisions).

**AUDIT RETURNED (agent a5099, 2026-07-02) - ground truth:** Report at `tasks/a509909e2cd095cde.output`. REAL to surface (just wasn't shown): full voucher detail (type/desc/terms/estimatedSaving/expiry/windows/RMV-RCV/approvalStatus); all branch fields (address/phone/email/website/about/hours/amenities/photos+moderation); taxonomy (primaryCategoryId subcategory-level + MerchantCategory + primaryDescriptorTagId cuisine + MerchantTag specialty; amenities on BRANCH); logo+banner (Merchant.logoUrl/bannerUrl); voucher-assistance = REAL `merchantFields.askHelp` + adminProposed concierge. HONEST GAPS: signatory name/title NOT persisted (only {version}); contractEndDate never set; contract text placeholder; acquisition source = only self-serve-vs-admin-draft from AuditLog (no source/lead/referral field); voucher value = client-side ADVISORY only (scoreVoucher, no enforced floor; RmvTemplate.minimumSaving is a default not a gate); automated business verification NOT built (verificationStatus = manual flag; company#/VAT free-text unvalidated; merchant CANNOT upload verification docs - admin-on-behalf only) BUT rich design spec exists (`2026-06-10-merchant-portal-admin-onboarding-design.md §5`: Google Place Details + Companies House + FHRS + duplicate-detection + findings snapshot + auto-approve-nothing); business-type/registered-office NOT captured (thinAreas hardcoded false). GO-LIVE GATES (REAL, correcting CLAUDE.md rule 7): submit = branch + contract_signed + 2 RMVs configured (status PENDING_APPROVAL|ACTIVE); go-live = +main-branch(isMainBranch/isActive) + confirmed main-branch location (MANUALLY_CONFIRMED). "docs uploaded / branch-user assigned / mandatory-fields" = NOT enforced. LOCATION: postcodes.io centroid = only coord at onboarding (POSTCODE_CENTROID); Google Places WIRED+live but only address-autofill + a suggestion into admin-review metadata (`__locationSuggestion`), NEVER the pin; ADDRESS_GEOCODED enum defined but never written; admin confirm = manual lat/lng NO map. EDIT: direct (websiteUrl/vat/company#/branch-contact/category/docs/suspend/confirm-location) + propose-lane (sensitive identity -> MerchantPendingEdit/BranchPendingEdit -> AdminApproval -> editApplier = ADMIN SELF-REVIEW, NOT merchant sign-off); per-merchant preference + merchant-approval-of-admin-edit = NOT-BUILT.

**OWNER DECISIONS LOCKED (2026-07-02):** (D1) LOCATION = AUTOMATED, NO manual lat/lng (owner: manual entry = human-error risk, automate as much as possible). Auto-resolve via Google-Places geocode (+ postcode baseline) -> operator ONE-CLICK confirm on a MAP (visual verify, nudge/flag if wrong, never type coords); use ADDRESS_GEOCODED tier; reverses blueprint admin D2 (manual-lat/lng-no-map); net-new parts (auto-promote suggestion to pin + write ADDRESS_GEOCODED + map-confirm UI) flagged honestly. (D2) BUSINESS VERIFICATION = design the findings CONCEPT per §5 spec (labelled not-built). (D3) ADMIN EDITS = real model (direct + propose-lane-admin-self-review) + FUTURE concepts (send-to-merchant-for-approval + per-merchant apply-direct-vs-propose preference).

**Revised onboarding-review build ask handed to owner** (8 blocks: separated header status/completion; merchant profile incl. logo/banner + unvalidated-company#/VAT + taxonomy; acquisition source; business-verification concept; branches full-fields + AUTOMATED map-confirm location; vouchers full-detail + askHelp + advisory value indicator; REAL 5-gate go-live checklist; agreement evidence + honest gaps; edit direct/propose + future concepts). Awaiting output.

**CORRECTION-MODEL feedback (owner, 2026-07-02, after Part B):** Owner: operators should be able to CORRECT submissions in place (author the fix) before approving - NOT just approve/reject/request-changes - to avoid a back-and-forth loop that puts merchants off (commercial + operational). "Who edits the proposed?" = the OPERATOR. Caveat: material changes (esp. voucher AMOUNT) need merchant approval. **OWNER DECISION LOCKED:** correction line = "MONEY + IDENTITY NAME". Two-tier: COSMETIC (wording/title/description/terms/image/availability/hours/website/contact/category) = operator Apply-and-approve (goes live, no round-trip); MATERIAL (voucher value/saving/discount-amount + business/trading name) = Send-to-merchant-for-approval. "Request changes" demoted to fallback (merchant-only-suppliable items). Real-vs-net-new: voucher concierge -> merchant review is REAL today; operator-direct COSMETIC apply + IDENTITY->merchant are NET-NEW (label as concepts). **"Correct on behalf" revision = PASS (assistant-reviewed).** Inline correction editor (operator authors the fix); material fields (voucher value/saving/amount + business/trading name) carry "NEEDS MERCHANT APPROVAL" marker + amber net-new note + button flips to "Send to merchant for approval"; cosmetic fields = "Apply and approve" (goes live). Correct-on-behalf first-class; "Request changes (needs merchant)" demoted to fallback. Honest real/net-new labels.

**QUEUE-STATE + AUDIT clarification (owner, 2026-07-02):** owner asked what happens to a request after operator acts (correct/request-changes) - does it stay in queue? how do we know we're awaiting the MERCHANT? grouped? audit history? Assistant answered the lifecycle: Approve/cosmetic-correct-and-approve -> leaves queue; Reject -> closed; Request-changes -> merchant's court; Correct-material -> Send-to-merchant -> merchant's court (net-new). Two courts: "Needs you" (unclaimed/claimed-by-you) vs "Awaiting merchant" (changes-requested / awaiting-correction-approval); awaiting-merchant age tracks MERCHANT wait; returns to Needs-you when merchant acts. Audit history = per-item timeline (actor+reason+before/after) rolling into Global Audit (1.8). **OWNER DECISION LOCKED:** queue surfacing = "Court pill + Needs-you / Awaiting-merchant TABS" (default Needs-you). **Build ask handed to owner:** (1) revise Approval Queue 1.3 = Needs-you/Awaiting-merchant tabs + court pill + merchant-wait-age + why-with-merchant note; (2) per-item AUDIT HISTORY timeline on the review detail. Awaiting output -> then Screen 1.4/1.3 complete. PENDING blueprint: D37 = correction model + material line + two-court queue state + per-item audit + "minimise back-and-forth" operating principle (all locked; bundle into next amendment).

**DOC-SYNC (owner asked 2026-07-02 "have you updated the blueprint etc."):** Members-and-Revenue = DONE (PR #351 commit c21e43fd: blueprint + master context + wave packs). Onboarding-review amendment = DONE (commit `19859b2f` stacked on the same branch; reviewed clean by assistant [em-dash/emoji scan clean, no fixes needed], pushed; **PR #351 broadened** to cover BOTH reconciliations). Blueprint adds §6.1.1 field inventory / §6.1.2 D33 automated map-confirm (reverses manual-lat/lng) / §6.1.3 D34 business-verification concept / §6.1.4 D35 edit model + FUTURE / §8.1 D36 real 5-gate correction + §6.3/§8.3/§12.1 corrections + §13 gated rows; wave-1 pack §1.4 updated. Master context needs nothing (not screen-specific). **STILL OPEN - OWNER DECISION:** CLAUDE.md rule 7 (approval gates) vs code - rule 7 says "mandatory-fields + docs-uploaded + 2 vouchers + main-branch + branch-user-assigned"; code enforces "branch + contract-signed + 2 RMVs + main-branch + location-confirmed". Is rule 7 INTENDED policy (code catches up) or correct-to-as-built? Blueprint D36 frames rule-7 items as intended-not-enforced; CLAUDE.md itself NOT touched pending owner call. **RESOLVED 2026-07-02 (owner approved assistant recommendation):** rule 7 rewritten to the 5 ENFORCED gates (>=1 branch + contract signed + 2 RMVs configured + main branch set + main-branch location confirmed) with the 3 intended-not-enforced items (mandatory profile fields, verification docs uploaded, branch-staff-user assigned) flagged aspirational + cross-ref blueprint 8.1 / D36. Committed `011926c9` on PR #351; verified on-disk (root + worktree CLAUDE.md line 84 both carry it). **Doc-sync through screen 1.6 now fully CLOSED** = PR #351's 5 commits (Members-and-Revenue `c21e43fd` + onboarding-review `19859b2f` + correction/queue/directory/360 `cb727198` + voucher-benchmark `327dc8fa` + rule-7 `011926c9`).

**STAFF ON-BEHALF + OPERATOR-PARITY feedback (owner, 2026-07-02, after Merchant 360):** Owner: the admin must be able to MANAGE merchant staff on-behalf (add / suspend / reactivate staff, reset password, activate) because some merchants will not use the Merchant Portal at all; and more broadly, WHATEVER a merchant can do on the portal, the operator must be able to do on their behalf. The Merchant 360 Staff tab is READ-ONLY today -> becomes a real management surface. **DECISION D44 (assistant design calls; owner is the CP-1 checkpoint):** (1) LANE = Operations-capable + fully audited (NOT Super-Admin-only; consistent with D38 reclassification + owner's "Super-Admin not in day-to-day"; Super Admin superset). (2) DIRECT, no merchant round-trip (the point is the merchant can't/won't act) - but every action carries actor + reason + before/after into the per-merchant Audit -> Global Audit. (3) TWO POPULATIONS mirroring the real staged-identity model: PORTAL members (MerchantAdmin + MerchantMembership: Owner / Branch-manager / Staff + branch scope + canManageVouchers) and APP/BRANCH logins (BranchUser scan/validate). (4) RESET reality (honest, do NOT paper over): a portal-member "reset" = re-issue a tokenised claim/reset link (operator NEVER sets or sees a password - a hard platform security lock, same as admin-user invites); app/branch-user reset = direct (the portal supports reset/deactivate for app users today). (5) Soft-remove (status Removed) + assertNotLastOwner guard. (6) Locks kept: no impersonation / no login-as-merchant, branch redemption PIN never shown, canManageVouchers is the only real per-member capability (finer matrix = placeholder). Real-vs-net-new: Merchant Portal Staff-and-Access is REAL (shipped v1); the ADMIN on-behalf equivalent is NET-NEW (zero admin staff-management endpoints today; tab was read-only) - labelled. PARITY PRINCIPLE (broader): operator-parity-on-behalf for non-portal-using merchants, subject to the D37 lanes (account-maintenance / cosmetic = direct + audited; money / identity-name = merchant-approval where a merchant can approve; commercial stays gated). **Build ask handed to owner** (Staff-and-access on-behalf surface: 2 populations; invite-via-claim-link; role / scope / canManageVouchers; re-issue-link reset; soft-remove + last-owner guard; app-user reset / deactivate; PIN + impersonation locks; full state set). Awaiting output. PENDING blueprint (reconcile PROMPTLY per doc-sync - flush D42 + D43 + D44 in ONE amendment once the two remaining Merchant-360 pastes land): D44 staff-on-behalf + operator-parity-on-behalf principle (fold into blueprint 1.6 / authority-matrix + a new register entry) + D42 Performance enrichment + D43 Redemptions lookup.

**BRANCH MANAGER + NOTIFICATION ROUTING + PIN ACCESS feedback (owner, 2026-07-02, after Merchant 360):** three linked points refining the branch model.

**D45 (branch manager + notification routing).** Every branch should always resolve to at least one manager/recipient. Owner refines the earlier "assign a branch user is mandatory" (already downgraded to intended-not-enforced in the rule-7 rewrite / D36): rather than a HARD go-live gate, DEFAULT the branch manager to the OWNER when the merchant assigns no dedicated user, and make it CLEAR to the merchant they should assign one (or explicitly choose owner-as-branch-manager). Rationale: branch managers receive automated branch emails (redemption notifications, retention, anything branch-associated); a multi-branch owner does NOT want to be bombarded by every branch's redemption emails, so the branch-assigned MANAGER is the default recipient and the OWNER OPTS IN per branch if they wish. Recipient identity = the branch's assigned Branch-manager (a portal member with Branch-manager role scoped to that branch, has an email) -> defaults to Owner if none. Real-vs-net-new: the recipient-routing + per-branch/owner notification PREFERENCES are NET-NEW / Phase-6 (Phase 6 comms unwired; Merchant Portal build ledger flags alert-recipients + notif-prefs as un-built schema gaps; redemption email to branches deferred) -> design as a labelled CONCEPT, not live. Assigning a Branch-manager scoped to a branch on the Staff-and-access tab (D44) IS the assignment mechanism; the Branches tab SHOWS the resolved manager + recipients + owner-opt-in. Go-live checklist refinement of D36: "each branch has a manager" = a SOFT advisory item (defaults to owner + nudge), NOT a hard blocker (consistent with the enforced-gate set).

**D46 (branch PIN admin on-behalf view + reset - REVERSES a hard lock).** Owner: the branch-specific redemption PIN is critical (no PIN -> no redemption); staff/managers/owners forget it; if they can't reach the Merchant Portal the ADMIN PANEL must be able to VIEW it and CHANGE it on the merchant's behalf. **This REVERSES the repeated "branch PIN never shown / hidden by policy" invariant** (design-system PIN input, blueprint, master context, Staff-tab paste, audit-redaction notes). Backend ground truth: Branch.redemptionPin is AES-256-GCM encrypted (decryptable server-side); the Merchant Portal already has GET/PUT/send PIN routes (SMS via Twilio live; email Resend Phase-6) + a dev decrypt script (`prisma/get-branch-pin.ts`) -> feasible; the ADMIN on-behalf view/reset is NET-NEW admin capability on real infra. **Assistant design call (security-conscious; owner is CP-1 checkpoint; flagged for CONSCIOUS acceptance, alternative offered):** NOT always-visible -> REVEAL-ON-DEMAND + AUDITED (mirrors the customer-PII reveal-on-demand pattern): PIN masked by default on the Branches tab; one-click "Reveal PIN" requires a reason + logs actor+reason+timestamp to Audit; "Reset / change PIN" requires a reason, sets/regenerates the PIN, sends it to the branch recipients via the existing send channel (SMS live). Audit records THAT a PIN was revealed/reset by whom + why but NEVER the PIN VALUE (so "PIN value never appears in logs/audit" stays TRUE; "PIN never shown ANYWHERE by default" becomes "revealed deliberately on the Branches tab, audited"). The Staff-tab lock ("PIN never shown here") is UNAFFECTED - PIN lives on the Branches tab, not Staff. If owner prefers always-visible or reset-only-no-view, one word and the paste adjusts. **Build ask (Branches-tab enhancement) = the paste AFTER Staff:** per-branch resolved manager + recipient list + owner-opt-in toggle (labelled net-new/Phase-6 concept) + default-to-owner nudge; PIN masked + Reveal(reason, audited) + Reset(reason, sends to branch); echo the manager/recipient + PIN-reveal on the onboarding-review branches block; go-live checklist "branch manager" softened to advisory. Handed to owner (queued after Staff). PENDING blueprint (same flush as D42/D43/D44): D45 branch-manager-default + notification-routing concept + D36 checklist softening; D46 PIN on-behalf view/reset reversing the PIN-never-shown invariant (update design-system PIN pattern + master context + audit-redaction wording + blueprint 1.6/Branches + register entries).

**MERCHANT CONTACT + BRANCH USERS + REDEMPTION-HISTORY clarification (owner, 2026-07-02).** Three more Merchant-360 refinements. **D47 (merchant contact on Overview).** Owner: the Overview top block should show the merchant's CONTACT DETAILS (email + phone) so operators can reach out. Ground truth (schema-checked `prisma/schema.prisma`): NO merchant-business-level email/phone (Merchant has only businessName / tradingName / websiteUrl); the merchant contact = the OWNER / primary MerchantAdmin `email` (real, unique, verify-gated) + `phone` (OPTIONAL, may be null) + firstName/lastName; branch phone/email exist per-branch (branch.email stays dark). Design: Overview identity/status area gains a "Contact" block = primary owner name + email + phone + copy/mailto/tel, labelled "Primary contact (account owner)"; phone -> "not set" when null; multi-owner -> primary + "+N more owners". Real fields; no fabrication. **D45 extension (branch users on the Branches tab).** Owner wants the branch card to show the assigned USER(S) - so it shows BOTH the resolved Branch-manager (portal member, D45) AND the branch's assigned app/scan logins (BranchUser) = full branch staffing. Folded into the D45 Branches paste. **Redemption-history clarification (point 3; owner unsure what we decided): CONFIRMED DECIDED = D42 + D43.** Merchant 360 gets (D42) an ENRICHED Performance tab = AGGREGATE only, never individual customer identity; PLUS (D43) a REDEMPTIONS view / individual-redemption LOOKUP = per-redemption rows (redemption code, voucher, branch, date/time, validated) with CUSTOMER identity masked by default + REVEAL-ON-DEMAND + audited (support scenario: "a customer says they redeemed at merchant X, track it down"); reachable in Merchant 360 + global search + Customer 360. THIRD queued paste (after Staff, after the combined Overview+Branches paste). Preserves the locked "redemption rows never show individual identity by default" + "PII reveal-on-demand + audited". **COMBINED "Merchant 360 additions" paste (SUPERSEDES the prior Branches-only paste) = Overview contact (D47) + Branches staffing[manager+app users] + recipients + PIN (D45/D46) + onboarding-review echo + go-live-checklist branch-user softened to advisory; HANDED (queued after Staff).** PENDING blueprint (same flush as D42-D46): D47 merchant-contact-on-Overview. Finishing-360 paste order now: (1) Staff&Access D44 [out] -> (2) combined Overview+Branches D45/D46/D47 [handed] -> (3) Performance+Redemptions D42/D43. Then flush D42-D47 to blueprint in one amendment.

**MERCHANT 360 D44 + D45/D46/D47 LANDED + ASSISTANT-VERIFIED (owner built both pastes; 9 screenshots + DOM-verified build logs). VERDICT: PASS.** (1) STAFF AND ACCESS (D44): renamed tab (ON-BEHALF/NET-NEW); acting-for banner + 3 locks (no-impersonation / PIN-never-shown-here / operators-never-set-or-see-passwords); PORTAL MEMBERS (5 = Owner / Branch-manager / 2 Staff / 1 Invited-pending, each w/ role + branch scope + can-manage-vouchers) with Change-role-scope / Toggle-vouchers / Re-issue-access-link / soft-Remove; APP-BRANCH LOGINS grouped by branch + Add-app-login(net-new); invite-via-tokenised-claim-link (no admin-set password) w/ empty + duplicate-admin-email validation; last-owner guard; permission-denied names merchant:manage-staff; 6 states verified. (2) OVERVIEW Primary-contact (D47): owner name + verified email + optional phone (Not-set variant) + Copy/Email/Call + multi-owner (+N) + honest 'no business-level contact' note; Acquisition-source retained; 3 variants verified. (3) BRANCHES (D45/D46 + D45-ext): Branch-staffing = resolved manager (assigned OR Owner-default+nudge) + assigned app/scan logins w/ status; notification-routing recipients + owner-opt-in toggle (OFF default) labelled Concept/Phase-6; PIN masked + Reveal(reason-required, audited, value never logged) + Reset(reason, regenerates + delivers via existing channel) labelled Net-new/on-behalf; reveal/reset flows + toggle + permission-denied(branch:manage-pin) verified; onboarding-review echoes staffing + PIN; go-live checklist softened (5 hard gates + a new Advisory 'each branch has a manager, defaults to owner' non-blocker). **CP-1 micro-watches (non-blocking copy/precision, NOT rebuilds):** (a) Staff-tab 'Redemption PIN never shown' chip -> tighten to '...never shown HERE' so it doesn't read as an absolute platform lock now Branches reveals on-demand; (b) PIN-reset wording 'delivers to the branch recipients via the existing channel' conflates the LIVE SMS-to-branch PIN-send channel w/ the PHASE-6 email notification-recipient routing -> tighten so email-to-manager isn't implied live (fix in blueprint wording too); (c) cosmetic synthetic overlap: owner phone == High Street branch phone -> vary.

**FINAL Merchant-360 paste HANDED = D42 Performance enrichment (by-branch + by-voucher + savings-delivered + validation-rate[QR/manual] + category-performance-benchmark; all AGGREGATE-only, never individual identity, net-new roll-ups) + D43 Redemptions tab (12th tab, after Performance = individual redemption rows [code / voucher / branch / date-time / validated+method / saving / CUSTOMER masked-by-default + Reveal-customer reason-required+audited] + filters[branch/voucher/date/validation] + code-search + single redemption detail w/ Customer-360 link) + global-search redemption-code entry (top-bar recognises an 8-char code -> redemption detail; concept if results surface not built).** Design calls flagged for CP-1: Redemptions as a 12th top-level tab (vs nesting under Performance) - owner may veto; privacy nuance locked = 'redemption rows never show individual identity BY DEFAULT; reveal-on-demand + audited' (aggregate Performance has NO reveal control; Redemptions tab does). Offered to split into 2 pastes (Performance / Redemptions) if the single generation comes back thin. After it lands: **Merchant 360 COMPLETE -> flush D42-D47 to blueprint in ONE amendment** (incl. D46 PIN-wording precision + aggregate-vs-individual privacy nuance + Redemptions-tab). Then 1.7 create-draft -> 1.11 bell, then Members & Revenue, then CP-1.

**MERCHANT 360 D42 + D43 LANDED + ASSISTANT-VERIFIED (owner built the final paste; 5 screenshots + DOM logs). VERDICT: PASS. Merchant 360 now functionally COMPLETE (12 tabs).** D42 Performance enriched: kept redemptions-over-time / active-member-reach / rating-trend / top-vouchers; ADDED redemptions-by-branch (HS 214 / Harbourside 98) + redemptions-by-voucher (full list) + savings-delivered (£3,240 net-new) + validation-rate (96%, QR 82 / manual 18 split) + category-performance-benchmark (position + median, net-new cross-merchant-agg honesty); footer aggregate-only / never-identity / net-new. D43 Redemptions tab (12th, after Performance): 7-row filterable history (code / voucher / branch / redeemed / status+method / saving / customer), customer masked-by-default + inline Reveal (reason-required dialog, audited, row-vs-reveal click fixed), filters (branch/status chips + voucher select + code-search live), single redemption detail (full record + reveal-on-demand + Open Customer 360), states populated / empty / loading / permission-denied(redemption:reveal-customer). Global-search entry: top-bar recognises a code -> 'Redemption <code>' result -> opens the detail (masked + reveal), verified end-to-end. Aggregate-vs-individual line held strict. (Minor synthetic nit for CP-1: global-search recogniser accepts 6-8 chars; real code is 8.)

**D48 RAISED - CUSTOMER-IDENTITY VISIBILITY MODEL (owner questioning mask-by-default, 2026-07-02).** Owner: is masking customer identity on redemptions necessary? 'We're the platform, we always have access; we're not sharing externally; maybe role-based for certain users only, not everyone.' **Assistant feedback (grounded, UK-GDPR):** owner's CORE INSTINCT is right + IS the compliant direction = ROLE-BASED access, don't hide from those who need it. ONE correction: 'controller so no privacy issue' is not quite right - being the controller gives LAWFUL access for legitimate purposes (support/fraud/ops) but UK-GDPR still requires data-MINIMISATION (only those who need it) + ACCESS-CONTROL (Art.32) + ACCOUNTABILITY (Art.5(2)) for INTERNAL access too (insider-risk, DSAR, ICO expectations); over-broad unlogged internal PII browsing IS a risk. **Recommended model (D48): (i) aggregate surfaces NEVER show identity (not role-dependent; pure data-min; keep); (ii) individual-redemption identity ROLE-GATED = visible-by-default for customer-facing roles (Support / senior Ops / Super-Admin), reveal-with-reason for restricted roles, NO access for roles with no need; (iii) EVERY identity access audited regardless of role (cheap; converts broad-exposure risk into controlled+accountable access; helps DSAR + the already-flagged D32 retained-PII concern).** Per-role->tier mapping lands with the capability matrix (already a pending 'later decision' / net-new). AskUserQuestion posed to lock the model (A role-gated[rec] / B visible-to-all-audited / C keep-mask-for-everyone). After lock -> small Redemptions-tab adjustment paste + blueprint privacy-posture update; then Merchant 360 COMPLETE -> flush D42-D48 in one amendment.

**D48 ROLE-GATED LANDED + ASSISTANT-VERIFIED (owner built the adjustment; 3 screenshots + DOM logs). VERDICT: PASS. MERCHANT 360 NOW COMPLETE (all 12 tabs + role-gated privacy model).** 3 tiers driven by the top-bar role switcher, verified end-to-end: sees-by-default (Support/Operations/Super-Admin = names inline, no reveal, opening still audited, GREEN banner + toast 'your role sees it by default; view recorded to Audit'); reveal-with-reason (Sales/Finance = 'Customer - hidden' + Reveal->reason->audited, AMBER banner); no-access (Content = locked 'Not permitted for your role', no reveal control, rest of row intact, detail names redemption:view-customer, GREY banner). Role-context banner + illustrative-tiering footnote. Detail + global-search result follow the same gating. Fixed 'Denied' state button removed (no-access now role-driven); Populated/Loading remain. Aggregate Performance untouched/identity-free. CP-1 micro-watch (copy, non-blocking): the single-detail CUSTOMER note is stale in the sees-by-default tier ('Identity is hidden by default... revealing is on-demand') - contradicts inline display; make it tier-aware (sees-by-default: 'visible to your role; views audited; never in aggregate'); the sees-by-default TOAST is already correct. **MERCHANT 360 DONE -> D42-D48 BLUEPRINT FLUSH now DUE (doc-sync). Flush DISPATCHED (background subagent; worktree docs/admin-members-revenue clean at 011926c9; PR #351 OPEN; author-only, no-commit; assistant reviews the full diff + em-dash/emoji scan + commits + pushes to PR #351).** Then screen 1.7 create-draft (paste handed in parallel).

**FLUSH DONE + PUSHED (commit `c1535ca5`; PR #351 now 6 commits ahead of main, verified LIVE via `gh api compare` + `git ls-remote` - the `gh pr view` cached count lagged at 5 per the known-stale-field rule).** Subagent authored: blueprint (§3.2 the 4 new caps merchant:manage-staff / branch:manage-pin / redemption:view-customer / redemption:reveal-customer + M5 12-tab row + M28 aggregate-half clarification + §6.1 tab list + NEW §6.1.6 D42-D48 narratives + §6.1.1 & §6.3 PIN+Staff+Redemptions invariant reversals + §13 eight gated rows + §16 register D42-D48) + wave-1 pack §1.6 (full 12-tab rewrite) + §1.4 PIN line + acceptance list + clickable flows + targeted master-context corrections. **Assistant reviewed the FULL diff:** faithful, well-placed, honest; subagent's judgment calls (kept aggregate 'never show identity' statements at M28/§6.1.1; kept PIN 'not shown here' on non-Branches surfaces) all CORRECT. **Assistant CAUGHT + FIXED one miss:** master-context §C.3 Branch glossary bullet still said 'A branch redemption PIN is NEVER shown to an operator' -> corrected to the D46 reveal-on-demand+audited wording. Belt-and-braces em/en-dash/emoji scan CLEAN on all staged added lines. **Blueprint pending-list now EMPTY (D27-D48 all reconciled). Doc-sync FULLY CURRENT through Merchant 360.** Wave 1 continues: 1.7 create-draft (paste handed) -> 1.8 Global Audit -> 1.9 Admin Users and Roles -> 1.10 Operational Status -> 1.11 Notifications/bell -> then Members and Revenue screens -> CP-1.

**SCREEN 1.7 LANDED (create-draft LIGHT path) = PASS + BIG NEW DIRECTION D49 (assisted/rep-led onboarding).** 1.7 build PASS: '+ New merchant' on the Approval Queue top bar -> 'New merchant draft' 6-field form (business legal name / trading optional / owner first+last / owner email / owner phone optional) + SAFE copy ('no password set here; a secure setup email WILL be sent'); Success ('Draft created · PENDING · NOT LIVE' + what-happens-next + 'Admin-created draft' acquisition source + Open-360 / Create-another / Back-to-queue); EMAIL_ALREADY_EXISTS + DRAFT_CREATE_FAILED states. Minor gap (owner-flagged): the 'Leads and Onboarding' NAV lands STRAIGHT on the form (no section landing) -> fixed by the section-landing redesign below. **D49 (assisted/rep-led onboarding) - owner direction:** TWO onboarding channels: (A) SELF-SERVE (merchant registers online -> admin approval loop; existing) + (B) ASSISTED / REP-LED (a Redeemo rep physically with the business builds the ENTIRE account on-behalf on the admin panel: business profile / category / subcategory / tags / type / contact, branches, branch users, the 2 mandatory RMV vouchers, documents - so a non-technical merchant never touches the Merchant Portal). CONTRACT: the OWNER signs in-person on the rep's device ('hand over the iPad'), captured as the OWNER's act + evidence (identity / timestamp / IP / method); the rep must NOT be able to accept for the owner; **admin-never-signs stays PROHIBITED (legal lock preserved).** On completion the fully-built shell TRANSFERS to the owner's email (tokenised claim) -> the owner sets their own password + takes ownership. Real-vs-net-new: shell-create + claim/transfer REAL (create-draft + issueMerchantClaim); the FULL on-behalf onboarding wizard NET-NEW (composes the D44 operator-parity-on-behalf principle across every onboarding step). **FORK LOCKED (owner chose A, 2026-07-02):** go-live for a rep-completed onboarding = REP APPROVES INLINE + audited + the in-person verification captured as evidence + rep-created-and-approved merchants FLAGGED for light post-hoc audit sampling (fastest, matches decide-there-and-then, keeps accountability, consistent with today's single-actor model). Capabilities: merchant:create-draft (light, exists) + merchant:assisted-onboard (heavy, net-new). **Build split (2 pastes): Paste A (HANDED) = Leads & Onboarding SECTION LANDING (inbound self-serve pointer + 2 admin-created paths [create-draft-and-hand-off + assisted-onboarding] + in-progress list) + the assisted STEPPER shell + steps 1-5 (business / branches / staff / vouchers / documents on-behalf, reusing D33 map-confirm + D44 staff + D41 vouchers); Paste B (after A) = contract-handover (owner-signs-on-device, evidenced, admin-never-signs) + review & go-live (real 5-gate checklist, rep-approves-inline + audited/flagged per the locked fork) + handover/transfer to owner email.** After both land: capture D49 in the blueprint (expands M4 Leads and Onboarding, currently PARTIAL). Wave-slot: a substantial expansion of M4 but stays within the Wave-1 governance spine.

**PART A LANDED (owner built; 2 screenshots + build log). DESIGN = PASS:** landing (inbound self-serve pointer + View-in-queue + 2 admin-created cards [Create-draft-and-hand-off / Assisted-onboarding NET-NEW, each w/ best-when + capability line] + in-progress-assisted list w/ step-reached + Resume) + assisted STEPPER (navy on-behalf header 'Acting on behalf, in person' + audited + Save-and-continue-later; 5 build steps Business-profile / Branches[D33 map-confirm] / Staff[D44 claim-link] / Vouchers[2 RMV + advisory + category benchmark] / Documents; dashed Contract / Go-live-review / Handover 'next build' placeholders; still-needed-for-go-live hints; net-new + on-behalf + audited + password-at-handover honesty). **BUG (owner-flagged, NOT a design issue): LAYOUT REGRESSION** - the full-width workspace mains (Assisted stepper + Merchant 360 + Directory->merchant-detail) render RIGHT-SHIFTED with a big empty left gap; the Approval Queue + Leads landing are FINE. Assistant hypothesis: an unclosed / mis-nested element in the new stepper main -> the mains rendered AFTER it in source (m360, merchant-detail) misnest inside it + inherit its narrow / right-shifted width (matches: queue/landing render-before = OK, m360/detail render-after = broken; the stepper's own DOM checks passed on content-presence but missed the visual shift). **Fix paste HANDED** (inspect stepper-main tag balance; confirm each workspace main is a sibling in the shared content-area container at the same left-edge + width as Queue/landing; do not touch the stepper's internal two-column design; verify by measuring 5 screens' content-area left-edge + width). After the fix verifies clean -> assistant re-checks -> hand Part B (contract-handover + go-live + transfer).

**LAYOUT BUG FIXED + VERIFIED (owner built the fix; build log + 4 screenshots).** Root cause = a stray duplicate `</div></main></sc-if>` triplet left at the end of the Create-draft main (from the 1.7 build) closed the content column early, so every main after it (assisted stepper, Merchant 360, Directory->merchant-detail) fell OUT of the content column into the sidebar flex-row + inherited the narrow right-shifted width; removing the 3 stray lines fixed it; all 5 screens now measure identical (left 250, width 674); the stepper's internal two-column rail+panel untouched. Assisted steps 1-5 render correctly. **NEW owner refinement (folds into D49): FULL-SCREEN PRESENTATION MODE.** Owner: the stepper 'looks like a dashboard inside a dashboard'; 'Start assisted onboarding' should open a FULL-SCREEN focused surface (hide the left nav + operator top bar) so the whole flow runs IN FRONT OF THE MERCHANT without exposing the rest of the admin panel / other merchants' data / operator tools = merchant-safe. Honest framing: a focus/presentation mode of the OPERATOR's own session (still operator, still audited), NOT a separate login or a security boundary. Owner also noted the step content is representative/overview-level (full field capture = real-build detail) - accepted for the prototype. **Part B FOLDED WITH the full-screen conversion into ONE paste (HANDED):** Part 1 = full-screen mode + Exit-to-admin (chrome hidden); Part 2 = steps 6-8: STEP 6 Contract (owner-signs-in-person via an explicit HAND-TO-OWNER owner-only accept panel; the operator CANNOT accept for them; evidence = owner name/title + timestamp + IP + method + agreement version; admin-never-signs LEGAL LOCK preserved; signatory-name capture flagged NET-NEW vs today's version/timestamp/IP/method-only); STEP 7 Go-live review (real 5-gate checklist + named-gate-fail block + rep IN-PERSON VERIFICATION attestation + 'Approve and take live' serious confirm -> live + FLAGGED for post-hoc audit [single-actor create+approve honesty per the locked fork]); STEP 8 Handover (transfer to owner email via tokenised setup link -> owner sets own password + takes ownership of the already-built+live account; operator never sets/sees a password; final 'assisted onboarding complete' + link to 360). After it lands: assisted flow COMPLETE -> capture D49 (incl. full-screen presentation mode) in the blueprint -> resume 1.8 Global Audit -> 1.9 Admin Users -> 1.10 Operational Status -> 1.11 bell -> Members and Revenue -> CP-1.

**PART B + FULL-SCREEN MODE LANDED = PASS (owner built; 9 screenshots + build log).** Full-screen presentation mode (sidebar + top bar hidden, Exit-to-admin, focus-mode honesty note); Step 6 Contract (agreement V2.3 summary + hard 'operator cannot accept' + Hand-to-owner -> owner-only accept [name+title+confirm, accept disabled until both] + evidence [signatory name/title, timestamp, IP, method, version] + admin-never-signs + net-new signatory-capture note); Step 7 Go-live (5-gate checklist + demo All-green/Gate-fail toggle + in-person-verification attestation gating Approve-and-take-live + single-actor honesty + post-hoc audit flag); Step 8 Handover (owner-email + tokenised setup link -> owner sets own password + takes ownership + 'Account handed over' + never-saw-password + flagged-for-post-hoc-audit + link to 360). **OWNER FEEDBACK (D49 COMPREHENSIVENESS PASS):** (1) the stepper is a SKELETON - the primary buttons (Capture business profile / Edit on behalf / Add branch / Re-confirm on map / Invite on behalf / add vouchers) do NOTHING + the FIELDS are not comprehensive; owner wants each step to become a REAL capture form with the FULL field set MIRRORING the merchant-portal onboarding (profile + branches + vouchers + staff + documents; 'a lot more comprehensive'). (2) VOUCHER QUALITY/STACK TOOL: mirror the merchant-portal voucher-value guidance (comparison vs similar voucher types of similar categories) so the rep builds good-value mandatory RMVs (the current advisory indicator + category benchmark is a start; enrich to the portal's scoreVoucher/stack). (3) CONTRACT SIGNATURE (locked): add an OPTIONAL on-screen finger/stylus signature capture (tablet/touchscreen) IN ADDITION to name/title/accept - but NOT a gate (name+title+accept remains the acceptance; the drawn signature is optional where a touchscreen exists). **GROUNDING AUDIT DISPATCHED (background subagent a52d8...)** to extract the comprehensive per-step onboarding field inventory from the REAL merchant-portal sources (specs 2026-06-20-m2-onboarding-design + m2-voucher-builder-extraction + 2026-06-23-branches-programme + 2026-06-22-staff-access + 2026-06-25-specialty-recuration + the merchant-portal prompt-pack/blueprint + prisma schema) -> structured inventory to scratch `assisted-onboarding-field-inventory.md`. After it returns: hand the build asks to flesh out each step (full fields + wired capture forms + the voucher quality tool + the optional signature). Tier-2 comprehensiveness expansion of D49; the merchant-portal onboarding is the fidelity target. (Blueprint D49 capture deferred until this comprehensiveness pass lands, so the blueprint reflects the full flow in one write.)

**AUDIT RETURNED (a52d8..., full inventory at scratch `assisted-onboarding-field-inventory.md`; grounded on LIVE merchant-web onboarding components + prisma, not just specs).** KEY FINDINGS: real onboarding = 6-step staircase CATEGORY-first (Category+identity -> Profile -> Main branch -> 2 flagship RMVs -> Sign agreement -> Submit); **CATEGORY IDENTITY is a HARD PREREQUISITE for vouchers** (determines eligible flagship types + RMV templates) - was MISSING as an explicit assisted step. Business profile = logo/banner/description[req]/website/registered-name[req]/trading/company#/VAT-toggle+number (Tier-1 lean; business-type/registered-address/owner-title/values/social/price-range are NET-NEW/M3-deferred). Category identity = primary-category -> subcategory -> cuisine[conditional] -> specialties -> generated-label. Branch = name/description/address(Google-Places-lookup)/line1/line2/city/postcode/country(GB)/phone/email/website + MULTI-WINDOW hours(Mon-Sun, split, 24h, copy) + amenities(category-eligible) + banner + up-to-8-moderated-photos + 4-digit-PIN (location auto-resolved + admin map-confirm, no manual coords). Vouchers = 2 mandatory flagship RMVs, pick 1-of-5 eligible types (BOGO/Spend&Save/Discount-fixed+percent/Freebie/Package; TL+Reusable INELIGIBLE), per-type structured fields, auto-composed title/desc, photo, estimatedSaving(£5 floor), category terms(Fair/Caution/Restrictive), askHelp toggle. **Voucher quality tool = REAL scoreVoucher (Too-weak/Good/Great, £5 hardcoded floor, strong+improve lists, ADVISORY-not-a-gate); the 'compares to similar businesses' peer-benchmark is MOTIVATIONAL ONLY - NO real cross-merchant benchmark exists (our category-median comparison = NET-NEW/not-built).** Staff = NOT part of onboarding (owner=default branch user; extra staff day-2) -> OPTIONAL on-behalf. Documents = OUT of onboarding (no merchant route; admin-only) -> OPTIONAL on-behalf, not a gate. Contract = click-to-agree persists version/ts/IP/method; signatory NAME+TITLE NOT persisted + NO signature-drawing today -> both NET-NEW. Gates: submit checklist = 3 (branch, contract, 2 RMVs); main-branch auto; location-confirm = admin-at-approve (5 go-live gates correct at approve).

**FIELD SCOPE LOCKED (owner chose 'Real + richer set', 2026-07-02):** capture the real onboarding fields AND the richer merchant-portal-prototype fields (business type, registered/head-office address, owner title/position, values/highlights, social links, price/service options), net-new ones clearly labelled 'not stored yet, needs schema (M3)'. Minor calls confirmed: category-first reorder; staff+docs OPTIONAL; voucher tool = real scoreVoucher + benchmark-labelled-net-new. **Target rail (9 steps): Category -> Profile -> Branch(es) -> Vouchers -> Staff[opt] -> Documents[opt] -> Contract -> Go-live -> Handover.** **BUILD-ASK 1 of ~4 HANDED = Category-and-identity (new step 1) + Business profile (REAL public-profile [logo/banner/description-REQ/website] + REAL name-registration [legal-name-REQ/trading/company#/VAT] + NET-NEW richer section labelled not-stored-yet); wire 'Capture business profile' + 'Edit on behalf' to open the real form.** Remaining: 2 Branch (full fields + multi-window hours + amenities + 8 photos + PIN + address-lookup + map-confirm + add-more); 3 Vouchers (2 RMVs + type-picker + per-type fields + terms + REAL scoreVoucher tool); 4 Staff[opt] + Documents[opt] + Contract optional-signature-draw (name+title+accept stays the acceptance; drawn signature optional, NOT a gate). After all 4 land: capture D49 (full assisted flow) in the blueprint in ONE write.

**FIELD-PARITY requirement (owner, 2026-07-02, raised while pass 1 builds):** the comprehensive onboarding field set (category identity chain + richer business profile + full branch incl. multi-window hours + full voucher detail) must be reflected CONSISTENTLY across three surfaces, not just captured in assisted onboarding: (1) the assisted-onboarding CAPTURE (build passes 1-4, in flight); (2) the onboarding-review DETAIL (1.4a, reached from the Approval Queue - expand its field inventory to match; the queue LIST stays lean, fields live in the detail); (3) the Merchant 360 WORKSPACE - Business identity tab = full category chain (primary -> subcategory -> cuisine -> specialties -> generated-label; currently FLATTENED) + the richer profile fields (business-type / head-office-address / owner-title+position / values / social / price+service, labelled net-new); Branches tab = MULTI-WINDOW hours (currently single-window) + full branch fields; Vouchers tab = already full per D41. Also the Merchant Directory category display + blueprint §6.1.1 field inventory + §6.1 M360 tab descriptions. **Sequencing: FINISH the 4 assisted-capture passes first (they finalize the canonical field set), THEN ONE FIELD-PARITY reconciliation pass across Merchant 360 + onboarding-review (avoids churn if capture fields shift), and expand blueprint §6.1.1 + M360 tabs in the D49 write.** Captured now so it is not lost.

**PASS 1 LANDED = PASS (owner built; 3 screenshots).** 9-step rail reordered (Category first, before Vouchers; auto-advance-from-approve lands step 9). Step 1 Category-and-identity (11 top-level tiles -> subcategory chips -> cuisine multi-select[appears only for cuisine-bearing subcats, min 1] -> optional specialty chips -> live generated label 'Chinese Restaurant' + 'Category set'; 'add your own' deferred-labelled; hard-prerequisite + top-level-change-discards-drafts confirm-guard). Step 2 Business profile wired ('Capture business profile'/'Edit on behalf' open the real form): REAL public (logo/banner/description-600-REQ-counter/website) + REAL registration (legal-name-REQ/trading/company#-hint/VAT-toggle+number) + NET-NEW richer section in a distinct purple dashed card (business-type 7 UK / conditional charity#-UTR / head-office-address / head-office-phone+email / owner-title+job-title / values chips / social links / price-range + service-options). On-behalf audited save + summary + still-needed hint. **OWNER FEEDBACK on pass 1 (3 items):** (1) TAXONOMY ADD-YOUR-OWN + MANAGEMENT -> D50: when subcategory / cuisine / specialty is not listed, the operator should ADD new ones; and since we're in the admin panel there should be an admin TAXONOMY / CATEGORY MANAGEMENT surface to create/edit categories / subcategories / cuisines / specialty tags. Owner acknowledges SOME are HARDCODED (top-level 11 + per-category rules: voucher-eligibility, amenity-eligibility, descriptor composition = code-coupled, not just data) -> flag honestly. Pairs with the existing 'Suggested-tag Moderation' (Trust and Safety) sidebar item (moderation side; management = create side). OPEN QUESTION for when we design it: operator add-your-own = immediate vs route-to-Suggested-tag-Moderation vs hybrid (use-for-this-merchant-now + flag-for-global-approval); taxonomy is platform-wide (all merchants + customer discovery) so LEAN moderation/controlled. (2) OWNER / RESPONSIBLE-PERSON contact = richer: currently only title + job-title; add MOBILE / phone / email + optional SECONDARY contact + fuller responsible-person details (REAL: MerchantAdmin holds email+phone+jobTitle; NET-NEW: title, secondary contact). (3) DROPDOWNS + 'Other' escape: position/job-title (and other known-set fields) = select/chips with an 'Other -> freetext' fallback; consistent pattern. Broader directive: 'think about what info we should hold as a platform managing the merchant' -> apply comprehensive-data + structured-options principle across ALL remaining steps. **PASS-1 REFINEMENT HANDED** = owner/responsible-person block (title dropdown / first+last / position dropdown+Other / email / mobile / phone + optional secondary-contact toggle) + dropdown-with-Other pattern (job-title options + Other; keep chips for type/price/service/values). **D50 taxonomy management = NEW admin module, SEQUENCED AFTER the assisted passes** (NOT blocking; assisted 'add your own' stays deferred-labelled until the module exists, then hook it into the module + Suggested-tag Moderation). Sequence: pass-1 refinement -> pass 2 Branch -> pass 3 Vouchers -> pass 4 Staff/Docs/Contract-signature -> field-parity pass -> D49 blueprint write; D50 taxonomy module designed as its own surface after.

**PASS-1 REFINEMENT LANDED = PASS (owner built; 4 screenshots).** Owner/responsible-person block (title dropdown Mr/Mrs/Ms/Mx/Dr/Prof/Other + first/last + position chips [Owner/Founder/Director/Partner/GM/BranchMgr/MktgMgr/Other] + Other->freetext + contact email + mobile + optional landline + 'Add a secondary contact' toggle; honest REAL[account email+phone+jobTitle] vs NET-NEW[title/extras/secondary]); dropdown/chips-with-Other pattern consistent (business-type/price/service/values keep chip pattern). **OWNER CATCH (category-conditional fields):** the net-new SERVICE OPTIONS (Dine-in/Takeaway/Delivery/Click&collect) show for ALL categories incl. Automotive->Car-wash where they are irrelevant. Design principle (mirrors the REAL system's CategoryAmenity + per-subcategory cuisine gating): category-specific richer fields must be CATEGORY-DRIVEN, showing only options relevant to the chosen category. Service options is net-new so we have design freedom -> per-category option map (net-new, CategoryServiceOption-style). **FIX HANDED** = category-conditional service options (per-category sets: Food / Automotive / Retail / Health+Fitness / Home+Trades+Professional / generic-fallback + Other-escape) + apply the principle to any category-specific field (price + generic values can stay cross-category); stays net-new-labelled. **CARRY to pass 2 (Branch): amenities are REAL category-eligible (CategoryAmenity) -> build amenities as CATEGORY-GATED, not a fixed list.** Sequence: this fix -> pass 2 Branch -> pass 3 Vouchers -> pass 4 Staff/Docs/Contract -> field-parity pass -> D49 write. **Micro-fix HANDED (owner catch):** the secondary-contact block is missing the Title field (only first/last) -> make secondary MIRROR the primary owner/responsible-person block exactly (add Title dropdown + full parity: title / first / last / position / email / mobile / landline). Can build alongside the service-options fix.

**BOTH FIXES VERIFIED = PASS (owner built; 1 screenshot): STEP 2 BUSINESS PROFILE COMPLETE.** Category-driven service options (Food/Automotive/Retail/Health/Fitness/Home&Trades/Professional per-category sets + generic fallback + Other-escape; label names the category; price + generic values stay cross-category; per-category map in net-new section) + secondary-contact full parity (title dropdown + first/last + position chips+Other + email + mobile + landline, mirrors primary). **PASS 2 of 4 HANDED = Branches step (step 3) real capture form** (wire Add-branch / Edit-on-behalf / Re-confirm-on-map): BASICS (name-REQ + description-600 + use-business-desc prefill); ADDRESS (Google-Places lookup -> autofill line1-REQ/line2/city-REQ/postcode-REQ; country GB default); CONTACT (phone/email/website); MULTI-WINDOW OPENING HOURS (per-day Mon-Sun toggle + one-or-more open/close windows + Open-24h + copy-Mon-to-weekdays/all + client-validated no-overlap/order); AMENITIES (CATEGORY-ELIGIBLE chips, real CategoryAmenity gating, per-category sets + Other); IMAGES (branch banner + up-to-8 moderated photos PENDING-until-approved; branch logo=merchant logo default); 4-digit REDEMPTION PIN (rep sets, encrypted); LOCATION automated (auto-resolved pin + one-click Confirm/Nudge, NO manual coords, reuse D33); Add-another-branch (first=main, others regular, added-branches list); on-behalf audited + still-needed hint. States: empty/filled, address-lookup-pick, multi-window hours (2-window/24h/closed), category-eligible amenities x2 categories, photo grid w/ pending, PIN, map-confirm, added-branches list. Then pass 3 Vouchers -> pass 4 Staff/Docs/Contract -> field-parity -> D49 write.

**D51 (internal operator notes) - PARKED idea (owner, 2026-07-02; explicitly 'not for the current stage').** An internal NOTES / annotations surface where operators add freeform notes on a MERCHANT (Merchant 360) and a CUSTOMER (Customer 360), with add / edit / RETRACT + history + optional ATTACHMENTS; also in the ONBOARDING section (near documents) + other relevant surfaces (approval / case items). Design considerations to encode when picked up: author + timestamp on every note; RETRACT = soft-delete-with-history (keep the record, mark retracted) NOT hard-delete, for accountability; every add/edit/retract AUDITED (-> Global Audit); attachments = operator-uploaded, signed-link storage (like documents); role-gated (new cap notes:read / notes:write); NET-NEW schema (no MerchantNote/CustomerNote model today) + surface. **PRIVACY FLAG (locked consideration): notes about a CUSTOMER are personal data -> DSAR-DISCLOSABLE (a customer can request what notes we hold on them) -> must be factual/professional, role-gated, audited; design as if the subject could read them.** Merchant notes = less sensitive (B2B) but still audited. **Sequencing: PARKED after the assisted-onboarding work; natural to fold the Merchant 360 Notes tab into the FIELD-PARITY pass (already reopens M360), spec Customer 360 + onboarding notes then. Not now.**

**PASS 2 LANDED = PASS (owner built; 5 screenshots): branch capture end-to-end.** List mode (empty -> Add-main-branch; first=main, others regular; cards w/ address + collapsed-hours + amenities + main/regular badge + location-confirmed + Edit-on-behalf) + 8-section form (Basics name-REQ + desc-600 + use-business-desc / Address Google-lookup->autofill / Contact phone+email+website / Multi-window hours Mon-Sun + 24h + split + copy + validated / category-eligible Amenities + Other / Images banner + up-to-8-moderated-photos / 4-digit PIN / automated Location map-confirm) + on-behalf audited + Add-another-branch. Removed 3 stale shadowing handlers. **CUSTOMER-APP CROSS-CHECK (owner asked 'anything missing vs what customers see for branches?'): source-grounded against `selectedBranchSchema` in apps/customer-app/src/lib/api/merchant.ts.** Customer branch payload = name / address(line1,line2,city,postcode,country) / lat,lng / phone / email / websiteUrl / logoUrl / bannerUrl / about / openingHours / photos / amenities / distance / isOpenNow / avgRating,reviewCount / myReview / isFavourited. **ONLY GAP = branch LOGO (logoUrl):** customer-facing per-branch logo (defaults to merchant logo but a branch CAN have its own) was NOT captured. Everything else the customer sees IS captured (banner / photos / about / hours / amenities / address / contact / website; lat-lng auto-resolved via map-confirm; distance / open-status / rating / review / favourite are derived/accumulated, not onboarding inputs). **REFINEMENT HANDED = add BRANCH LOGO in Images (inherit merchant logo by default + 'upload a different logo for this branch' override; real Branch.logoUrl, customer-facing).** Confirmed the rest: address IS Google-lookup automated (no manual coords); amenities ARE category-eligible + Other (add-more routes to D50 taxonomy when built; subcategory-level refinement possible in the real CategoryAmenity model). After the logo refinement: pass 2 COMPLETE -> pass 3 Vouchers (type-picker + per-type fields + scoreVoucher tool) -> pass 4 Staff/Docs/Contract -> field-parity -> D49 write.

**BRANCH LOGO REFINEMENT VERIFIED = PASS: PASS 2 (BRANCH) COMPLETE.** Branch logo in Images (inherits merchant logo NM preview + 'Inheriting the merchant logo' note + 'Upload a different logo for this branch' -> custom-logo state 'overrides for this branch only, goes to moderation' + 'Revert to merchant logo' escape; on-behalf audited). **PASS 3 of 4 HANDED = Vouchers step (step 4) = the flagship-voucher BUILDER (biggest step).** Captures the 2 mandatory flagship RMVs (custom vouchers = day-2, noted-not-built). Per-voucher builder: (1) TYPE PICKER 5 eligible (BOGO-rec / Spend&Save / Discount-fixed+percent / Freebie / Package) + 2 disabled (Time-Limited + Reusable, 'add as custom later'); category-driven eligibility; RMVs no-expiry. (2) 'YOU DECIDE' per-type structured fields (BOGO buy+free+value+cheaper-rule / Spend threshold+save / Discount-fixed amount+minspend / Discount-percent pct+ref / Freebie item+worth+needs-purchase / Package items+price+normal-total; estimatedSaving derived read-only or editable per type). (3) 'WHAT CUSTOMERS WILL SEE' photo(landscape>=1200x600) + auto-composed-editable title(60) + description(300) + estimatedSaving(£5-floor warning + Set-to-£5) + concierge askHelp toggle. (4) YOUR TERMS core(3 always-on) + per-type fair + category caution + restrictive + add-your-own(80, auto-restrictive-tag); each tagged Fair/Caution/Restrictive. (5) 'HOW THIS VOUCHER STACKS UP' = REAL scoreVoucher Too-weak/Good/Great meter (weak if <£5 OR too-restrictive OR 4+ improvements; great if 0-improvements + generous + no-restrictive + <=3 terms) + What's-strong + What-could-be-better lists (<=4 each) + ADVISORY-not-a-gate (weak CAN still submit; admin review = backstop) + NO fake peer benchmark (no real cross-merchant benchmark exists; scoreVoucher IS the real tool). On-behalf audited + still-needed hint. States: 0/2 / 1/2 / 2/2; builder on 2 types (BOGO=Great, Discount-percent=Too-weak w/ below-£5 + improvements); terms picker w/ tags + add-your-own; concierge on; auto-compose-then-edit preview. Offered to split if thin (type+fields, then preview+terms+score). Then pass 4 Staff/Docs/Contract -> field-parity -> D49 write.

**PASS 3 REVIEWED = NEEDS REFINEMENT (owner built; 4 screenshots).** The builder shell is right (type picker 5+2, per-type fields, auto-compose title/desc, below-£5 note + Set-to-£5, terms Fair/Caution/Restrictive + add-your-own, advisory Too-weak/Good/Great meter, on-behalf audited) BUT it is NOT category-driven and diverges from the merchant-portal flagship builder. **Owner directive: mirror the merchant-portal prototype's FLAGSHIP-voucher builder (not custom).** Specific gaps: (1) placeholders/examples are food-hardcoded ('e.g. a cooked breakfast', 'order a treatment') even when Professional Services / Automotive is the category -> fields + auto-composed title/desc must be CATEGORY-SENSIBLE; (2) NO suggestion chips -> the portal has category-driven suggestion chips on every guided field (faster, fewer typos) -> add them; (3) the concierge 'Ask the Redeemo team to help with this offer' toggle is UNNECESSARY in assisted (Redeemo IS building it) -> remove; (4) terms revolve around food & drink even for a professional-services business -> terms must be CATEGORY-CONDITIONAL (service business = no dine-in/while-stocks); (5) 'How this voucher stacks up' is a BOTTOM block -> make it a STICKY side rail that follows scroll so the rep sees the score change live; (6) the scoring must match the portal: personalized title + personalized description LIFT the score; too many / too restrictive terms DROP it toward Too weak; surface the PERCENTAGE (e.g. '20% off £30', '£8 back on £30, about 27%'). **PLUS owner follow-up (always-on terms):** 'One voucher per customer each visit' + 'Not valid with any other voucher' should NOT be always-on -> always-on = genuinely-GENERIC terms only; those two move to optional (pre-tickable).

**GROUND TRUTH EXTRACTED from the SHIPPED merchant-web flagship builder** (read directly, not from spec): `apps/merchant-web/lib/voucher/config.ts` (CATEGORY_DATA = 11 top-level keys + fallback: per-category bogoBuy/freebie/spendAmt/spendSave chips + term FLAGS booking/fullPrice/dineIn/whileStocks/subjectAvail/oneTreatment; FREE_QUAL_SUGGEST + PKG_ITEM_SUGGEST + PKG_ITEM_PLACEHOLDERS per category; FIXED chips for discount amount[5,10,15]/percent[10,15,20]/typical-order[20,30,50]/min-spend[15,25,40], freebie-worth[4,6,8], package-price[25,40,60]/normal[35,55,80], BOGO-free[3 fixed]) + `terms.ts` (CORE 3 fair + per-type fair + category-conditional caution via categoryCaution(flags) + discount-percent restrictive min-spend + countTermTiers thresholds: becomingRestrictive = restrictive>=1 OR total>=5 OR caution>=2; tooRestrictive = restrictive>=2 OR total>=7 OR caution>=4; tooManyTerms = total>=5) + `scoring.ts` (scoreVoucher: titleClear = len>=8 OR '%off/£off'; descHelpful = len>=30 AND EDITED-from-suggestion; generosity absolute+relative per type; belowMinSaving <£5; tier = weak if below£5 OR tooRestrictive OR improveCount>=4, great if 0-improvements + generous + no-restrictive + <=3 terms, else good; strengths+improvements SURFACE the % ; PLATFORM_FRAMING = motivational-only, no real peer numbers) + `compose.ts` (composeTitle/composeDescription/deriveSaving/deriveSavingPercent per type) + `components/onboarding/vouchers/BuilderForm.tsx` (LAYOUT: form LEFT [flex-1] + preview+ScoreCard in a RIGHT aside; per-type FieldBlock+SuggestionChips+MoneyField; 'Use our suggestion' reset on edited title/desc; the concierge askHelp Switch IS present in the portal but REMOVE for assisted) + `ScoreCard.tsx` (3-segment meter + 'What is strong' + 'What could make it better' + aria-live). Config is TOP-LEVEL only (CC-15, no subcategory config) -> assisted resolves the Step-1 category to its top-level parent (Automotive->Auto&Garage, Professional Services->Home&Local, unknown->fallback). Ground truth also saved for later reference in the field-inventory scratch.

**DECISIONS:** **D52 (assisted voucher builder = faithful port of the merchant-portal flagship builder):** category-driven fields/placeholders/suggestion-chips/examples/terms keyed off the Step-1 category (top-level parent), the REAL scoreVoucher advisory maths (personalized title+description lift; too-many/too-restrictive terms drop to Too weak; percentages surfaced; advisory-not-a-gate; no fake peer numbers), a STICKY always-visible score+preview rail (owner asked left; because the stepper already owns a left step-nav rail, place the score rail on the RIGHT of the builder panel and make it sticky/scroll-follow so it never collides with the step nav and is never a bottom block), and NO concierge 'ask the Redeemo team' toggle (Redeemo is the builder in assisted). **D53 (always-on voucher terms = generic-only):** proposed always-on core = 'Tell staff you're using a Redeemo voucher before you order or pay' + 'Show the voucher in the Redeemo app to redeem' + 'This voucher has no cash value and can't be exchanged for cash'; 'Not valid with any other voucher' + 'One voucher per customer each visit' move to OPTIONAL (pre-tickable, not locked). NOTE: this DEVIATES from the merchant-portal CORE (which has tell-staff + no-combine + per-visit as its 3 fair core, all as unlocked checkboxes with per-type default-ticks) -> flag as an owner-confirm; the portal model 'all terms are unlocked checkboxes' is itself an acceptable alternative. **D54 (expand D50 -> admin TAXONOMY & RULES management module):** owner (2026-07-02) wants an admin surface to manage NOT JUST categories/subcategories/cuisines/specialties/tags but their DEPENDENT CONTENT: per-category voucher suggestion chips + per-type examples, the category->term FLAGS (which caution terms apply per category), amenities-per-category eligibility (CategoryAmenity), and any other category/subcategory-dependent config. ARCHITECTURAL NOTE (honest): today ALL of this is HARDCODED in code (config.ts CATEGORY_DATA + term flags + CategoryAmenity seed; CC-15 top-level only) and is read by BOTH the merchant-portal builder AND (soon) the admin assisted builder -> making it admin-managed = promoting hardcoded config to DATA + one source of truth feeding both surfaces (net-new schema + backend, well beyond the prototype). PARKED/sequenced AFTER the assisted passes; pairs with the existing Suggested-tag Moderation (Trust & Safety) sidebar item (management=create side, moderation=review side); open question stands (operator add-your-own = immediate vs route-to-moderation vs hybrid; taxonomy is platform-wide so lean controlled).

**PASS-3 REFINEMENT HANDED** (mirror the merchant-portal flagship builder, 6 changes): (1) category-drive ALL placeholders/chips/examples/terms off the Step-1 category [full 11-category data table provided]; (2) add category-driven SUGGESTION CHIPS to every guided field; (3) REMOVE the concierge toggle; (4) category-conditional terms + generic-only always-on [D53]; (5) STICKY score+preview rail; (6) REAL scoreVoucher maths [personalized title+desc lift, too-many-terms drop, percentages surfaced, advisory]. Offered to split if it returns thin: (a) category-driving + chips + terms + concierge-removal, then (b) sticky rail + scoring maths. After it verifies -> pass 4 Staff/Docs/Contract-signature -> field-parity pass (fold D51 Notes tab) -> D49 blueprint write; D50/D54 taxonomy module as its own surface after.

**PASS-3 REFINEMENT LANDED = PASS (owner built; verified vs Auto & Garage; 3 screenshots).** All six changes in + working: (1) category-driven off Step-1 category resolved to top-level parent (Auto&Garage shows service chips + NO dine-in/food terms); (2) suggestion chips under every field (category-driven + fixed sets); (3) concierge toggle REMOVED; (4) generic-only always-on (tell-staff / show-in-app / no-cash-value, unbadged) + 'not valid with any other' + 'one per visit' moved to pre-ticked removable optionals + per-type Fair (Food/Travel bill/one-table/before-service wording) + category Caution + Freebie while-stocks + Discount-percent Restrictive min-spend + add-your-own auto-Restrictive + becoming/too-restrictive notices; (5) sticky preview+score rail on the right, pins + follows scroll; (6) real scoreVoucher maths verified live (edit description -> BOGO lifts to Great; add restrictive terms -> drops to Too weak; % surfaced; advisory-not-a-gate; neutral closing line, no fake peer benchmark). PASS 3 (Vouchers) CONTENT COMPLETE. **REMAINING = LAYOUT-ONLY refinement (owner, non-blocking):** the builder content is capped to a NARROW CENTRED column (big empty side gutters ~390px each side; earlier 'width 674' cap), so Sections 2/3/4 are squeezed into ~half of an already-narrow width. Owner wants the field sections WIDER with breathing room (as wide as Section 1 'Choose a voucher type') + the sticky rail on the right of it. **LAYOUT FIX HANDED** = widen the whole builder to fill the workspace between the step-nav rail and the right edge (kill the dead side gutters, comfortable ~24-32px margins); Section 1 spans full builder width across the top; two-column body below = WIDE form column (majority, ~60-65%, roomy fields) + SLIM sticky rail (~340-360px fixed, pinned/scroll-follow) on the right. After it verifies -> pass 4 Staff/Docs/Contract-signature -> field-parity pass (fold D51 Notes tab) -> D49 blueprint write.

**WIDTH OVERCORRECTED (owner, 2026-07-03; 2 screenshots): 'fill the workspace' went FULL-BLEED -> single fields + terms rows now stretch the full ~1400px, label stranded far-left + value/FAIR-badge far-right with dead space between.** Assistant owns the overshoot. **CORRECTION HANDED = comfortable capped middle-ground (not the old squeeze, not full-bleed):** cap the FORM column (Sections 2/3/4) to a natural single-column form width (~700-760px) so title/description/estimated-saving/each-terms-row read at a comfortable width and never stretch edge-to-edge; two related money fields (Spend threshold / Save amount) may sit as a 2-up pair within that width; terms rows capped to the form width with the Fair/Caution/Restrictive badge kept CLOSE to its label (right-aligned within the ~700px row, not across 1400px); sticky preview+score rail (~340px) stays on the right beside the form; cap the OVERALL builder (Section 1 card-grid + form + rail) to ~1180-1280px, aligned/centred with modest margins (not the old ~390px empty gutters). Section 1's 5-card grid can keep the wider row at that capped width. Net: comfortably wide, aligned, no field/term runs full-screen. After it verifies -> pass 4.

**WIDTH CORRECTION VERIFIED = PASS: PASS 3 (VOUCHERS) FULLY COMPLETE (content + layout).** Owner built + measured (Discount / Travel & Hotels): Step-4 wrapper capped 1180px centred; two-column grid = form minmax(0,760px) + rail clamp(300,26vw,340) space-between; verified at 1500px container = form 760px, title input 469px, terms row 718px with Fair badge 13px from edge (beside label, no dead gap), overall builder ~1160px, sticky rail position:sticky top:6px both cards intact, money fields 2-up. Category-correct example ('20% off your stay' Travel & Hotels) + % surfaced in score ('A generous £15 saving, about 30% off £50', Good). Content/chips/terms/scoring/sticky all unchanged. **PASS 4 of 4 HANDED = Staff (step 5, OPTIONAL) + Documents (step 6, OPTIONAL) + Contract signature-draw (step 7).** (Ignored: a stale-claim/coding-agent reply bled into the screenshot's left panel from another session; irrelevant to the prototype.) After pass 4 verifies -> field-parity reconciliation pass (fold D51 Notes tab) -> D49 blueprint write; D50/D54 taxonomy module as its own surface after.

**PASS 4 LANDED + FULL ASSISTED FLOW VERIFIED = PASS (owner built; 7 screenshots + build log): ASSISTED ONBOARDING COMPLETE END-TO-END (9 steps).** Step 5 Staff (3 populations: A branch-manager-defaults-to-owner + branch-email routing [D45 net-new] w/ add-branch-first empty state; B portal managers via claim-link, first/last/email/role[Branch manager|Staff, Owner implicit]/scope[All|Specific->which-branches]/can-manage-vouchers[Branch-manager-only], real MerchantMembership cap 8, operator-never-sets-password; C till/scan users one-per-branch real BranchUser cap 20; D44 on-behalf-manage note; skippable, not a gate) + Step 6 Documents (generic slots BV1/BV2/price-list/additional + add-another, upload-on-behalf->Pending-moderated, verification-status pill, net-new-labelled, skippable, not a gate) + Step 7 Contract (owner-signs-in-person, operator-cannot-accept, hand-to-owner, V2.3 summary owner/legal-owned pending-sign-off; build log confirmed BOTH signature paths: drawn->'Captured on device' / not-drawn->'Not provided', Accept still gated name+title+confirm, admin-never-signs intact) + Step 8 Go-live (5 enforced gates + in-person attestation gating Approve + single-actor honesty + post-hoc-audit flag + confirm modal; matches D49 fork) + Step 9 Handover (tokenised setup link, owner sets own password, operator never sees it, transfer/claim real). Assistant reviewed all 7 = coherent, every locked decision holds, NO defects.

**TWO OWNER ADDITIONS (2026-07-03):** (1) **Per-document notes/annotations** on Step 6 (operator annotation per uploaded document) -> small in-scope addition, HANDED now. (2) **D55 (admin Agreements/Policies/Content management module, version-controlled)** -> owner wants an admin surface to manage the Redeemo Merchant Agreement + other contracts/policies/legal content shown across customer-app / customer-web / merchant-portal, with version control, so edits happen from the admin panel. ARCHITECTURAL NOTE (honest): today agreement text = placeholder/MVP, the real binding legal text is owner/legal-owned + a launch sign-off GATE, the static customer-web legal pages are the launch source of truth, and an unwired/deferred `CmsContent` concept already exists in the codebase (+ MerchantContract.tcVersion, the V2.3 this assisted flow references) -> D55 = that CmsContent path made real + versioned + surfaced across 3 product surfaces = substantial net-new backend. PARKED as its own admin surface alongside D50/D54 taxonomy; NOT folded into the flow. **BOUNDARY kept:** subjective/internal operator notes stay OFF the merchant-facing contract signing screen (merchant watches it in full-screen); per-DOCUMENT factual notes OK on Step 6; broader internal notes on contract/merchant/customer live on the internal review side (Merchant 360 / onboarding-review) via D51, folded into the field-parity pass.

**PER-DOCUMENT NOTES REFINEMENT HANDED** = Step 6, optional operator note per document (Add-a-note -> small text area; operator-internal, not merchant/customer-facing; on-behalf + audited; edit/remove; note-icon + preview when present; all slots incl. Additional). Slice of the broader D51 notes system (rest stays parked). After it verifies -> field-parity reconciliation pass (fold D51 Notes tab) -> D49 blueprint write; then D50/D54 taxonomy + D55 agreements/policies as their own admin surfaces.

**PER-DOC NOTES VERIFIED = PASS: ASSISTED ONBOARDING FLOW FULLY COMPLETE (9 steps + all refinements).** Operator-internal note per document (Add-a-note -> text area, icon+preview when present, edit/hide/remove, gated on upload, cleared with file removal, audited toast, 'not shown to merchant/customers' caption). **FIELD-PARITY PASS STARTED. 5-piece plan: (1) M360 Business identity [full category chain + richer profile + owner block]; (2) M360 Branches [multi-window hours + full branch fields]; (3) M360 Notes tab [D51]; (4) onboarding-review detail 1.4a expand; (5) Merchant Directory category display. Vouchers tab already full (D41). Blueprint §6.1.1 + tab descriptions updated in the D49 write.**

**FIELD-PARITY #1 (M360 Business identity) LANDED = PASS w/ ONE LABELLING CORRECTION (owner built; described in text, no screenshot).** Built: full category chain (primary -> subcategory -> cuisine[s] -> specialties -> derived 'British Restaurant' descriptor; primary keeps Operations/reclass edit) + real profile (logo/banner/legal-name/trading/description/website/company#/VAT-registered-flag+number) + richer net-new M3 card (business-type w/ conditional charity#/UTR, head-office address/phone/email, price-range, values chips, category-conditional service-options Food&Drink-set, social links) + owner/responsible block (title/first/last/position/email/mobile/landline + optional secondary, honestly split: email+mobile+position Real, title+name-split+landline+secondary net-new-M3); Lanes footnote explains M3; edit-on-behalf (9 buttons + lane tags) + audited banner unchanged. **CORRECTION (assistant caught, source-verified):** the category CHAIN is ENTIRELY REAL/persisted (`src/api/merchant/onboarding` identity endpoint persists `subcategoryId` + `primaryDescriptorTagId` + `specialtyTagIds` -> Merchant.primaryCategoryId + primaryDescriptorTagId + tags MerchantTag[]) -> **specialties must be Real, NOT M3**; only 'add your own' (new taxonomy creation) is deferred (D50/D54). Owner block labelling confirmed correct (account stores single name+phone+jobTitle -> title/name-split/landline/secondary rightly net-new). **REPRESENTATIVE-RECORD decision:** keep representative data (whole admin prototype is representative); do NOT wire per-merchant plumbing; only requirement = 360 view internal coherence (all tabs = same merchant; other M360 tabs are Old Foundry too, so consistent -> leave). **SPECIALTIES CORRECTION HANDED.** After it verifies -> field-parity #2 M360 Branches.

**SPECIALTIES CORRECTION VERIFIED = PASS (owner, text confirmation): M360 BUSINESS IDENTITY TAB COMPLETE.** Chain reads Subcategory/Cuisine(s)/Specialties = Real, Primary category editable Operations lane, Category label Derived; 5 M3 tags remain only in richer-profile + owner blocks (unchanged); 'add your own' deferred note present. **FIELD-PARITY #2 (M360 Branches) HANDED** = multi-window hours upgrade (Mon-Sun, split windows, 24h, closed) + full branch field set (address+map-confirmed, amenities category-eligible, banner+8 photos, branch logo inherit/override, main/regular badge, status) + branch manager (D45) + assigned users (D47) + PIN reveal/reset preserved (D46, do-not-regress) + real-vs-net-new labelling (branch-manager routing + branch-email = M3, rest Real). Awaiting owner build.

**FIELD-PARITY #2 (M360 Branches) LANDED = PASS (owner built; 2 screenshots).** Multi-window hours verified (Mon-Thu split + SPLIT tag, single Friday, Sat 24h, Sun closed, BranchOpeningHours, today highlighted) + full field set (full address + map-confirmed status, phone/email/website, category amenities incl. Other, banner + up-to-8 photos w/ approved/pending/free-slot moderation, branch logo override[High Street]/inherit[Harbourside], main/regular badge, Active status) + D45 resolved branch manager (Devan Roy) + owner-default assign nudge + D47 assigned app-login users (read+reach, manage on Staff tab) + branch-email routing (owner-also-receives opt-in, Phase-6 concept) + D46 PIN reveal/reset (reason+audit, net-new on-behalf) preserved + location automated (Confirm/Nudge, no coord typing) + real-vs-net-new labels correct. **2 minor non-blocking notes:** (a) synthetic-data coherence - High Street branch shows quaysidepizza.example email/website + 'old iron foundry' About in Bristol while the merchant header is 'Brew and Bloom Coffee Ltd, Bath' (stitched fixtures; optional tidy for demo coherence); (b) a D56 preview crept in - build added 'DIRECT · Edit' on hours + 'NET-NEW · ON-BEHALF' on PIN while the location footnote says 'hours editing not yet supported' (small contradiction; the lane tags are a helpful D56 preview but the direct-vs-pending assignment is the formal D56 decision - reconcile the footnote there, leave as-is now). **COLLAPSIBLE-CARDS REFINEMENT (owner think-out-loud, 2026-07-03):** branch cards are info-dense (full hours + staffing + PIN + location map each) -> with multiple branches = long scroll; same density on voucher cards. Owner wants COLLAPSIBLE branch (and voucher) cards: collapsed = rich summary/overview; expand = full detail. **HANDED (Branches collapsible + mirror on Vouchers):** collapsed header = logo/initial + name + main/regular + active/suspended + chevron; collapsed summary line = locality + today's-hours/open-now + manager + N users + N/8 photos + amenity count + PIN-set + location-confirmed; expand = current full detail unchanged; default = MAIN branch expanded, others collapsed; same pattern on Vouchers (collapsed = type + title + saving + status + tier; expand = full). **COLLAPSIBLE VERIFIED = PASS (owner built; 3 screenshots + build log).** Branch cards collapse/expand (main expanded default, others collapsed, toggle works, no duplication); voucher cards collapsed-by-default expand to full detail. Vouchers tab also surfaced another D56 preview banner ('cosmetic changes apply + approve directly; a value or saving change routes to the merchant for approval') = concrete cosmetic-vs-value lane split, good input for the D56 field->lane assignment. **OWNER FLAG: EXPANDED cards (branch + voucher) TOO DENSE** - field-parity got everything in but it stacks as one long column w/ weak grouping/hierarchy; owner wants better layout/structure for UX. **DENSITY/HIERARCHY REFINEMENT HANDED (requirements-led per locked feedback `feedback_claude_design_requirements_led.md` - give problem + content grouping + goal, Claude Design OWNS presentation; do NOT prescribe layout):** problem = dense expanded cards; content grouping requirement = BRANCH detail splits into Customer-facing (banner/logo/About/hours/amenities/photos) vs Operations (staffing/manager/app-users/branch-email/PIN/location), VOUCHER detail splits into What-customers-see (image/title/type/description/terms/saving) vs Performance&value (redemptions/availability-reuse-kind/advisory+benchmark/approval-history/correct-on-behalf); condense the long explanatory footnotes (PIN/notif-routing/location/benchmark) into muted captions or info-tooltips; preserve EVERY field/tag/edit-affordance/PIN-behaviour/real-vs-net-new-label + collapsed summaries; stay in the admin design system + brand (navy/cream/brand-red-coral, no emojis, no em-dashes). **DENSITY/HIERARCHY REFINEMENT VERIFIED = PASS (owner, text + DOM confirmation; no screenshot).** Expanded branch + voucher cards now split into two labelled bands - branch: 'What customers see' (cream, 'Shown in the Redeemo app': banner/logo/identity/address/contact/website/about/hours/amenities/photos) + 'Operations · internal' (neutral, 'Not shown to customers': staffing/manager/app-users/email-routing/PIN/location); voucher: 'What customers see' (image/title/type/description/terms/value+saving+expiry) + 'Performance & value · Internal' (redemptions/availability/reuse/kind/advisory+benchmark/approval-history/correct-on-behalf). 4 long footnotes (PIN/notif-routing/location/benchmark) condensed to muted captions + full text in 'i' tooltips; 4 em-dashes removed. Every field/tag/edit-affordance/real-vs-net-new-label/collapsed-summary + PIN reveal/reset preserved (DOM-verified).

**FIELD-PARITY #3 (M360 Notes tab, D51) HANDED (requirements-led).** New 'Notes' tab on M360 (net-new; no notes model today; adds to the 12-tab bar). Content+functions: note list (author+role / timestamp / body / attachments / status active|retracted, newest first); ADD (free-text + optional attachment [signed-link storage like documents], audited); EDIT (role-gated, marks edited + keeps prior version in history); RETRACT (SOFT-delete for accountability - stays visible, 'Retracted by X on date' muted/struck + optional reason, NEVER hard-delete); per-note HISTORY (add/edit/retract w/ who+when); every add/edit/retract AUDITED (-> Global Audit); role-gated notes:read (view) + notes:write (add/edit/retract) w/ no-permission state. Framing: internal B2B merchant notes (less sensitive than customer notes but factual/professional/audited); CUSTOMER-side notes (personal data, DSAR-disclosable) = separate Customer 360 surface, NOT here; whole tab net-new-labelled; consistent w/ the per-document notes pattern but merchant-level + broader (attachments + retract-history). Brand/design-system, no emojis/em-dashes. After it verifies -> #4 onboarding-review detail 1.4a expand -> #5 Merchant Directory category display -> D49 blueprint write.

**FIELD-PARITY #3 (M360 Notes tab) LANDED = PASS (owner built; 1 screenshot + build log).** New 'Notes' tab in the M360 bar (between Documents + Staff-and-access); 'NET-NEW · NEEDS SCHEMA' label; framing caption matches spec (internal B2B, audited, customer-notes->Customer 360 DSAR, net-new); ADD composer + Attach-a-file + Save (signed-link caption + audited-entry caption); notes list w/ author+role+timestamp+body+attachment+status; ACTIVE note (Priya Nair, History(1), Retract); RETRACTED note (Shebin C., EDITED badge, struck body, 'Retracted by X on date' + soft-delete-accountability caption, attachment Open, expandable HISTORY Added+Edited w/ who+when). Build log verified: add-with-attachment (3->4, audited toast), edit+history (prior kept), retract-with-reason, role-gating (Finance read-only tier, Content no-access tier). No defects.

**FIELD-PARITY #4 (onboarding-review detail 1.4a expand) HANDED (requirements-led; M360 = parity target).** Expand the Approval-Queue review-detail field inventory to match the now-comprehensive M360 (queue LIST stays lean, fields in the DETAIL): Business identity full category chain (not flattened) + richer profile + owner block w/ real-vs-M3 labels; Branches multi-window hours + full fields + PIN reveal-on-demand(reason+audit); Vouchers 2 RMVs in full + advisory score; Documents + operator notes; Staff (portal managers + branch users + branch-manager assignment) + Contract evidence (version/signatory-name+title/method/timestamp/IP, net-new-labelled). Reuse the collapsible + two-band (customer-facing vs operations) grouping so the review detail isn't a dense wall. KEEP review chrome unchanged (go-live checklist/gates, claim/approve/request-changes/reject action bar, thin-areas + activity, role-gating). Offered to split if thin (identity+profile, then branches+vouchers+docs+staff+contract). After it verifies -> #5 Merchant Directory category display -> D49 blueprint write.

**FIELD-PARITY #4 (onboarding-review detail expand) LANDED = PASS (owner built; 2 screenshots + build log).** Review detail now mirrors M360's comprehensive inventory (DOM-verified): full category chain ('British Restaurant' DERIVED + 'chain is real, only add-your-own deferred' caption) + owner/responsible block (ACCOUNT·PARTLY-REAL: position/email/mobile REAL, title/first/last-split/landline/secondary M3) + richer-profile parity card (business-type/head-office/price/values/service-options[Food&Drink-only]/social, all M3) + branch two-band (customer-facing hours/amenities/photos + operations staffing/PIN-reveal/location) + documents(status+notes) + staff-captured + contract-evidence(version/signatory-name+title/method/ts/IP net-new). Review CHROME untouched (checklist/gates/thin-areas/activity/action-bar/role-gating). Record internally coherent (all Old Foundry - Elaine Turner/Foundry Works Bristol). D56 preview accumulating (Lanes caption = Direct/Operations-immediate vs Correct-on-behalf two-tier [cosmetic applies, business/trading-name routes to merchant] + company#/VAT/category Super-Admin-gated-today honesty). 'Two Reveal PIN buttons' = the two branches each w/ own PIN, not duplication. No defects.

**FIELD-PARITY #5 (Merchant Directory category display) HANDED (requirements-led; LAST field-parity piece).** Directory list shows each merchant's category as the resolved descriptor label (e.g. 'British Restaurant', same generated label as M360 identity, from primary+cuisine) instead of flattened/raw single category; list STAYS LEAN (one label/chip per row; full chain lives in 360 + review detail, not the list); keep any category filter working (by primary/top-level); presentation Claude Design's; brand/design-system, no emojis/em-dashes. After it verifies -> FIELD PARITY COMPLETE -> D49 blueprint write (capture the full assisted flow + field-parity + fold D51 Notes + M360 tab descriptions + §6.1.1 field inventory; register D49-D56 - D50/D54 taxonomy, D51 notes[partial built], D52/D53 voucher builder+terms, D55 agreements/policies, D56 edit-on-behalf).

**FIELD-PARITY #5 (Merchant Directory category) LANDED = PASS: FIELD PARITY COMPLETE across all surfaces.** Directory rows show resolved category descriptor (Pizzeria / Yoga Studio / Speciality Coffee Shop / Record Shop / Sourdough Bakery / Cocktail Bar), one lean chip per row (full chain lives in 360 + review detail), top-level category filter verified (Restaurants and cafes -> exactly the 4 matching merchants). Directory also carries rich filters (Category/Region/Lifecycle/Activity/Verification) + summary cards (Supply base / Verification / Activity / Distribution). No defects. **COMPREHENSIVE FIELD SET now consistent across: assisted CAPTURE (passes 1-4) + M360 (Business identity / Branches / Vouchers[D41] / Notes) + onboarding-review DETAIL + Merchant Directory.**

**MILESTONE: assisted-onboarding (D49) + field-parity arc COMPLETE.** Remaining prototype roadmap: (A) DOC-SYNC = D49-D56 blueprint reconciliation (due; PR #351 worktree); (B) D56 edit-on-behalf design pass (parked-but-next; makes M360 edit buttons real + live/pending model); (C) Wave-1 governance screens 1.8 Global Audit / 1.9 Admin Users & Roles / 1.10 Operational Status / 1.11 Notifications; (D) parked admin modules D50/D54 taxonomy + D55 agreements/policies + D51 customer/onboarding notes; (E) Members & Revenue screens; (F) CP-1 sign-off. **Next-step fork surfaced to owner (AskUserQuestion): doc-sync now [recommended] vs D56 edit pass vs governance screens.**

**OWNER RE-ANCHORED to the WAVE SEQUENCE (2026-07-03) + 2 corrections. Assistant double-checked §10.** (1) NOTES TAB CLARIFICATION: the MERCHANT Notes tab (D51) IS done; only the Customer 360 notes + onboarding-section notes are the deferred remainder (Customer 360 surface doesn't exist yet). My AskUserQuestion wording wrongly implied it was unfinished. (2) WAVE CONFIRM: we ARE on Wave 1; 1.1 Sign-in / 1.2 Ops Home / 1.3 Approval Queue / 1.4 Review-Actioner / 1.5 Merchant Directory / **1.6 Merchant 360 / 1.7 Create-draft all DONE** - the assisted-onboarding (D49) + field-parity + Notes work were owner-driven DEEPENINGS of 1.6+1.7, NOT a departure. **REMAINING WAVE-1 SEQUENCE: 1.8 Global Audit -> 1.9 Admin Users & Roles -> 1.10 Operational Status -> 1.11 Notifications/bell -> CP-1 sign-off** (then Members & Revenue Wave 2/3). (3) MERCHANT DIRECTORY category-filter DEFECT (owner-flagged on 1.5): the CATEGORY filter pills ('Restaurants and cafes'/'Cafes'/'Food and drink'/'Health and beauty'...) are MADE-UP, not the platform's real taxonomy; no subcategories; too many pills. **DIRECTORY FIX HANDED (requirements-led):** real 11 top-level categories (Food&Drink / Beauty&Wellness / Health&Fitness / Out&About / Shopping / Home&Local / Travel&Hotels / Health&Medical / Family&Kids / Auto&Garage / Pet Services) + subcategory drill-down (representative subcats consistent w/ the row descriptors: Pizzeria/Yoga Studio/Cocktail Bar...) + DROPDOWN/searchable-select not pills (region optional same) + must filter correctly (top-level shows all its subcats; subcat narrows) + combine w/ region/lifecycle/activity/verification + work across Table/Map/Group-by + fix the Distribution 'top categories' to real names. **DOC-SYNC (D49-D56 blueprint) = BATCHED to just before CP-1** (so the spec is current at sign-off; per owner build-priority). After the directory fix verifies -> RESUME wave at 1.8 Global Audit.

**DOC-SYNC BROUGHT FORWARD (owner-directed 2026-07-03, while the directory fix builds in Claude Design): reconcile the blueprint + wave-1 pack + master context with D49-D56 + field parity NOW.** Worktree `.worktrees/admin-members-revenue` verified clean, on `docs/admin-members-revenue`, up to date w/ origin at `c1535ca5` (PR #351 head). Blueprint already scaffolded for this (§8.2 Representative-assisted channel, BC-2 versioned-content/taxonomy contract, §6.1 Merchant 360, §13 gated/future, §16 register). **SUBAGENT DISPATCHED (author-only, no-commit; tracker invisible to worktree subagent so full D49-D56 content passed in the brief).** Scope: §8.2 full 9-step assisted flow (D49); §6.1 Notes tab + comprehensive field inventory + collapsible/two-band + D56 edit-lanes + D52 voucher builder; §6.2 deferred customer notes (D51); BC-2 + §13 taxonomy (D50/D54) + agreements/policies (D55) modules; §3.2 caps (merchant:assisted-onboard/notes:read/notes:write); §16 D49-D56 rows; wave-1 pack 1.6+1.7+1.5; master-context light touch. Constraints: no em-dashes/emojis, honest REAL/NET-NEW/NOT-YET-IN-CODE, DESIGN-ONLY, expand-not-delete. **Assistant to review full diff (em-dash/emoji scan + label honesty + no-clobber, D42-D48-style belt-and-braces) -> commit -> push to PR #351 -> flag owner-eye items.** Then resume wave at 1.8 (after directory fix verifies).

**DOC-SYNC DONE + PUSHED (commit `776aee1e`; PR #351 now 7 commits, head verified LIVE via `gh pr view` = 776aee1e).** Subagent returned; assistant reviewed the FULL diff (3 files only: blueprint +70 / wave-1-pack +43 / master-context +8; 101 ins / 20 del). **Belt-and-braces verified:** em/en-dash count on added lines = 0; non-ASCII = only § + · (approved); the 20 deletions are ALL intentional paragraph-modifications (12->13 tab, panes-table split, §16 intro rewrite, BC-2 flags expand, edit-affordance rewrite) with expanded + replacements = NO content loss (confirmed: §16 intro preserved D37-D48 + added D49-D56; 13-tab list complete [Notes after Staff-and-access]; BC-2 kept CmsContent/RmvTemplate/TermsClause flags + added the 2 modules; the D39 'Reframed' Directory paragraph lives in the WAVE PACK not the blueprint = preserved+augmented there). §8.2.1-8.2.3 assisted narrative = comprehensive + honest (REAL vs NET-NEW on every element, cross-refs 8.1/8.3/8.4/3.2/6.1/D33/D36/D41/D44/D45/D51); §16 has 7 rows for D49-D56 (D50/D54 combined per instruction); §9 authority-matrix row added; §3.2 new caps; §6.1 field parity + Notes + D56 lanes; §6.2 deferred customer notes; §13 gated/future rows. **1 minor nicety FLAGGED to owner (not fixed):** batch dated 2026-07-03 (D49-D51 were first raised 07-02; '2026-07-02 to 2026-07-03' marginally more precise; 07-03 = reconciliation date, defensible; owner may request adjust). **BLUEPRINT + WAVE-1 PACK + MASTER CONTEXT NOW FULLY RECONCILED with the prototype through D56.** Doc-sync current. Awaiting the Merchant Directory category-filter fix from Claude Design; then RESUME wave at 1.8 Global Audit.

**DIRECTORY CATEGORY-FILTER FIX LANDED = PASS (owner built; 1 screenshot + build log).** Real 11 top-level categories (Food&Drink/Beauty&Wellness/Health&Fitness/Out&About/Shopping/Home&Local/Travel&Hotels/Health&Medical/Family&Kids/Auto&Garage/Pet Services) as a searchable DROPDOWN + subcategory drill-down (representative subcats consistent w/ row descriptors; picking top shows all [Food&Drink=8], picking sub narrows [Pizzeria=1]); REGION also converted to dropdown; filtering real + combines w/ region/lifecycle/activity/verification/search (search matches resolved descriptor); verified across Table/Map[16 pins]/Group-by (groups on real top-level names); Distribution 'top categories' fixed to real names (Food&Drink 8/Shopping 3/Out&About 2/Beauty&Wellness 2). Rest of directory unchanged. **OWNER FOLLOW-UP: convert the remaining Lifecycle/Activity/Verification PILL rows to dropdowns too for consistency/tidiness (Category+Region now dropdowns).** **DROPDOWN-CONSISTENCY REFINEMENT HANDED:** Lifecycle (7 opts) / Activity (3) / Verification (5) -> dropdowns matching the Category/Region pattern; keep 'Featured only' as a TOGGLE (binary flag, not multi-option); result = clean row of filter dropdowns + the Featured toggle; all filtering/combining/rescoping across summary+Distribution+Table/Map/Group-by preserved; brand/design-system, no emojis/em-dashes. After it verifies -> RESUME wave at 1.8 Global Audit.

**DROPDOWN-CONSISTENCY REFINEMENT VERIFIED = PASS: MERCHANT DIRECTORY (1.5) FULLY COMPLETE.** All 5 filters (Category/Region/Lifecycle/Activity/Verification) now dropdowns in a clean row + Featured-only stays a toggle; filtering combines across all + search + rescopes summary/Distribution/Table/Map/Group-by (verified Lifecycle->Suspended=2, Verification->Verified=8 + Activity->Dormant=2, count rescopes). **WAVE RESUMED at 1.8.** AuditLog model grounded (REAL, indexed): entityId+entityType('customer'|'merchant'|'branch'|'admin') / event / actorId+actorType / before+after(Json) / reason / metadata(Json) / ipAddress+userAgent+deviceId+sessionId / createdAt; indexes on entityId+entityType, event, createdAt, actorId = query-by-actor/entity/event/time all REAL. Redaction concern = metadata/payload + PIN. **SCREEN 1.8 (Global Audit / Activity Explorer) HANDED (requirements-led):** queryable log (filter by actor/entity-type+entity/event/time + search, all map to real indexed fields); entry = actor(name+role)/event/entity-link/timestamp/reason + expand for before->after diff + request context(IP/device/session); REDACTION hard rule (secrets/payload/PIN NEVER shown [D46 PIN-never-in-audit]; customer identity role-gated per D48, never blanket); governance-backstop framing (every audited action: approvals/on-behalf corrections+edits/PIN reveals/note add-edit-retract/staff changes/suspensions/redemption-identity-reveals/assisted-onboarding steps; M360 Audit tab = scoped slice); honesty (AuditLog infra+indexes REAL; explorer surface net-new; some event TYPES net-new = correction-flow/notes/assisted-steps); audit:read cap + permission-denied; states populated/loading/empty(distinct-from-error)/error/permission-denied/narrow; reuse shell, no design-system rebuild; brand/no-emojis/no-em-dashes; synthetic only. Owner signs off once at CP-1 (wave end), not per-screen. Then 1.9 Admin Users & Roles -> 1.10 Operational Status -> 1.11 Notifications -> CP-1.

**SCREEN 1.8 (Global Audit) LANDED = PASS (owner built; 7 screenshots + build log).** Queryable log (Actor/Entity-type/specific-entity/Event/Time dropdowns + search, combine + rescope count, verified Customer=1); HONEST-STATUS banner (infra+indexes REAL / explorer + correction-flow+notes+assisted event types NET-NEW / secrets+payloads+PIN never shown / customer identity role-gated); 14 synthetic entries spanning every audited flow; entry = actor+role / event(+NET-NEW tag) / entity-link-to-M360-Audit-slice / timestamp, expand = reason + before/after diff (MATERIAL flag on the correction) + request-context(IP/device) + redaction note; PIN entries = explicit 'PIN value never written to audit' note; customer identity role-gated (mirrors D48): default-sees-inline (Ops/Super/Support) / reveal-with-reason (Finance, verified end-to-end w/ reason dialog + audit toast) / no-access denied (Sales/Content); all 6 states + demo switcher. **CP-1 carry (not a defect):** build broadened `audit:read` to Super/Ops/Support/Finance to make the identity tiers demonstrable; exact `audit:read` role set = later capability-matrix decision, confirm at CP-1. No defects. **WAVE at 1.9.** AdminRole enum grounded (REAL, 5 roles): SUPER_ADMIN/OPERATIONS/FINANCE/CONTENT/SUPPORT - NO SALES in code (prototype role-switcher's 'Sales' = persona-not-in-enum, blueprint §3.1 has SALES/LEAD-OWNER + Platform-Manager as proposed/future personas; reconcile honestly). AdminUser REAL = firstName/lastName/email/role/isActive/lastLoginAt/approvals; NO AdminInvite model + NO AdminCapabilityGrant in code -> tokenised invite + per-person grants + step-up = net-new/future. **SCREEN 1.9 (Admin Users & Roles) HANDED (requirements-led):** user list (name/email/role/status[isActive]/last-active); INVITE new admin (name+email+role -> tokenised invite, admin sets OWN password, operator never sets it, mirrors merchant-claim/handover; NET-NEW no admin-invite model); deactivate/reactivate (isActive flip + confirm); assign/change role (5 REAL roles; SALES/Platform-Manager = proposed/future labelled); per-role CAPABILITY set/matrix (incl. thin FINANCE[revenue/billing read]/CONTENT[CMS/taxonomy]/SUPPORT[cases+reveals] honest homes; role->cap model REAL fixed-role RBAC); BOOTSTRAP-first-SUPER_ADMIN note (chicken-and-egg); per-person grants + step-up = FUTURE labelled not-faked (AdminCapabilityGrant unshipped); LAST-SUPER-ADMIN guard (can't deactivate/demote last active SUPER_ADMIN, mirrors merchant last-owner, NET-NEW logic); role-gated admin-manage(invite/deactivate/assign) + admin-read(view) + permission-denied; confirm Insights+Platform sidebar groups render here (CP-0 carry); states populated/loading/empty/error/permission-denied; honest REAL vs NET-NEW; reuse shell; brand/no-emojis/no-em-dashes; synthetic only. Then 1.10 Operational Status -> 1.11 Notifications -> CP-1.

**SCREEN 1.9 (Admin Users & Roles) LANDED = PASS (owner built; 8 screenshots + build log).** 7 synthetic admins (name/email/role-pill/status/last-active; Priya=BOOTSTRAP+last-super-admin lock glyph, Shebin=YOU, Elena=Deactivated); INVITE (name+email+role->tokenised setup link, dup-email rejection, operator-never-sets-password success panel, NET-NEW); deactivate/reactivate (confirm + audit toast); change-role (5 real roles); LAST-SUPER-ADMIN guard (blocks deactivate + demote, row + in-dialog warning + blocked toast, NET-NEW); capability matrix 'What each role can do' (per-role tabs + blurb + caps + THIN-TODAY on Finance/Support/Content honest homes + 'Fixed-role RBAC real'); **SALES-not-in-enum handled EXACTLY right** = 5 real assignable roles + Sales/Lead-owner + Platform-Manager as FUTURE proposed personas ('not in the role enum today'); NOT-YET-POSSIBLE future = per-person grants (AdminCapabilityGrant unshipped) + step-up re-auth; bootstrap-first-Super-Admin note; honesty banner (real: list/5-enum/deactivate/role-assign vs net-new: invite/last-super-guard/grants/step-up); role-gated admin-read(view; Sales/Content denied) + admin-management(Super Admin manage); all 5 states + demo switcher; Insights+Platform sidebar confirm. No defects. CP-1 carry (shared w/ 1.8): demo broadened role sets to show tiers; exact per-cap role assignments = later matrix decision. **WAVE at 1.10.** Grounded: `/health` = LIVENESS-ONLY (`app.get('/health',()=>({status:'ok'}))`, NO dep checks) = REAL shallow; worker (src/worker.ts) REALLY runs queues (EMAIL_QUEUE + MAINTENANCE_QUEUE + outbox-reconciler + stale-claim sweep) but admin-facing heartbeat NOT surfaced = net-new; NO GIT_SHA/deployed-SHA + NO DB/Redis health signal in code = net-new/external. **SCREEN 1.10 (Operational Status) HANDED (requirements-led; BC-1 boundary contract):** BOUNDED allow-listed signals only (API-liveness[REAL /health liveness-only, honest liveness!=full-health] / Database-Neon[net-new deep probe] / Redis[net-new] / worker-heartbeat[worker REAL, heartbeat-surfacing net-new] / deployed-SHA[net-new/external] / APM+Logs[EXTERNAL link-out] / providers-Stripe+Twilio+Resend+FCM[EXTERNAL, NEVER shown operationally-live] / secrets NEVER rendered); each signal shows its layers consistently (what/how-checked+source/state/freshness/maturity-caveat); state vocab per signal healthy/degraded/down/unverified/external/gated/unavailable (honest, not fabricated-green); NO transient dated snapshot (dynamic concept, not frozen deploy moment - owner-instr-7); incident model = FUTURE labelled; bounded (not generic observability; deep telemetry = external APM/logs); role-gated + permission-denied; states populated(mixed)/loading/error/permission-denied; honest REAL(liveness+worker-runs) vs NET-NEW(deep DB/Redis/heartbeat/SHA) vs EXTERNAL(APM/logs/providers); reuse shell; brand/no-emojis/no-em-dashes; synthetic only, providers never live, no secret rendered. Then 1.11 Notifications -> CP-1.

**D57 RAISED (admin access model beyond single fixed role) - owner think-out-loud, 2026-07-03 (while 1.10 builds).** Owner: can a specific user get a COMBINATION of modules, e.g. Operations + Finance + Content? **CURRENT STATE (honest): NO** - AdminRole is a SINGLE fixed role per admin (5-role enum; `role` scalar NOT array); the only 'everything' is SUPER_ADMIN (over-broad, not a curated subset). This IS exactly the per-person-grants gap already flagged FUTURE on 1.9. **TWO models to enable (real Tier-3 authz + schema decision):** (1) **MULTI-ROLE assignment** - user holds several roles, effective access = UNION; simpler, coarser (whole roles); schema role->roles[] or join table; (2) **PER-PERSON CAPABILITY GRANTS** (AdminCapabilityGrant, design-only/unshipped) - base role + add/remove individual capabilities; finer, more powerful, harder to reason/audit. **ASSISTANT REC:** multi-role for the common 'combine a few modules' case (simple union); per-person grants as the finer escalation for genuine single-capability exceptions; Tier-3 authz+schema change (not a prototype tweak), belongs to the deferred capability-matrix decision. **Handling NOW = owner fork (AskUserQuestion): (a) surface as a labelled-FUTURE concept on 1.9 now [cheap, honest, keeps momentum]; (b) capture + defer to the Tier-3 auth pass; (c) design it properly now (Tier-3, would pause the wave).** 1.10 continues regardless. **OWNER CHOSE (a) show-as-future-concept on 1.9.** **1.9 D57 FUTURE-CONCEPT REFINEMENT HANDED (requirements-led):** enrich the 'Not yet possible' area to present combined/multi-module access as labelled-FUTURE showing BOTH models (multi-role assignment = union of whole roles; per-person capability grants = base role + individual cap add/remove) + honest current state (one fixed role today, Super Admin = only 'everything'); small honest note in Change-role + Invite dialogs ('one role today, combining is planned') WITHOUT faking a multi-select (control stays single-select); nothing else on 1.9 changes; brand/no-emojis/no-em-dashes; NO backend built. Independent of 1.10 (build either order). D57 itself (which model, when) stays deferred to the Tier-3 auth/capability-matrix pass. Both 1.10 (Operational Status) + this 1.9 refinement now in flight.

**SCREEN 1.10 (Operational Status) LANDED = PASS (owner built; 2 screenshots + build log). EXEMPLARY on the BC-1 honesty.** Caution banner up front ('boundary contract, not a fabricated green board'); summary strip 1 Real/live + 4 Net-new/unverified + 3 External + ticking 'last checked' clock (no dated snapshot); 8 signal cards each w/ layers (WHAT/HOW-CHECKED+source/caveat/state-pill/freshness/maturity-tag): API-liveness Healthy+REAL ('/health 200 = liveness-only, NOT full health'); DB/Redis/worker-heartbeat/deployed-SHA Unverified+NET-NEW (real dep/worker but no admin probe/heartbeat/SHA surfacing; 'No signal' freshness; SHA 'no hardcoded dated deploy moment'); APM/Logs/Providers External (link-out, never-rendered-in-app, never-live, no secret/key); Incident model FUTURE labelled; honest state vocab (healthy/degraded/down/unverified/external/gated/unavailable, no fabricated green); role-gated (Super+Ops nav; Denied names cap + audited); states populated/loading/error/permission-denied. No defects. **WAVE at 1.11 (LAST screen).** Notification model grounded (REAL, rich): recipientType(USER/MERCHANT_ADMIN/BRANCH_USER/ADMIN)+recipientId / title/body/type / channel / referenceId+referenceType(deep-link) / isRead+readAt / sentAt; indexed bell-feed (recipientType+recipientId+isRead + ordered); 9 REAL ADMIN types (BROADCAST/MERCHANT_SUBMITTED/MERCHANT_RESUBMITTED/CLAIM_STALE/OWNER_EMAIL_BOUNCED/DELIVERY_FAILED/REVIEW_ASSIGNED/MAINTENANCE_DEGRADED/MAINTENANCE_RECOVERED); NO per-admin prefs model; some emitters deferred (REVIEW_ASSIGNED needs assign-flow; DELIVERY_FAILED/BOUNCED email-enable-gated). **SCREEN 1.11 (Notifications/bell) HANDED (requirements-led):** header BELL feed (top-bar bell unread-count -> popover: title/body/type/timestamp/read-state + deep-link + mark-all + link-to-page); NOTIFICATIONS PAGE (full list + read/unread+type filters + per-item mark-read+open); mark-read/all (isRead/readAt REAL); DEEP-LINK to referenced item (referenceType/Id -> queue/merchant/branch REAL); realistic mix of the 9 REAL admin types w/ honest note that some emitters net-new/deferred (review-assigned needs assign-flow; delivery/bounce email-gated); per-admin mute/PREFS = FUTURE labelled; empty 'all caught up' DISTINCT from error; per-admin scope (feed = signed-in admin's OWN, recipient=this admin); states populated/loading/empty(all-caught-up)/error/permission-denied; honest REAL(model/bell-feed/mark-read/deep-link/types) vs NET-NEW+FUTURE(prefs/deferred-emitters); reuse shell; brand/no-emojis/no-em-dashes; synthetic only. **After 1.11 + the 1.9 D57 refinement both verify -> WAVE 1 COMPLETE -> assemble CP-1 (owner one-time wave sign-off; carry list: Finance Ops-Home visual / 1.3 narrow-view / 1.1 submitting-spinner / CLAUDE.md rule-7[done] / audit:read+status role sets = later matrix decision / D57 combined-access).**

**1.9 D57 COMBINED-ACCESS REFINEMENT LANDED = PASS (owner built; 2 screenshots).** Full-width 'Combined / multi-module access' FUTURE card below the roles grid: Multi-role assignment (union of whole roles) + Per-person capability grants (base role + individual caps, x-ref to Not-yet-possible) + honest current-state footnote (one fixed role today, Super Admin = only 'everything', role control stays single-select, forward-pointers not working control); Change-role + Invite dialogs carry the muted 'one role today, combining planned' note w/ single-select preserved. Nothing else changed.

**D58 RAISED (reframe admin-users as Redeemo STAFF + richer staff record + data-minimisation/HR boundary) - owner, 2026-07-03 (on 1.9).** Owner: don't call them 'admins' - these are Redeemo STAFF/team members (with admin-panel access); hold MORE info on them at invite + on the record. (Owner sentence trailed off 'hold an account of the...'; gist captured, invited to add.) **ASSISTANT (grounded, UK-GDPR minimisation consistency):** reframe 1.9 to Redeemo staff/team (DISTINCT from the merchant-side 'Staff and access' = merchant's own staff; suggest label 'Team & Roles' or 'Staff & Roles'); expand invite + staff record BUT apply the data-minimisation + right-system boundary = the panel holds ACCESS-management + operational context ONLY, NOT full HR/personal data (home address / DOB / NI-tax# / salary / emergency-contacts / personal-contacts / HR-docs) which belongs in the HR system (Zoho People per project Zoho One usage), not the admin panel. **Proposed staff fields (NET-NEW on top of REAL name/work-email/role/status/last-active/created):** preferred-name / work-phone / job-title / department(Ops/Finance/Support/Content/Trust&Safety/Leadership) / manager-reports-to / start-date / employment-type(Employee|Contractor) / staff-id / working-location / invited-by+date. INVITE = essentials (name/work-email/role/job-title/department + optional manager/start/type); RECORD = full set. Real AdminUser = lean auth record (name/email/role/isActive/lastLoginAt) so ALL richer fields = net-new schema. **1.9 STAFF-REFRAME REFINEMENT HANDED (requirements-led):** terminology (Redeemo staff/team, not admins; distinct from merchant staff) + richer staff record + invite-vs-record split + data-minimisation/HR-boundary honest note + REAL-vs-NET-NEW labels; keep all else on 1.9 (role mgmt / capability matrix / combined-access future / last-super-admin guard / states); brand/no-emojis/no-em-dashes. 1.11 (Notifications) still the FINAL wave screen; CP-1 after 1.11 + this reframe both verify.

**D58 EXPANDED + BOUNDARY CORRECTED (owner clarification, 2026-07-03): the Redeemo admin panel is the SINGLE CENTRALIZED internal platform - NO separate HR tool - so staff records AND staff ONBOARDING live HERE.** Owner: 'we won't have any other platform to maintain our internal things; this is our centralized platform for all Redeemo requirements, including onboarding staff.' **Assistant RETRACTS the prior 'HR data belongs in a separate system (Zoho People)' boundary** - there is no separate system. **REFRAMED CORRECTLY:** this grows into a proper **Staff / People MODULE** (new internal admin domain, analogous to Members & Revenue): staff records + STAFF ONBOARDING (structured, like merchant onboarding but for Redeemo's own team) + access & roles + eventual offboarding. **Judgment that STILL applies (not 'hold elsewhere' but 'hold here PROPERLY'):** sensitive HR/personal/pay data (salary / personal contacts / DOB / HR docs) held here but ROLE-GATED (visible to a People/HR capability + the staff member + their manager, NOT every operator) + GDPR-disciplined (lawful basis / retention / staff DSAR). **The FULL People module = its own Tier-3 brainstorm-first workstream** (real onboarding flow + fuller HR record + access-gating; substantial new domain), sequenced AFTER Wave 1 alongside the other parked modules (D50/D54 taxonomy, D55 agreements). **CORRECTED 1.9 REFRAME HANDED (supersedes the prior one):** Redeemo staff/team terminology + operational + basic-employment fields (job-title/department/manager/start-date/employment-type/work-phone/preferred-name/staff-id/location/invited-by) + invite-vs-record split + REPLACE the wrong 'HR-goes-elsewhere' note with the CENTRALIZED-PLATFORM framing (staff records + onboarding live here; this screen = access + operational/employment LAYER; fuller HR record + sensitive-field role-gating + staff-onboarding flow = COMING People module; sensitive data will be role-gated + GDPR-disciplined) + forward-pointer to the People module; REAL(name/work-email/role/status/last-active/created) vs NET-NEW(all richer) labels; keep all else on 1.9; brand/no-emojis/no-em-dashes. 1.11 still FINAL wave screen; People module = post-Wave-1 Tier-3 workstream.

**D58 FINAL LANDING (owner walked back the 'centralized, no HR tool' overstatement, 2026-07-03): Zoho People (or similar) IS retained as the HR system; the admin panel just needs the BASIC info to onboard a staff member to the PANEL + manage their access.** Converges back to the assistant's ORIGINAL recommendation (two bounces: assistant 'basics + HR elsewhere' -> owner 'centralized, hold everything' -> owner 'no, I overstated, just basics for panel onboarding' -> back to basics + HR elsewhere). **FINAL 1.9 reframe (supersedes BOTH prior versions):** Redeemo staff/team terminology (distinct from merchant 'Staff and access') + BASIC operational/employment fields for PANEL onboarding + access (name/work-email/role/job-title/department/manager/start-date/employment-type + optional work-phone/preferred-name/staff-id/location/invited-by+date) + invite-vs-record split + boundary note = the panel holds what's needed to onboard to the PANEL + manage access + basic work context; the fuller HR/employment lifecycle data (pay / DOB / home-address / personal-contacts / HR-docs) lives in the HR SYSTEM (Zoho People), NOT the panel. **DROPPED: the 'full People/HR module holding sensitive data + staff-onboarding-flow here' (that was the overstatement) - NO new People-module workstream.** REAL(name/work-email/role/status/last-active/created) vs NET-NEW(richer fields) labels; keep all else on 1.9; brand/no-emojis/no-em-dashes. 1.11 still FINAL wave screen; CP-1 after 1.11 + this reframe both verify.

**SCREEN 1.11 (Notifications/bell) LANDED = PASS (owner built; 3 screenshots + build log). WAVE 1 = ALL 11 SCREENS BUILT.** Header bell popover (live unread count / recent 5 / icon+title+2-line-body+type-pill+timestamp+unread-dot+tint / Mark-all-read / View-all / 'Your own feed' / deep-link+mark-read on click); full Notifications page (All/Unread/Read tabs + type dropdown + 'Showing N of 9' + per-item Open[deep-link]+Mark-read + Mark-all-read; read rows 'Read {ts}', unread rose left-accent+dot); all 9 REAL types w/ honest EMITTER-DEFERRED tags on review-assigned (needs assign-flow) + email-delivery/bounce (email-integration-gated) + future-card restatement; per-admin scope (recipient=signed-in); FUTURE per-admin mute/prefs card w/ disabled 'Manage preferences (not available)'; all 5 states (populated/loading/empty-'All caught up'-distinct-from-error/error+Retry/permission-denied); real-time badge on mark-read/all. No defects. **PENDING before CP-1: the 1.9 Team&Roles STAFF-REFRAME (final corrected paste) not yet shown built.**

**D59 RAISED (assign-then-claim workflow) - owner, 2026-07-03 (while reviewing 1.11).** Owner: certain roles should be able to ASSIGN other users to pick up specific tasks/merchants/approvals, rather than everyone self-claiming; the assignee RECEIVES it (notification) + CLAIMS to pick it up. **This IS the assign-flow already stubbed** (the ADMIN_REVIEW_ASSIGNED notification type is REAL but its emitter is DEFERRED precisely because no assign-flow exists; flagged on 1.11 'EMITTER DEFERRED - needs an assign-flow that isn't built'). **Design shape:** assigner ROLES via a new capability (e.g. approval:assign / task:assign; likely Super Admin + a lead/senior-Operations tier, ties to the capability matrix + possibly the Platform-Manager mid-tier persona) can ASSIGN a queue item / merchant / approval / task to a specific user; assignee gets ADMIN_REVIEW_ASSIGNED (NOW fires); assignee CLAIMS to act. **KEY: assignment ROUTES, claim COMMITS** - single-actor accountability preserved (assignment says 'yours to pick up', assignee still claims-to-act); self-claim + assign COEXIST. **Net-new:** AdminApproval has claimedById (self-claim) but NO assignedToId/assignedById/assignedAt -> schema addition + assign capability + fire the existing notification + assigned-state on the queue + audited (who-assigned/who-assigned-to/who-claimed). Spans queue (1.3/1.4) + notifications (1.11) + capability model. **Sequencing = owner fork (AskUserQuestion): (a) early Wave-2 focused pass [recommended - keeps CP-1 a clean Wave-1 sign-off, gives the cross-surface workflow proper design]; (b) focused pass now before CP-1; (c) capture + decide later.** **OWNER CHOSE (a): D59 = FIRST focused Wave-2 pass, AFTER CP-1** (brainstorm/design the cross-surface assign-then-claim workflow properly: queue assigned-state + assign action + assign capability + fire ADMIN_REVIEW_ASSIGNED + assignee-claims + audit + schema assignedToId/assignedById/assignedAt). **IMMEDIATE: waiting on the 1.9 Team&Roles STAFF-REFRAME build (final corrected paste already handed) to CLOSE WAVE 1 -> then assemble CP-1 (owner one-time wave sign-off) -> then D59 opens Wave 2.**

**1.9 TEAM & ROLES STAFF-REFRAME (D58) LANDED = PASS (owner built; build log).** Terminology reframe throughout (nav 'Team & Roles' / title / 'Team member' header / 'N team members' / 'Invite team member' / denied-empty-error copy; 'admin' only as 'admin-panel access/capability', never the person); expanded staff record via 'View record' toggle (preferred-name / work-phone / reports-to / employment-type / start-date / staff-id / location + 'On the record' invited-by/date/account-created; real green tag on account-created, net-new tags on richer + real-vs-net-new note); invite essentials (name/work-email/job-title/department[6-team dropdown]/role) + 'Optional now' (manager dropdown of active team members w/ titles / start-date / employment-type); success names role+title+dept; HR-BOUNDARY callout (panel = onboard-to-panel + access + basic work context ONLY; NOT the HR system; pay/DOB/home-address/personal-contacts/HR-docs -> Zoho People); nothing lost (role mgmt / deactivate-reactivate / capability matrix / proposed personas / combined-access future / last-super-admin guard verified firing / all states). Caveat: text-typing into invite fields not exercisable in the test harness (click-driven controls + read paths verified; text inputs follow the already-verified invite pattern) = harness limitation NOT a defect. No defects.

## CP-1 REPORT (Wave 1 sign-off) - assembled 2026-07-03, assistant verdict PASS + recommend APPROVE & LOCK

**Wave 1 delivered the operational + governance SPINE of the Redeemo Admin Panel across 11 screens**, all assistant-reviewed, on the CP-0 locked design-system + shell, synthetic-data-only, honest REAL/NET-NEW/EXTERNAL labelling throughout. **All 11 PASS:** 1.1 Sign-in (email->OTP + states) / 1.2 Ops Home (role-aware + operational member-revenue snapshot D-CD6) / 1.3 Approval Queue (unified triage + two-court Needs-you|Awaiting-merchant) / 1.4 Review-Actioner (claim->review->correct-on-behalf two-tier[cosmetic-direct|material-to-merchant]->merchant round-trip->per-item audit) / 1.5 Merchant Directory (supply-window + enriched table + Table/Map/Group-by + real-taxonomy dropdown filters) / 1.6 Merchant 360 (13-tab workspace incl. field-parity + Notes + collapsible two-band cards; assisted-onboarding D49; redeemed + edit-on-behalf lanes) / 1.7 Create-draft + full 9-step assisted onboarding (D49) / 1.8 Global Audit (queryable, PIN/secrets redacted, identity role-gated D48) / 1.9 Team & Roles (staff records + roles + capability matrix + invite + last-super-admin guard + combined-access future D57 + staff-reframe D58) / 1.10 Operational Status (bounded honest board BC-1, no fabricated greens) / 1.11 Notifications (bell + page, per-admin, emitter-deferred honesty). Plus the assisted-onboarding + field-parity arc that deepened 1.6/1.7.
**Wave-1 pack acceptance = ALL MET:** 5 review surfaces share one shell+bar; claim/approve honest single-actor; docs signed-link; PIN never shown; redemption identity role-gated (D48); Ops Home no fabricated KPIs; Global Audit + Team&Roles governance-early w/ grants+step-up FUTURE; Operational Status bounded + providers never live + no secret + no transient snapshot.
**Honesty posture (locked):** every net-new/future/external element labelled; no fabricated data; providers never operationally-live; secrets/PINs never rendered; redaction + role-gating throughout.
**Decisions locked Wave 1:** D37-D59 (all in the tracker; blueprint reconciled through D56 on PR #351; **D57-D59 need a small closing doc-sync**).
**CARRY LIST:** (micro-watches, non-blocking visual/copy) Finance Ops-Home visual · 1.3 narrow-view replace-not-stack · 1.1 submitting spinner. (later capability-matrix decision) exact audit:read / operational-status / admin-manage role sets [broadened in demos to show tiers] + D57 combined-access model [multi-role vs per-person grants]. (parked, Wave-2+ brainstorm-first) D50/D54 Taxonomy & Rules · D55 Agreements/Policies/Content · D51 Customer + onboarding notes · D56 edit-on-behalf live/pending · **D59 assign-then-claim = FIRST Wave-2 pass** · Members & Revenue module screens · Customer 360. (doc-sync to close) D57-D59 blueprint reconciliation.
**ASSISTANT VERDICT: PASS. Recommend the owner APPROVE + LOCK Wave 1 (CP-1).** **OWNER APPROVED + LOCKED CP-1 (2026-07-03: 'It's approved. Lock it in.'). WAVE 1 LOCKED.** Now: (1) run the D57-D59 doc-sync to close the blueprint (PR #351), then (2) open WAVE 2 with D59 (assign-then-claim) as the first focused pass.

**D57-D59 DOC-SYNC DONE + PUSHED (commit `529f0858`; PR #351 now 8 commits, head verified LIVE via `git ls-remote` + `gh api pulls/351` = 529f0858; the `gh pr view` 7/776aee1e was the known-stale cache).** Subagent returned; assistant reviewed FULL diff (3 files only: blueprint +17 / wave-1-pack +10 / master-context +6; 24 ins / 9 del). Belt-and-braces verified: em/en-dash on added lines = 0; non-ASCII = only · (4); the 9 deletions are ALL intentional Team-and-Roles rewords + §16-intro expansion (NO content loss: §16 intro kept 'D49 to D56 are owner-DECIDED' + appended D57-D59; D56 intact x4; 3 §16 D-rows + §13 rows present; key content Team-and-Roles/multi-role/per-person-grants/assign-then-claim/'assignment ROUTES'/Zoho-People/assignedToId/ADMIN_REVIEW_ASSIGNED all landed). Edits: §3.1 persona-evolution note / §3.2 D57 combined-access / §3.4 Team-and-Roles framing / §5 M20 row reframe (D58; subagent correctly placed on M20 not §6.1=Merchant360) / §9 D59 assign-then-claim / §13 3 rows / §16 intro + 3 rows; wave-1-pack §1.9 Team-and-Roles + §1.11 D59-emitter-deferred + acceptance; master-context §C.1/§C.2/§D. **BLUEPRINT + WAVE-1 PACK + MASTER CONTEXT NOW FULLY RECONCILED with the prototype through D59 (all Wave-1 decisions D37-D59 captured).** Doc-sync current. **WAVE 1 fully CLOSED (screens LOCKED + docs reconciled). NEXT: Wave 2 opens with D59 (assign-then-claim) - assistant to bring a proper design (who-can-assign / assigned-vs-claimed queue states / routes-vs-commits accountability / net-new schema) before any build-ask.**

**RE-ANCHOR REVIEW (owner asked 'are we drifting?', 2026-07-03).** Assistant read the full prepared bundle: manifest (upload-execution-manifest.md = CP-0[design-system+shell] -> Wave1/CP-1 -> Wave2/CP-2 -> Wave3/CP-3 -> CP-4 outputs; design-system built ONCE, later waves REFERENCE not re-establish) + master-context + 3 wave packs + §15 wave plan. **PREPARED WAVE PACKS:** wave-1 (spine, DONE+CP-1-locked) / wave-2 (2.1-2.13 relationships/CRM/onboarding/support/trust, UNSTARTED except pull-forward) / wave-3 (3.1-3.5 commercial/content-taxonomy/insights, UNSTARTED). **DRIFT ASSESSMENT (honest, bounded):** Wave 1 done+locked ON the pack, BUT the batch cadence blurred by: (1) PULLED Wave-2 assisted onboarding (pack 2.11+2.12) FORWARD into Wave 1 = built the rich account-building WIZARD (D49) but NOT the pack's 2.11 LEAD PIPELINE (lead board new/contacted/agreed/invited/converted/dead + in-person visit record + field-verification evidence + onboarding provenance) -> 2.11/2.12 PARTIAL; (2) went VERY deep on Merchant 360 (field parity / voucher builder D52/D53 / Notes D51 / edit-on-behalf D56 / density passes) beyond the pack's Wave-1 360 scope, owner-driven pass-by-pass; (3) added D49-D59 - but most MAP to the plan: D49=2.11/2.12, **D59 assign-then-claim = the Wave-2 2.3 Tasks/Follow-ups assign flow (the pack literally notes the ADMIN_REVIEW_ASSIGNED hook 'unemitted, awaiting the assign/reassign flow')**, D50/D54 taxonomy + D55 agreements = Wave-3 3.2 Content&Taxonomy (parked correctly), D51/D52/D53/D56/D57/D58 = Wave-1-surface refinements. Members&Revenue (D-CD6) = nav + Ops-Home snapshot done (Wave 1), the 2.13 SCREENS unbuilt (Wave 2, on plan). Everything TRACKED (D-register) + doc-synced (blueprint through D59, PR #351); Wave 2/3 packs still valid. **RE-ANCHOR PLAN:** return to manifest cadence = Wave 2 by the wave-2 pack, one screen at a time (like Wave 1), reconciled: 2.11 back-half wizard + contract/signature DONE (don't rebuild) -> Wave-2 2.11 = the LEAD PIPELINE front-end only; D59 = 2.3; remaining fresh = 2.1 Customer 360 / 2.2 Comms / 2.3 Tasks[D59] / 2.4 Account Health / 2.5 Reviews-Mod / 2.6 Fraud-Reversals / 2.7 Suggested-tag-Mod / 2.8 Support-Cases / 2.9 DSAR / 2.10 View-as / 2.11 assisted lead-pipeline / 2.12 full signature-channel comparison / 2.13 Members&Subscriptions + Revenue snapshot. Suggested start = 2.1 Customer 360 (symmetry w/ Merchant 360) OR 2.13 Members&Subscriptions (D-CD6 revenue engine). **DISCIPLINE to hold:** classify each mid-wave idea (this-screen / pull-forward / new-module) -> fold into the current screen OR DEFER to its proper wave as a tracked decision; hold the wave scope; don't expand the current wave indefinitely. Awaiting owner: where to open Wave 2 (2.1 vs 2.13) + confirm re-anchor.

**WAVE 2 OPENED at 2.1 Customer 360 (owner: 'go with your recommendation' = reconcile-plan-first + start Customer 360). SCREEN 2.1 (Customer 360) LANDED = STRONG PASS on build (owner built; 10 screenshots).** 10-tab workspace (Profile[masked+Reveal-PII reason dialog + No-location-plotting, no map]/Subscription[Annual £69.99 Active + real fields + Free=absence note + provider-gated grant/cancel/refund FUTURE]/Redemptions[£247.60 lifetime + table merchant-branch/voucher/saving/when + aggregated-to-this-customer]/Reviews[left + Open-in-Reviews-Moderation link]/Favourites[branches+vouchers+interests]/Devices[sessions + Future revoke + never-mapped]/Comms[timeline + email-dark + payload-never-rendered]/Relationship-history[NET-NEW customer-scoped CRM timeline, distinct from Global-Audit w/ link]/Consent[coarse marketing+terms REAL + Future granular centre]/Lifecycle[active/inactive/suspended/deleted]); net-new-surface tag + honest-status banner; Reveal-PII reason-gated + audited; Deleted-anonymised DPIA toggle (identifiers erased, DOB/gender/postcode/interests retained-by-design + open-data-min/DSAR flag); states permission-denied(Sales, names cap)/masked/deleted/loading/populated. Excellent honesty. **4 OWNER COMMENTS:** (1) INTERESTS + registration completeness: show ALL registration data + the REAL interest categories the customer tapped (Food&Drink/Automotive/Arts etc, real taxonomy not generic labels); give interests prominence. (2) **PII role-gating (= D48 again, now for Customer 360): owner questions blanket-mask; 'our customers are members, we're not sharing externally, appropriate roles should see it.'** -> **D60: apply the LOCKED D48 role-gated model to Customer 360** = sees-by-default for customer-facing roles (Support/senior-Ops/Super-Admin; inline, view still audited) / reveal-with-reason for restricted (e.g. Finance) / no-access for no-need (Content/Sales); EVERY access audited; role-context note. UK-GDPR framing preserved (role-gated + audited + minimised, NOT blanket-open to every role; consistent w/ D48). (3) **CUSTOMER DIRECTORY gap: no overview-of-all-customers like the Merchant Directory** -> this IS the next planned screen **2.13 Members & Subscriptions** (every customer = a member, Free = no subscription); build it to MIRROR the Merchant Directory (summary strip + enriched table + filters + search + views + Customer-360 drill-in). (4) REDEMPTIONS too simple: enrich -> full voucher (type/title/description/terms/estimatedSaving/card) + redemption specifics (code/branch/date-time/validation-status+method) properly presented (expandable/detail). **2.1 REFINEMENT HANDED (comments 1+2+4 = amendments to Customer 360): D60 PII role-gating + real-interests+registration-completeness + redemptions-enrichment.** Comment 3 = 2.13 (next screen). After the 2.1 refinement verifies -> 2.13 Customer/Members directory (mirror Merchant Directory).

**D61 RAISED (customer-messaging model + where it happens) - owner query 2026-07-04 (push? + email/SMS/notification from Customer 360?).** GROUNDED STATE: NotificationChannel enum = PUSH/EMAIL/SMS/IN_APP but only IN_APP live; EMAIL(Resend) dark/Phase-6; **PUSH(FCM) NOT set up** (no device/push tokens in schema; notify.ts 'PUSH/SMS deferred, FCM=Phase 6'); SMS(Twilio) wired for OTP-only (transactional), marketing/notification-SMS not built; consent = coarse newsletterConsent+marketingConsentAt only (granular per-channel/category = FUTURE). Push works BOTH individual (device token) AND group/segment (FCM topics/segments). **ASSISTANT ADVICE (3 messaging patterns):** (a) AUTOMATED/lifecycle = the bulk, system-triggered, NOT an admin action; (b) SEGMENT/campaign marketing = broadcast to an audience via a composer (Communications 2.2 / Campaigns Wave-3), NOT per-individual; (c) INDIVIDUAL/contextual = one customer via SUPPORT case-reply or RESEND, template-based+consent-checked+audited, NOT a free-text blast. **Customer 360 = SEE messaging (comms history + consent), NOT a routine free-text send-blast** (consent/channel-validity + spam/abuse/audit reasons; raw individual PUSH blasts aren't the norm = push is automated+segment; individual CONTEXTUAL messaging via support/resend IS normal). Deliberate individual messaging routes through Support&Cases (2.8) or resend, ALL provider-gated (Phase 6). Prototype = design messaging as labelled-FUTURE provider-gated + consent-checked + audited concepts, no live send. **OPTION offered to owner:** a labelled-FUTURE 'message this member' concept on the Customer-360 Comms tab (support-contextual + consent-checked + audited + provider-gated) IF wanted; else messaging lives in Communications/Support/Campaigns. No build this turn (2.1 refinement already in flight). **D61 RESOLVED (owner 'go with your recommendations', 2026-07-04): Customer 360 stays READ-ONLY for messaging** (assistant's lean = the latter option); NO 'message this member' send-blast on the profile; all sending lives in Communications (2.2) / Support & Cases (2.8) / Campaigns (Wave-3), template-based + consent-checked + audited + provider-gated (Phase 6). Customer 360 connects via the Comms timeline (what was sent) + linked-support-cases in the Relationship-history tab, NO profile send button. **2.1 refinement UNCHANGED** (no messaging affordance folded in; stays PII-role-gating[D60] + real-interests+registration-completeness + redemptions-enrichment). Waiting on the 2.1 refinement build.

**2.1 CUSTOMER 360 REFINEMENT LANDED = PASS (owner built; 4 screenshots).** All 3 amendments verified: (1) D60 PII role-gating = Operations sees identity/contact INLINE + green 'SEES BY DEFAULT · view recorded to Audit' banner + honest 'members / tiered / never blanket-open' framing (reveal-with-reason + no-access tiers via role-switch, consistent w/ D48); (2) registration completeness = full field set (name/email/phone/DOB/gender/full-address/marketing-consent/newsletter) + prominent 'INTERESTS SELECTED AT REGISTRATION' w/ REAL categories (Food&Drink/Beauty&Wellness/Health&Fitness/Arts-Out&About/Shopping); (3) redemptions enriched = each row expands to full VOUCHER (type/title/description/terms) + REDEMPTION RECEIPT (code/branch/date-time/voucher-type/validation-method+status/saving), 'View full detail'/'Hide detail'. **CP-2 MICRO-WATCH (non-blocking):** the top 'HONEST STATUS' banner still says 'Customer PII is masked until revealed with a reason' = OLD blanket-mask wording, now superseded by the D60 tiered model in the green banner below; update to tiered framing on next Customer-360 touch. No defects.

**SCREEN 2.13 (Members & Subscriptions / Customer Directory) HANDED (requirements-led; owner comment-3 = mirror Merchant Directory).** Grounded: User + Subscription models REAL; SubscriptionStatus = Trialling/Active/Past-due/Cancelled/Expired; plans Free(=no sub)/Monthly£6.99/Annual£69.99; MRR derived; no admin customer/subscription endpoints today = whole read-layer net-new. **Build:** (A) Members list = the customer directory: dense searchable/filterable list, Free = ABSENCE of a subscription (not a 'Free plan' row); summary strip (total + by-tier + by-status + active/dormant + revenue tiles) rescopes w/ filters; enriched table cols = member (D60 row-identity: name for sees-by-default roles / masked handle for restricted/no-access, NEVER address/DOB/sensitive in a row - full record on Customer 360) / tier / status pill / plan+interval / cycle-anchor / promo / joined / activity signal; dropdown filters (tier/status/activity) + search; views Table + Group-by (tier/status/region-text), **NO MAP (customer-home prohibition)**; row drills into Customer 360; grant/cancel/refund FUTURE. (B) Revenue snapshot = MRR(derived=active-subs×price×interval)/ARR/new-vs-churned/Past-due-dunning-list; MRR/ARR derived-read-side net-new; refunds/chargebacks/dunning-beyond-PAST_DUE/source-breakdown FUTURE; behavioural/demographic NOT here (DPIA-gated Insights Wave-3). States loading/empty/permission-denied(customer:read+subscription:read+revenue:view)/D60-row-gating/derived-net-new-notice/not-built-labels/past-due. Mirror Merchant Directory look (summary+enriched-table+dropdown-filters+search+Group-by) as a matched pair BUT no map. Brand/no-emojis/no-em-dashes. NAV nuance flagged (lightly): owner may want a 'Customers' entry in Relationships symmetric w/ Merchant Directory; the Members&Subscriptions list IS the customer directory (build there, drills into Customer 360). After it verifies -> continue Wave 2 (trust & safety intakes 2.5/2.7/2.6, support/DSAR/view-as, comms/tasks[D59]/account-health, assisted lead-pipeline).

**2.13 BUILD-ASK RE-ISSUED (owner switched session model + asked for a rethink, 2026-07-04).** Fresh pass surfaced 4 ambiguities the first ask under-specified, now locked in the paste: (1) TIER SEMANTICS = tier reflects the subscription row's plan whatever its status (Cancelled/Expired members show their last plan + status pill); Free = NO subscription row at all; cancel-at-period-end stays Active (+ 'ends <date>' marker) until period end - prevents tier-breakdown/MRR disagreement; (2) DORMANT defined = has access (Active/Trialling) but no redemptions in ~60 days (usage-based, mirrors Merchant Directory's definition; dormancy != subscription state); (3) MRR edges = cancel-at-period-end still counts until period end; new-vs-churned window = rolling 30 days, stated; (4) SEARCH respects D60 role-gating (masked-handle roles can't use search as a PII side-door; search by email = trusted roles only). Same scope otherwise (directory mirror + revenue snapshot + no-map + states).

**SCREEN 2.13 LANDED = PASS on semantics + maths + suitability, w/ 1 LAYOUT DEFECT (owner built; 2 screenshots + build log).** **Maths VERIFIED by assistant:** 4 free+7 monthly+5 annual=16 ✓; statuses 2/6/2/1/1=12 subs+4 free=16 ✓; **MRR £38.47 = 3 monthly×£6.99 + 3 annual×(£69.99/12) = £38.4675 ✓**; ARR £462 = ×12 = £461.61 ✓; '8 active access' = Active 6+Trialling 2 ✓ **and faithful to the REAL redemption guard (ACTIVE/TRIALLING only, PAST_DUE excluded)**; 7 engaged+1 dormant(Sofia) ✓. Tier semantics verified (Nadia Cancelled+last-Annual, Liam Expired, Priya Active+'ends 12 Aug 2026', Free='No plan'); dormant usage-based filter isolates Sofia; D60 3-tier verified (Ops/Super names+email inline+green banner; Finance masked handles+amber+search-DISABLED no-side-door; Sales/Content denied naming caps); past-due links to member; export gated+future; no map + deliberate note; group-by works; drill to Customer 360. **CP-2 CARRIES:** (a) MRR DEFINITION = excludes Trialling+Past-due (conservative; Stripe-style often includes past-due/trialling) - derived-definition to be LOCKED by owner/Finance when revenue reporting is built (tile honestly states its formula); (b) 2.1 honest-status banner stale blanket-mask wording (logged earlier). **DEFECT (fix handed): narrow/responsive card view renders BELOW the desktop table (stacked duplicate Mei Lin/Isla/Jack cards) instead of REPLACING it - same replace-not-stack class as the Wave-1 1.3 carry.** Fix paste = narrow cards only at narrow width/Narrow demo state, never both. After fix verifies -> continue Wave 2 (trust & safety intakes 2.5 Reviews Moderation next).

**NARROW-VIEW FIX VERIFIED = 2.13 COMPLETE + CLOSES THE WAVE-1 1.3 CARRY.** Root cause = inline `style=display:flex` on each `.q-narrow` block outranking the stylesheet's `display:none`; fix moved flex into the class + stripped inline display; replace-not-stack now correct across ALL THREE lists (Approval Queue + Merchant Directory + Members) - the CP-1 carry '1.3 narrow-view replace-not-stack' is CLOSED by root-cause. Verified: desktop Populated = table only; Narrow = cards only.

**SCREEN 2.5 (Reviews Moderation) HANDED (requirements-led).** Grounded: INTAKE REAL = ReviewReport model (reviewId/reportedByUserId/reason OFFENSIVE|SPAM|FAKE|OTHER/comment/createdAt, @@unique reviewId+reporter) + Review.isReported (INDEXED) + reportReason + isHidden + isDeleted flags + verified-review derivation (redemptionId); OUTLET NET-NEW = no admin list/act endpoints. Build: reported-reviews queue (row = review [rating/comment/branch+merchant/verified-badge/date] + report info [reason chips/report COUNT (multiple reporters aggregate)/report comments/oldest-report age w/ age tinting]); reviewer identity D60-gated + Customer-360 link; REPORTER masked-by-default for ALL roles (reveal-with-reason; judging content rarely needs reporter identity + retaliation-risk data-min call); actions w/ reason+confirm+audit = HIDE (isHidden REAL; removed from customer surfaces, kept in record, destructive-confirm) / RESOLVE-DISMISS (keep visible, clears reported state); closes the Customer-360 Reviews tab 'Open in Reviews Moderation' loop; 'every intake has an outlet' framing; honesty = intake+flags REAL, outlet/actions/routes NET-NEW, review:moderate cap net-new; states empty(no reports)/detail/destructive-confirm/stale/loading/error/permission-denied. After it verifies -> 2.7 Suggested-tag Moderation -> 2.6 Fraud/Reversals.

**SCREEN 2.5 LANDED = PASS (owner built; 4 screenshots).** Queue (5 open/11 reports) aggregates multiple reports per review (reason chips Offensive/Spam/Other + count + oldest-report age tinting); verified-redemption badges; author D60-gated (green AUTHOR-NAMES-VISIBLE banner + Customer-360 link); REPORTER masked-for-everyone + reason-gated Reveal + retaliation-risk note; expandable Reports-and-detail (per-report reason/comment/no-comment/age); Resolve/dismiss dialog (reason required -> Audit); Hide dialog (destructive red, 'NOT deleted - stays in record marked hidden, can be restored', reason -> Audit w/ name+time); all state pills. No defects.

**D62 RAISED+RECOMMENDED (pre-publication review screening) - owner question 2026-07-04: 'reviews show immediately; anything preventing swear words/abusive/discriminatory/incriminating content?'** HONEST ANSWER: NO - publish-first/moderate-after; no profanity filter, no automated screening, no pre-approval in code today; only reactive controls (report intake REAL + isHidden REAL + the new 2.5 outlet); abusive review SHOWS until reported+hidden. Maps to the deferred review-system-v2 workstream (spam/foul-language/rate-limit controls, memory §AI). **3 options weighed: pre-moderation (kills freshness, perma ops burden, not the norm) / reactive-only (today; the exposure) / HYBRID = RECOMMENDED (industry standard): publish immediately BUT automated screening at submission = HARD-BLOCK outright profanity/slurs (word-list + normalisation, customer asked to rephrase, never publishes) + AUTO-FLAG borderline (defamation/accusations/aggression; publishes but lands in the 2.5 queue immediately as 'Auto-flagged' WITHOUT waiting for a customer report; ML moderation service can strengthen later) + customer reports as the third net.** Screening layer = NET-NEW backend (review-system v2); 2.5 queue = the single outlet for BOTH intake sources. **2.5 ENRICHMENT HANDED:** 'Auto-flagged' source chip alongside customer-report items (distinct source labelling; auto-flag reason e.g. 'possible defamation: names an individual + accuses of a crime'); a small labelled-FUTURE 'Automated screening' explainer card (hard-block/auto-flag/reports = 3 nets; net-new, review-system v2); no other changes. After it verifies -> 2.7 Suggested-tag Moderation. **D62 RESOLVED (owner, 2026-07-04): HYBRID SCREENING LOCKED** = (1) hard-block outright profanity/slurs at submission (word-list + normalisation; customer asked to rephrase; never publishes); (2) auto-flag borderline (defamation/accusation/aggression) = publishes + lands in the 2.5 queue immediately as 'Auto-flagged' (no customer report needed; ML moderation can strengthen later); (3) customer reports = third net. Screening engine = NET-NEW backend, belongs to the review-system-v2 workstream (deferred-index §AI) when built; the 2.5 enrichment (already handed) shows the intended model honestly in the prototype. Pre-moderation explicitly rejected.

**2.5 D62 ENRICHMENT LANDED = PASS: 2.5 COMPLETE.** 2 auto-flagged items (Green Fork Deli possible-defamation 1d10h + Brew&Bloom aggressive-language 11h) w/ distinct indigo AUTO-FLAGGED chip + FUTURE tag + automated reason inline + expand 'Automated screening detail / no customer report attached' + same Resolve/Hide actions; report cards gain neutral 'Customer report' source chip; three-net FUTURE·NET-NEW explainer above queue + honest today-line; queue 7 open (5 reported + 2 auto) oldest-first across sources; aggregation/D60/reporter-masking/dialogs untouched.

**SCREEN 2.7 (Suggested-tag Moderation) HANDED (requirements-led).** Grounded: MerchantSuggestedTag model REAL (merchantId + free-text tag + status default PENDING, @@unique merchant+tag); **discovery consumption REAL + LIVE** (3 read sites in customer search filter status=APPROVED - an approved tag genuinely matches customer searches TODAY); intake NET-NEW (no merchant create-path writes rows) + moderation outlet NET-NEW (no admin approve/reject). 'Net-new on both ends, real in the middle' = approving has real customer-facing effect. Build: PENDING queue (tag text + merchant [M360 link] + merchant category/descriptor context for relevance judgment + suggested date); actions Approve (goes live in customer search - state the real consequence) / Reject w/ reason, both audited; duplicate-hint concept (tag duplicating a curated taxonomy Tag or another merchant's approved tag -> hint, net-new; prefer curated taxonomy where it exists - pairs w/ D50/D54 module: management=create side, moderation=review side); operator guidance (relevant/not-spam/not-duplicate); note assisted-onboarding 'add your own' will route here once intake exists; honesty labels (model REAL / consumption REAL+LIVE / intake+outlet NET-NEW / tag:moderate cap net-new); states empty/populated/approve+reject-confirm/loading/error/permission-denied. After it verifies -> 2.6 Fraud & Reversals.

**2.5 SCREENSHOT REVIEW (owner, 2026-07-04):** D60-tiered green banner correct; three-net explainer + Today-line correct; auto-flagged card fully differentiated (indigo reason inline + FUTURE chip + expanded 'Automated screening detail' panel); oldest-first ordering VERIFIED across sources (3d8h > 1d16h > 1d10h auto > 1d6h, auto item genuinely interleaved). TWO em-dash copy defects found (reporter-lock note 'retaliation risk - reveal is reason-gated'; screening-detail 'at submission - no customer report is attached') = micro-fix folded into the 2.7 hand-off paste. 2.5 remains PASS.

**2.5 EM-DASH MICRO-FIX LANDED (owner build report, 2026-07-04):** all 3 em-dashes swept (reporter-lock note + screening-detail + Hide-dialog 'not deleted' line). Closed.

**SCREEN 2.7 LANDED = PASS (owner built; 4 screenshots; owner's report labels it '2.6' = Claude Design local numbering, pack number stays 2.7; NEXT screen 2.6 Fraud & Reversals).** Net-new-surface explainer exact (model REAL / consumption REAL+LIVE 'immediate customer-facing effect' / tag:moderate cap / deferred intake incl. wizard add-your-own routing / Taxonomy-and-Rules parked = create side); Pending 6 oldest-first w/ tag + merchant M360 link + category chain + curated-tags context; FUTURE-CONCEPT duplicate hints BOTH variants (Dog-friendly vs curated 'Dog friendly'; Outdoor seating vs another merchant's approved tag); Approve confirm states immediate-live + Audit; Reject dialog 4 quick reasons + required free text + 'merchant not notified in this build' honesty; Recently-actioned 4 rows w/ actor·role + age + reject reasons + traceability note; all states incl. honest empty + queue-clear. ONE DATA NIT (fold into next paste): Old Foundry Kitchen card shows 'Restaurants and cafes · Modern British' as its category chain - 'Restaurants and cafes' is not a true top-level category (should be 'Food and drink · Restaurants and cafes'; Modern British is a cuisine tag); other cards use true top-levels correctly.

**SCREEN 2.6 (Fraud and Redemption Reversals) HANDED (requirements-led).** Grounded: RedemptionScreenshotEvent REAL + LIVE capture (userId/redemptionId/platform ios|android w/ DB CHECK/occurredAt; iOS post-fact screenshot detection on code surfaces posts telemetry via POST /redemption/:code/screenshot-flag w/ Redis 5s dedup; Android FLAG_SECURE blocks so few/no android rows) but NO admin read surface = this screen is the first outlet (read side NET-NEW). Reversal FULLY SCHEMA-GATED (grep confirms zero reversal fields on VoucherRedemption; on the Staff&Access 9-item stop-and-report schema ledger) = label the whole reversal half FUTURE/schema-gated. VERIFIED-REVIEW COUPLING WARNING mandatory: Review.redemptionId (ON DELETE SET NULL) drives the Verified badge - reversing a redemption must DECIDE whether the linked review loses Verified (owner decision when reversal is designed). isTestData filter honesty (QA rows excluded). States + capability redemption:reverse net-new.

**2.7 DATA NIT LANDED (Old Foundry Kitchen chain now 'Food and drink · Restaurants and cafes'). SCREEN 2.6 LANDED = PASS (owner built; 3 screenshots + detailed report).** Half 1 fraud signals: REAL·LIVE-CAPTURE + NET-NEW-SURFACE chips; honest explainer (iOS detect-only, Android OS-blocked so all-iOS); 8-event feed newest-first w/ C360 links + voucher/merchant·branch·code + iOS pills; patterns-by-customer rollup (Yusuf Kaya 'Repeated' 4ev/3merch floats top, one-offs below, single-screenshot-is-not-fraud caution); FUTURE signals card (velocity/PIN/re-presentation); test-data-excluded card. Half 2 reversal: FUTURE + SCHEMA-GATED framing + redemption:reverse gating (Operations sees DISABLED reverse w/ lock note = capability separation demonstrated live); code lookup -> full detail incl. LINKED REVIEW 'Yes · carries Verified badge' surfaced at lookup level; confirm/consequences (restore cycle use + savings adjust + RED verified-review OPEN-DECISION framing + reason + understanding checkbox + 'design preview only' on submit) verified via build report - confirm-panel close-up screenshot requested non-blocking alongside 2.8. Realistic touch: same voucher re-redeemed across a cycle boundary (12d18h apart, different codes) = plausible. ONE DATA NIT folded into 2.8 paste: synthetic code 'I5P2 7LW3' contains letter I - real 34-char alphabet excludes O and I. Cross-boundary check clean otherwise.

**SCREEN 2.8 (Support and Cases) HANDED (requirements-led, BUILD-VS-INTEGRATE framing per pack).** Grounded: NOTHING built server-side (SupportTicket model = deferred Profile Sub-PR 2; customer-app GetHelpModal = Coming-soon stub); ONLY real artefact = SUPPORT_TOPICS constants in customer app (Account issue/Subscription/Technical problem/Voucher dispute/General enquiry/Other) - dormant, no producer. Owner's standing direction: Zoho One for CRM + contracts + HELPDESK alongside the custom platform; planned monitored inbox support@redeemo.co.uk (D-F, email dark). So 2.8 shows the case queue + case detail as the in-panel surface w/ an explicit build-vs-integrate panel (Option A build SupportTicket in-platform / Option B integrate Zoho Desk w/ panel deep-links; owner decision when Sub-PR 2 triggers); honesty labels = everything NET-NEW except the 6 topic constants (REAL, dormant).

**2.8 INTAKE-SURFACE CORRECTION (owner recollection -> code-verified, 2026-07-04).** Owner recalled live ticket flows on customer app + customer web + merchant portal. VERIFIED: customer app = Get-help entry + REAL 6-topic dropdown constants but submission NOT wired (Coming-soon stub, Sub-PR 2); customer WEB = NO support/ticket form at all in shipped code (only static FAQ/terms/privacy mentioning support email) = known gap; merchant-web = /help route exists but is an honest ComingSoonModule 'Being built' placeholder (full experience lives in the merchant-portal PROTOTYPE only). Owner's memory = designed intent across all 3 surfaces; nothing files a ticket yet. 2.8 ADDENDUM HANDED (replaces intake note): queue serves BOTH customer + merchant cases from all 3 surfaces once live; per-case 'Raised via' value (Customer app / Website / Merchant portal / Email); >=2 merchant-raised cases incl. 1 via merchant portal; empty state names the 3 surfaces.

**SCREEN 2.8 LANDED = PASS ON CORE, ADDENDUM MISSED (owner built; 7 screenshots + narration).** RIGHT: open-decision panel exact ('This is unresolved; no winner is chosen', A/B one-trade-off-line each); queue 7 open genuinely oldest-first (3d7h->5h) w/ 6 real topic chips + 2 merchant-raised (CS-1028/CS-1025); split-pane detail w/ context cards (subscription/redemptions/prior-cases; merchant case shows 'Not applicable (merchant)'), thread, queued-reply honesty, staff-only internal notes; voucher-dispute LINKED REDEMPTION (EVIDENCE) panel + 'Open in redemption lookup' cross-link lands pre-filled on Fraud screen; 3 audited dialogs (Reassign w/ Unassigned option / Change status 4-state / Close w/ required resolution note); Content-role denied + honest empty verified per narration; sidebar gains SUPPORT AND CASES section (Case Queue 12 + DSAR GATED + Read-only View-as GATED). I5P2->J5P2 code fix confirmed via narration. MISSES (fix folded into 2.9 paste): (1) intake ADDENDUM not applied - no 'Raised via' chips, decision-panel footer still old two-channel wording, empty state not naming 3 surfaces; (2) EM-DASH in global search placeholder '...cases, audit - or a redemption code' (shell-level, every screen); (3) 2.6 reversal-confirm close-up STILL OWED.

**SCREEN 2.9 (DSAR and Data Requests) HANDED (requirements-led, LEGAL-GATED framing).** Grounded: customer SELF-SERVE deletion REAL + complete (POST /delete-account: OTP action-token gated; anonymise-in-place email->deleted_<id>@deleted.redeemo.co.uk + phone null + names '[Deleted]' + passwordHash null + deletedAt + status DELETED; all sessions revoked; AUTH_ACCOUNT_DELETED audit; history rows retained under anonymised identity). Admin-side = ALL NET-NEW (no request tracking, no export tooling anywhere, no admin-initiated erasure, no rectification workflow). LEGAL-GATED: ICO registration outstanding pre-launch; process needs legal sign-off; DSAR inbox = info@redeemo.co.uk (D-F). Build: request queue (Access/Erasure/Rectification; C360 link; channel; received date; 1-month statutory deadline countdown + overdue flag; status incl. Refused-with-grounds); detail w/ identity-verification-first honesty; ACCESS = data-category export-preview (profile/subscription/redemptions/favourites/reviews/cases/audit/comms) + FUTURE no-export-bundle-generator note; ERASURE = shows the REAL self-serve anonymisation + admin-initiated-would-reuse-it + prefer-self-serve note; RECTIFICATION -> profile-edit reality; deadline honesty card (1 month + 2-month complex extension + refusal-must-cite-grounds); dsar:manage cap net-new (Super Admin + designated only); 4 synthetic requests incl. 1 OVERDUE; honest empty (nothing routes here automatically yet).

**2.8 FIXES LANDED + SCREEN 2.9 LANDED = PASS (owner built; 3 screenshots + narration).** Fixes: Raised-via chips live on every case (via Email/Customer app/Website/Merchant portal; CS-1028 attributed Merchant portal); decision-panel footer now the exact 3-surface honesty wording; search-placeholder em-dash fixed per narration. BONUS worth keeping: System-generated internal note on the voucher-dispute case ('Screenshot fraud signal exists for this customer. Judge on the redemption record, not the screenshot alone') = cross-surface intelligence from the fraud feed into cases; fine as-is inside the net-new surface. 2.6 REVERSAL-CONFIRM CLOSE-UP: captured per narration (exact contents described: 3 consequences + red verified-review open-decision + required reason) but the image itself never reached the assistant; CARRY CLOSED on narration fidelity. 2.9 DSAR: PROCESS-PREVIEW/Pending-legal-review banner exact (ICO outstanding + real-today self-serve deletion described precisely + net-new-all-admin-side + email-only intake + D60 PII rules); queue 4 requests w/ deadline countdowns + overdue-flag sort-first; DR-2038 detail = statutory-deadline-passed banner + Step-1 identity-verification honesty ('itself a legal-gated design decision, not yet settled') + Step-2 8-category subject-access preview + FUTURE no-export-bundle note + Extend-deadline/Refuse-with-grounds/Mark-complete + 'actions are audited when operated for real' footer; erasure->self-serve reality + C360 hook lands on deleted/anonymised state; rectification; SUPER_ADMIN+SUPPORT gating; states verified per narration. ONE DATA NIT (fold into 2.10 paste): DR-2041 received 22 Jun -> deadline 22 Jul; at prototype-today (~3-4 Jul, consistent w/ DR-2038 overdue-by-3 + DR-2033 30-days-left) it should read ~18-19 days left, NOT 12.

**SCREEN 2.10 (Read-only View-as) HANDED (requirements-led, SECURITY-GATED framing).** Grounded: NOTHING exists in code (no impersonation/view-as endpoints anywhere) = entirely NET-NEW + genuinely security-sensitive. Locked design rules in the ask: NEVER session-token impersonation (server-side read-projection through admin auth only); READ-ONLY absolute (all mutating controls disabled); reason required to start; time-boxed (30-min auto-expiry) + end-now; start/end AUDITED w/ reason; always-visible banner (viewing-as + read-only + ends-at countdown); D60 PII tiering still applies; entry from C360/M360; cap view-as:start tight (Support + Super Admin); simplified framed preview of key surfaces NOT a full app rebuild.

**DSAR DATE FIX LANDED (DR-2041 received date -> 16 Jun; 12-days-left now arithmetically consistent w/ shared prototype-today ~4 Jul). SCREEN 2.10 LANDED = PASS (owner built; 6 screenshots + narration).** Locked-rules card = all 7 guardrails verbatim (never-impersonation server-side read-projection / absolute read-only / reason+audit / 30-min time-box + End-now / always-visible banner / D60 PII no-side-door / Support+SuperAdmin-only cap viewas:use); idle state w/ View-as-customer + View-as-merchant CTAs; ACTIVE CUSTOMER = navy VIEW-AS-ACTIVE banner (subject link + read-only statement + LIVE ticking countdown + End now) + device-frame simplified projection (Home feed/Voucher detail/Subscription tabs; Bristol·BS6; voucher tab has DISABLED Redeem + 'Disabled in view-as' badge per narration) + honest caption; ACTIVE MERCHANT = browser-frame (URL bar + view-as·read-only pill + navy portal sidebar + dashboard w/ 'New voucher · disabled' + metrics/chart); START DIALOG = SECURITY-GATED+READ-ONLY chips + never-credentials explainer + Customer/Merchant toggle + required audited reason (confirm disabled until reason) + 30-min duration card w/ Can/Cannot lists incl. 'Cannot: see data your role could not see' + 'Start and end are written to Audit' footer; RECENT SESSIONS audit-trail table (7m04s ended-manually / 30m00s auto-expired / 8m12s) exact; End-now->expired + C360 pre-fill + M360 pre-fill (after merchant picker) + denied-role state verified per narration. No defects found. NEXT: 2.2 Communications.

**SCREEN 2.2 (Communications) HANDED (requirements-led, D61 sending-home model).** Grounded: CommunicationLog = REAL outbox (recipientType/recipientId/channel/type/subject/status default QUEUED/externalId/sentAt/payload Json; payload = rendered message enabling reconciler re-enqueue, NULLED at terminal state because it can carry reset links/PINs -> log shows METADATA not content long-term); 9 REAL transactional email templates in code (password reset / merchant account setup / customer sign-in code / business sign-in code / business email verify / business account-deletion / admin new-merchant-submitted / admin resubmitted / branch redemption PIN); email DARK (Resend SDK installed unimported; queues QUEUED rows; D-F sender policy DECIDED noreply@+reply-to-support@/merchants@/info@; live gated on domain verify + SPF/DKIM/DMARC + monitored inboxes + bounce webhooks + SEC.1 limiter); SMS LIVE transactional-only (Twilio OTP + branch PIN); push NOT SET UP (no FCM, no device tokens); in-app LIVE merchant+admin bells (real writer + read endpoints), customer app bell icon exists in HomeHeader but NO feed behind it (no customer notification read endpoint). Build: channel status board (4 channels honest); comms log = NET-NEW READER over REAL rows (filter recipient/channel/type/status; metadata-not-content honesty note); template library read-only (9 real, system-triggered; management NET-NEW pairs w/ parked D55 module); one-off send per D61 (template-based only, consent-checked newsletterConsent real, audited, provider-gated = email says QUEUED not sent while dark); cross-refs Support-reply queue + Campaigns Wave 3 for bulk; caps comms:read/comms:send.

**SCREEN 2.2 LANDED = PASS ON CORE + ENRICHMENT HANDED (owner built; 5 screenshots reviewed directly, 2026-07-05).** RIGHT: 4-channel honest board exact (Email built-not-live w/ sender-policy line; SMS live-transactional-only; Push not-set-up; In-app live-merchants+admins w/ customer-bell-no-feed honesty); metadata-never-content card verbatim; log 10 rows Queued-emails + Sent-SMS mix w/ 360 links + filters; template library = the 9 REAL rows w/ correct channels/triggers + NET-NEW parked-module footer; send dialog = D61 rules restated + queued-not-delivered footer + 2 labelled net-new marketing templates. GAPS (enrichment handed): (a) template list NOT filtered by audience (Merchant toggle still offers customer templates); (b) consent-blocked + queued-confirmation states not evidenced. ENRICHMENT also adds: log-row detail drawer (metadata + view-template link + content-cannot-be-shown line); read-only template preview w/ merge-field placeholders; D63 ownership note; FUTURE 'Planned automated sends' section.

**D63 RAISED + RECOMMENDED (template editing ownership) - owner question 'should we edit templates from here?':** RECOMMENDED SPLIT = transactional templates CODE-OWNED + read-only in panel (they carry reset links/PINs; UI editing = real risk); marketing templates panel-editable LATER via the parked D55 content-management module. Encoded in the enrichment paste pending owner lock.

**D64 RAISED (planned automated sends) - owner asks 2026-07-05:** (1) redemption-details email to merchant branch manager per validated redemption = maps to the DEFERRED merchant-portal M3 'redemption notification slice' + alert-recipients schema-ledger item; (2) monthly redemption statement to branch managers + owners = ALREADY in locked SR4 merchant-portal Phase 4 architecture ('automated monthly statements'); (3) assistant-found GAP: NO customer email-verification template among the real 9 (only business variant) despite app hard-blocking on email verify; plus advisory Phase 6 list (welcome / subscription receipt + renewal reminder / payment-failed / cancellation confirm / customer deletion confirm / password-changed notice). All added to the prototype as FUTURE 'Planned automated sends'; the real send-wiring = Phase 6 + SR4 decisions.

**WORKFLOW CHANGE (owner request, 2026-07-05): screenshot review via cheaper model.** Pasted-in-chat images land in Fable context directly (cost already incurred) so delegation post-paste saves nothing. NEW FLOW from next batch: owner saves screenshots to a folder (suggested ~/Desktop/proto-screens/) + says so; assistant dispatches a SONNET subagent to Read the image files against the build-ask checklist and report compact findings; Fable takes the PASS/refine call on the findings.

**2.2 ENRICHMENT LANDED = 2.2 COMPLETE (first Sonnet-subagent screenshot review, 2026-07-05).** Sonnet findings: items 3-6 PASS w/ verbatim evidence (drawer honest line exact; preview {{first name}}/{{reset link}} placeholders + never-real-values caption; D63 ownership note encoded; all 10 planned automated sends incl. the customer-email-verification real-gap honesty). Items 1-2 (audience-filtered templates + consent-blocked + queued-confirmation) NOT in screenshot set - accepted via owner's written report (wording matches ask verbatim incl. 'Blocked: no marketing consent' + disabled confirm + 'Queued; sends when email goes live' + dropdown resets on audience switch); NON-BLOCKING carry: 2 send-dialog state screenshots in next folder batch. Sonnet false-alarm resolved: log 'Sent' badges = genuinely delivered SMS rows (correct lifecycle), not a tense defect. No em-dash/emoji violations found.

**SCREEN 2.3 (Tasks and Follow-ups) HANDED (requirements-led, D59 assign-then-claim pass the owner requested in Wave 1).** Grounded: task system ENTIRELY NET-NEW (no Task model anywhere). REAL adjacent machinery this extends: AdminApproval claim-to-act (claim/release w/ claimer-or-SUPER_ADMIN guard), the hourly 24h stale-claim sweep (lastStaleAlertAt dedup, in-app reminder), the live admin bell; plus the M8-deferred assigned-review notification type = named intent that becomes real WITH this feature. D59 model encoded: task:assign holders (Super Admin + Ops lead) create + assign -> assignee bell-notified -> task sits 'Assigned, awaiting claim' until assignee CLAIMS (claim = acceptance + ownership; assignment routes, never bypasses claim) -> In progress -> Done; unassigned tasks -> team pool anyone claims; assigned-unclaimed >24h -> stale flag to assigner + reminder to assignee (mirrors real sweep). Task anatomy: title/note/type (Follow up with merchant / Call back customer / Review case / Prepare for visit / Custom)/linked entity w/ 360 links/due date + overdue/creator/assignee/status (Open-unassigned / Assigned-awaiting-claim / Claimed-in-progress / Done / Cancelled). Views: My tasks (claimed + awaiting-my-claim) / Team pool / All (assigners) / Recently done. Caps: create+claim all roles, task:assign gated. ~8 synthetic tasks incl. 1 stale-28h + 1 overdue.

**SCREEN 2.3 LANDED = PASS (Sonnet folder review #2, 2026-07-05).** Items 1-4/6/7 PASS w/ verbatim evidence (honesty card all 4 real-vs-net-new facts incl. 'that notification type becomes real with this feature'; 'Assignment routes work, but never bypasses claiming' banner; awaiting-your-claim group prominent above claimed-by-you; all 5 types + 5 statuses + 4 linked-entity kinds; claim dialog 'Claiming accepts the task and makes you its owner'; tab arithmetic 3+2+2=7 all + 2 done = 9 consistent; 'Completing is audited'). Items 5+8 (assign-mode dialog line + non-assigner note variant + loading/empty/error + gating) accepted via owner written report per 2.2 convention. SONNET FALSE ALARM OVERRULED: overdue-red 'inconsistency' (3-4 Jul red vs 5-6 Jul black) is CORRECT with prototype-today = 5 Jul (red = strictly past due; 5 Jul = due today). REAL micro-fixes folded into 2.4 paste: (1) 'From:' vs 'Creator:' label unification; (2) stale card needs Assigned-on timestamp so 28h is auditable. 2.2 send-dialog screenshots (consent-blocked + queued) ABSENT AGAIN = carry #3, re-requested.

**SCREEN 2.4 (Account Health) HANDED (requirements-led).** Grounded: NO health model exists anywhere = all derivations NET-NEW over REAL data (subscriptions w/ cancelAtPeriodEnd + PAST_DUE status; redemption history + dormancy per the locked 2.13 definition access-but-no-redemptions ~60d; voucher freshness; merchant sessions). Build: two halves. CUSTOMER HEALTH = watchlists (dormant members / payment-failed PAST_DUE / cancel-at-period-end pending losses / trial-ending) each w/ derivation stated inline + C360 links. MERCHANT HEALTH = watchlists (declining redemptions vs prior period / no ACTIVE vouchers / stale voucher content / no portal logins) w/ M360 links. Each row action = CREATE FOLLOW-UP TASK (2.3 integration, pre-filled type+linked entity) or template-send via Communications (consent-gated, cross-ref); NO auto-emails (Campaigns Wave 3). Honesty: derivations labelled net-new; thresholds presented as adjustable concepts pending owner lock; isTestData excluded note. Caps health:read; states + empty per watchlist.

**TASKS MICRO-FIXES LANDED ('Created by' unified + Assigned-timestamp on awaiting-claim cards w/ consistent 28h arithmetic). 2.2 SEND-DIALOG CARRY CLOSED (verbatim-correct quoted copy: 'Blocked: no marketing consent' + disabled Queue; 'Queued; sends when email goes live. The send is audited.'). SCREEN 2.4 LANDED = PASS (owner built; 2 screenshots reviewed directly, 2026-07-05).** Honesty banner exact (no stored score / computed live over real data / thresholds adjustable concepts awaiting product decision / isTestData excluded / no automated sending, win-back = later surface); 'All clear' positive empty framing; ALL ARITHMETIC VERIFIED vs shared today=4 Jul (dormant 3 May=62d + 1 May=64d; past-due 1 Jul=3d + 28 Jun=6d; trials 8 Jul=4d + 10 Jul=6d; stale 2 Apr=93d; quiet 30 May=35d; counts 8=2+2+2+2 + 5=2+1+1+1); 8 watchlists w/ inline derivations; row actions = Create-follow-up-task prefilled (2.3 integration) on all + Send-message deep-link on customers only (correct); 360 links. NIT (non-blocking): Denied chip absent from state toggle; owner report claims permission-denied verified - trust, fold if missing. NEXT: 2.11 Leads pipeline -> 2.12 signature comparison -> CP-2 assembly.

**SCREEN 2.11 (Leads and Onboarding pipeline) HANDED (requirements-led).** Grounded: prospect/lead model ENTIRELY NET-NEW (no Lead table anywhere). REAL machinery it feeds: admin create-draft-merchant + claim-link flow (email dark + operator readback), the approval queue, and the assisted-onboarding WIZARD already built in this prototype (Passes 1-4). Future intake source: customer-app 'Request a merchant' stub (Sub-PR 2 deferred) - labelled. Stages: Lead -> Contacted -> Visit booked -> Draft created -> Wizard in progress -> Submitted for approval -> Live; Lost-with-reason audited exit at any stage. Lead anatomy: business name / category guess / locality / contact / source (rep visit, inbound, customer request FUTURE) / assigned rep (D59 assign-then-claim semantics) / next action + due. Convert = Create-draft (REAL flow) -> continues into the existing wizard (cross-link, not rebuild). Caps lead:manage. ~10 synthetic leads across stages; presentation (kanban vs list) left to Claude Design per requirements-led rule.

**2.11 COURSE-CORRECT (owner screenshot, 2026-07-05): the Leads-and-Onboarding HUB ALREADY EXISTS** (built during the assisted-onboarding passes): inbound self-serve LIVE pointer card (6 awaiting -> queue), Create-a-draft-and-hand-off card, Assisted-onboarding NET-NEW card, IN-PROGRESS ASSISTED ONBOARDINGS resume list (3 w/ step-reached + updated-by) + 'Nothing here is live... go-live happens only after the final review' footer. Original 2.11 ask would have collided. REVISED ASK HANDED = AMENDMENT not new screen: add a PROSPECT PIPELINE section for PRE-DRAFT stages only (Lead -> Contacted -> Visit booked -> convert into the two existing admin-created routes; Lost-with-reason exit); post-conversion stages already represented by existing hub surfaces (in-progress list + approval queue). Keep all existing hub content untouched. ALSO: em-dash found in hub intro copy ('bring on directly - either by handing') = fix folded in.

**2.11 AMENDMENT LANDED = PASS (owner built; 4 screenshots reviewed directly, 2026-07-05).** Prospect pipeline: NET-NEW chip + honesty note (no lead table / Request-a-merchant future intake / assign-then-claim / Lost-requires-reason-audited); 3 pre-draft columns w/ counts + descriptors (Lead 2 / Contacted 3 / Visit booked 2); cards = name + category '(guess)' + locality + source chips (Rep visit / Inbound enquiry / Customer request + FUTURE INTAKE) + contact + next-action w/ due + rep + Move/Convert/Task/Lost (Visit-booked correctly drops Move); OVERDUE arithmetic PERFECT vs today=5 Jul (2/3/4 Jul flagged, 6-9 Jul not); LOST section w/ Old City Vinyl audited reason (semicolon not dash); count 2+3+2+1=8 as asked; intro em-dash fixed; per narration Add-lead validation + Convert prefill + lead:manage gating + states verified. RESIDUAL DEFECT (fix folded into 2.12 paste): em-dash sweep needed on PRE-EXISTING hub copy - >=5 instances ('onboard themselves - you review' / 'A short form - six essentials' / 'in person - profile...documents - so they never' / eyebrow 'ADMIN-CREATED - BRING A MERCHANT ON' / footer 'Started but not finished - resume any time').

**SCREEN 2.12 (Contract signature channels) HANDED (requirements-led, decision-support surface).** Grounded: merchant contract = 12-month, click-to-agree REAL + SHIPPED (accepted during merchant onboarding, timestamped, contractStatus on record; contract text inline in onboarding service); Zoho Sign = named option in plans since day 1, EXTERNAL, NOT integrated; in-person attest at handover = designed in this prototype's assisted-onboarding Pass 4 (rep-driven, merchant sets password at handover, every step audited). Build: a compact comparison surface (in the agreements/legal context near the wizard's contract step) laying the 3 channels side by side on: what exists today / evidence trail / friction / cost / in-person suitability / dependency; honest status labels per channel (REAL-SHIPPED vs PROTOTYPE-DESIGNED vs EXTERNAL-NOT-INTEGRATED); explicit OPEN OWNER DECISION framing (no winner picked); cross-ref parked D55 agreements module for version-controlled contract text.

**HUB EM-DASH SWEEP LANDED = VERIFIED (visible copy clean: 'Fast: under a minute' / 'in person: profile, branches...' / eyebrow 'ADMIN-CREATED: BRING...' / 'Started but not finished; resume any time' / 'They onboard themselves; you review'). OWNER CATCH (2026-07-05): lead:manage gating WRONG - Sales/Lead-owner role was restricted from the prospect pipeline (my build-ask error: gated to Operations+SuperAdmin only; the role named Lead owner could not open leads). FIX HANDED: lead:manage -> Sales/Lead-owner + Operations + Super Admin (Finance/Content/Support stay restricted); assign-then-claim unchanged (assignment rights stay Ops-lead + SuperAdmin); keep honest labelling that Sales role itself = FUTURE concept (real enum has 5 roles, no SALES).

**D65 RAISED + RESOLVED (owner direction, 2026-07-05): NO external e-signature setup now; INTERNAL evidence-pack signature = the third channel.** Owner paused the original 2.12 paste (never sent) to ask if Zoho Sign could be replaced by something internal. ANSWER: yes - internal 'evidence-pack signature' = upgrade of the shipped click-to-agree: versioned contract text (parked D55 agreements module owns it) + real ceremony (scroll-through + typed name + optional drawn signature) + generated signed-PDF snapshot of the exact contract version stored in R2 (REAL doc infra) + evidence record (authenticated identity / timestamp / IP + user-agent ALREADY real AuditLog fields / hash of the signed file for tamper-detection) + admin verification view. HONEST TRADE-OFF stated: internal cannot replicate the NEUTRAL third party holding evidence ('you control your own records' counter-argument); for a standard 12-month B2B agreement a simple e-signature + strong evidence pack is common UK practice; sufficiency check belongs to the ALREADY-PLANNED pre-launch solicitor review (no new legal gate added). Zoho/external = future special-case option only, deliberately NOT set up. REVISED 2.12 PASTE HANDED (replaces original; channel 3 = internal evidence-pack PLANNED/NET-NEW; quiet not-a-fourth-column line about external providers; Sales/Lead-owner capability fix at top).

**LEAD-CAP FIX LANDED (Sales/Lead-owner now holds lead-management; assignment rights unchanged; Sales-future labelling kept). SCREEN 2.12 LANDED = PASS, NO DEFECTS (owner built; 2 screenshots, 2026-07-05). WAVE 2 SCREENS COMPLETE (13/13).** 2.12: 3 channels w/ honest chips (Real-shipped / Designed-in-this-prototype / Planned-net-new) x 7 dimensions; external-provider quiet line verbatim; what-this-decision-affects 3 cards; DECISION-PENDING card w/ illustrative-not-recommendation mix; gated Super Admin + Content behind 'Legal / T&C / FAQ' nav (rename to 'Legal and Agreements' recommended + handed as one-liner). HONESTY CHECK PASSED AT FULL STRENGTH: shipped MerchantContract stores signedAt + ipAddress + tcVersion (v1.0) + signatureMethod (+ nullable zohoSignRequestId since day 1) - the click-to-agree evidence claim 'version accepted, timestamp, IP, authenticated account' is code-accurate; D65 internal path slots into existing signatureMethod model.

**CP-2 REPORT ASSEMBLED (2026-07-05) - Wave 2: Relationships, CRM, Onboarding, Support, Trust. 13/13 screens PASSED.**
Screens: 2.1 Customer 360 (D60 role-tiered PII; full registration data; enriched redemptions) · 2.13 Members & Subscriptions (16-member composition verified; MRR GBP38.47 arithmetic checked; D60 search-not-a-side-door) · 2.5 Reviews Moderation (D62 hybrid three-net model; auto-flagged intake interleaved oldest-first) · 2.7 Suggested-tag Moderation (real-in-the-middle honesty; both duplicate-hint variants) · 2.6 Fraud & Reversals (REAL screenshot telemetry first outlet; reversal FUTURE/schema-gated; verified-review open-decision warning) · 2.8 Support & Cases (build-vs-integrate open decision; 3-surface intake honesty; Raised-via) · 2.9 DSAR (process-preview/legal-gated; real self-serve deletion vs net-new admin side; deadline arithmetic consistent) · 2.10 Read-only View-as (zero-defect first build; 7 locked guardrails; never-impersonation) · 2.2 Communications (4-channel honest board; real outbox metadata-not-content; 9 real templates; D61 send rules; D63/D64) · 2.3 Tasks & Follow-ups (D59 assign-then-claim; extends real claim machinery) · 2.4 Account Health (8 net-new watchlists over real data; all arithmetic verified; task/comms integrations) · 2.11 Leads hub amendment (pre-draft prospect pipeline; hub em-dash sweep; Sales-cap fix) · 2.12 Signature channels (D65; no external setup).
Decisions this wave: D60 (member PII tiering) / D61 (sending home) / D62 (hybrid screening, review-system v2 carries) / D63 (template ownership split) / D64 (planned automated sends + customer-email-verification gap) / D65 (internal evidence-pack signature).
Connective tissue: Health->Tasks prefill + Health->Comms deep-link + Cases<->Fraud cross-links + fraud-signal system note on cases + 360 View-as entries + Leads->draft/wizard handoff.
CP-2 CARRIES: (1) 2.1 honest-status banner stale blanket-mask wording (pre-D60); (2) MRR definition lock (excl. Trialling+Past-due) = owner/Finance when revenue built; (3) Account Health Denied chip absent from toggle (report claims state exists); (4) VERIFY Media/Photo Review nav item (un-gated in sidebar; confirm what's behind it); (5) CP-1 carries: Finance Ops-Home eyeball / 1.1 submitting spinner / exact role-sets for audit:read+status+admin-manage / D57 combined-access model; (6) nav rename 'Legal and Agreements' one-liner.
NEXT AFTER SIGN-OFF: batched blueprint doc-sync D60-D65 + wave-2 outcomes -> PR #351; then Wave 3 (commercial/content/insights) -> CP-3.

**CP-2 APPROVED + LOCKED (owner, 2026-07-05: 'Approved CP two'). DOC-SYNC + RECONCILIATION STARTED** per owner direction: blueprint aligned w/ the execution log (D60-D65 + wave-2 outcomes + new capabilities) + gap scan across the other admin-panel docs; usual worktree flow (author-only subagent, assistant diff-review, commit, push to PR #351, head verified via ls-remote + gh api).

**DOC-SYNC #4 SHIPPED (2026-07-05): commit `9b4776e9` pushed to PR #351 (verified live: 9 commits, head 9b4776e95384f0649caba7496d41b40421c1a67e via gh api + ls-remote).** Author subagent was CANCELLED mid-run (collateral of the owner's interrupted mis-sent message) after writing its edits but before its report; Fable completed review + gap scan directly. Diff +58/-18, single file. REVIEW PASSED: zero em/en-dashes in additions; all 18 deletions = rewrites (module rows M4/M6-M11/M13-M16/M25 + taxonomy line + DPIA facts + view-as decision row + approved-decisions line); D60-D65 all registered as proper §16 table rows w/ correct statuses (D60/D61/D62/D65 DECIDED w/ dates; D63 RECOMMENDED; D64 RAISED); §15 wave-status paragraph = Wave 1 11/11 CP-1 2026-07-03 + Wave 2 13/13 CP-2 2026-07-05 + all 6 carries verbatim; new caps registered (tag:moderate, redemption:reverse, dsar:manage, viewas:use, comms:send, task:assign, lead:manage, health:read + comms:read/health variants at line ~110). GAP SCAN (report-only, no edits): upload-bundle wave-2 pack (lines ~16/67) + 00-admin-master-context (line ~34) still carry pre-D60 blanket 'masked-until-revealed / reveal-on-demand' customer-PII wording = HISTORICAL issue docs, superseded by D60; no re-issue needed (the live prototype already encodes D60); flag ONLY if the bundle is ever re-uploaded for a fresh project. No contradictions found with D61-D65 (pack's signature-channel line asked to 'compare without approving one legally' = consistent). NEXT: Wave 3 (Campaigns / Featured Placement / Trending / Promo Codes and Trials / Insights-Reporting) -> CP-3. Owner-side open items: 'Legal and Agreements' rename one-liner + Media/Photo Review screenshot.

**WAVE 3 OPENED (2026-07-05). Pack re-anchored (wave-3-commercial-content-insights-pack.md: 5 concept areas expanding to 12 build units). LOCKED BUILD ORDER:** 3.1a Campaigns -> 3.1b Featured Placement (payment-status go-live gate) -> 3.1c Trending (algorithmic view + curation concept) -> 3.5a Revenue and Billing deep (MRR-definition carry surfaces here as decision input) -> 3.5b Promo Codes and Trials -> 3.5c Redemptions and Engagement (aggregate-only) -> 3.5d Conversion -> 3.2a Taxonomy Management (partially realises parked D50/D54 at prototype level) -> 3.2b Versioned Legal concept (joins 2.12 under 'Legal and Agreements') -> 3.2c Announcements (FUTURE, small) -> 3.3 Insights and Reporting (operational concept + DPIA fail-closed behavioural) -> 3.4 Feature flags/config (read-only register) -> CP-3. Rationale: commercial cluster first (new territory, billing-gate labelling momentum); Members-depth second (extends Wave-2 ground; money-honesty patterns fresh); content/taxonomy mid; Insights after aggregate patterns established; flags smallest last.

**SCREEN 3.1a (Campaigns) HANDED (requirements-led).** Grounded: Campaign model REAL (name/description/bannerImageUrl/targetLocations[]/startDate/endDate/status default DRAFT + CampaignMerchant join REAL) + richer Plan-4b targeting fields (targetLocalityIds/branchAnchorId/radiusMiles/LAD/counties/regions/isNational/priority) exist in schema but are DELIBERATELY UNREAD today (schema-only forward contract - label); NO admin surface anywhere; whole merchant-paid cluster GATED on unbuilt merchant billing (no live money ever shown). Build: campaign list (status/dates/targeting summary/merchant count); create-edit (name/description/banner concept/all-UK-or-selected-areas targeting w/ note richer geo fields reserved in data model/date-bounded schedule/cost field BILLING-GATED); detail w/ opted-merchants add-remove (join real); status transitions confirm + audit; gated-billing notice pattern; cap campaign:manage net-new; ~4 synthetic campaigns across statuses; full state set.

**3.1a PAUSED BY OWNER FOR CAMPAIGNS BRAINSTORM (2026-07-05) - original ask NOT sent. GROUNDING DIG:** (1) customer app ALREADY renders campaign banners TODAY (home feed queries ACTIVE date-windowed campaigns, take 5, CampaignCarousel on Home + GET /customer/campaigns/:id/merchants exists) BUT the live query is LOCATION-BLIND = exactly the owner's Manchester-shows-in-London fear; known + deliberate Plan 4b deferral. (2) Location model ALREADY DESIGNED + LOCKED (Plan 4 spec §12): full targeting contract on Campaign (targetLocalityIds / branchAnchorId+radiusMiles / LAD / counties / regions / countries / isNational / priority, schema SHIPPED Plan 4a M1) + 9-rung matching ladder (locality > branch-radius > catchment > LAD > county > region > country > national > PLATFORM FALLBACK banner when nothing matches - irrelevant-area campaigns never show; specificity outranks width; priority DESC; carousel cap 5). Admin prototype must ENCODE this ladder, not invent a new model. (3) Genuinely un-brainstormed: types / participation / charging.

**D66 RAISED + RESOLVED (owner locked all 3 recommendations, 2026-07-05): CAMPAIGN PRODUCT MODEL.** (a) TYPES: two - PLATFORM OCCASION (multi-merchant, e.g. Valentine's-in-Bristol; banner opens in-app campaign page listing participants, endpoint exists) + EXCLUSIVE (one merchant buys the banner; tap goes straight to their merchant profile or a chosen voucher). Platform announcements stay separate (3.2c). (b) PARTICIPATION: v1 admin/rep-curated merchant list; merchant-portal opt-in marketplace (merchant requests to join open campaign -> admin approves -> charge on acceptance) designed alongside as labelled FUTURE concept. (c) PRICING (billing-gated, nothing charged today): occasion = admin-set per-merchant participation fee per campaign (total = fee x participants); exclusive = negotiated flat price for slot+area+duration; Featured keeps its per-placement cost+radius model; Trending stays free/algorithmic. REVISED 3.1a BUILD-ASK HANDED encoding D66 + the Plan 4b ladder targeting UI + location-blind-today honesty.

**D66 EXTENDED (owner substance requirement + assistant grounding, 2026-07-05): D66-d/e/f.** GROUNDING: CampaignMerchant join ALREADY carries per-participant costGbp + paymentStatus + stripePaymentIntentId + own start/end dates (= D66-c fee model is schema-aligned, not just designable); NO participant->voucher link exists (offer attachment = genuinely schema-gated net-new); merchant-web has a REAL 'Promote' section (honest Coming-soon teaser explicitly promising Featured placement + Location-targeted campaigns). (d) CAMPAIGN OFFERS REQUIRED: occasion participant appears to customers ONLY once their campaign offer is live (existing voucher tagged to campaign OR new campaign-specific voucher, naturally TIME_LIMITED scoped to campaign window); no offer = 'Offer pending' + excluded; converts campaign page from themed directory into occasion-specific deals. (e) CUSTOMER CAMPAIGN PAGE INTENT: hero banner + title + dates + ends-in countdown + editorial intro + OFFER CARDS (not merchant tiles); normal voucher-detail/favourites/redemption behaviour; admin detail carries a design-intent preview panel; honest note = today's live page is merchant-list-only + location-blind. (f) MERCHANT PORTAL 'PROMOTE' FLOWS (FUTURE concept, named after the real section): open campaigns w/ fee -> request-to-join WITH proposed offer -> status ladder (Requested/Accepted/Offer approved/Live) -> post-campaign redemptions report; Featured self-serve requests also live in Promote (ties to 3.1b payment gate); admin mirror = join-requests queue on campaign detail. FINAL 3.1a PASTE v3 HANDED (v2 explicitly superseded, not sent).

**3.1a LANDED = NOT YET PASS (Sonnet folder review #3, 2026-07-05; 12 screenshots incl. 4 duplicates).** GOOD: list columns exact (type/status/dates/targeting/WHO/COST-GATED w/ fee maths '£180 · £45 x 4'); targeting step = full ladder + explainer VERBATIM; banner upload + carousel preview; gated-cost boxes throughout; occasion create/edit 3-step; add/remove participant dialogs; no dash/emoji violations. DEFECTS (refinement handed): (1) OFFER LAYER ENTIRELY MISSING = the D66-d core (no Offer pending/live chips, no offer slot, no substance rule, 4 identical participant rows, no excluded-from-preview modelling); (2) NO campaign-level delete/cancel for ANY status = the owner's reported bug (only Edit/End-campaign/remove-participant exist); (3) payment chips (REAL schema fields) absent from participant rows; (4) customer-page preview panel (D66-e) missing (only carousel mini-preview); (5) FUTURE card doesn't name 'Promote' / no status ladder / no report / no queue (D66-f partial); (6) context card missing 2 facts (join carries fee+payment; offer link schema-gated); (7) DATE/STATUS contradictions (Live 9-16 Feb + Scheduled 2-15 Mar vs today=5 Jul; both should be Ended or redated); (8) owner test campaign targeting line garbled ('Bsea'/'tendering' misspelt Tendring) = render full locality names; (9) add-merchant picker not area-aware (offers London/Brighton for Bristol-only campaign). Not-evidenced (request w/ next batch): exclusive detail + tap-through destination, transition confirm dialogs, Draft/Scheduled/Ended details, empty/loading/error/denied states. LIFECYCLE MODEL LOCKED into refinement: Draft=Delete (confirm+audit); Scheduled=Cancel (kept in record, audited); Live=End-early-with-reason NEVER delete; Ended/Cancelled=retained no delete; participant removal free in Draft/Scheduled, withdraw-with-reason on Live.

**D67 RECORDED (owner decision from the security-activation session, relayed 2026-07-05; NOT for immediate action - owner: 'record it, bring it up when appropriate'):** READ-ONLY ADMIN VISIBILITY OF VOUCHER REDEMPTIONS is REQUIRED before complete pre-launch staging acceptance can pass. Completes the journey: merchant creates voucher -> customer discovers + redeems -> Merchant Portal reflects -> ADMIN can inspect + reconcile the same event. Required outcome: authorised admin finds + inspects a redemption event w/ status / voucher title+type+code-safe id / merchant+branch / customer-safe identity / redeemed + validated timestamps + validation channel / saving / lifecycle-audit provenance / links to C360+M360+branch+voucher; dense, honest states (loading/no-results/denied/failed/expired-cancelled-invalid per real semantics/missing-redacted-customer/event-delayed). HARD BOUNDARIES: read-only; NO reversal/refund/mutation/manual-correction/impersonation/unrestricted-PII/provider/schema/migration/deployment; document API gaps, never invent data. INTEGRATION PLAN (when reached): slot into Wave 3 at 3.5c, SPLIT: 3.5c-i Redemptions and Engagement (aggregate-only per pack, unchanged) + 3.5c-ii REDEMPTION EVENT INSPECTOR (per-event, D60-gated customer-safe identity; the aggregate-only rule stays intact on -i because -ii is a separate event surface). NOTE overlap: 2.6 Fraud lookup already prototypes the by-code event inspection (customer/voucher/merchant/branch/times/saving/linked-review) = extend to search/browse + provenance + links, do NOT rebuild; keep clean separation from 2.6's FUTURE reversal half (D67 is read-only). GROUNDING KNOWN: VoucherRedemption fields real (redemptionCode/isValidated/validatedAt/validationMethod/estimatedSaving/isTestData); merchant join via branch (no merchantId on redemption); merchant-side redemption list real (merchant-web M3); ADMIN-side API + surface = NET-NEW. BEYOND-PROTOTYPE deliverables the owner block requires when picked up (full Superpowers spec + plan for the LIVE feature, source cross-check table, permission/PII/audit/tenant model, API reuse-vs-net-new, customer->merchant->admin consistency test plan, adversarial review, remaining owner decisions) = a SEPARATE workstream scheduled after the prototype slice; production code/PR gated on separate approval. Staging-acceptance-gate status makes this the highest-priority item in the 3.5 cluster when Wave 3 reaches it.

**3.1a REWORK ROUND (Sonnet folder review #4, 2026-07-05/06; 12 files incl. 2 stale).** LANDED + VERIFIED: lifecycle actions on rows + details (Delete draft / Cancel campaign / End early; owner's delete bug structurally closed); context-card completion VERBATIM (join-carries-fee+payment real / offer-link net-new); Promote future card w/ full ladder + queue + report; customer-page preview structure + honest note verbatim + 29-day countdown correct; Exclusive tap-through destination shown (voucher); dates fixed (Live spans today=5 Jul; all fee/countdown arithmetic consistent); seeded targeting full place names. STILL NOT EVIDENCED: the LIVE Bristol detail page (= where offer chips / payment chips / participant rows / End-early reason dialog live; list shows '3 of 4 offers live' so data landed, row UI unverified); Cancel confirm + Cancelled record; any transition dialog; area-aware picker; non-populated states. NEW DEFECTS (closing paste handed): (1) Exclusive shows Occasion offer-readiness copy despite configured destination voucher = readiness must be TYPE-AWARE (exclusive ready when destination voucher live); (2) targeting input still FREE TEXT ('Bathg/Batg' echoed verbatim; prior fix was seed-only) = replace w/ pick-from-list locality/district/region picker validated against the place register, no free text; (3) seed 'created by: Shebin Chandran' too close to owner's real name = swap to synthetic staff set. Folder hygiene reminder (2 stale files incl. an IDE transcript survived the clear-first instruction). Wizard step-2 disabled Next w/ empty fields = presumed legit validation gate, confirm in passing.

**D66-g RAISED + RECORDED (owner think-out-loud, 2026-07-06): CAMPAIGN COMMS + CROSS-MODULE WIRING.** Extends D64 w/ a campaign email family (all Phase-6/dark-gated; payment ones also billing-gated): (1) campaign invitation (occasion + fee + response deadline); (2) join-request acknowledgement (Promote path, FUTURE w/ D66-f); (3) acceptance + joining details (fee confirmed + offer requirements + offer-approval deadline); (4) offer approved / needs changes = RIDES REAL MACHINERY (campaign offers are vouchers; voucher approval lane + merchant notification already shipped); (5) going-live reminder; (6) payment request + receipt (real per-participant fee/paymentStatus fields; charging billing-gated); (7) campaign ended + performance summary (= D66-f report delivered). KEY REALISATION: merchant in-app bell is LIVE today while email is dark -> invitations designed bell-first w/ email queued (invite flow NOT blocked on Phase 6). CROSS-MODULE MAP (no new screens; each lands in a planned surface): Communications 2.2 = campaigns group in Planned-automated-sends; Revenue and Billing 3.5a = campaign+featured income as a SEPARATE merchant-paid revenue stream line (gated) distinct from member-subscription MRR; Insights 3.3 = campaign performance as operational aggregate (per-customer campaign behaviour DPIA-gated); M360 = participation history on merchant timeline; approval queue = offers reuse existing voucher lane. SMALL ADDENDUM PASTE HANDED (Communications planned-sends campaigns group + bell-first note + Campaign-detail 'Connected surfaces' footnote); independent of the 3.1a closing paste.

**D66-h RAISED + RECORDED (owner, 2026-07-06): CONCURRENCY VISIBILITY + COVERAGE MAP.** (1) Multiple concurrent campaigns per location = already half-encoded (carousel cap 5 + specificity + priority) but INVISIBLE to the operator -> PER-AREA SLOT PREVIEW (pick area -> ordered carousel as a customer there sees it; overflow state when >5 match, weakest cut) + scheduling overlap note in wizard review ('N other campaigns live in these areas during this window'). (2) COVERAGE MAP (owner nice-to-have, promoted to build item): schematic UK map of Live (solid) + Scheduled (outlined w/ start date) footprints (radius circles / shaded areas / national wash) + COVERAGE GAPS panel = areas w/ active members + merchants but NO campaign, ranked by member count, 'customers see the fallback banner', each w/ 'Plan a campaign here' -> create wizard pre-filled targeting; honesty = real geography (branch pins/member localities), matching service pending, map is a concept not a live engine. COVERAGE VIEW PASTE HANDED as a Campaigns third view; SEQUENCED AFTER the 3.1a closing-fixes round verifies (kept out of the evidence round deliberately). Owner-side queue now: (1) closing fixes + evidence shots, (2) D66-g comms addendum, (3) D66-h Coverage view.

**3.1a CLOSING ROUND (Sonnet folder review #5, 2026-07-06; 12 shots; screenshots had landed in ~/Downloads - Fable moved them; future note: browser default-saves to Downloads).** DESIGN VERIFIED = the substance passes: OFFER LAYER CONFIRMED to spec (3 Offer-live w/ linked vouchers + 1 Offer-pending 'Excluded from the customer page until this offer is live' + independent Paid/Pending payment chips + preview shows exactly 3 offer cards + '1 participant(s) hidden' note); PICK-FROM-LIST targeting PASS ('you cannot type a place that is not on it', chips echo clean); seed names fixed (Priya Nair); CONNECTED SURFACES footnote verbatim x4; COVERAGE VIEW largely PASS (map legend solid-Live/dashed-Scheduled/national-wash; gaps ranked desc by member count w/ Plan-a-campaign-here; Bristol slot preview 5 ranked + 6th greyed 'Cut: carousel caps at five'; wizard overlap note names the 5 competitors; synthetic set scaled to 10 campaigns, tab counts reconcile 1+2+6+1+0). REVIEWER FLAG OVERRULED: top-right 'Shebin C.' chip = the shell's signed-in demo operator identity since Wave 1, not seed data. RESIDUALS -> FINAL EVIDENCE SWEEP (folded into 3.1b paste): countdown convention (preview says 9 days for 14 Jul at today 6 Jul; panel convention = EXCLUSIVE per DSAR = 8; standardize); slot-preview fallback case for an uncovered area not demonstrated; Exclusive-readiness fix STILL zero evidence (no Camden detail, 2 rounds running); End-early dialog / Cancel confirm + Cancelled record (Cancelled(0) proves never executed) / add-merchant picker / non-populated state all unevidenced; D66-g COMMS ADDENDUM NOT APPLIED (no Communications shot; campaign-emails group absent). 3.1a status: DESIGN PASS, evidence sweep outstanding.

**SCREEN 3.1b (Featured Placement) HANDED (requirements-led) + final 3.1a evidence sweep folded in.** Grounded: FeaturedMerchant model REAL + COMPLETE (merchantId/startDate/endDate/costGbp/radiusMiles/targetLocations[]/sortByDistance/paymentStatus default PENDING/stripePaymentIntentId unused/isActive default false); customer featured rail LIVE on Home today; INTEGRITY GAP = discovery does NOT check paymentStatus (an unpaid placement could go live) = D10 known issue; this surface DESIGNS THE FIX: paymentStatus = the visible hard go-live gate (blocked state when start date arrives unpaid; NO silent override; complimentary-placement-with-reason = FUTURE concept flagged for owner decision); billing gated (cost recorded, nothing charged). Build: list (merchant/dates/radius+areas/cost/payment chip/placement status incl BLOCKED-unpaid/isActive); create (merchant pick + branch-anchored radius + areas pick-from-list reusing campaign pickers + dates + cost + sortByDistance explained); lifecycle mirrors campaigns (cancel/end-early conventions); Promote FUTURE card (merchant requests placement via portal: branch/area/duration/price/pay -> admin approves -> gate applies); note Coverage-view extension candidate (featured footprints on the map, later); cap featured:manage (Ops+SuperAdmin); synthetic 4-5 placements incl ONE BLOCKED past-start-unpaid (the integrity case made visible) + live-paid + scheduled-pending + ended; full states.

**D66-i RAISED + RECORDED (owner, 2026-07-06): FEATURED = FULL LOCATION PARITY W/ CAMPAIGNS.** GROUNDING: customer featured rail caps at FEATURED_TAKE=10 (real constant, homeRailBuilders.ts) ranked by the location ladder w/ locationConfidence gating; rail HIDES entirely when area supply is zero (real v1.2 hide rule). REAL BUSINESS RISK SURFACED: no admin-side cap on placements sold per area while only 10 can ever show = an 11th paid placement may never be seen (featured overflow w/ money attached). ADDENDUM HANDED (paste together w/ 3.1b): (1) SHARED Growth-and-Commercial Coverage view w/ layer toggles Campaigns/Featured/Both (featured = radius circles: solid live-paid / dashed scheduled / red-edged blocked-unpaid; gaps panel gains Featured dimension 'no featured rail at all - it hides' + Sell-a-placement-here action); (2) per-area FEATURED slot preview (up to 10 real cap, ladder-ordered, beyond-10 greyed 'may never be seen', uncovered case 'No featured rail shown here'); (3) CAPACITY AWARENESS in sell/create flow (area's active-placement count + at/over-capacity warning; per-area capacity = adjustable concept pending product decision, default = rail cap 10); (4) list gains per-area grouping/filter.

**D56 RAISED (Merchant 360 edit-on-behalf parity + live/pending state) - owner think-out-loud, 2026-07-03.** Admins/reps should EDIT almost all fields for EXISTING merchants from M360 (parity with assisted-onboarding CAPTURE; completes D44). **Assistant recommendation (grounded; extends the existing MerchantPendingEdit/BranchPendingEdit + Option B applier - B1 SHIPPED the merchant->admin approve/reject direction; this adds the admin-direct lane + the admin->merchant-confirm direction = Option B B2 direct-edit + a new admin-proposes/merchant-confirms path):** TWO LANES - (1) **admin-direct** = immediate/live/audited, no pending (platform-owned + corrective + operational: category/reclass, status suspend/reactivate, location map-confirm, PIN reset[D46], verification/documents/notes, amenities, photo moderation, error fixes); (2) **merchant-confirm-required** = PENDING until confirmed then live (merchant self-representation + commercial + legal-identity: legal/registered name, description, public contact, voucher offers/terms, opening hours, public branch name). STATE MODEL: each field/section = **Live** / **Pending** (old value stays live + shown to customers; diff old->new; 'awaiting merchant confirmation') / optional **Draft**; 360-level 'N pending changes' indicator; admin-direct never enters Pending. **GO-LIVE for pending = merchant confirms (portal notification/next login) OR admin IN-PERSON ATTESTS ('confirmed with owner') for portal-non-using/rep-managed merchants** (mirrors the assisted contract in-person model; evidence-trailed). EXACT field->lane assignment = OWNER DECISION, locked when we design the pass. **SEQUENCING: finish the field-parity DISPLAY pass (#2-#5, display-only, in flight) FIRST, THEN a focused 'M360 edit-on-behalf + live/pending' design pass** (touches every 360 tab's edit button -> one coherent pass; belongs to the Option B track). Parked-but-next, not now.
